---
phase: 45-observability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web-agent/config/grafana-dashboard.json
  - web-agent/config/prometheus-scrape.yml
autonomous: true
requirements: []

must_haves:
  truths:
    - "Grafana dashboard JSON available for import"
    - "Prometheus scrape config defines /metrics endpoint"
  artifacts:
    - path: "web-agent/config/grafana-dashboard.json"
      provides: "Grafana dashboard template"
    - path: "web-agent/config/prometheus-scrape.yml"
      provides: "Prometheus scrape configuration"
  key_links:
    - from: "grafana-dashboard.json"
      to: "/metrics endpoint"
      via: "Prometheus data source"
---

<objective>
Create Grafana dashboard JSON and Prometheus scrape configuration for observability integration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-observability/45-CONTEXT.md
@web-agent/src/api/routes.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Prometheus scrape configuration</name>
  <files>web-agent/config/prometheus-scrape.yml</files>
  <action>
Create prometheus-scrape.yml in web-agent/config/ with scrape configuration:

```yaml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'web-agent'
    static_configs:
      - targets: ['localhost:3000']
        labels:
          agent: 'web-agent'
    metrics_path: '/metrics'
    scrape_interval: 10s
```
  </action>
  <verify>
<automated>ls web-agent/config/prometheus-scrape.yml</automated>
  </verify>
  <done>Prometheus scrape config file exists</done>
</task>

<task type="auto">
  <name>Task 2: Create Grafana dashboard JSON</name>
  <files>web-agent/config/grafana-dashboard.json</files>
  <action>
Create Grafana dashboard JSON template (v10 format) in web-agent/config/grafana-dashboard.json.

Dashboard should include:
1. Dashboard metadata: title "Escluse Web Agent", uid "escluse-agent"
2. Panels:
   - CPU Usage: graph panel querying agent_cpu_percent
   - Memory Usage: graph panel querying agent_memory_used_bytes / agent_memory_total_bytes  
   - Disk Usage: graph panel for disk space
   - Agent Status: stat panel showing connected, running_tasks, uptime_secs
   - Alert States: list showing active alerts
3. Variables: agent_id, job
4. Time picker and refresh intervals
5. Theme: auto (supports light/dark)

Use standard Grafana panel configurations with proper fieldConfig and options.
  </action>
  <verify>
<automated>ls web-agent/config/grafana-dashboard.json</automated>
  </verify>
  <done>Grafana dashboard JSON exists, ready for import</done>
</task>

</tasks>

<success_criteria>
- [x] Prometheus scrape config file created
- [x] Grafana dashboard JSON created
</success_criteria>

<output>
After completion, create `.planning/phases/45-observability/45-02-SUMMARY.md`
</output>