---
phase: 45-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web-agent/src/handlers/metrics.rs
  - web-agent/src/api/routes.rs
  - agent-core/crates/agent-metrics/src/lib.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Alerting thresholds trigger when CPU > 80%, Memory > 85%, Disk > 90%"
    - "Metrics endpoint exports Prometheus-format metrics"
    - "Heartbeat includes current metrics values"
  artifacts:
    - path: "web-agent/src/handlers/metrics.rs"
      provides: "metrics.report handler implementation"
      contains: "fn handle_report"
    - path: "web-agent/src/api/routes.rs"
      provides: "/metrics Prometheus endpoint"
      contains: "route(\"/metrics\""
    - path: "agent-core/crates/agent-metrics/src/lib.rs"
      provides: "Metrics collection utilities"
  key_links:
    - from: "web-agent/src/api/routes.rs"
      to: "agent_metrics::collect_system_metrics"
      via: "Function call"
      pattern: "agent_metrics::collect_system_metrics"
---

<objective>
Implement alerting thresholds for system metrics (CPU, Memory, Disk). Add threshold-based alerting logic that triggers when metrics exceed configured limits.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-observability/45-CONTEXT.md
@web-agent/src/handlers/metrics.rs
@web-agent/src/api/routes.rs
@agent-core/crates/agent-metrics/src/lib.rs
</context>

<interfaces>
<!-- Key types and contracts from existing codebase -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From web-agent/src/handlers/metrics.rs:
```rust
pub struct SystemMetrics {
    pub cpu_percent: f64,
    pub memory_used_bytes: u64,
    pub memory_total_bytes: u64,
    pub memory_percent: f64,
    pub disk_usage: Vec<DiskUsage>,
    pub network_rx_bytes: u64,
    pub network_tx_bytes: u64,
}

pub struct MetricsReport {
    pub system: SystemMetrics,
    pub containers: Vec<ContainerMetrics>,
}
```

From agent-core/crates/agent-metrics/src/lib.rs:
- CollectSystemMetrics struct with cpu_percent, memory_used_bytes, memory_total_bytes, memory_percent(), disk_usage, network_rx_bytes, network_tx_bytes fields
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Define alerting thresholds configuration</name>
  <files>web-agent/src/config.rs</files>
  <action>
Add alerting thresholds configuration to the agent config. Add a new AlertsConfig section with:
- cpu_threshold_percent: f64 = 80.0
- memory_threshold_percent: f64 = 85.0  
- disk_threshold_percent: f64 = 90.0

Add AlertConfig struct with these fields and include in the main Config. Ensure configuration can be loaded from config.toml and CLI overrides work.
  </action>
  <verify>
<automated>grep -n "cpu_threshold\|memory_threshold\|disk_threshold" web-agent/src/config.rs</automated>
  </verify>
  <done>Alert thresholds configurable via config.toml with sensible defaults (80%, 85%, 90%)</done>
</task>

<task type="auto">
  <name>Task 2: Implement alerting logic in metrics handler</name>
  <files>web-agent/src/handlers/metrics.rs</files>
  <action>
Add alerting logic to the metrics.report handler. Import the thresholds config and after collecting metrics, check each threshold:

1. Create Alert struct with: level (warning/critical), metric_type, value, threshold, message
2. After metrics collection in handle_report, iterate through SystemMetrics and compare against thresholds
3. If cpu_percent > cpu_threshold: create Alert { level: "warning", metric_type: "cpu", value, threshold }
4. Same for memory_percent and disk_usage
5. Add alerts vector to MetricsReport: pub alerts: Vec<Alert>
6. Return alerts in the report response

Alert struct:
```rust
#[derive(Debug, Serialize)]
pub struct Alert {
    pub level: String,       // "warning" | "critical"
    pub metric_type: String, // "cpu" | "memory" | "disk"
    pub value: f64,
    pub threshold: f64,
    pub message: String,
}
```
  </action>
  <verify>
<automated>grep -n "pub struct Alert\|alerts:" web-agent/src/handlers/metrics.rs</automated>
  </verify>
  <done>MetricsReport includes alerts array when thresholds exceeded</done>
</task>

<task type="auto">
  <name>Task 3: Verify Prometheus format includes alerting</name>
  <files>web-agent/src/api/routes.rs</files>
  <action>
Update the /metrics endpoint to include threshold-aware Prometheus metrics:

1. Import alerting config: use crate::config::AlertsConfig
2. Get threshold values (default or from config)
3. Add new gauge metrics for thresholds:
   - agent_alert_cpu_threshold
   - agent_alert_memory_threshold  
   - agent_alert_disk_threshold
4. Add gauge metrics for current alert states:
   - agent_alert_cpu_active (1 if alerting, 0 otherwise)
   - agent_alert_memory_active
   - agent_alert_disk_active
5. Calculate alert states based on collected sys metrics
  </action>
  <verify>
<automated>grep -n "alert_threshold\|alert_active" web-agent/src/api/routes.rs</automated>
  </verify>
  <done>/metrics endpoint exposes threshold values and alert states in Prometheus format</done>
</task>

</tasks>

<success_criteria>
- [x] Alert thresholds configurable (config.toml)
- [x] MetricsReport includes alerts when thresholds exceeded
- [x] /metrics Prometheus format includes threshold metrics
</success_criteria>

<output>
After completion, create `.planning/phases/45-observability/45-01-SUMMARY.md`
</output>