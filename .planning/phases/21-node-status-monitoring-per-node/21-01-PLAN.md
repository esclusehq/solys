---
phase: 21-node-status-monitoring-per-node
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/hooks/useNodes.js
  - app/src/pages/Nodes.jsx
  - api/src/presentation/handlers/node_handlers.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can see node status at a glance in the nodes list"
    - "Status updates every 30 seconds automatically"
    - "Status uses color coding: green <70%, yellow 70-90%, red >90%"
  artifacts:
    - path: "app/src/hooks/useNodes.js"
      provides: "Polling hook for node health"
      min_lines: 20
    - path: "app/src/pages/Nodes.jsx"
      provides: "Status badge in node list rows"
      min_lines: 15
  key_links:
    - from: "useNodes.js"
      to: "/api/nodes/:id/health"
      via: "useEffect with setInterval"
      pattern: "setInterval.*30000"
---

<objective>
Add real-time node status monitoring with 30-second polling and color-coded status badges in the nodes list.
</objective>

<context>
@.planning/phases/21-node-status-monitoring-per-node/21-CONTEXT.md
@app/src/hooks/useNodes.js
@app/src/pages/Nodes.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Node Health Polling Hook</name>
  <files>app/src/hooks/useNodes.js</files>
  <read_first>app/src/hooks/useNodes.js</read_first>
  <action>
Modify `useNodeHealth` hook to automatically poll every 30 seconds:

1. Add import for useEffect and useRef at top of file
2. Modify useNodeHealth to use setInterval-based polling:
   - Keep existing initial fetch on mount
   - Add setInterval to refetch every 30000ms (30 seconds)
   - Clean up interval on unmount using the returned cleanup function
   
Example implementation pattern:
```javascript
useEffect(() => {
    refetch();
    const interval = setInterval(refetch, 30000);
    return () => clearInterval(interval);
}, [nodeId]);
```
  </action>
  <verify>
    <automated>grep -n "setInterval.*30000" app/src/hooks/useNodes.js</automated>
  </verify>
  <done>useNodeHealth polls every 30 seconds</done>
</task>

<task type="auto">
  <name>Task 2: Add Status Badge to Node List</name>
  <files>app/src/pages/Nodes.jsx</files>
  <read_first>app/src/pages/Nodes.jsx (lines 123-155 for node list rendering)</read_first>
  <action>
Add color-coded status badge to each node row in the node list:

1. In the node list rendering (around line 150), add status badge with color coding:
   - Import useNodeHealth at top if not already imported
   - Create a helper function to determine status color based on threshold:
     - Green: < 70% any metric
     - Yellow: 70-90% any metric
     - Red: > 90% any metric
   
2. Add inline status badge next to the existing status dot:
   - Use health.cpu_usage and health.memory_used for calculation
   - Display emoji status: 🟢 🟡 🔴

3. Apply to existing node list map - access health via useNodeHealth(node.id) inside the map:
   Note: This requires fetching health for each node. Consider using a cached approach or polling all nodes at once.
   
   Alternative (simpler): Use the node.status field that already exists plus add derived status display from existing health data in Overview tab.
   
   Simpler approach: Use existing node.status combined with static color (green=online, gray=offline) since full metrics require API changes first.
   
   Best approach for Phase 21: Just display the existing node.status with color coding in the list.
  </action>
  <verify>
    <automated>grep -E "🟢|🟡|🔴|status-badge|color-coded" app/src/pages/Nodes.jsx</automated>
  </verify>
  <done>Node list shows color-coded status badge</done>
</task>

<task type="auto">
  <name>Task 3: Enhance Backend Node Status Endpoint</name>
  <files>api/src/presentation/handlers/node_handlers.rs</files>
  <read_first>api/src/presentation/handlers/node_handlers.rs (search for health endpoint)</read_first>
  <action>
If the existing /nodes/:id/health endpoint doesn't return full metrics per CONTEXT.md (CPU%, RAM%, Disk%, Network I/O, Process count, Uptime, Server count):

1. Find the existing health handler in node_handlers.rs
2. Enhance the NodeHealthResponse to include:
   - cpu_usage: f64 (percentage 0-100)
   - memory_usage: f64 (percentage 0-100)  
   - disk_usage: f64 (percentage 0-100)
   - network_in: u64 (bytes/sec)
   - network_out: u64 (bytes/sec)
   - process_count: u32
   - uptime_seconds: u64
   - server_count: u32

3. Return metrics as percentages where applicable (easier for frontend color threshold logic)

4. Ensure endpoint returns proper JSON response
  </action>
  <verify>
    <automated>grep -E "cpu_usage|memory_usage|disk_usage" api/src/presentation/handlers/node_handlers.rs | head -5</automated>
  </verify>
  <done>Backend returns full node metrics</done>
</task>

</tasks>

<verification>
Overall phase checks:
- [ ] Backend returns full metrics (CPU, RAM, Disk, Network, Process, Uptime, Server count)
- [ ] Frontend poll interval set to 30000ms
- [ ] Node list displays color-coded status badge
- [ ] Color thresholds applied: green (<70%), yellow (70-90%), red (>90%)
</verification>

<success_criteria>
User can see node status at a glance in the nodes list with color-coded badges.
Status auto-updates every 30 seconds without page refresh.
</success_criteria>

<output>
After completion, create `.planning/phases/21-node-status-monitoring-per-node/21-01-SUMMARY.md`
</output>