---
phase: 43-service-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - release/package/solys.service
  - release/package/install.sh
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Systemd service file exists at /etc/systemd/system/solys.service"
    - "Service auto-starts on boot via systemctl enable"
    - "Service restarts automatically on failure (Restart=always)"
    - "install.sh handles service installation and startup"
  artifacts:
    - path: release/package/solys.service
      provides: systemd service unit
      min_lines: 20
    - path: release/package/install.sh
      provides: Service installation in install script
      contains: systemctl
  key_links:
    - from: install.sh
      to: solys.service
      via: systemctl enable solys
      pattern: systemctl enable solys
---

<objective>
Ensure systemd service integration for auto-start and background running — verify service file and install script handle service lifecycle.

Purpose: Agent starts automatically on boot and restarts automatically on failure.

Output: Verified systemd service integration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-service-mode/43-CONTEXT.md
@release/package/solys.service
@release/package/install.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify systemd service file</name>
  <files>release/package/solys.service</files>
  <read_first>release/package/solys.service</read_first>
  <action>
Verify solys.service contains all required systemd directives per D-01 to D-05:

Check that the service file includes:
- [Unit] section with Description and After=network-online.target
- [Service] section with:
  - Type=simple
  - User=root
  - WorkingDirectory=/opt/solys
  - ExecStart=/opt/solys/solys
  - Restart=always
  - RestartSec=10
- [Install] section with WantedBy=multi-user.target

If any directives are missing, add them. The service file should match the decisions:
- D-01: systemd (Linux standard)
- D-02: Service file at /etc/systemd/system/solys.service
- D-03: Auto-start via systemctl enable solys
- D-04: Start via systemctl start solys
- D-05: Restart on failure with 10s delay
  </action>
  <verify>
<automated>grep -q "Restart=always" release/package/solys.service && grep -q "RestartSec=10" release/package/solys.service && echo "Service has restart config"</automated>
  </verify>
  <acceptance_criteria>
- solys.service contains "Restart=always"
- solys.service contains "RestartSec=10"
- solys.service contains "Type=simple"
- solys.service contains "[Install]" section
  </acceptance_criteria>
  <done>
Systemd service file verified with restart-on-failure configuration.
</done>
</task>

<task type="auto">
  <name>Task 2: Verify install.sh handles service integration</name>
  <files>release/package/install.sh</files>
  <read_first>release/package/install.sh</read_first>
  <action>
Verify install.sh handles systemd service lifecycle:

1. Check if install.sh includes service file installation:
   - Copy solys.service to /etc/systemd/system/
   - Run systemctl daemon-reload

2. Check if install.sh handles auto-start:
   - Run systemctl enable solys (per D-03)
   - Run systemctl start solys (per D-04)

3. If commands are missing, add them to the main() function after binary installation.

The service commands should run AFTER the binary is downloaded and config is set.
  </action>
  <verify>
<automated>grep -q "systemctl enable solys" release/package/install.sh && grep -q "systemctl start solys" release/package/install.sh && echo "Service commands present"</automated>
  </verify>
  <acceptance_criteria>
- install.sh contains "systemctl daemon-reload" or copies service file
- install.sh contains "systemctl enable solys"
- install.sh contains "systemctl start solys"
  </acceptance_criteria>
  <done>
Install script handles service enable and start.
</done>
</task>

<task type="auto">
  <name>Task 3: Verify security hardening in service file</name>
  <files>release/package/solys.service</files>
  <read_first>release/package/solys.service</read_first>
  <action>
Verify systemd security directives are present:

Check for security hardening options (from CONTEXT.md code insights):
- NoNewPrivileges=true
- PrivateTmp=true
- ProtectSystem=strict
- ReadWritePaths=/var/log/solys /etc/solys

If any are missing, add them to the [Service] section.

Also add RUST_LOG=info environment variable if not present.
  </action>
  <verify>
<automation>grep -q "NoNewPrivileges=true" release/package/solys.service && echo "Security hardening present"</automation>
  </verify>
  <acceptance_criteria>
- solys.service contains "NoNewPrivileges=true"
- solys.service contains "PrivateTmp=true"
- solys.service contains "ProtectSystem"
  </acceptance_criteria>
  <done>
Service file has security hardening directives.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install→systemd | Service file installed as root, requires proper permissions |
| systemctl→agent | Agent runs as root with systemd control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | E | systemctl access | accept | Requires root, standard system protection |
| T-43-02 | D | service file placement | mitigate | Verify service file path is /etc/systemd/system/ |
</threat_model>

<verification>
1. solys.service exists and contains all required directives
2. install.sh handles systemctl enable/start
3. Security hardening directives present
</verification>

<success_criteria>
- Systemd service file is complete with all D-01 to D-05 decisions
- Install script handles service lifecycle (enable, start)
- Security hardening present (NoNewPrivileges, PrivateTmp, ProtectSystem)
- Agent auto-starts on boot and restarts on failure
</success_criteria>

<output>
After completion, create `.planning/phases/43-service-mode/43-01-SUMMARY.md`
</output>