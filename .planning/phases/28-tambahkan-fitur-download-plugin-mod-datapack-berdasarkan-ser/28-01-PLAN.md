---
phase: 28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/pages/servers/ServerDetailsPage.jsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can see Plugins/Datapacks tab on server detail page based on server loader type"
    - "Modded loaders (Paper, Spigot, Forge, Fabric, etc.) show Plugins tab"
    - "Vanilla servers show Datapacks tab"
    - "PluginManager component is properly integrated with server context"
  artifacts:
    - path: "app/src/pages/servers/ServerDetailsPage.jsx"
      provides: "Server detail page with plugins tab integration"
      contains: "Plugins tab conditional rendering based on mc_loader"
  key_links:
    - from: "app/src/pages/servers/ServerDetailsPage.jsx"
      to: "app/src/components/PluginManager.jsx"
      via: "import and conditional render"
      pattern: "import PluginManager"
---

<objective>
Add plugin/mod/datapack download feature to server detail page - integrate PluginManager component with server-type-aware tab.

Purpose: Enable users to browse and install plugins (for modded servers) or datapacks (for vanilla servers) directly from the server detail page.
Output: Updated ServerDetailsPage.jsx with conditional Plugins/Datapacks tab
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-tambahkan-fitur-download-plugin-mod-datapack-berdasarkan-ser/28-CONTEXT.md
@app/src/pages/servers/ServerDetailsPage.jsx
@app/src/components/PluginManager.jsx
@app/src/hooks/usePlugins.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Plugins/Datapacks tab to ServerDetailsPage.jsx</name>
  <files>app/src/pages/servers/ServerDetailsPage.jsx</files>
  <action>
Add PluginManager import and implement conditional tab based on server mc_loader field:

1. Add import at top of file:
```javascript
import PluginManager from '../../components/PluginManager'
```

2. Modify tabs array (around line 21-25) to add conditional tab:
```javascript
const moddedLoaders = ['PAPER', 'SPIGOT', 'BUKKIT', 'PURPUR', 'FORGE', 'FABRIC', 'NEOFORGE']
const isModded = server?.config?.mc_loader && moddedLoaders.includes(server.config.mc_loader)
const pluginsTab = isModded 
  ? { id: 'plugins', label: 'Plugins', icon: '🧩' }
  : { id: 'plugins', label: 'Datapacks', icon: '📦' }

const tabs = [
  { id: 'overview', label: 'Overview', icon: '📊' },
  { id: 'files', label: 'Files', icon: '📁' },
  pluginsTab,
  { id: 'logs', label: 'Logs', icon: '📋' },
]
```

3. Add conditional rendering for activeTab === 'plugins' after line ~326 (after files tab):
```javascript
{activeTab === 'plugins' && server && (
  <div className="flex-1 min-h-0">
    <PluginManager 
      serverId={id} 
      serverVersion={server.config?.minecraft_version} 
      serverLoader={server.config?.mc_loader}
      mode={isModded ? 'plugin' : 'datapack'}
    />
  </div>
)}
```

NOTE: The `isModded` and `moddedLoaders` variables must be defined inside the component render (after `if (!server)` check) so they have access to the server data.
  </action>
  <verify>
    <automated>grep -n "import PluginManager" app/src/pages/servers/ServerDetailsPage.jsx && grep -n "activeTab === 'plugins'" app/src/pages/servers/ServerDetailsPage.jsx</automated>
  </verify>
  <done>
    - ServerDetailsPage.jsx imports PluginManager component
    - Tabs array includes Plugins or Datapacks based on mc_loader
    - Conditional rendering displays PluginManager when plugins tab is active
    - Props passed: serverId, serverVersion, serverLoader, mode (plugin/datapack)
  </done>
</task>

</tasks>

<verification>
Manual verification:
1. Visit server detail page for a modded Minecraft server (Paper/Spigot/Forge) - should see "Plugins" tab
2. Visit server detail page for a vanilla Minecraft server - should see "Datapacks" tab
3. Click the tab - PluginManager component should load with marketplace and installed views
</verification>

<success_criteria>
- Modded loaders show "Plugins" tab with PluginManager in 'plugin' mode
- Vanilla servers show "Datapacks" tab with PluginManager in 'datapack' mode
- Tab conditionally appears based on server.config.mc_loader field
</success_criteria>

<output>
After completion, create `.planning/phases/28-tambahkan-fitur-download-plugin-mod-datapack-berdasarkan-ser/28-01-SUMMARY.md`
</output>
