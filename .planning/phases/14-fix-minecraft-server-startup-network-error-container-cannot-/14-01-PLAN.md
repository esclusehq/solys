---
phase: 14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "Minecraft server containers can resolve DNS for mojang.com"
    - "Bridge network mode is properly configured in web-agent"
  artifacts:
    - path: "api/crates/web-agent/src/handlers/server.rs"
      contains: "network_mode: Some(\"bridge\".to_string())"
  key_links: []
---

<objective>
Verify the DNS fix (bridge network mode) from Phase 13 is still working.
</objective>

<context>
@.planning/phases/13-fix-minecraft-server-startup-network-error-dns-resolution-fa/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify bridge network code exists</name>
  <files>api/crates/web-agent/src/handlers/server.rs</files>
  <action>Verify that the bridge network configuration exists in both handle_create and handle_start functions. Look for "network_mode: Some(\"bridge\".to_string())" at lines around 142 and 284.</action>
  <verify>
    <automated>grep -n "network_mode.*bridge" api/crates/web-agent/src/handlers/server.rs</automated>
  </verify>
  <done>Bridge network mode present in both create and start handlers</done>
</task>

<task type="auto">
  <name>Task 2: Verify code compiles</name>
  <files>api/</files>
  <action>Run cargo check to ensure the web-agent compiles without errors.</action>
  <verify>
    <automated>cd api && cargo check --package web-agent 2>&1</automated>
  </verify>
  <done>Code compiles successfully</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Verify a test Minecraft server can resolve DNS and start successfully</what-built>
  <how-to-verify>
    1. Create a new Minecraft server via the UI or API
    2. Check container network: docker inspect <container> --format '{{json .NetworkSettings.Networks}}'
    3. Verify "bridge" network is attached
    4. Check logs for DNS resolution: docker logs <container> | grep -i "mojang\|dns\|session"
    5. Verify server starts without DNS errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Code contains bridge network configuration
2. Code compiles without errors
3. Test container has bridge network and resolves DNS
</verification>

<success_criteria>
- [x] Bridge network configuration verified in code
- [x] Code compiles
- [x] Test container can resolve mojang.com DNS
</success_criteria>

<output>
After completion, create `.planning/phases/14-fix-minecraft-server-startup-network-error-container-cannot-/14-01-SUMMARY.md`
</output>