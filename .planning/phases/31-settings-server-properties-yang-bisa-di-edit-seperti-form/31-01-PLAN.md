---
phase: 31-settings-server-properties-yang-bisa-di-edit-seperti-form
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/presentation/handlers/server_handlers.rs
  - api/src/domain/services/server_service.rs
  - api/src/domain/repositories/server_repository.rs
  - api/src/presentation/routes/server_routes.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - User can view current server.properties values in form fields
    - User can edit server.properties fields like MOTD, maxplayers, gamemode, difficulty
    - User gets real-time validation feedback on fields
    - User can save changes and they persist to container
    - User sees success/error feedback after save
  artifacts:
    - path: "api/src/presentation/handlers/server_handlers.rs"
      provides: "POST /servers/:id/properties endpoint"
      contains: "handle_update_properties"
    - path: "app/src/features/servers/components/ServerPropertiesForm.jsx"
      provides: "Server properties form with validation"
      exports: ["ServerPropertiesForm"]
    - path: "app/src/lib/api.js"
      provides: "API methods for properties"
      contains: "getServerProperties, updateServerProperties"
  key_links:
    - from: "ServerPropertiesForm"
      to: "/servers/:id/properties"
      via: "api.getServerProperties / api.updateServerProperties"
      pattern: "api\\.(get|update)ServerProperties"
    - from: "server_handlers.rs"
      to: "terminal exec"
      via: "docker exec command"
      pattern: "exec.*server\\.properties"
---

<objective>
Add server properties editing form in Settings tab - create backend API to read/write server.properties and frontend form with real-time validation
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/31-settings-server-properties-yang-bisa-di-edit-seperti-form/31-CONTEXT.md

# Frontend - existing Settings tab structure
@app/src/pages/servers/ServerDetailsPage.jsx
- Lines 346-417: Settings tab content with Discord webhook form

# Backend - terminal handlers for exec commands
@api/src/presentation/handlers/terminal_handlers.rs
- Read and write file operations via docker exec
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GET/PATCH /servers/:id/properties API endpoints</name>
  <files>api/src/presentation/handlers/server_handlers.rs, api/src/presentation/routes/server_routes.rs</files>
  <action>
Add two new endpoints in server_handlers.rs:

1. `get_server_properties` - GET endpoint that:
   - Executes `cat /data/server.properties` inside the container via terminal handler
   - Parses the key=value format into a JSON object
   - Returns properties as JSON: { motd: "...", max-players: 100, gamemode: "survival", ... }

2. `update_server_properties` - PATCH endpoint that:
   - Accepts JSON body with properties to update
   - Builds complete server.properties content with all fields
   - Writes to /tmp/server.properties via terminal, then `mv /tmp/server.properties /data/server.properties`
   - Returns success/error response

Add corresponding routes in server_routes.rs:
```rust
.router("/:id/properties", get(get_server_properties).patch(update_server_properties)
```

Use existing exec infrastructure from terminal_handlers.rs.
  </action>
  <verify>
<automated>grep -n "fn get_server_properties\|fn update_server_properties" api/src/presentation/handlers/server_handlers.rs</automated>
  </verify>
  <done>GET /servers/{id}/properties returns JSON, PATCH /servers/{id}/properties accepts JSON body and writes to file</done>
</task>

<task type="auto">
  <name>Task 2: Add server properties API client methods</name>
  <files>app/src/lib/api.js</files>
  <action>
Add two methods in api.js:

```javascript
export async function getServerProperties(serverId) {
  return api.get(`/servers/${serverId}/properties`)
}

export async function updateServerProperties(serverId, properties) {
  return api.patch(`/servers/${serverId}/properties`, properties)
}
```

Export them in the exports section.
  </action>
  <verify>
<automated>grep -n "getServerProperties\|updateServerProperties" app/src/lib/api.js</automated>
  </verify>
  <done>api.js exports getServerProperties and updateServerProperties functions</done>
</task>

<task type="auto">
  <name>Task 3: Create ServerPropertiesForm component</name>
  <files>app/src/features/servers/components/ServerPropertiesForm.jsx</files>
  <action>
Create a new ServerPropertiesForm component:

1. Fetches current properties on mount using getServerProperties(serverId)
2. Renders form fields for editable properties:
   - MOTD (text input)
   - max-players (number input, range 1-1000)
   - gamemode (select: survival, creative, adventure, spectator)
   - difficulty (select: peaceful, easy, normal, hard)
   - level-seed (text input)
   - view-distance (number input)
   - simulation-distance (number input)
   
3. Real-time validation:
   - max-players: 1-1000, show error if out of range
   - gamemode: must be valid option
   - difficulty: must be valid option
   - Show validation errors below each field

4. Save button calls updateServerProperties(serverId, formData)
5. Show toast/alert on success or error

Styling: Use Tailwind classes matching existing Settings tab (gray-800, gray-700, text-white, etc.)
  </action>
  <verify>
<automated>ls -la app/src/features/servers/components/ServerPropertiesForm.jsx</automated>
  </verify>
  <done>ServerPropertiesForm renders with form fields, validates input, saves changes</done>
</task>

<task type="auto">
  <name>Task 4: Integrate ServerPropertiesForm into Settings tab</name>
  <files>app/src/pages/servers/ServerDetailsPage.jsx</files>
  <action>
In ServerDetailsPage.jsx, import ServerPropertiesForm:

```javascript
import ServerPropertiesForm from '../../features/servers/components/ServerPropertiesForm'
```

In the Settings tab section (lines 346-417), add ServerPropertiesForm after the Discord Webhook section and before Server Information:

Find the Settings tab content around line 346-417. After line 369 (after Discord webhook save button), add:

```jsx
<div className="mb-6">
  <h4 className="text-gray-400 text-sm mb-2">Server Properties</h4>
  <ServerPropertiesForm serverId={id} />
</div>
```

The form will replace or extend the existing settings area.
  </action>
  <verify>
<automated>grep -n "ServerPropertiesForm" app/src/pages/servers/ServerDetailsPage.jsx</automated>
  </verify>
  <done>ServerPropertiesForm renders in Settings tab, user can edit properties</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Properties JSON passes from frontend through REST API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Injection | server_properties | Properties values | mitigate | Validate all property values server-side, reject shell metacharacters |
| T-31-02 | Tampering | update_properties | mitigate | Only allow known property keys, validate value formats |
</threat_model>

<verification>
Backend: Start API, curl GET/PATCH /servers/:id/properties
Frontend: Visit server details, click Settings tab, verify form renders
</verification>

<success_criteria>
GET /servers/{id}/properties returns JSON with property values
PATCH /servers/{id}/properties writes to server.properties
ServerPropertiesForm displays in Settings tab
Validation shows errors for invalid values
Save persists changes to container
</success_criteria>

<output>
After completion, create `.planning/phases/31-settings-server-properties-yang-bisa-di-edit-seperti-form/31-01-SUMMARY.md`
</output>