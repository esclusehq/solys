---
phase: 85-perbaiki-edit-dan-delate-template
plan: 02
type: verify
wave: 1
depends_on:
  - 85-01
files_modified: []
autonomous: false
requirements: []
user_setup: []
must_haves:
  truths:
    - "Edit and delete flows work for official templates (owner/admin)"
    - "No regression for user-created templates"
    - "Error messages are accurate (404 vs 500)"
---

<objective>
Verify the fix from 85-01 is correct and no regressions exist.

Test owner/admin edit/delete flows for official templates, confirm non-existent IDs return 404, and verify user-created templates still work.
</objective>

<execution_context>
@/home/rhnbztnl/.config/opencode/get-shit-done/workflows/verify-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/85-perbaiki-edit-dan-delate-template/85-RESEARCH.md
@.planning/phases/85-perbaiki-edit-dan-delate-template/85-01-PLAN.md
</context>

<verification_tasks>

<task type="smoke">
<action>
1. Run `cargo build` in `api/` directory — verify compilation
2. Run `cargo test` in `api/` directory — verify existing tests pass
</action>
<expect>
- `cargo build` exits 0 with no warnings
- `cargo test` exits 0 with no failures
</expect>
</task>

<task type="smoke">
<action>
1. Start the API server locally
2. GET `/api/v1/templates/{uuid-of-known-official-template}` — verify 200 and template body
3. GET `/api/v1/templates/00000000-0000-0000-0000-000000000000` — verify 404
4. List `/api/v1/templates` — verify list still works (regression check)
</action>
<expect>
- Known official template returns 200 with display_name, id, etc.
- Non-existent UUID returns 404 with appropriate error message
- List returns all templates without errors
</expect>
</task>

<task type="smoke">
<action>
1. Authenticate as a user with `is_superadmin` or check the DB for user roles
2. PUT `/api/v1/templates/{official-template-uuid}` with updated fields — verify 200
3. DELETE `/api/v1/templates/{official-template-uuid}` — verify 200
</action>
<expect>
- PUT returns 200 with updated template
- DELETE returns 200 (or 204) — template is soft-deleted or removed
</expect>
</task>

<task type="regression">
<action>
1. Authenticate as a regular user
2. Create a new template via POST `/api/v1/templates/`
3. GET the newly created template by ID — verify 200
4. PUT the template — verify 200
5. DELETE the template — verify 200
</action>
<expect>
- All CRUD operations work for user-created templates (same as before)
</expect>
</task>

</verification_tasks>

<manual_checklist>
- [ ] `cargo build` exits 0
- [ ] `cargo test` exits 0
- [ ] Official template GET returns 200 (not 404)
- [ ] Non-existent template GET returns 404 (not 500)
- [ ] Official template edit (PUT) works for admin
- [ ] Official template delete works for admin
- [ ] User-created template CRUD still works
- [ ] Template listing still works
- [ ] No "Failed to load template" toast on valid official templates
</manual_checklist>

<rollback>
If verification fails:
1. Revert the handler error differentiation if it introduces false positives (e.g., map "QueryError" strings incorrectly)
2. Check if `version` column already exists on the target database — migration is idempotent via `IF NOT EXISTS`
3. If `version` is still missing, verify the migration ran: check `api/migrations/` for missing migration
</rollback>
