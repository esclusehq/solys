---
phase: 52-improve-api-docs
plan: 06
type: execute
wave: 2
depends_on:
  - 52-01
files_modified:
  - docs/api/nodes.md
  - docs/api/nodes/api-keys.md
  - docs/api/nodes/registration.md
  - docs/api/nodes/commands.md
  - docs/api/nodes/websocket.md
  - docs/api/billing.md
  - docs/api/billing/subscriptions.md
  - docs/api/billing/webhooks.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-03: Nodes endpoints (17+) and billing endpoints (5) documented across 8 pages"
    - "D-05: Field-level schema tables on every page"
    - "D-06: curl + Node.js SDK + Python SDK examples per endpoint"
    - "D-04: HTTP interface only, no proprietary implementation"
    - "Nodes page covers all node management endpoints (list, get, create, update, delete, online, status, metrics, health)"
    - "Node API keys page documents key generation, listing, revocation, and deletion"
    - "Node registration page documents registration tokens and node registration flow"
    - "Node commands page documents command queue and result endpoints"
    - "Node WebSocket page documents the node WebSocket connection protocol"
    - "Billing page covers checkout, plans, portal, and webhook endpoint"
    - "Billing subscriptions page documents subscription CRUD"
    - "Billing webhooks page documents webhook event types and security"
  artifacts:
    - path: "docs/api/nodes.md"
      provides: "Enhanced node management documentation"
    - path: "docs/api/billing.md"
      provides: "Enhanced billing documentation"
    - path: "docs/api/nodes/api-keys.md"
      provides: "Node API key management documentation"
    - path: "docs/api/nodes/registration.md"
      provides: "Node registration documentation"
    - path: "docs/api/nodes/commands.md"
      provides: "Node command queue documentation"
    - path: "docs/api/nodes/websocket.md"
      provides: "Node WebSocket protocol documentation"
    - path: "docs/api/billing/subscriptions.md"
      provides: "Subscription management documentation"
    - path: "docs/api/billing/webhooks.md"
      provides: "Billing webhook event documentation"
  key_links:
    - from: "nodes.md"
      to: "nodes/api-keys.md, nodes/registration.md, etc."
    - from: "nodes/websocket.md"
      to: "/api/overview WebSocket section"
    - from: "billing.md"
      to: "billing/subscriptions.md, billing/webhooks.md"
---

<objective>
Document all node management and billing API endpoints across 8 pages.

Purpose: Per D-03 (cover ALL public API endpoints). Nodes (17+ endpoints across 5 pages) and Billing (5 endpoints across 3 pages) are the second and third largest API groups. This plan enhances the existing nodes.md and billing.md and creates all their sub-pages.
Output: 2 enhanced pages + 6 new pages.
</objective>

<execution_context>
@/home/rhnbztnl/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rhnbztnl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-improve-api-docs/52-RESEARCH.md
@.planning/phases/52-improve-api-docs/52-PATTERNS.md
@docs/api/nodes.md (existing — to enhance)
@docs/api/billing.md (existing — to enhance)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance nodes.md and create nodes/api-keys.md + nodes/registration.md</name>

  <read_first>
    - file to be modified: docs/api/nodes.md (166 lines)
    - files to be created: docs/api/nodes/api-keys.md, docs/api/nodes/registration.md
    - RESEARCH.md for node endpoint inventory (list, get, create, update, delete, online, status, metrics, health x3, resources)
    - PATTERNS.md for node page pattern (lines 215-223)
  </read_first>

  <files>
    - docs/api/nodes.md (modified — enhanced)
    - docs/api/nodes/api-keys.md (new)
    - docs/api/nodes/registration.md (new)
  </files>

  <action>
    **File 1: Enhance `docs/api/nodes.md`**

    Restructure the existing 166-line page with the following changes:
    - Add code groups (curl + Node.js SDK + Python SDK) on all endpoint sections
    - Add query/path parameter tables
    - Add "Possible Errors" tables
    - Add links to sub-pages (api-keys, registration, commands, websocket)
    - Keep existing content (list, get, register, update, delete, status values, resources, heartbeat, quotas)
    - PRESERVE the Node Resources, Node Heartbeat, Quotas, and Node Status Values sections

    Enhance with the same template pattern used in Plan 04 (servers.md). The key endpoints should have code groups.

    **File 2: Create `docs/api/nodes/api-keys.md`**

    Document the node API key management endpoints. Prefix: `/api/v1/nodes/{id}/keys`.

    ```markdown
    # Node API Keys

    Manage node API keys for agent-to-API authentication. Node API keys use the `esk_` prefix and are used for node registration and communication.

    ## List API Keys

    ```http
    GET /api/v1/nodes/{id}/keys
    ```

    Retrieves all API keys for a specific node.

    ## Generate API Key

    ```http
    POST /api/v1/nodes/{id}/generate-key
    ```

    Creates a new API key for node authentication.

    ### Example Request

    ::: code-group

    ```bash [curl]
    curl -X POST https://api.esluce.com/api/v1/nodes/node_abc123/generate-key \
      -H "Authorization: Bearer ${ESCLUSE_API_KEY}"
    ```

    ```typescript [Node.js SDK]
    const key = await client.nodes.generateKey('node_abc123');
    ```

    ```python [Python SDK]
    key = client.nodes.generate_key('node_abc123')
    ```

    :::

    ### Example Response
    ```json
    {
      "data": {
        "id": "key_abc123",
        "key": "esk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
        "label": "default",
        "created_at": "2026-05-10T12:00:00Z"
      }
    }
    ```

    ## Revoke API Key

    ```http
    PUT /api/v1/nodes/{node_id}/keys/{key_id}/revoke
    ```

    Revokes an API key, immediately invalidating it.

    ## Delete API Key

    ```http
    DELETE /api/v1/nodes/{node_id}/keys/{key_id}
    ```

    Permanently removes an API key.

    ### Possible Errors

    | HTTP | Code | Description |
    |------|------|-------------|
    | 404 | `NODE_NOT_FOUND` | Node does not exist |
    | 401 | `NODE_KEY_INVALID` | API key not found or already revoked |

    ### Related Pages
    - [Authentication Guide](/api/auth) — How to use API keys for authentication
    - [Nodes API](/api/nodes) — Node management
    ```

    **File 3: Create `docs/api/nodes/registration.md`**

    Document registration token endpoints and the node registration flow. Prefix: `/api/v1/nodes/{id}/tokens` and `/api/v1/nodes/register`.

    ```markdown
    # Node Registration

    Register new compute nodes and manage registration tokens.

    ## Registration Flow

    1. Generate a registration token for a node: `POST /api/v1/nodes/{id}/tokens`
    2. On the target machine, run the agent with the registration token
    3. The agent connects to the API and completes registration
    4. The node appears in your node list

    ## List Registration Tokens

    ```http
    GET /api/v1/nodes/{id}/tokens
    ```

    ## Create Registration Token

    ```http
    POST /api/v1/nodes/{id}/tokens
    ```

    Creates a new registration token for node enrollment.

    ### Request Body
    ```json
    {
      "label": "Production Node 2"
    }
    ```

    ## Delete Registration Token

    ```http
    DELETE /api/v1/nodes/{id}/tokens/{token_id}
    ```

    ## Register Node

    ```http
    POST /api/v1/nodes/register
    ```

    Registers a new node using a registration token and API key. Called by the node agent during setup.

    ### Request Body
    ```json
    {
      "name": "My Node",
      "registration_token": "tok_abc123",
      "api_key": "esk_xxx"
    }
    ```

    ### Possible Errors

    | HTTP | Code | Description |
    |------|------|-------------|
    | 401 | `NODE_KEY_INVALID` | Invalid or revoked API key |
    | 404 | Token not found | Invalid registration token |

    ### Related Pages
    - [Nodes API](/api/nodes) — Node management
    - [Node API Keys](/api/nodes/api-keys) — API key management
    ```

  </action>

  <verify>
    <automated>
      grep -c "Generate API Key" docs/api/nodes/api-keys.md
    </automated>
  </verify>

  <done>
    nodes.md enhanced with code groups and sub-page links. api-keys.md and registration.md created with all relevant endpoints.
  </done>

  <acceptance_criteria>
    1. `grep -c "code-group" docs/api/nodes.md` >= 1 — code tabs added to existing page
    2. `grep -c "List API Keys" docs/api/nodes/api-keys.md` >= 1
    3. `grep -c "Generate API Key" docs/api/nodes/api-keys.md` >= 1
    4. `grep -c "Revoke API Key" docs/api/nodes/api-keys.md` >= 1
    5. `grep -c "Registration Flow" docs/api/nodes/registration.md` >= 1
    6. `grep -c "Register Node" docs/api/nodes/registration.md` >= 1
    7. `grep -c "Possible Errors" docs/api/nodes/api-keys.md` >= 1
    8. `grep -c "Related Pages" docs/api/nodes/registration.md` >= 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create nodes/commands.md and nodes/websocket.md</name>

  <read_first>
    - files to be created: docs/api/nodes/commands.md, docs/api/nodes/websocket.md (both new)
    - RESEARCH.md for node command and WebSocket endpoint inventory
  </read_first>

  <files>
    - docs/api/nodes/commands.md (new)
    - docs/api/nodes/websocket.md (new)
  </files>

  <action>
    Create TWO files for node command and WebSocket documentation.

    **File 1: `docs/api/nodes/commands.md`**

    ```markdown
    # Node Commands

    Manage the command queue for compute nodes. Commands are sent to nodes for execution by the agent.

    ## Queue Command

    ```http
    POST /api/v1/nodes/{id}/commands
    ```

    Queues a command for the node agent to execute. The agent picks up pending commands on its next poll cycle.

    ### Request Body
    ```json
    {
      "command": "restart_server",
      "params": {
        "server_id": "srv_abc123"
      }
    }
    ```

    ### Example Request

    ::: code-group

    ```bash [curl]
    curl -X POST https://api.esluce.com/api/v1/nodes/node_abc123/commands \
      -H "Authorization: Bearer ${ESCLUSE_API_KEY}" \
      -H "Content-Type: application/json" \
      -d '{"command": "restart_server", "params": {"server_id": "srv_abc123"}}'
    ```

    ```typescript [Node.js SDK]
    await client.nodes.queueCommand('node_abc123', {
      command: 'restart_server',
      params: { serverId: 'srv_abc123' }
    });
    ```

    ```python [Python SDK]
    client.nodes.queue_command('node_abc123',
      command='restart_server',
      params={'server_id': 'srv_abc123'}
    )
    ```

    :::

    ## Submit Command Result

    ```http
    POST /api/v1/nodes/{id}/commands/result
    ```

    Called by the node agent to submit the result of a completed command.

    ### Request Body
    ```json
    {
      "command_id": "cmd_abc123",
      "status": "completed",
      "output": "Server restarted successfully"
    }
    ```

    ### Possible Errors

    | HTTP | Code | Description |
    |------|------|-------------|
    | 404 | `NODE_NOT_FOUND` | Node does not exist |
    | 503 | `NODE_OFFLINE` | Node is offline (command result may still be processed) |

    ### Related Pages
    - [Nodes API](/api/nodes) — Node management
    - [Node WebSocket](/api/nodes/websocket) — Real-time node communication
    ```

    **File 2: `docs/api/nodes/websocket.md`**

    ```markdown
    # Node WebSocket Connection

    Nodes communicate with the Escluse API via a persistent WebSocket connection. This enables real-time command dispatch, heartbeat reporting, and event streaming.

    ## Connection

    ```http
    GET /api/ws/node
    ```

    Establish a WebSocket connection for node communication. Requires node API key authentication.

    ### Headers
    | Header | Value |
    |--------|-------|
    | `Authorization` | `Bearer esk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` |

    ### Authentication
    The node must authenticate using its `esk_` prefixed API key in the `Authorization` header during the WebSocket handshake.

    ## Message Format

    All WebSocket messages follow a JSON format:

    ```json
    {
      "type": "message_type",
      "data": { ... },
      "timestamp": "2026-05-10T12:00:00Z"
    }
    ```

    ## Node → API Messages

    | Type | Description | Frequency |
    |------|-------------|-----------|
    | `heartbeat` | Node resource usage and server statuses | Every 30 seconds |
    | `command_result` | Result of an executed command | On command completion |
    | `server_event` | Server status change (started, stopped, crashed) | On event |
    | `log_batch` | Batch of log entries from node servers | Configurable interval |

    ## API → Node Messages

    | Type | Description |
    |------|-------------|
    | `execute_command` | Instructs the node to execute a command |
    | `update_config` | Updates node configuration |
    | `ping` | Keep-alive ping (node should respond with `pong`) |

    ## Heartbeat

    The node sends a heartbeat every 30 seconds:

    ```json
    {
      "type": "heartbeat",
      "data": {
        "node_id": "node_xyz",
        "resources": {
          "cpu": { "used": 3.2, "total": 8, "usage_percent": 40 },
          "memory": { "used": "12GB", "total": "32GB", "usage_percent": 37.5 },
          "disk": { "used": "250GB", "total": "500GB", "usage_percent": 50 }
        },
        "servers": [
          { "id": "srv_abc", "status": "running", "cpu": 12.5, "memory": "2GB" }
        ]
      }
    }
    ```

    ## Reconnection

    If the WebSocket connection drops, the node should:
    1. Wait with exponential backoff (1s, 2s, 4s, 8s, max 60s)
    2. Reconnect using the same API key
    3. Resume normal operation

    ### Related Pages
    - [Nodes API](/api/nodes) — Node management
    - [Node API Keys](/api/nodes/api-keys) — API key management
    - [WebSocket Overview](/api/overview#websocket) — General WebSocket documentation
    ```

  </action>

  <verify>
    <automated>
      test -f docs/api/nodes/commands.md && test -f docs/api/nodes/websocket.md
    </automated>
  </verify>

  <done>
    commands.md documents queue + result endpoints. websocket.md documents connection, authentication, message formats, and reconnection protocol.
  </done>

  <acceptance_criteria>
    1. `grep -c "Queue Command" docs/api/nodes/commands.md` >= 1
    2. `grep -c "Submit Command Result" docs/api/nodes/commands.md` >= 1
    3. `grep -c "WebSocket Connection" docs/api/nodes/websocket.md` >= 1
    4. `grep -c "heartbeat" docs/api/nodes/websocket.md` >= 1 — heartbeat message documented
    5. `grep -c "Reconnection" docs/api/nodes/websocket.md` >= 1 — reconnection protocol
    6. `grep -c "Possible Errors" docs/api/nodes/commands.md` >= 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Enhance billing.md and create billing/subscriptions.md + billing/webhooks.md</name>

  <read_first>
    - file to be modified: docs/api/billing.md (181 lines)
    - files to be created: docs/api/billing/subscriptions.md, docs/api/billing/webhooks.md (both new)
    - RESEARCH.md for billing endpoint inventory
    - PATTERNS.md for billing page pattern (lines 226-236)
  </read_first>

  <files>
    - docs/api/billing.md (modified — enhanced with code groups and sub-page links)
    - docs/api/billing/subscriptions.md (new)
    - docs/api/billing/webhooks.md (new)
  </files>

  <action>
    **File 1: Enhance `docs/api/billing.md`**

    Restructure the existing 181-line page with:
    - Code groups on key endpoints (checkout, plans, portal)
    - Path/query parameter tables
    - "Possible Errors" tables
    - Links to sub-pages: subscriptions, webhooks
    - Keep existing content: Get Subscription, Plans (table), Create Checkout Session, Get Invoices
    - REMOVE Webhook Events section (lines 85-142) — moved to billing/webhooks.md
    - REMOVE Webhook Security section (lines 152-167) — moved to billing/webhooks.md
    - REMOVE Refund Eligibility table — keep or move to subscription page
    - Keep Usage Limits section

    **File 2: Create `docs/api/billing/subscriptions.md`**

    ```markdown
    # Subscriptions

    Manage your subscription plan and view subscription details.

    ## Current Subscription

    ```http
    GET /api/v1/billing/subscriptions/current
    ```

    Returns details about the currently active subscription.

    ### Example Response
    ```json
    {
      "data": {
        "id": "sub_abc123",
        "plan": "pro",
        "status": "active",
        "current_period_start": "2026-05-01T00:00:00Z",
        "current_period_end": "2026-06-01T00:00:00Z",
        "features": {
          "max_nodes": 3,
          "max_servers": 15,
          "modpack_support": true
        },
        "usage": {
          "servers": 3,
          "nodes": 1
        }
      }
    }
    ```

    ### Possible Errors
    | HTTP | Code | Description |
    |------|------|-------------|
    | 403 | `BIL_SUBSCRIPTION_EXPIRED` | Subscription has expired |

    ## Change Plan

    To upgrade or downgrade your subscription plan, use the [Create Checkout Session](/api/billing) endpoint with the desired plan ID.

    ## Cancel Subscription

    To cancel your subscription, visit the [Customer Portal](/api/billing#customer-portal).

    ### Related Pages
    - [Billing API](/api/billing) — Checkout, plans, and invoices
    - [Usage & Quotas](/api/usage) — Current resource usage
    - [Error Codes](/api/errors) — Complete error code reference
    ```

    **File 3: Create `docs/api/billing/webhooks.md`**

    Move the webhook events and security sections from the current billing.md (lines 85-167) here, then expand.

    ```markdown
    # Billing Webhooks

    Escluse sends webhook events for subscription and billing lifecycle changes. Configure your webhook endpoint in the dashboard.

    ## Webhook Events

    ### Subscription Created

    Sent when a new subscription is created after successful checkout.

    ```json
    {
      "event": "subscription_created",
      "data": {
        "subscription_id": "sub_abc123",
        "plan": "pro",
        "customer_email": "user@example.com"
      }
    }
    ```

    ### Subscription Updated

    Sent when a subscription plan changes or is renewed.

    ```json
    {
      "event": "subscription_updated",
      "data": {
        "subscription_id": "sub_abc123",
        "plan": "pro",
        "status": "active"
      }
    }
    ```

    ### Subscription Canceled

    Sent when a subscription is canceled.

    ```json
    {
      "event": "subscription_canceled",
      "data": {
        "subscription_id": "sub_abc123",
        "effective_date": "2026-06-01T00:00:00Z"
      }
    }
    ```

    ### Refund Processed

    Sent when a refund has been processed.

    ```json
    {
      "event": "refund_processed",
      "data": {
        "refund_id": "ref_xyz",
        "amount": 1500,
        "reason": "customer_request",
        "eligibility": {
          "type": "full",
          "days_since_subscription": 3
        }
      }
    }
    ```

    ## Webhook Security

    Webhook payloads are signed with HMAC-SHA256. Verify the signature before processing:

    ```javascript
    const crypto = require('crypto');

    function verifyWebhook(payload, signature, secret) {
      const hmac = crypto.createHmac('sha256', secret);
      const digest = hmac.update(payload).digest('hex');
      return crypto.timingSafeEqual(
        Buffer.from(digest),
        Buffer.from(signature)
      );
    }
    ```

    ### Possible Errors
    | HTTP | Code | Description |
    |------|------|-------------|
    | 400 | `BIL_WEBHOOK_SIGNATURE` | Webhook payload signature is invalid |

    ### Related Pages
    - [Billing API](/api/billing) — Checkout, plans, and invoices
    - [Error Codes](/api/errors) — Complete error code reference
    ```
  </action>

  <verify>
    <automated>
      test -f docs/api/billing/subscriptions.md && test -f docs/api/billing/webhooks.md
    </automated>
  </verify>

  <done>
    billing.md enhanced with code groups and sub-page links. subscriptions.md created. webhooks.md created with event types and security verification.
  </done>

  <acceptance_criteria>
    1. `grep -c "code-group" docs/api/billing.md` >= 1 — code tabs added
    2. `grep -c "Webhook Events" docs/api/billing.md` == 0 — webhook events moved to sub-page
    3. `grep -c "Current Subscription" docs/api/billing/subscriptions.md` >= 1
    4. `grep -c "Webhook Security" docs/api/billing/webhooks.md` >= 1
    5. `grep -c "Subscription Created" docs/api/billing/webhooks.md` >= 1
    6. `grep -c "Refund Processed" docs/api/billing/webhooks.md` >= 1
    7. `grep -c "crypto.timingSafeEqual" docs/api/billing/webhooks.md` >= 1 — webhook verification code
    8. `grep -c "Possible Errors" docs/api/billing/subscriptions.md` >= 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → reader | Static content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-13 | Information Disclosure | nodes/websocket.md | mitigate | WebSocket protocol details describe public API; no internal implementation or auth secrets exposed |
| T-52-14 | Information Disclosure | billing/webhooks.md | mitigate | Webhook secret verification code uses example values; no real webhook secrets documented |
</threat_model>

<verification>
1. All 8 files exist at expected paths
2. `cd docs && npm run docs:build` succeeds
3. Sub-pages link back to parent pages
4. Code examples use placeholder values only
</verification>

<success_criteria>
- [ ] `docs/api/nodes.md` enhanced with code groups + sub-page links
- [ ] `docs/api/nodes/api-keys.md` created
- [ ] `docs/api/nodes/registration.md` created
- [ ] `docs/api/nodes/commands.md` created
- [ ] `docs/api/nodes/websocket.md` created with protocol docs
- [ ] `docs/api/billing.md` enhanced (webhook sections moved to sub-pages)
- [ ] `docs/api/billing/subscriptions.md` created
- [ ] `docs/api/billing/webhooks.md` created (events + security)
</verification>

<output>
After completion, create `.planning/phases/52-improve-api-docs/52-06-SUMMARY.md`
</output>
