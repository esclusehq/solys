---
phase: 52-improve-api-docs
plan: 07
type: execute
wave: 2
depends_on:
  - 52-01
files_modified:
  - docs/api/webhooks.md
  - docs/api/alerts.md
  - docs/api/settings/s3.md
  - docs/api/settings/cloudflare.md
  - docs/api/templates/server.md
  - docs/api/templates/plugins.md
  - docs/api/templates/modpacks.md
  - docs/api/agents.md
  - docs/api/jobs.md
  - docs/api/usage.md
  - docs/api/runtimes.md
  - docs/api/deploy.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-03: Remaining endpoints (25+ across 9 resource groups) documented across 12 pages"
    - "D-05: Field-level schema tables on every page"
    - "D-06: curl + Node.js SDK + Python SDK examples per endpoint"
    - "D-04: HTTP interface only, no proprietary implementation"
    - "Webhooks page documents webhook CRUD endpoints"
    - "Alerts page documents alert rules CRUD + alert history"
    - "Settings pages document S3 and Cloudflare configuration endpoints"
    - "Templates pages document server, plugin, and modpack template endpoints"
    - "Agents page documents agent management endpoints"
    - "Jobs and Usage pages document job tracking and resource usage endpoints"
    - "Runtimes page documents available server runtimes"
    - "Deploy page documents Modrinth deploy endpoints"
  artifacts:
    - path: "docs/api/webhooks.md"
      provides: "Webhook CRUD documentation"
    - path: "docs/api/alerts.md"
      provides: "Alert rules + history documentation"
    - path: "docs/api/settings/s3.md"
      provides: "S3 backup configuration documentation"
    - path: "docs/api/settings/cloudflare.md"
      provides: "Cloudflare DNS configuration documentation"
    - path: "docs/api/templates/server.md"
      provides: "Server template documentation"
    - path: "docs/api/templates/plugins.md"
      provides: "Plugin template documentation"
    - path: "docs/api/templates/modpacks.md"
      provides: "Modpack template documentation"
    - path: "docs/api/agents.md"
      provides: "Agent management documentation"
    - path: "docs/api/jobs.md"
      provides: "Job management documentation"
    - path: "docs/api/usage.md"
      provides: "Usage and quotas documentation"
    - path: "docs/api/runtimes.md"
      provides: "Available runtimes documentation"
    - path: "docs/api/deploy.md"
      provides: "Deploy API documentation"
  key_links:
    - from: "each page"
      to: "/api/errors"
      via: "Possible Errors tables"
---

<objective>
Document the remaining API resource groups: webhooks, alerts, settings, templates, agents, jobs, usage, runtimes, and deploy.

Purpose: Per D-03 (cover ALL public API endpoints). These 12 pages cover ~25 endpoints across 9 resource groups that did not exist in the original 4-page docs structure.
Output: 12 new markdown pages.
</objective>

<execution_context>
@/home/rhnbztnl/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rhnbztnl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-improve-api-docs/52-RESEARCH.md
@.planning/phases/52-improve-api-docs/52-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create webhooks.md, alerts.md, settings/s3.md, settings/cloudflare.md</name>

  <read_first>
    - files to be created: docs/api/webhooks.md, docs/api/alerts.md, docs/api/settings/s3.md, docs/api/settings/cloudflare.md (all new)
    - RESEARCH.md for endpoint inventories (webhooks: 5, alerts: 5, settings: 3)
    - PATTERNS.md for template pattern
  </read_first>

  <files>
    - docs/api/webhooks.md (new)
    - docs/api/alerts.md (new)
    - docs/api/settings/s3.md (new)
    - docs/api/settings/cloudflare.md (new)
  </files>

  <action>
    Create FOUR files for webhooks, alerts, S3 settings, and Cloudflare settings.

    **File 1: `docs/api/webhooks.md`**

    ```markdown
    # Webhooks API

    Manage outgoing webhooks for receiving real-time notifications about server and billing events.

    ## List Webhooks

    ```http
    GET /api/v1/webhooks
    ```

    Retrieves all configured webhooks.

    ## Create Webhook

    ```http
    POST /api/v1/webhooks
    ```

    Creates a new webhook endpoint.

    ### Request Body
    ```json
    {
      "name": "Discord Notifications",
      "url": "https://discord.com/api/webhooks/...",
      "events": ["server.started", "server.stopped", "server.crashed"],
      "secret": "whsec_..."
    }
    ```

    ### Example Request

    ::: code-group

    ```bash [curl]
    curl -X POST https://api.esluce.com/api/v1/webhooks \
      -H "Authorization: Bearer ${ESCLUSE_API_KEY}" \
      -H "Content-Type: application/json" \
      -d '{"name": "Discord Notifications", "url": "https://discord.com/api/webhooks/...", "events": ["server.started", "server.stopped"]}'
    ```

    ```typescript [Node.js SDK]
    const webhook = await client.webhooks.create({
      name: 'Discord Notifications',
      url: 'https://discord.com/api/webhooks/...',
      events: ['server.started', 'server.stopped']
    });
    ```

    ```python [Python SDK]
    webhook = client.webhooks.create(
      name='Discord Notifications',
      url='https://discord.com/api/webhooks/...',
      events=['server.started', 'server.stopped']
    )
    ```

    :::

    ## Get Webhook

    ```http
    GET /api/v1/webhooks/{id}
    ```

    ## Update Webhook

    ```http
    PUT /api/v1/webhooks/{id}
    ```

    ## Delete Webhook

    ```http
    DELETE /api/v1/webhooks/{id}
    ```

    ### Possible Errors
    | HTTP | Code | Description |
    |------|------|-------------|
    | 400 | `VAL_INVALID_FORMAT` | Invalid webhook URL or event type |
    | 404 | `GEN_NOT_FOUND` | Webhook not found |

    ### Related Pages
    - [Billing Webhooks](/api/billing/webhooks) — Billing-specific webhook events
    - [Alerts API](/api/alerts) — Alert rule configuration
    - [Error Codes](/api/errors) — Complete error code reference
    ```

    **File 2: `docs/api/alerts.md`**

    ```markdown
    # Alerts API

    Configure alert rules and view alert history for monitoring server and node events.

    ## Create Alert Rule

    ```http
    POST /api/v1/alert-rules
    ```

    Creates a new alert rule with conditions and notification targets.

    ### Request Body
    ```json
    {
      "name": "High CPU Usage",
      "metric": "cpu_usage",
      "condition": "greater_than",
      "threshold": 90,
      "duration_seconds": 300,
      "actions": ["webhook", "email"]
    }
    ```

    ## List Alert Rules

    ```http
    GET /api/v1/alert-rules
    ```

    ## Get Alert Rule

    ```http
    GET /api/v1/alert-rules/{id}
    ```

    ## Update Alert Rule

    ```http
    PUT /api/v1/alert-rules/{id}
    ```

    ## Delete Alert Rule

    ```http
    DELETE /api/v1/alert-rules/{id}
    ```

    ## Alert History

    ```http
    GET /api/v1/alert-history
    ```

    Retrieves the history of triggered alerts, including resolved and unresolved alerts.

    ### Possible Errors
    | HTTP | Code | Description |
    |------|------|-------------|
    | 400 | `VAL_INVALID_FORMAT` | Invalid metric or condition value |
    | 404 | `GEN_NOT_FOUND` | Alert rule not found |

    ### Related Pages
    - [Webhooks API](/api/webhooks) — Webhook configuration
    - [Error Codes](/api/errors) — Complete error code reference
    ```

    **File 3: `docs/api/settings/s3.md`**

    ```markdown
    # S3 Storage Settings

    Configure S3-compatible storage for server backups. Supports any S3-compatible provider (AWS S3, Backblaze B2, Cloudflare R2, etc.).

    ## Get S3 Settings

    ```http
    GET /api/v1/settings/s3
    ```

    Retrieves the current S3 storage configuration (secrets are masked).

    ### Example Response
    ```json
    {
      "data": {
        "endpoint": "https://s3.us-east-1.amazonaws.com",
        "bucket": "my-escluse-backups",
        "region": "us-east-1",
        "access_key_id": "AKIA***WXYZ",
        "configured": true
      }
    }
    ```

    ## Update S3 Settings

    ```http
    PUT /api/v1/settings/s3
    ```

    Updates S3 storage configuration.

    ### Request Body
    ```json
    {
      "endpoint": "https://s3.us-east-1.amazonaws.com",
      "bucket": "my-escluse-backups",
      "region": "us-east-1",
      "access_key_id": "AKIAIOSFODNN7EXAMPLE",
      "secret_access_key": "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
    }
    ```

    ### Example Request

    ::: code-group

    ```bash [curl]
    curl -X PUT https://api.esluce.com/api/v1/settings/s3 \
      -H "Authorization: Bearer ${ESCLUSE_API_KEY}" \
      -H "Content-Type: application/json" \
      -d '{"endpoint": "https://s3.us-east-1.amazonaws.com", "bucket": "my-escluse-backups", "region": "us-east-1", "access_key_id": "AKIAIOSFODNN7EXAMPLE", "secret_access_key": "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"}'
    ```

    ```typescript [Node.js SDK]
    await client.settings.updateS3({
      endpoint: 'https://s3.us-east-1.amazonaws.com',
      bucket: 'my-escluse-backups',
      region: 'us-east-1',
      accessKeyId: 'AKIAIOSFODNN7EXAMPLE',
      secretAccessKey: 'wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY'
    });
    ```

    ```python [Python SDK]
    client.settings.update_s3(
      endpoint='https://s3.us-east-1.amazonaws.com',
      bucket='my-escluse-backups',
      region='us-east-1',
      access_key_id='AKIAIOSFODNN7EXAMPLE',
      secret_access_key='wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY'
    )
    ```

    :::

    ### Possible Errors
    | HTTP | Code | Description |
    |------|------|-------------|
    | 400 | `VAL_INVALID_FORMAT` | Invalid S3 endpoint or credentials format |
    | 403 | `GEN_FORBIDDEN` | Insufficient permissions |

    ### Related Pages
    - [Servers API](/api/servers) — Server management
    - [Error Codes](/api/errors) — Complete error code reference
    ```

    **File 4: `docs/api/settings/cloudflare.md`**

    ```markdown
    # Cloudflare DNS Settings

    Configure Cloudflare DNS integration for automatic domain-to-IP mapping for game servers.

    ## Get Cloudflare Settings

    ```http
    GET /api/v1/settings/cloudflare
    ```

    Retrieves the current Cloudflare DNS configuration (secrets are masked).

    ## Update Cloudflare Settings

    ```http
    PUT /api/v1/settings/cloudflare
    ```

    Updates Cloudflare DNS configuration.

    ### Request Body
    ```json
    {
      "api_token": "your_cloudflare_api_token",
      "zone_id": "your_cloudflare_zone_id",
      "auto_refresh": true
    }
    ```

    ### Example Request

    ::: code-group

    ```bash [curl]
    curl -X PUT https://api.esluce.com/api/v1/settings/cloudflare \
      -H "Authorization: Bearer ${ESCLUSE_API_KEY}" \
      -H "Content-Type: application/json" \
      -d '{"api_token": "your_cloudflare_api_token", "zone_id": "your_cloudflare_zone_id", "auto_refresh": true}'
    ```

    ```typescript [Node.js SDK]
    await client.settings.updateCloudflare({
      apiToken: 'your_cloudflare_api_token',
      zoneId: 'your_cloudflare_zone_id',
      autoRefresh: true
    });
    ```

    ```python [Python SDK]
    client.settings.update_cloudflare(
      api_token='your_cloudflare_api_token',
      zone_id='your_cloudflare_zone_id',
      auto_refresh=True
    )
    ```

    :::

    ## Test Cloudflare Connection

    ```http
    POST /api/v1/settings/cloudflare/test
    ```

    Tests the Cloudflare API connection with the configured credentials.

    ### Possible Errors
    | HTTP | Code | Description |
    |------|------|-------------|
    | 400 | `VAL_INVALID_FORMAT` | Invalid API token format |
    | 400 | Test failed | Cloudflare API connection test failed |

    ### Related Pages
    - [Nodes API](/api/nodes) — Node management (DNS is used for node-based servers)
    - [Error Codes](/api/errors) — Complete error code reference
    ```
  </action>

  <verify>
    <automated>
      for f in webhooks.md alerts.md settings/s3.md settings/cloudflare.md; do
        test -f "docs/api/$f" || exit 1
      done
    </automated>
  </verify>

  <done>
    Four resource pages created documenting webhook CRUD, alert rules + history, S3 settings, and Cloudflare settings. Each has code groups and error references.
  </done>

  <acceptance_criteria>
    1. `grep -c "Create Webhook" docs/api/webhooks.md` >= 1
    2. `grep -c "Create Alert Rule" docs/api/alerts.md` >= 1
    3. `grep -c "Alert History" docs/api/alerts.md` >= 1
    4. `grep -c "Get S3 Settings" docs/api/settings/s3.md` >= 1
    5. `grep -c "Update Cloudflare" docs/api/settings/cloudflare.md` >= 1
    6. `grep -c "Test Cloudflare" docs/api/settings/cloudflare.md` >= 1
    7. `grep -c "Possible Errors" docs/api/webhooks.md` >= 1
    8. `grep -c "code-group" docs/api/settings/s3.md` >= 1
    9. `grep -c "code-group" docs/api/settings/cloudflare.md` >= 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create templates/server.md, templates/plugins.md, templates/modpacks.md, and agents.md</name>

  <read_first>
    - files to be created: docs/api/templates/server.md, docs/api/templates/plugins.md, docs/api/templates/modpacks.md, docs/api/agents.md (all new)
    - RESEARCH.md for templates and agents endpoint inventory
  </read_first>

  <files>
    - docs/api/templates/server.md (new)
    - docs/api/templates/plugins.md (new)
    - docs/api/templates/modpacks.md (new)
    - docs/api/agents.md (new)
  </files>

  <action>
    Create FOUR files for templates and agents.

    **File 1: `docs/api/templates/server.md`**

    ```markdown
    # Server Templates

    Pre-configured server templates for quick deployment. Templates define the server configuration, game type, resource allocation, and options.

    ## List Server Templates

    ```http
    GET /api/v1/templates
    ```

    Retrieves all available server templates.

    ### Example Response
    ```json
    {
      "data": [
        {
          "id": "tpl_abc123",
          "name": "Minecraft Paper 1.20.4",
          "game_type": "minecraft",
          "description": "Optimized Minecraft server with PaperMC",
          "resources": { "cpu": 2, "memory": "4GB" }
        }
      ]
    }
    ```

    ### Example Request

    ::: code-group

    ```bash [curl]
    curl -X GET https://api.esluce.com/api/v1/templates \
      -H "Authorization: Bearer ${ESCLUSE_API_KEY}"
    ```

    ```typescript [Node.js SDK]
    const templates = await client.templates.list();
    ```

    ```python [Python SDK]
    templates = client.templates.list()
    ```

    :::

    ### Possible Errors
    | HTTP | Code | Description |
    |------|------|-------------|
    | 401 | `AUTH_TOKEN_INVALID` | Authentication required |

    ### Related Pages
    - [Plugin Templates](/api/templates/plugins) — Plugin template bundles
    - [Modpack Templates](/api/templates/modpacks) — Modpack configuration templates
    - [Servers API](/api/servers) — Server creation using templates
    - [Error Codes](/api/errors) — Complete error code reference
    ```

    **File 2: `docs/api/templates/plugins.md`**

    ```markdown
    # Plugin Templates

    Pre-configured plugin bundles that can be installed on servers with a single click.

    ## List Plugin Templates

    ```http
    GET /api/v1/plugin-templates
    ```

    Retrieves all available plugin template bundles.

    ### Example Response
    ```json
    {
      "data": [
        {
          "id": "ptpl_abc123",
          "name": "Essential Protections",
          "description": "Anti-grief, permissions, and protective plugins",
          "plugins": [
            { "id": "plg_001", "name": "WorldGuard" },
            { "id": "plg_002", "name": "GriefPrevention" }
          ]
        }
      ]
    }
    ```

    ### Related Pages
    - [Server Templates](/api/templates/server) — Server deployment templates
    - [Servers API - Plugins](/api/servers/plugins) — Per-server plugin management
    ```

    **File 3: `docs/api/templates/modpacks.md`**

    ```markdown
    # Modpack Templates

    Pre-configured modpack templates for modded Minecraft and other supported games.

    ## List Modpack Templates

    ```http
    GET /api/v1/modpack-templates
    ```

    Retrieves all available modpack templates.

    ### Example Response
    ```json
    {
      "data": [
        {
          "id": "mptpl_abc123",
          "name": "FTB Revelations",
          "game_type": "minecraft",
          "version": "1.12.2",
          "mod_count": 180,
          "description": "The flagship FTB modpack"
        }
      ]
    }
    ```

    ### Related Pages
    - [Server Templates](/api/templates/server) — Server deployment templates
    - [Error Codes](/api/errors) — Complete error code reference
    ```

    **File 4: `docs/api/agents.md`**

    ```markdown
    # Agents API

    Manage Solys agents deployed on compute nodes.

    ## List Agents

    ```http
    GET /api/v1/agents
    ```

    Retrieves all registered agents.

    ## Create Agent

    ```http
    POST /api/v1/agents
    ```

    Registers a new agent on a node.

    ### Request Body
    ```json
    {
      "node_id": "node_abc123",
      "version": "1.2.3"
    }
    ```

    ## Get Agent

    ```http
    GET /api/v1/agents/{id}
    ```

    ## Delete Agent

    ```http
    DELETE /api/v1/agents/{id}
    ```

    ## Available Agents

    ```http
    GET /api/v1/agents/available
    ```

    Lists available agent versions and download links for supported platforms.

    ### Example Response
    ```json
    {
      "data": {
        "versions": [
          {
            "version": "1.2.3",
            "platforms": {
              "linux/amd64": "https://get.esluce.com/agent/v1.2.3/solys-linux-amd64",
              "linux/arm64": "https://get.esluce.com/agent/v1.2.3/solys-linux-arm64",
              "windows/amd64": "https://get.esluce.com/agent/v1.2.3/solys-windows-amd64.exe"
            }
          }
        ]
      }
    }
    ```

    ### Possible Errors
    | HTTP | Code | Description |
    |------|------|-------------|
    | 404 | `NODE_NOT_FOUND` | Node does not exist |
    | 404 | `GEN_NOT_FOUND` | Agent not found |

    ### Related Pages
    - [Nodes API](/api/nodes) — Node management
    - [Node API Keys](/api/nodes/api-keys) — Agent authentication
    ```
  </action>

  <verify>
    <automated>
      for f in templates/server.md templates/plugins.md templates/modpacks.md agents.md; do
        test -f "docs/api/$f" || exit 1
      done
    </automated>
  </verify>

  <done>
    Four pages created documenting server/plugin/modpack templates and agent management. Each has endpoint documentation and cross-references.
  </done>

  <acceptance_criteria>
    1. `grep -c "Server Templates" docs/api/templates/server.md` >= 1
    2. `grep -c "Plugin Templates" docs/api/templates/plugins.md` >= 1
    3. `grep -c "Modpack Templates" docs/api/templates/modpacks.md` >= 1
    4. `grep -c "List Agents" docs/api/agents.md` >= 1
    5. `grep -c "Available Agents" docs/api/agents.md` >= 1 — agent download links
    6. `grep -c "Possible Errors" docs/api/agents.md` >= 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create jobs.md, usage.md, runtimes.md, and deploy.md</name>

  <read_first>
    - files to be created: docs/api/jobs.md, docs/api/usage.md, docs/api/runtimes.md, docs/api/deploy.md (all new)
    - RESEARCH.md for jobs, usage, runtimes, and deploy endpoint inventory
  </read_first>

  <files>
    - docs/api/jobs.md (new)
    - docs/api/usage.md (new)
    - docs/api/runtimes.md (new)
    - docs/api/deploy.md (new)
  </files>

  <action>
    Create FOUR files for remaining API resources.

    **File 1: `docs/api/jobs.md`**

    ```markdown
    # Jobs API

    Track asynchronous background jobs such as server deployment, backup restoration, and plugin installation.

    ## List Jobs

    ```http
    GET /api/v1/jobs
    ```

    Retrieves a list of all jobs, sorted by creation date.

    ### Query Parameters
    | Name | Type | Required | Default | Description |
    |------|------|----------|---------|-------------|
    | `status` | string | No | — | Filter by status (pending, running, completed, failed) |
    | `page` | integer | No | 1 | Page number |
    | `limit` | integer | No | 20 | Items per page |

    ## Get Job

    ```http
    GET /api/v1/jobs/{id}
    ```

    Retrieves details and current status of a specific job.

    ### Example Response
    ```json
    {
      "data": {
        "id": "job_abc123",
        "type": "server_deploy",
        "status": "running",
        "progress": 65,
        "created_at": "2026-05-10T12:00:00Z",
        "completed_at": null,
        "error": null
      }
    }
    ```

    ### Possible Errors
    | HTTP | Code | Description |
    |------|------|-------------|
    | 404 | `GEN_NOT_FOUND` | Job not found |

    ### Related Pages
    - [Servers API](/api/servers) — Server operations generate jobs
    - [Error Codes](/api/errors) — Complete error code reference
    ```

    **File 2: `docs/api/usage.md`**

    ```markdown
    # Usage & Quotas

    View current resource usage and plan quotas.

    ## Get Usage

    ```http
    GET /api/v1/usage
    ```

    Returns current resource usage across all servers and nodes.

    ### Example Response
    ```json
    {
      "data": {
        "servers": { "current": 3, "limit": 15 },
        "nodes": { "current": 1, "limit": 3 },
        "storage_gb": { "used": 25.5, "limit": 100 },
        "bandwidth_gb": { "used": 150, "limit": 1000, "period": "monthly" }
      }
    }
    ```

    ## Get Quotas

    ```http
    GET /api/v1/usage/quotas
    ```

    Returns detailed quota information per resource type.

    ### Possible Errors
    | HTTP | Code | Description |
    |------|------|-------------|
    | 403 | `BIL_SUBSCRIPTION_EXPIRED` | Subscription has expired |

    ### Related Pages
    - [Billing API](/api/billing) — Plan limits and upgrades
    - [Error Codes](/api/errors) — Complete error code reference
    ```

    **File 3: `docs/api/runtimes.md`**

    ```markdown
    # Runtimes API

    List available server runtimes and game types supported by the platform.

    ## List Runtimes

    ```http
    GET /api/v1/runtimes
    ```

    Retrieves all available server runtimes and their supported game versions.

    ### Example Response
    ```json
    {
      "data": [
        {
          "id": "minecraft",
          "name": "Minecraft",
          "versions": ["1.20.4", "1.20.1", "1.19.4"],
          "loaders": ["vanilla", "paper", "fabric", "forge"],
          "default_version": "1.20.4",
          "icon": "https://cdn.esluce.com/icons/minecraft.png"
        },
        {
          "id": "bedrock",
          "name": "Minecraft Bedrock",
          "versions": ["1.20.70", "1.20.60"],
          "loaders": ["vanilla"],
          "default_version": "1.20.70"
        }
      ]
    }
    ```

    ### Possible Errors
    | HTTP | Code | Description |
    |------|------|-------------|
    | 401 | `AUTH_TOKEN_INVALID` | Authentication required |

    ### Related Pages
    - [Servers API](/api/servers) — Create server with specific runtime
    - [Error Codes](/api/errors) — Complete error code reference
    ```

    **File 4: `docs/api/deploy.md`**

    ```markdown
    # Deploy API

    Manage Modrinth deployments and deployment projects. For per-server deployment, see [Server Deployment](/api/servers/deploy).

    ## List Deploy Projects

    ```http
    GET /api/v1/deploy/projects
    ```

    Retrieves all deployment projects.

    ## List Deploy Servers

    ```http
    GET /api/v1/deploy/servers
    ```

    Retrieves all servers configured as deployment targets.

    ### Possible Errors
    | HTTP | Code | Description |
    |------|------|-------------|
    | 401 | `AUTH_TOKEN_INVALID` | Authentication required |

    ### Related Pages
    - [Server Deployment](/api/servers/deploy) — Per-server deployment operations
    - [Error Codes](/api/errors) — Complete error code reference
    ```
  </action>

  <verify>
    <automated>
      for f in jobs.md usage.md runtimes.md deploy.md; do
        test -f "docs/api/$f" || exit 1
      done
    </automated>
  </verify>

  <done>
    Four pages created documenting jobs, usage/quotas, runtimes, and deploy API. Each has endpoint documentation with examples and error references.
  </done>

  <acceptance_criteria>
    1. `grep -c "List Jobs" docs/api/jobs.md` >= 1
    2. `grep -c "Get Usage" docs/api/usage.md` >= 1
    3. `grep -c "Get Quotas" docs/api/usage.md` >= 1
    4. `grep -c "List Runtimes" docs/api/runtimes.md` >= 1
    5. `grep -c "Deploy Projects" docs/api/deploy.md` >= 1 — or "List Deploy Projects"
    6. `grep -c "Deploy Servers" docs/api/deploy.md` >= 1 — or "List Deploy Servers"
    7. All 4 files exist: `ls docs/api/jobs.md docs/api/usage.md docs/api/runtimes.md docs/api/deploy.md 2>&1 | wc -l` returns 4
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → reader | Static content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-15 | Information Disclosure | settings/s3.md | mitigate | S3 credentials use AWS example values (AKIAIOSFODNN7EXAMPLE) — not real credentials per RESEARCH.md best practices |
| T-52-16 | Information Disclosure | settings/cloudflare.md | mitigate | Cloudflare API token uses placeholder `your_cloudflare_api_token` — no real tokens documented |
| T-52-17 | Information Disclosure | agents.md | mitigate | Agent download URLs use placeholder version and platform paths |
</threat_model>

<verification>
1. All 12 files exist at expected paths
2. `cd docs && npm run docs:build` succeeds
3. All example credentials are placeholders (grep for "AKIAIOSFODNN7EXAMPLE" is the one intentional AWS example)
4. No real API keys, tokens, or secrets in any page
</verification>

<success_criteria>
- [ ] `docs/api/webhooks.md` — webhook CRUD
- [ ] `docs/api/alerts.md` — alert rules + history
- [ ] `docs/api/settings/s3.md` — S3 config
- [ ] `docs/api/settings/cloudflare.md` — Cloudflare config
- [ ] `docs/api/templates/server.md` — server templates
- [ ] `docs/api/templates/plugins.md` — plugin templates
- [ ] `docs/api/templates/modpacks.md` — modpack templates
- [ ] `docs/api/agents.md` — agent management + download links
- [ ] `docs/api/jobs.md` — job tracking
- [ ] `docs/api/usage.md` — usage + quotas
- [ ] `docs/api/runtimes.md` — available runtimes
- [ ] `docs/api/deploy.md` — deploy API
</success_criteria>

<output>
After completion, create `.planning/phases/52-improve-api-docs/52-07-SUMMARY.md`
</output>
