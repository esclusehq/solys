---
phase: 52-improve-api-docs
plan: 08
type: execute
wave: 2
depends_on:
  - 52-01
files_modified:
  - docs/api/sdks/node.md
  - docs/api/sdks/python.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-11: SDK quickstart guides on docs site, full reference in GitHub repos"
    - "D-04: HTTP interface only, no proprietary implementation"
    - "Node.js SDK page covers installation, initialization, authentication, basic usage (2-3 operations), error handling, and links to GitHub"
    - "Python SDK page covers the same structure in Python syntax"
    - "Each SDK page follows the quickstart guide structure prescribed by D-11"
  artifacts:
    - path: "docs/api/sdks/node.md"
      provides: "Node.js SDK quickstart guide"
      contains: "npm install @escluse/sdk"
    - path: "docs/api/sdks/python.md"
      provides: "Python SDK quickstart guide"
      contains: "pip install escluse"
  key_links:
    - from: "sdks/node.md"
      to: "github.com/escluse/sdk-node"
      via: "link for full API reference"
    - from: "sdks/python.md"
      to: "github.com/escluse/sdk-python"
      via: "link for full API reference"
    - from: "overview.md"
      to: "sdks/node.md, sdks/python.md"
      via: "SDK section links"
---

<objective>
Create SDK quickstart guides for Node.js and Python.

Purpose: Per D-11 (SDK quickstarts on docs site, full reference in GitHub repos). These pages help developers get started with the Escluse SDKs quickly — installation, initialization, basic usage, error handling, and next steps.
Output: 2 new SDK quickstart pages.
</objective>

<execution_context>
@/home/rhnbztnl/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rhnbztnl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-improve-api-docs/52-CONTEXT.md (D-11)
@.planning/phases/52-improve-api-docs/52-RESEARCH.md (SDK Integration Patterns, lines 837-880)
@.planning/phases/52-improve-api-docs/52-PATTERNS.md (SDK page patterns, lines 261-280)

<interfaces>
## SDK Quickstart Structure (from RESEARCH.md lines 839-848)

Every SDK quickstart follows this structure:
1. Installation — package manager command
2. Initialization — API key setup, environment variables
3. Authentication — how to authenticate SDK calls
4. Basic Usage — 2-3 common operations (list servers, create server, check status)
5. Error Handling — SDK-specific error types, try/catch patterns
6. Next Steps — link to GitHub repo for full API reference

## SDK Code Pattern (from RESEARCH.md lines 849-878)

Node.js SDK:
```typescript
import { Escluse } from '@escluse/sdk';
const client = new Escluse({ apiKey: process.env.ESCLUSE_API_KEY });
const servers = await client.servers.list();
```

Python SDK:
```python
from escluse import Escluse
client = Escluse(api_key="your-api-key")
servers = client.servers.list()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Node.js SDK quickstart guide at sdks/node.md</name>

  <read_first>
    - file to be created: docs/api/sdks/node.md (new)
    - RESEARCH.md lines 837-878 for SDK integration patterns
    - PATTERNS.md lines 261-280 for SDK page structure
    - CONTEXT.md D-11 for SDK content scope
  </read_first>

  <files>
    - docs/api/sdks/node.md (new)
  </files>

  <action>
    Create `docs/api/sdks/node.md` following the quickstart guide structure (RESEARCH.md lines 839-848). This is a narrative/tutorial page, not an endpoint reference. Every code example must be clearly labeled and explained.

    ```markdown
    # Node.js SDK

    Official Node.js SDK for the Escluse API. Source code and full API reference available on [GitHub](https://github.com/escluse/sdk-node).

    ## Installation

    ```bash
    npm install @escluse/sdk
    ```

    Requires Node.js 18 or later.

    ## Initialization

    Create a client instance with your API key:

    ```typescript
    import { Escluse } from '@escluse/sdk';

    const client = new Escluse({
      apiKey: process.env.ESCLUSE_API_KEY
    });
    ```

    We recommend storing your API key in an environment variable rather than hardcoding it. The SDK will also read from the `ESCLUSE_API_KEY` environment variable automatically if no `apiKey` option is provided.

    ## Authentication

    The SDK handles authentication automatically. All requests include the required `Authorization: Bearer` header. 

    For user-facing applications, you can also use the `auth` module for login flows:

    ```typescript
    // Login with email/password
    const { session } = await client.auth.login({
      email: 'user@example.com',
      password: 'your-password'
    });

    // The SDK stores the session and automatically refreshes tokens
    ```

    ## Basic Usage

    ### List Servers

    ```typescript
    const { data: servers } = await client.servers.list();
    console.log(servers);
    ```

    ### Create a Server

    ```typescript
    const server = await client.servers.create({
      name: 'My Minecraft Server',
      gameType: 'minecraft',
      nodeId: 'node_xyz',
      resources: {
        cpu: 2,
        memory: '4GB'
      },
      options: {
        version: '1.20.4',
        mcLoader: 'paper',
        eulaAccepted: true
      }
    });

    console.log(`Server ${server.id} created with status: ${server.status}`);
    ```

    ### Get Node Status

    ```typescript
    const { data: node } = await client.nodes.get('node_xyz');
    console.log(`Node status: ${node.status}`);
    ```

    ## Error Handling

    The SDK throws typed errors for API failures:

    ```typescript
    import { EscluseError } from '@escluse/sdk';

    try {
      const servers = await client.servers.list();
    } catch (error) {
      if (error instanceof EscluseError) {
        console.error(`API Error [${error.code}]: ${error.message}`);
        // error.code: 'SRV_NOT_FOUND', 'AUTH_TOKEN_EXPIRED', etc.
        // error.status: HTTP status code
      } else {
        console.error('Network error:', error);
      }
    }
    ```

    Common error codes:

    | Code | Description |
    |------|-------------|
    | `AUTH_TOKEN_EXPIRED` | Access token expired — SDK should auto-refresh |
    | `SRV_NOT_FOUND` | Server ID does not exist |
    | `RATE_LIMITED` | Too many requests — implement retry with backoff |

    See the full [Error Code Catalog](/api/errors) for the complete list.

    ## Pagination

    List endpoints return paginated responses. The SDK provides pagination helpers:

    ```typescript
    // Automatic pagination
    for await (const page of client.servers.listPaginated()) {
      for (const server of page.data) {
        console.log(server.name);
      }
    }
    ```

    ## Next Steps

    - Full API reference: [github.com/escluse/sdk-node](https://github.com/escluse/sdk-node)
    - [Python SDK](/api/sdks/python) — Python SDK quickstart
    - [API Overview](/api/overview) — Base URL, rate limits, response format
    - [Error Codes](/api/errors) — Complete error code reference
    ```

    Key implementation notes:
    - The SDK might use `@escluse/sdk` or another package name — use that exact name
    - The client constructor might be `new Escluse()` or `new EscluseClient()` — use `Escluse` as documented in RESEARCH.md
    - Error class might be `EscluseError` or `ApiError` — use `EscluseError`
    - Pagination helper `listPaginated()` is speculative; if the SDK doesn't have it, use the regular `list()` method
    - All code examples use camelCase per JavaScript conventions (listed in CONVENTIONS.md)
  </action>

  <verify>
    <automated>
      grep -c "npm install" docs/api/sdks/node.md
    </automated>
  </verify>

  <done>
    Node.js SDK quickstart page created with installation, initialization, auth, basic usage, error handling, pagination, and next steps.
  </done>

  <acceptance_criteria>
    1. `grep -c "npm install" docs/api/sdks/node.md` >= 1 — installation command
    2. `grep -c "Escluse" docs/api/sdks/node.md` >= 3 — client class referenced
    3. `grep -c "servers.list\|servers.create\|nodes.get" docs/api/sdks/node.md` >= 2 — basic usage examples
    4. `grep -c "Error Handling" docs/api/sdks/node.md` >= 1 — error handling section
    5. `grep -c "github.com/escluse/sdk-node" docs/api/sdks/node.md` >= 1 — links to GitHub repo
    6. `grep -c "```typescript" docs/api/sdks/node.md` >= 3 — multiple TypeScript code blocks
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create Python SDK quickstart guide at sdks/python.md</name>

  <read_first>
    - file to be created: docs/api/sdks/python.md (new)
    - RESEARCH.md lines 871-878 for Python SDK patterns
    - PATTERNS.md lines 261-280 for SDK page structure
  </read_first>

  <files>
    - docs/api/sdks/python.md (new)
  </files>

  <action>
    Create `docs/api/sdks/python.md` following the same quickstart guide structure as the Node.js page, but adapted for Python syntax and conventions.

    ```markdown
    # Python SDK

    Official Python SDK for the Escluse API. Source code and full API reference available on [GitHub](https://github.com/escluse/sdk-python).

    ## Installation

    ```bash
    pip install escluse
    ```

    Requires Python 3.9 or later. We recommend using a virtual environment.

    ## Initialization

    Create a client instance with your API key:

    ```python
    from escluse import Escluse

    client = Escluse(api_key="your-api-key")
    ```

    We recommend storing your API key in an environment variable rather than hardcoding it:

    ```python
    import os
    from escluse import Escluse

    client = Escluse(api_key=os.environ["ESCLUSE_API_KEY"])
    ```

    ## Authentication

    The SDK handles authentication automatically using the API key provided during initialization. All requests include the required `Authorization: Bearer` header.

    ### User Authentication

    For user-facing applications, use the `auth` module:

    ```python
    # Login with email/password
    session = client.auth.login(
        email="user@example.com",
        password="your-password"
    )

    # The SDK stores the session and automatically refreshes tokens
    ```

    ## Basic Usage

    ### List Servers

    ```python
    servers = client.servers.list()
    for server in servers["data"]:
        print(f"{server['name']} — {server['status']}")
    ```

    ### Create a Server

    ```python
    server = client.servers.create(
        name="My Minecraft Server",
        game_type="minecraft",
        node_id="node_xyz",
        resources={"cpu": 2, "memory": "4GB"},
        options={
            "version": "1.20.4",
            "mc_loader": "paper",
            "eula_accepted": True
        }
    )

    print(f"Server {server['id']} created with status: {server['status']}")
    ```

    ### Get Node Status

    ```python
    node = client.nodes.get("node_xyz")
    print(f"Node status: {node['status']}")
    ```

    ## Error Handling

    The SDK raises typed exceptions for API failures:

    ```python
    from escluse import Escluse, EscluseError

    client = Escluse(api_key="your-api-key")

    try:
        servers = client.servers.list()
    except EscluseError as e:
        print(f"API Error [{e.code}]: {e.message}")
        # e.code: 'SRV_NOT_FOUND', 'AUTH_TOKEN_EXPIRED', etc.
        # e.status_code: HTTP status code
    except ConnectionError as e:
        print(f"Network error: {e}")
    ```

    Common error codes:

    | Code | Description |
    |------|-------------|
    | `AUTH_TOKEN_EXPIRED` | Access token expired — SDK should auto-refresh |
    | `SRV_NOT_FOUND` | Server ID does not exist |
    | `RATE_LIMITED` | Too many requests — implement retry with backoff |

    See the full [Error Code Catalog](/api/errors) for the complete list.

    ## Pagination

    List endpoints return paginated responses with pagination metadata:

    ```python
    response = client.servers.list(page=1, limit=20)
    servers = response["data"]
    pagination = response["pagination"]

    print(f"Page {pagination['page']} of {pagination['pages']}")
    ```

    ## Next Steps

    - Full API reference: [github.com/escluse/sdk-python](https://github.com/escluse/sdk-python)
    - [Node.js SDK](/api/sdks/node) — Node.js SDK quickstart
    - [API Overview](/api/overview) — Base URL, rate limits, response format
    - [Error Codes](/api/errors) — Complete error code reference
    ```

    Key implementation notes:
    - The client class is `Escluse` from `escluse` module
    - API key parameter is `api_key` (snake_case per Python conventions)
    - The `from escluse import Escluse` pattern is used in RESEARCH.md
    - Error class is `EscluseError` (matching Node.js pattern)
    - All code examples use snake_case per Python conventions
  </action>

  <verify>
    <automated>
      grep -c "pip install" docs/api/sdks/python.md
    </automated>
  </verify>

  <done>
    Python SDK quickstart page created with installation, initialization, auth, basic usage, error handling, pagination, and next steps.
  </done>

  <acceptance_criteria>
    1. `grep -c "pip install" docs/api/sdks/python.md` >= 1 — installation command
    2. `grep -c "from escluse import" docs/api/sdks/python.md` >= 1 — import shown
    3. `grep -c "client.servers.list\|client.servers.create\|client.nodes.get" docs/api/sdks/python.md` >= 2 — basic usage
    4. `grep -c "Error Handling" docs/api/sdks/python.md` >= 1 — error section
    5. `grep -c "```python" docs/api/sdks/python.md` >= 3 — multiple Python code blocks
    6. `grep -c "github.com/escluse/sdk-python" docs/api/sdks/python.md` >= 1 — links to GitHub repo
    7. Both SDK pages exist: `ls docs/api/sdks/node.md docs/api/sdks/python.md | wc -l` returns 2
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → reader | Static content; contains SDK code examples |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-18 | Information Disclosure | Both SDK pages | mitigate | All code examples use `process.env.ESCLUSE_API_KEY` or `"your-api-key"` placeholders — never real credentials |
| T-52-19 | Tampering | SDK code examples | accept | Code examples are illustrative; users should verify against actual SDK source. Full reference is in GitHub repos |
</threat_model>

<verification>
1. Both files exist at `docs/api/sdks/`
2. Each page follows the 6-section quickstart structure
3. Code examples use placeholder values only
4. Each page links to GitHub repo for full API reference
5. `cd docs && npm run docs:build` succeeds
</verification>

<success_criteria>
- [ ] `docs/api/sdks/node.md` — installation, init, auth, usage, error handling, next steps
- [ ] `docs/api/sdks/python.md` — same structure in Python
- [ ] Each page has 3+ code examples
- [ ] Each page links to GitHub repo
- [ ] `npm run docs:build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/52-improve-api-docs/52-08-SUMMARY.md`
</output>
