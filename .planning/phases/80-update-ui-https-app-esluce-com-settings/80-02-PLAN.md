---
phase: 80-update-ui-https-app-esluce-com-settings
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app/src/pages/settings/SettingsPage.jsx
  - app/src/pages/settings/ProfileSettings.jsx
  - app/src/pages/settings/ApiKeySettings.jsx
  - app/src/pages/settings/WebhookSettings.jsx
  - app/src/pages/settings/RestartDefaultsSettings.jsx
autonomous: true
requirements:
  - RESTYLE-01
  - RESTYLE-02
  - NO-CHANGE-01
user_setup: []

must_haves:
  truths:
    - "Tab bar uses cosmic theme: cyan active indicator, muted inactive tabs, cosmic border separator"
    - "Profile tab content appears inside glass-panel container with 4 sub-section groupings (Personal Information, Account Security, Login Activity, Danger Zone) separated by cosmic-themed dividers"
    - "All inputs across all tabs use cosmic theme: cosmic-card background, cosmic-cyan focus ring, rounded-xl, cosmic-border"
    - "All secondary buttons (Cancel) use cosmic-nebula background with cosmic-border and hover effect"
    - "All section containers use glass-panel with cosmic-border throughout all tabs"
    - "Status badges use cosmic /10 background pattern (cosmic-red/10, cosmic-green/10)"
    - "Tables use cosmic-border instead of border-gray-700"
    - "Danger Zone uses border-red-500/60 accent (not border-red-500)"
    - "All heading text uses --color-text-main, all labels use --color-text-muted"
  artifacts:
    - path: "app/src/pages/settings/SettingsPage.jsx"
      provides: "Cosmic-themed tab bar + page title"
      contains: "var(--color-cosmic-cyan)"
    - path: "app/src/pages/settings/ProfileSettings.jsx"
      provides: "Cosmic-themed profile content with 4 sub-sections and dividers"
      contains: "Personal Information"
    - path: "app/src/pages/settings/ProfileSettings.jsx"
      provides: "Danger Zone with border-red-500/60 accent"
      contains: "border-red-500/60"
    - path: "app/src/pages/settings/ApiKeySettings.jsx"
      provides: "Cosmic-themed API keys + team content"
      contains: "glass-panel"
    - path: "app/src/pages/settings/WebhookSettings.jsx"
      provides: "Cosmic-themed webhook cards"
      contains: "var(--color-cosmic-card)"
    - path: "app/src/pages/settings/RestartDefaultsSettings.jsx"
      provides: "Cosmic-themed restart defaults form"
      contains: "rounded-xl"
  key_links:
    - from: "ProfileSettings.jsx"
      to: "glss-panel wrapper"
      via: "outer div class"
      pattern: "glass-panel"
    - from: "ProfileSettings.jsx"
      to: "divider elements between sections"
      via: "border-t border-[var(--color-cosmic-border)]"
      pattern: "border-\\[var\\(--color-cosmic-border\\)\\]"
    - from: "Input elements in all 5 files"
      to: "cosmic input pattern"
      via: "bg-[var(--color-cosmic-card)] + focus:ring-[var(--color-cosmic-cyan)]"
      pattern: "focus:ring-\\[var\\(--color-cosmic-cyan\\)\\]"
---

<objective>
**Apply cosmic theme restyle to SettingsPage.jsx shell and all 4 extracted component files. Add Profile tab sub-section grouping with dividers in ProfileSettings.jsx.**

**Purpose:** Transform all flat `bg-gray-700`/`bg-gray-800` styling across the Settings page to the cosmic `glass-panel` theme. ProfileSettings.jsx gets structural sub-section grouping with dividers (Personal Information, Account Security, Login Activity, Danger Zone). All inputs, buttons, tables, badges, and containers use the cosmic CSS variable pattern documented in UI-SPEC.md.

**Output:** 5 files modified — all CSS class replacements applied, Profile tab content reorganized into 4 grouped sections with cosmic dividers.
</objective>

<execution_context>
@/home/rhnbztnl/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rhnbztnl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/src/pages/settings/SettingsPage.jsx
@app/src/pages/settings/ProfileSettings.jsx
@app/src/pages/settings/ApiKeySettings.jsx
@app/src/pages/settings/WebhookSettings.jsx
@app/src/pages/settings/RestartDefaultsSettings.jsx
@app/src/index.css

<interfaces>
<!-- CSS Class Replacement Reference — Complete mapping from UI-SPEC.md and RESEARCH.md -->

Cosmic CSS variables (defined in index.css @theme):
```css
--color-cosmic-card: rgba(255, 255, 255, 0.03);
--color-cosmic-border: rgba(255, 255, 255, 0.08);
--color-cosmic-cyan: #0ddff2;
--color-cosmic-green: #10b981;
--color-cosmic-red: #ef4444;
--color-cosmic-orange: #f59e0b;
--color-cosmic-blue: #3b82f6;
--color-nebula: #0d0f1a;
--color-text-main: #e2e8f0;
--color-text-muted: #64748b;
```

IMPORTANT: `--color-bg-secondary` does NOT exist in index.css. Use `--color-cosmic-card` for input backgrounds.

Complete CSS Class Replacement Table:

| # | Old Pattern | New Pattern | Applies To |
|---|------------|-------------|------------|
| 1 | `bg-gray-700 text-white rounded focus:outline-none focus:ring-2 focus:ring-blue-500` (inputs) | `bg-[var(--color-cosmic-card)] text-[var(--color-text-main)] rounded-xl border border-[var(--color-cosmic-border)] focus:outline-none focus:ring-2 focus:ring-[var(--color-cosmic-cyan)] focus:border-[var(--color-cosmic-cyan)] transition-colors` | All `<input>`, `<select>`, `<textarea>` |
| 2 | `bg-gray-600 text-gray-400 rounded cursor-not-allowed` (disabled) | `bg-[var(--color-nebula)] text-[var(--color-text-muted)] rounded-xl border border-[var(--color-cosmic-border)] cursor-not-allowed` | Disabled email input |
| 3 | `focus:ring-2 focus:ring-red-500` (danger input) | `focus:ring-2 focus:ring-[var(--color-cosmic-red)] focus:border-[var(--color-cosmic-red)]` | Delete confirmation inputs |
| 4 | `text-gray-400` (label/description) | `text-[var(--color-text-muted)]` | All `<label>`, descriptive text |
| 5 | `text-gray-500 text-xs` (helper) | `text-[var(--color-text-muted)] text-xs` | Helper text below inputs |
| 6 | `text-white` (heading) | `text-[var(--color-text-main)]` | All `<h3>` section headings |
| 7 | `text-lg font-medium text-white mb-4` | `text-lg font-semibold text-[var(--color-text-main)] mb-4` | Section headings (consolidate font-medium→semibold) |
| 8 | `text-lg font-semibold text-white mb-4` | `text-lg font-semibold text-[var(--color-text-main)] mb-4` | Section headings (already semibold, change color) |
| 9 | `text-blue-400` (tab active / link) | `text-[var(--color-cosmic-cyan)]` | Tab active text, transfer link |
| 10 | `border-b-2 border-blue-400` (tab active) | `border-b-2 border-[var(--color-cosmic-cyan)]` | Tab bar active indicator |
| 11 | `text-gray-400 hover:text-white` (tab inactive) | `text-[var(--color-text-muted)] hover:text-[var(--color-text-main)]` | Tab bar inactive tabs |
| 12 | `border-b border-gray-700` (separator) | `border-b border-[var(--color-cosmic-border)]` | Tab bar separator, table headers, table rows, dividers |
| 13 | `pt-6 border-t border-gray-700` (divider) | `pt-6 border-t border-[var(--color-cosmic-border)]` | Dividers between sub-sections |
| 14 | `border border-gray-700` | `border-b border-[var(--color-cosmic-border)]` | Table row bottom borders |
| 15 | `bg-gray-700 rounded-lg` (card) | `bg-[var(--color-cosmic-card)] border border-[var(--color-cosmic-border)] rounded-xl` | Webhook cards, S3 profile cards, empty state box |
| 16 | `p-3 bg-gray-700 rounded` (list item) | `p-3 bg-[var(--color-cosmic-card)] border border-[var(--color-cosmic-border)] rounded-xl` | API key items, team member items, session items |
| 17 | `bg-gray-700 rounded` (QR panel) | `bg-[var(--color-cosmic-card)] border border-[var(--color-cosmic-border)] rounded-xl` | 2FA QR code panel |
| 18 | `bg-gray-800 rounded` (danger panel) | `bg-[var(--color-nebula)] border border-red-500/60 rounded-xl` | Delete confirmation panel |
| 19 | `border-red-900` (danger panel) | `border-red-500/60` | Delete confirmation panel border |
| 20 | `border-l-4 border-red-500 pl-4` | `border-l-4 border-red-500/60 pl-4` | Danger zone left accent |
| 21 | `p-4 bg-green-900 border border-green-700 rounded` | `p-4 bg-[var(--color-cosmic-green)]/10 border border-[var(--color-cosmic-green)]/30 rounded-xl` | New API key reveal banner |
| 22 | `px-2 py-1 rounded text-xs bg-red-600 text-white` | `px-2 py-1 rounded-xl text-xs font-normal bg-[var(--color-cosmic-red)]/10 text-[var(--color-cosmic-red)]` | Failure badge |
| 23 | `px-2 py-1 rounded text-xs bg-green-600 text-white` | `px-2 py-1 rounded-xl text-xs font-normal bg-[var(--color-cosmic-green)]/10 text-[var(--color-cosmic-green)]` | Healthy badge |
| 24 | `px-2 py-1 \${r.bg} text-white text-xs rounded` (role badge) | `px-2 py-1 \${r.bg} text-white text-xs rounded-xl border border-[var(--color-cosmic-border)]` | getRoleBadge in ApiKeySettings |
| 25 | `bg-gray-600 text-white rounded hover:bg-gray-500` (gray button) | `bg-[var(--color-nebula)] text-[var(--color-text-main)] rounded-xl border border-[var(--color-cosmic-border)] hover:bg-[rgba(255,255,255,0.06)] transition-colors` | Cancel/Test secondary buttons |
| 26 | `bg-blue-600 ... rounded` (blue button) | Same but `rounded-xl transition-colors` | Primary action buttons |
| 27 | `bg-red-600 ... rounded` (red button) | Same but `rounded-xl transition-colors` | Destructive buttons |
| 28 | `bg-green-600 ... rounded` (green button) | Same but `rounded-xl transition-colors` | Success buttons |
| 29 | `bg-orange-600 ... rounded` (orange button) | Same but `rounded-xl transition-colors` | Retry webhook |
| 30 | `text-gray-400 border-b border-gray-700` (table header) | `text-[var(--color-text-muted)] border-b border-[var(--color-cosmic-border)]` | `<th>` rows |
| 31 | `border-b border-gray-700 hover:bg-[rgba(255,255,255,0.03)]` (table row) | `border-b border-[var(--color-cosmic-border)] hover:bg-[rgba(255,255,255,0.03)] transition-colors` | `<tr>` in tables |
| 32 | `h-10 bg-gray-700 rounded animate-pulse` (skeleton) | `h-10 bg-[var(--color-cosmic-card)] border border-[var(--color-cosmic-border)] rounded-xl animate-pulse` | Loading skeletons |
| 33 | `text-center py-8 text-gray-400` (empty) | `text-center py-8 text-[var(--color-text-muted)]` | Empty states |
| 34 | `text-gray-400` (loading text) | `text-[var(--color-text-muted)]` | Loading indicators |
| 35 | `rounded` (buttons/panels) | `rounded-xl` | All `<button>`, panels, cards |
| 36 | `text-2xl font-bold text-white mb-6` (page title) | `text-2xl font-semibold text-white mb-6` | Page title (font-bold→font-semibold) |
| 37 | `text-blue-400 hover:text-blue-300` (link) | `text-[var(--color-cosmic-cyan)] hover:opacity-80` | Transfer ownership link |
| 38 | `ml-3 text-sm text-blue-400 hover:text-blue-300` | `ml-3 text-sm text-[var(--color-cosmic-cyan)] hover:opacity-80` | Transfer ownership link (extended) |
| 39 | `text-green-400` (success) | `text-[var(--color-cosmic-green)]` | Success/green text |
| 40 | `text-red-400` (danger) | `text-[var(--color-cosmic-red)]` | Danger/red text |
| 41 | `text-yellow-400` (pending) | `text-[var(--color-cosmic-orange)]` | Pending warning text |
| 42 | `text-gray-300` (table body) | `text-[var(--color-text-main)]` | Table body text |
| 43 | `bg-gray-600 rounded-full` (avatar initials) | `bg-[var(--color-nebula)] border border-[var(--color-cosmic-border)] rounded-full` | Avatar initials div |
| 44 | `bg-gray-800` (avatar empty/uploading) | `bg-[var(--color-nebula)]` | Avatar empty/uploading state |
| 45 | `bg-gray-800` (API key code bg) | `bg-[var(--color-nebula)]` | API key code block |
| 46 | `px-4 py-2 bg-gray-700 text-white rounded focus:outline-none` (select) | `px-4 py-2 bg-[var(--color-cosmic-card)] text-[var(--color-text-main)] rounded-xl border border-[var(--color-cosmic-border)] focus:outline-none focus:ring-2 focus:ring-[var(--color-cosmic-cyan)]` | Select dropdown |
| 47 | `rounded bg-gray-800 border-gray-600` (checkbox) | `rounded bg-[var(--color-cosmic-card)] border-[var(--color-cosmic-border)]` | Checkbox input |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Cosmic restyle SettingsPage.jsx shell — tab bar + page title</name>
  <files>app/src/pages/settings/SettingsPage.jsx</files>
  <read_first>
    - app/src/pages/settings/SettingsPage.jsx — current shell version (post-Plan 80-01, ~70 lines)
    - app/src/index.css lines 4-19 (cosmic theme variables)
  </read_first>

  <acceptance_criteria>
    1. Page title uses `text-2xl font-semibold text-white mb-6` (font-bold→font-semibold)
    2. Tab bar separator: `border-b border-[var(--color-cosmic-border)]` replaces `border-b border-gray-700`
    3. Active tab: `text-[var(--color-cosmic-cyan)] border-b-2 border-[var(--color-cosmic-cyan)]` replaces `text-blue-400 border-b-2 border-blue-400`
    4. Inactive tab: `text-[var(--color-text-muted)] hover:text-[var(--color-text-main)]` replaces `text-gray-400 hover:text-white`
    5. Tab buttons have `transition-colors` class
    6. grep confirms ZERO `border-gray-700`, `text-blue-400`, `text-gray-400` in the file's tab bar section
  </acceptance_criteria>

  <action>
    Apply cosmic CSS class replacements to `app/src/pages/settings/SettingsPage.jsx`. This file is the shell — only tab bar and page title need updating.

    **Page title (line with `text-2xl font-bold text-white mb-6`):**
    Change `font-bold` to `font-semibold`:
    ```
    <h1 className="text-2xl font-semibold text-white mb-6">Settings</h1>
    ```

    **Tab bar container (`border-b border-gray-700 mb-6`):**
    ```
    <div className="flex gap-4 border-b border-[var(--color-cosmic-border)] mb-6">
    ```

    **Tab buttons — conditional classes:**
    Change the ternary expression inside `className`:
    ```javascript
    className={`pb-2 px-1 text-sm font-medium transition-colors ${
      activeTab === tab.id
        ? 'text-[var(--color-cosmic-cyan)] border-b-2 border-[var(--color-cosmic-cyan)]'
        : 'text-[var(--color-text-muted)] hover:text-[var(--color-text-main)]'
    }`}
    ```

    **Exact replacements:**
    1. `font-bold` → `font-semibold` (on the h1 page title)
    2. `border-gray-700` → `border-[var(--color-cosmic-border)]` (tab bar divider)
    3. `text-blue-400 border-b-2 border-blue-400` → `text-[var(--color-cosmic-cyan)] border-b-2 border-[var(--color-cosmic-cyan)]` (active tab)
    4. `text-gray-400 hover:text-white` → `text-[var(--color-text-muted)] hover:text-[var(--color-text-main)]` (inactive tab)
    5. Add `transition-colors` to each tab button className

    **Do NOT change:**
    - The page heading text "Settings" (keep as-is)
    - The component structure or rendering logic
    - Tab order or tab IDs
    - The conditional rendering of child components
  </action>

  <verify>
    <automated>grep -c 'border-\[var(--color-cosmic-border)\]' /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app/src/pages/settings/SettingsPage.jsx && grep -c 'text-\[var(--color-cosmic-cyan)\]' /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app/src/pages/settings/SettingsPage.jsx && grep -c 'text-\[var(--color-text-muted)\]' /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app/src/pages/settings/SettingsPage.jsx && grep 'font-semibold' /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app/src/pages/settings/SettingsPage.jsx</automated>
  </verify>

  <done>SettingsPage.jsx shell has cosmic-themed tab bar and page title — no old border-gray-700 or text-blue-400 remain</done>
</task>

<task type="auto">
  <name>Cosmic restyle ProfileSettings.jsx + add sub-section grouping with dividers</name>
  <files>app/src/pages/settings/ProfileSettings.jsx</files>
  <read_first>
    - app/src/pages/settings/ProfileSettings.jsx — entire current file (post-Plan 80-01, ~440 lines with flat sections and old CSS)
    - app/src/index.css lines 4-19 (cosmic theme variables)
  </read_first>

  <acceptance_criteria>
    1. Outer profile tab content wrapped in `<div className="glass-panel p-6 border border-[var(--color-cosmic-border)]">` (not flat space-y-6)
    2. Profile tab has 4 sub-sections with dividers: Personal Information, Account Security, Login Activity, Danger Zone
    3. Each sub-section divider uses: `pt-6 border-t border-[var(--color-cosmic-border)] mt-6`
    4. Danger zone uses `border-l-4 border-red-500/60 pl-4` (not border-red-500)
    5. All inputs use cosmic pattern: bg-[var(--color-cosmic-card)] + focus:ring-[var(--color-cosmic-cyan)] + rounded-xl + border
    6. No `bg-gray-700`, `bg-gray-800`, `border-gray-700`, `text-gray-400`, `text-blue-400`, `text-red-400`, `text-green-400` remain
    7. Security tab content (tab === 'security') also uses glass-panel + cosmic classes
    8. Section headings use `text-lg font-semibold text-[var(--color-text-main)] mb-4`
    9. "Login History" heading changed to "Login Activity"
    10. Password form heading changed to "Account Security" (replaces "Change Password" as section heading)
  </acceptance_criteria>

  <action>
    Apply complete cosmic restyle + restructure to ProfileSettings.jsx. This is the most complex task — it combines structural reorganization with CSS replacement.

    **Step 1: Add glass-panel wrapper around profile tab content:**
    
    When `tab === 'profile'`, replace the root div:
    ```jsx
    // BEFORE:
    <div className="space-y-6">
    
    // AFTER:
    <div className="glass-panel p-6 border border-[var(--color-cosmic-border)]">
      <div className="space-y-6">
    ```

    **Step 2: Reorganize into 4 sub-sections with dividers:**
    
    The current flat layout is: avatar → display name → profile info → change password → login history → danger zone.
    
    Reorganize into:

    **Section 1 — Personal Information** (avatar + display name + profile info):
    ```
    <div>
      {/* Avatar — unchanged content, cosmic classes */}
      {/* Display Name — cosmic classes */}
      {/* Profile Information — cosmic classes, with heading "Profile Information" */}
    </div>
    
    {/* Divider 1 */}
    <div className="pt-6 border-t border-[var(--color-cosmic-border)] mt-6">
    
    **Section 2 — Account Security** (change password form):
    ```
    <div>
      <h3 className="text-lg font-semibold text-[var(--color-text-main)] mb-4">Account Security</h3>
      {/* Password form — moved from between profile info and login history, cosmic classes */}
    </div>
    
    {/* Divider 2 */}
    <div className="pt-6 border-t border-[var(--color-cosmic-border)] mt-6">
    
    **Section 3 — Login Activity** (login history table):
    ```
    <div>
      <h3 className="text-lg font-semibold text-[var(--color-text-main)] mb-4">Login Activity</h3>
      {/* Login history table / empty state / error state — cosmic classes */}
    </div>
    
    {/* Divider 3 */}
    <div className="pt-6 border-t border-[var(--color-cosmic-border)] mt-6">
    
    **Section 4 — Danger Zone** (delete account + transfer):
    ```
    <div>
      <div className="border-l-4 border-red-500/60 pl-4">
        <h3 className="text-lg font-semibold text-[var(--color-cosmic-red)] mb-2">Danger Zone</h3>
        {/* Description + delete confirm + transfer — cosmic classes */}
      </div>
    </div>
    ```

    **Step 3: Apply cosmic CSS replacements throughout (use the complete mapping above):**

    For every element in renderProfileTab and renderSecurityTab:

    **Input fields:** Replace `bg-gray-700 text-white rounded focus:outline-none focus:ring-2 focus:ring-blue-500` with:
    ```
    bg-[var(--color-cosmic-card)] text-[var(--color-text-main)] rounded-xl border border-[var(--color-cosmic-border)] focus:outline-none focus:ring-2 focus:ring-[var(--color-cosmic-cyan)] focus:border-[var(--color-cosmic-cyan)] transition-colors
    ```

    **Disabled input:** Replace `bg-gray-600 text-gray-400 rounded cursor-not-allowed` with:
    ```
    bg-[var(--color-nebula)] text-[var(--color-text-muted)] rounded-xl border border-[var(--color-cosmic-border)] cursor-not-allowed
    ```

    **Danger inputs** (delete password, delete confirm): Keep `focus:ring-2 focus:ring-red-500` but change to:
    ```
    focus:ring-2 focus:ring-[var(--color-cosmic-red)] focus:border-[var(--color-cosmic-red)]
    ```
    Plus add the standard cosmic bg/border: `bg-[var(--color-cosmic-card)] border border-[var(--color-cosmic-border)]`

    **Section headings** (`h3` elements): Replace `text-white` with `text-[var(--color-text-main)]` AND consolidate `font-medium` to `font-semibold`:
    - `text-lg font-medium text-white mb-4` → `text-lg font-semibold text-[var(--color-text-main)] mb-4`
    - `text-lg font-semibold text-white mb-4` → `text-lg font-semibold text-[var(--color-text-main)] mb-4`

    **Labels (`label` elements):** Replace `text-gray-400` with `text-[var(--color-text-muted)]`

    **Helper text (`p` elements with text-gray-500 text-xs):** Replace `text-gray-500` with `text-[var(--color-text-muted)]`

    **Section dividers (`pt-6 border-t border-gray-700`):** Replace `border-gray-700` with `border-[var(--color-cosmic-border)]`

    **Blue action buttons:** Keep `bg-blue-600 hover:bg-blue-700`, change `rounded` to `rounded-xl transition-colors`
    
    **Red destructive buttons:** Keep `bg-red-600 hover:bg-red-700`, change `rounded` to `rounded-xl transition-colors`
    
    **Green success buttons:** Keep `bg-green-600 hover:bg-green-700`, change `rounded` to `rounded-xl transition-colors`
    
    **Gray secondary buttons (Cancel, Refresh):** Replace `bg-gray-600 text-white rounded hover:bg-gray-500` with:
    ```
    bg-[var(--color-nebula)] text-[var(--color-text-main)] rounded-xl border border-[var(--color-cosmic-border)] hover:bg-[rgba(255,255,255,0.06)] transition-colors
    ```

    **Loading skeletons:** Replace `h-10 bg-gray-700 rounded animate-pulse` with:
    ```
    h-10 bg-[var(--color-cosmic-card)] border border-[var(--color-cosmic-border)] rounded-xl animate-pulse
    ```

    **Empty state text:** Replace `text-center py-8 text-gray-400` with `text-center py-8 text-[var(--color-text-muted)]`

    **Table header row:** Replace `text-gray-400 border-b border-gray-700` with:
    ```
    text-[var(--color-text-muted)] border-b border-[var(--color-cosmic-border)]
    ```

    **Table body rows:** Replace `border-b border-gray-700 hover:bg-[rgba(255,255,255,0.03)]` with:
    ```
    border-b border-[var(--color-cosmic-border)] hover:bg-[rgba(255,255,255,0.03)] transition-colors
    ```

    **Table body text:** Replace `text-gray-300` with `text-[var(--color-text-main)]`

    **Status dots (green):** Keep `bg-green-500 rounded-full` as-is

    **Session list items:** Replace `p-3 bg-gray-700 rounded` with:
    ```
    p-3 bg-[var(--color-cosmic-card)] border border-[var(--color-cosmic-border)] rounded-xl
    ```

    **QR code panel:** Replace `p-4 bg-gray-700 rounded` with:
    ```
    p-4 bg-[var(--color-cosmic-card)] border border-[var(--color-cosmic-border)] rounded-xl
    ```

    **Danger Zone:**
    - Change `border-l-4 border-red-500 pl-4` to `border-l-4 border-red-500/60 pl-4`
    - Heading: `text-red-400` → `text-[var(--color-cosmic-red)]`
    - Description: `text-gray-400` → `text-[var(--color-text-muted)]`
    - Delete Account button: keep `bg-red-600 hover:bg-red-700`, add `rounded-xl transition-colors`

    **Delete confirmation panel:**
    - `bg-gray-800 rounded border border-red-900` → `bg-[var(--color-nebula)] border border-red-500/60 rounded-xl p-4`
    - Warning text: `text-sm text-red-300` → `text-sm text-[var(--color-cosmic-red)]`
    - Labels: `text-gray-400` → `text-[var(--color-text-muted)]`

    **Transfer ownership link:**
    - `text-blue-400 hover:text-blue-300` → `text-[var(--color-cosmic-cyan)] hover:opacity-80`
    - Also update the inline `ml-3 text-sm text-blue-400 hover:text-blue-300` variant

    **Colored text:**
    - `text-green-400` (Protected login history, 2FA status) → `text-[var(--color-cosmic-green)]`
    - `text-red-400` (danger links) → `text-[var(--color-cosmic-red)]`
    - `text-blue-400` (links) → `text-[var(--color-cosmic-cyan)]`

    **2FA enable/verify buttons:**
    - `bg-green-600 text-white rounded hover:bg-green-700` → keep bg, add `rounded-xl transition-colors`

    **Security tab section wrappers:**
    - Wrap each section in `glass-panel p-6 border border-[var(--color-cosmic-border)]` (the main space-y-6 div wraps them all)

    **Step 4: Apply cosmic restyle to Security tab content (when tab === 'security'):**

    Same mapping rules as above:
    - All inputs get cosmic pattern
    - All section headings get text-[var(--color-text-main)]
    - All labels get text-[var(--color-text-muted)]
    - Section dividers get cosmic-border
    - QR panel gets cosmic-card
    - Session list items get cosmic-card + cosmic-border

    **Critical constraints:**
    - The "Account Security" heading replaces the "Change Password" heading — remove the old "Change Password" heading text. The password form content stays the same, just the heading changes.
    - The "Login Activity" heading replaces "Login History" — change the heading text.
    - Password form currently has a `<div className="pt-6 border-t border-gray-700">` wrapper. In the new structure, this becomes the Account Security section content. No separate divider needed inside — the divider BETWEEN sections is the one before Account Security.
    - Do NOT change handler logic, state initialization, or component props.
    - The profile tab's outer `<div className="glass-panel p-6 border border-[var(--color-cosmic-border)]">` wraps ALL 4 sections, not each individual section.
    - Keep the `space-y-6` inside the glass-panel to maintain vertical spacing between the 4 sections.
    - Do NOT use `--color-bg-secondary` (doesn't exist in index.css).
  </action>

  <verify>
    <automated>grep -c 'glass-panel\|var(--color-cosmic-border)\|border-red-500/60\|Personal Information\|Account Security\|Login Activity\|Danger Zone' /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app/src/pages/settings/ProfileSettings.jsx && grep -c 'bg-gray-700\|bg-gray-800\|border-gray-700' /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app/src/pages/settings/ProfileSettings.jsx</automated>
  </verify>

  <done>ProfileSettings.jsx restyled with cosmic theme, reorganized into 4 sub-sections with dividers, danger zone uses border-red-500/60</done>
</task>

<task type="auto">
  <name>Cosmic restyle ApiKeySettings.jsx, WebhookSettings.jsx, and RestartDefaultsSettings.jsx</name>
  <files>
    app/src/pages/settings/ApiKeySettings.jsx,
    app/src/pages/settings/WebhookSettings.jsx,
    app/src/pages/settings/RestartDefaultsSettings.jsx
  </files>
  <read_first>
    - app/src/pages/settings/ApiKeySettings.jsx — current file (post-Plan 80-01)
    - app/src/pages/settings/WebhookSettings.jsx — current file (post-Plan 80-01)
    - app/src/pages/settings/RestartDefaultsSettings.jsx — current file (post-Plan 80-01)
    - app/src/index.css lines 4-19 (cosmic theme variables)
  </read_first>

  <acceptance_criteria>
    1. All 3 files have ZERO `bg-gray-7`, `border-gray-7`, `text-gray-4` (for labels/headings — keep for revoke/Delete text appropriately), `text-blue-400`, `text-red-400`, `text-green-400` references
    2. All container/wrapper divs use glass-panel or cosmic-card pattern
    3. All inputs use cosmic pattern (cosmic-card bg + cosmic-cyan focus ring + rounded-xl + border)
    4. All secondary buttons (Cancel) use cosmic-nebula pattern
    5. All section headings use `text-lg font-semibold text-[var(--color-text-main)] mb-4`
    6. getRoleBadge in ApiKeySettings has `rounded-xl` and `border border-[var(--color-cosmic-border)]`
    7. Health status badges in WebhookSettings use `/10` background pattern
    8. API key reveal banner uses `/10` green background
    9. All blue/red/green buttons have `rounded-xl transition-colors` added
    10. Webhook card items use cosmic-card + cosmic-border + rounded-xl
  </acceptance_criteria>

  <action>
    Apply cosmic CSS class replacements to all 3 component files. Use the complete mapping table from the interfaces section above.

    ---

    **File: `ApiKeySettings.jsx`**

    Apply replacements to both `tab === 'api'` and `tab === 'team'` render paths:

    **API tab section:**
    - Outer wrapper: wrap in `glass-panel p-6 border border-[var(--color-cosmic-border)]`
    - Section heading: `text-lg font-medium text-white mb-2` → `text-lg font-semibold text-[var(--color-text-main)] mb-2`
    - Description text: `text-gray-400 text-sm mb-4` → `text-[var(--color-text-muted)] text-sm mb-4`
    - API key reveal banner: Replace `p-4 bg-green-900 border border-green-700 rounded mb-4` with:
      ```
      p-4 bg-[var(--color-cosmic-green)]/10 border border-[var(--color-cosmic-green)]/30 rounded-xl mb-4
      ```
    - Banner heading: `text-green-400` → `text-[var(--color-cosmic-green)]`
    - Banner code block: `bg-gray-800 text-green-400` → `bg-[var(--color-nebula)] text-[var(--color-cosmic-green)]`
    - Copy button: `bg-green-700 text-white rounded hover:bg-green-600` → `bg-[var(--color-cosmic-green)]/20 text-[var(--color-cosmic-green)] rounded-xl hover:bg-[var(--color-cosmic-green)]/30 transition-colors`
    - Generate button: keep `bg-blue-600 hover:bg-blue-700`, add `rounded-xl transition-colors`
    - Section divider: `pt-6 border-t border-gray-700` → `pt-6 border-t border-[var(--color-cosmic-border)]`
    - "Your API Keys" heading: `text-lg font-medium text-white mb-4` → `text-lg font-semibold text-[var(--color-text-main)] mb-4`
    - API key list items: Replace `flex items-center justify-between p-3 bg-gray-700 rounded` with:
      ```
      flex items-center justify-between p-3 bg-[var(--color-cosmic-card)] border border-[var(--color-cosmic-border)] rounded-xl
      ```
    - Key name: `text-white text-sm` → `text-[var(--color-text-main)] text-sm`
    - Key metadata: `text-gray-400 text-xs` → `text-[var(--color-text-muted)] text-xs`
    - Revoked text: `text-red-400 text-xs` → `text-[var(--color-cosmic-red)] text-xs`
    - Delete button: keep `bg-red-600 text-white text-sm rounded hover:bg-red-500`, add `rounded-xl transition-colors`

    **Team tab section:**
    - Outer wrapper: wrap in `glass-panel p-6 border border-[var(--color-cosmic-border)]`
    - "Your Role" heading: `text-lg font-medium text-white mb-2` → `text-lg font-semibold text-[var(--color-text-main)] mb-2`
    - Role description: `text-gray-400 text-sm` → `text-[var(--color-text-muted)] text-sm`
    - getRoleBadge: Replace `px-2 py-1 \${r.bg} text-white text-xs rounded` with:
      ```
      px-2 py-1 ${r.bg} text-white text-xs rounded-xl border border-[var(--color-cosmic-border)]
      ```
    - Invite section divider: `pt-6 border-t border-gray-700` → `pt-6 border-t border-[var(--color-cosmic-border)]`
    - Invite heading: `text-lg font-medium text-white mb-4` → `text-lg font-semibold text-[var(--color-text-main)] mb-4`
    - Invite email input: Apply cosmic input pattern (bg-[var(--color-cosmic-card)] + focus ring + rounded-xl)
    - Role select: Replace `px-4 py-2 bg-gray-700 text-white rounded focus:outline-none` with:
      ```
      px-4 py-2 bg-[var(--color-cosmic-card)] text-[var(--color-text-main)] rounded-xl border border-[var(--color-cosmic-border)] focus:outline-none focus:ring-2 focus:ring-[var(--color-cosmic-cyan)]
      ```
    - Invite button: keep `bg-blue-600 hover:bg-blue-700`, add `rounded-xl transition-colors`
    - Team Members section divider: update border
    - Team Members heading: update to cosmic
    - Team member list items: Replace `flex items-center justify-between p-3 bg-gray-700 rounded` with cosmic pattern
    - Avatar initials: `w-8 h-8 bg-gray-600 rounded-full` → `w-8 h-8 bg-[var(--color-nebula)] border border-[var(--color-cosmic-border)] rounded-full`
    - Member name: `text-white text-sm` → `text-[var(--color-text-main)] text-sm`
    - Member email: `text-gray-400 text-xs` → `text-[var(--color-text-muted)] text-xs`
    - "(You)" text: `text-gray-400 text-xs ml-2` → `text-[var(--color-text-muted)] text-xs ml-2`
    - Pending status: `text-yellow-400 text-xs` → `text-[var(--color-cosmic-orange)] text-xs`
    - Remove button: `text-red-400 hover:text-red-300 text-sm ml-2` → `text-[var(--color-cosmic-red)] hover:text-[var(--color-cosmic-red)]/80 text-sm ml-2`
    - Role Permissions divider: update border
    - Role Permissions heading: update to cosmic
    - Table header: `text-gray-400 border-b border-gray-700` → `text-[var(--color-text-muted)] border-b border-[var(--color-cosmic-border)]`
    - Table body: `text-gray-300` → `text-[var(--color-text-main)]`
    - Table rows: `border-b border-gray-700` → `border-b border-[var(--color-cosmic-border)] hover:bg-[rgba(255,255,255,0.03)] transition-colors`

    ---

    **File: `WebhookSettings.jsx`**

    - Outer wrapper: wrap in `glass-panel p-6 border border-[var(--color-cosmic-border)]`
    - Section heading: `text-lg font-medium text-white mb-2` → `text-lg font-semibold text-[var(--color-text-main)] mb-2`
    - Description: `text-gray-400 text-sm mb-4` → `text-[var(--color-text-muted)] text-sm mb-4`
    - Loading state: `text-gray-400` → `text-[var(--color-text-muted)]`
    - Empty state: `text-gray-400 text-center py-8` → `text-[var(--color-text-muted)] text-center py-8`
    - Webhook card items: Replace `p-4 bg-gray-700 rounded-lg` with:
      ```
      p-4 bg-[var(--color-cosmic-card)] border border-[var(--color-cosmic-border)] rounded-xl
      ```
    - Webhook name: `text-white font-medium` → `text-[var(--color-text-main)] font-medium`
    - Webhook URL: `text-gray-400 text-sm font-mono truncate max-w-md` → `text-[var(--color-text-muted)] text-sm font-mono truncate max-w-md`
    - Health badge (failure): `px-2 py-1 rounded text-xs bg-red-600 text-white` → `px-2 py-1 rounded-xl text-xs font-normal bg-[var(--color-cosmic-red)]/10 text-[var(--color-cosmic-red)]`
    - Health badge (healthy): `px-2 py-1 rounded text-xs bg-green-600 text-white` → `px-2 py-1 rounded-xl text-xs font-normal bg-[var(--color-cosmic-green)]/10 text-[var(--color-cosmic-green)]`
    - Metadata text: `text-sm text-gray-400 mb-4` → `text-sm text-[var(--color-text-muted)] mb-4`
    - Failure time text: `text-red-400` → `text-[var(--color-cosmic-red)]`
    - Test button: keep `bg-blue-600 text-white text-sm rounded hover:bg-blue-700`, add `rounded-xl transition-colors`
    - Retry button: keep `bg-orange-600 text-white text-sm rounded hover:bg-orange-700`, add `rounded-xl transition-colors`
    - Section divider after heading: already handled by glass-panel wrapper

    ---

    **File: `RestartDefaultsSettings.jsx`**

    - Outer wrapper: wrap in `glass-panel p-6 border border-[var(--color-cosmic-border)]`
    - Section heading: `text-lg font-medium text-white mb-2` → `text-lg font-semibold text-[var(--color-text-main)] mb-2`
    - Description: `text-gray-400 text-sm mb-4` → `text-[var(--color-text-muted)] text-sm mb-4`
    - All input fields: Apply cosmic input pattern
    - Labels: `text-gray-400 text-sm mb-1` → `text-[var(--color-text-muted)] text-sm mb-1`
    - Helper text: `text-gray-500 text-xs mt-1` → `text-[var(--color-text-muted)] text-xs mt-1`
    - Loading state: `text-gray-400` → `text-[var(--color-text-muted)]`
    - Save button: keep `bg-blue-600 hover:bg-blue-700`, add `rounded-xl transition-colors`

    **Critical constraints for ALL 3 files:**
    - Use the EXACT replacement strings from the mapping table in the interfaces section
    - Do NOT change any handler logic, state variables, or component structure
    - All `rounded` on buttons → `rounded-xl` (keep `rounded-full` for avatars/dots)
    - All `bg-blue-600`, `bg-red-600`, `bg-green-600`, `bg-orange-600` button classes: keep the bg color, just add `rounded-xl transition-colors`
    - Do NOT use `--color-bg-secondary` — use `--color-cosmic-card`
    - Do NOT change any `<form onSubmit>`, `<button onClick>`, or `<input onChange>` handlers
    - Keep all `disabled` state classes and logic
    - Add `transition-colors` to ALL buttons (blue, red, green, orange, and secondary gray)
  </action>

  <verify>
    <automated>grep -c 'var(--color-cosmic\-)\|var(--color-text-main)\|var(--color-text-muted)\|glass-panel' /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app/src/pages/settings/ApiKeySettings.jsx 2>/dev/null; echo "---"; grep -c 'var(--color-cosmic\-)\|var(--color-text-main)\|var(--color-text-muted)\|glass-panel' /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app/src/pages/settings/WebhookSettings.jsx 2>/dev/null; echo "---"; grep -c 'var(--color-cosmic\-)\|var(--color-text-main)\|var(--color-text-muted)\|glass-panel' /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app/src/pages/settings/RestartDefaultsSettings.jsx 2>/dev/null; echo "---"; grep -c 'bg-gray-7\|border-gray-7' /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app/src/pages/settings/ApiKeySettings.jsx /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app/src/pages/settings/WebhookSettings.jsx /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app/src/pages/settings/RestartDefaultsSettings.jsx 2>/dev/null</automated>
  </verify>

  <done>All 3 component files restyled with cosmic theme — no bg-gray-7 or border-gray-7 remain, all inputs use cosmic pattern</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→Supabase Auth | Password/email changes and 2FA enrollment call Supabase Auth API. All authenticated via current session JWT. |
| Browser→API | Webhook test/retry calls through webhooksApi. Account deletion/transfer through authStore. JWT-secured. |
| JSX→DOM | All user data rendered as React text content. Avatar URLs as `<img src>`. No dangerouslySetInnerHTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-RESTYLE-01 | Tampering | CSS class replacements | mitigate | Only CSS class names changed — no handler logic, no state, no API calls modified. All visual changes are pure CSS. |
| T-80-RESTYLE-02 | Spoofing | Tab navigation | mitigate | Tab state is client-side only. No authorization decision depends on tab bar CSS styling. |
| T-80-RESTYLE-03 | Information Disclosure | Profile sub-section grouping | mitigate | Section dividers and headings are visual structure only. No data access or permission changes. |
</threat_model>

<verification>
## Task Verification

- Task 1: grep confirms SettingsPage.jsx uses cosmic tab bar classes, no old border-gray-700 or text-blue-400
- Task 2: grep confirms ProfileSettings.jsx has 4 sub-section headings, glass-panel, border-red-500/60, ZERO bg-gray-7/border-gray-7
- Task 3: grep confirms all 3 files use cosmic CSS variables, ZERO bg-gray-7/border-gray-7

## Cross-Cutting Checks
- [ ] `grep -r "bg-gray-7\|border-gray-7\|text-gray-4" app/src/pages/settings/` returns 0 (after accounting for legitimate text-gray-500 in helper text — these should also be replaced to text-[var(--color-text-muted)])
- [ ] `grep -r "focus:ring-blue-500" app/src/pages/settings/` returns 0
- [ ] `grep -r "var(--color-cosmic-cyan)" app/src/pages/settings/` present in all 5 files
- [ ] `grep -r "glass-panel" app/src/pages/settings/` present in all tab content files
- [ ] `grep -r "text-lg font-semibold text-\[var\(--color-text-main\)\]" app/src/pages/settings/` present (all section headings consolidated)
- [ ] `grep -r "rounded-xl" app/src/pages/settings/` present on all buttons and panels
- [ ] `grep -r "bg-\[var\(--color-cosmic-card\)\]" app/src/pages/settings/` present on all inputs
- [ ] `grep -r "text-\[var\(--color-text-muted\)\]" app/src/pages/settings/` present on all labels
- [ ] No `--color-bg-secondary` references in any file
- [ ] Danger Zone uses `border-red-500/60` not `border-red-500`
</verification>

<success_criteria>
- [ ] `grep -c 'bg-gray-7' app/src/pages/settings/*.jsx` = 0
- [ ] `grep -c 'border-gray-7' app/src/pages/settings/*.jsx` = 0
- [ ] `grep -c 'focus:ring-blue-500' app/src/pages/settings/*.jsx` = 0
- [ ] `grep -c 'text-gray-400' app/src/pages/settings/*.jsx` = 0 (labels, descriptions, empty states)
- [ ] `grep -c 'text-blue-400' app/src/pages/settings/*.jsx` = 0
- [ ] `grep -c 'glass-panel' app/src/pages/settings/*.jsx` ≥ 5 (one per tab content area)
- [ ] ProfileSettings.jsx has EXACTLY 3 divider elements: `border-t border-[var(--color-cosmic-border)]`
- [ ] ProfileSettings.jsx has all 4 sub-section headings
- [ ] All button elements have `rounded-xl` (not `rounded`)
- [ ] Danger zone uses `border-red-500/60` in ProfileSettings.jsx
- [ ] Transferred ownership link uses `text-[var(--color-cosmic-cyan)]` not `text-blue-400`
</success_criteria>

<output>
After completion, create `.planning/phases/80-update-ui-https-app-esluce-com-settings/80-02-SUMMARY.md`
</output>
