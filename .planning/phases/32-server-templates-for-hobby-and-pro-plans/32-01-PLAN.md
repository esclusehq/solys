---
phase: 32-server-templates-for-hobby-and-pro-plans
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/domain/server/mod.rs
  - api/src/domain/server/template/mod.rs
  - api/src/domain/server/template/model.rs
  - api/src/domain/server/template/repository.rs
  - api/src/presentation/routes/mod.rs
  - api/src/presentation/handlers/mod.rs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Templates are stored in database with game type and variant support"
    - "API provides endpoint to fetch available templates"
    - "Templates include default image, port, environment variables"
  artifacts:
    - path: "api/src/domain/server/template/model.rs"
      provides: "Template entity with game_type, variant, image, port, env"
      min_lines: 60
    - path: "api/src/domain/server/template/repository.rs"
      provides: "Template CRUD operations"
      exports: ["list_templates", "get_template_by_id"]
    - path: "api/src/presentation/routes/mod.rs"
      provides: "Template API route registration"
  key_links:
    - from: "template/model.rs"
      to: "game_type.rs"
      via: "GameType identifier used as foreign key"
      pattern: "game_type.*identifier"
---

<objective>
Create backend template system to store pre-configured server templates per game type and variant.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@api/src/domain/server/entities/game_type.rs
@api/src/domain/server/model.rs
@api/src/domain/server/sqlx_repository.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create template domain module</name>
  <files>api/src/domain/server/template/mod.rs</files>
  <action>
Create new module at api/src/domain/server/template/mod.rs with exports for model and repository.

```rust
pub mod model;
pub mod repository;

pub use model::*;
pub use repository::*;
```
  </action>
  <verify>File exists with module exports</verify>
  <done>Template module created and exports available</done>
</task>

<task type="auto">
  <name>Task 2: Create template model/entity</name>
  <files>api/src/domain/server/template/model.rs</files>
  <action>
Create template model with fields:
- id: Uuid (primary key)
- game_type: String (minecraft, palworld, rust, valheim)
- variant: String (vanilla, forge, fabric, paper, spigot)
- display_name: String
- description: Option<String>
- docker_image: String
- default_port: i32
- default_env: JSONB (environment variables)
- default_startup_command: Option<String>
- is_active: bool
- created_at, updated_at: NaiveDateTime

Include FromRow derive for sqlx, Serialize/Deserialize for JSON.

Implement fallback method that returns hardcoded templates when DB is empty:
- Minecraft variants: vanilla, paper, spigot, forge, fabric
- Palworld: default
- Rust: default
- Valheim: default
  </action>
  <verify>File compiles, model has all required fields</verify>
  <done>Template model with database fields and fallback method</done>
</task>

<task type="auto">
  <name>Task 3: Create template repository</name>
  <files>api/src/domain/server/template/repository.rs</files>
  <action>
Create template repository trait and implementation:
- list_templates() -> Vec<Template>
- list_templates_by_game(game_type: &str) -> Vec<Template>
- get_template(game_type: &str, variant: &str) -> Option<Template>

Use sqlx::query_as for async DB queries. Include fallback to hardcoded templates if DB query returns empty.
  </action>
  <verify>File compiles, repository has required methods</verify>
  <done>Template repository with async DB operations and fallback</done>
</task>

<task type="auto">
  <name>Task 4: Register template module in server domain</name>
  <files>api/src/domain/server/mod.rs</files>
  <action>
Add "pub mod template;" to api/src/domain/server/mod.rs to expose the template module.
Also add re-exports: pub use template::{Template, TemplateRepository};
  </action>
  <verify>Module compiles in server domain</verify>
  <done>Template module accessible from server domain</done>
</task>

<task type="auto">
  <name>Task 5: Add template API routes</name>
  <files>api/src/presentation/routes/mod.rs</files>
  <action>
Add template routes to the API:
- GET /api/v1/templates - list all templates
- GET /api/v1/templates?game_type={type} - filter by game type

Create handler in a new file or add to existing server handlers that uses TemplateRepository to fetch templates and returns them as JSON.
  </action>
  <verify>Routes compile, endpoint responds to GET /api/v1/templates</verify>
  <done>Template API endpoint available</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Database | Untrusted input (query params) filtered in handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Injection | template query | mitigate | Use parameterized queries via sqlx, no raw SQL |
| T-32-02 | Information Disclosure | template endpoint | accept | Templates are non-sensitive, no auth required |
</threat_model>

<verification>
[ ] Backend compiles: `cd api && cargo check`
[ ] Template module accessible: `use crate::domain::server::template::Template`
[ ] Routes registered: check router includes template routes
</verification>

<success_criteria>
Templates can be fetched via API endpoint, database model supports game type + variant, fallback templates available when DB is empty
</success_criteria>

<output>
After completion, create `.planning/phases/32-server-templates-for-hobby-and-pro-plans/32-01-SUMMARY.md`
</output>