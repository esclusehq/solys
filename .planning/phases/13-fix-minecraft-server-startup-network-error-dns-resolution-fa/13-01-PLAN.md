---
phase: 13
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web-agent/src/handlers/runtime.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Minecraft server containers can resolve DNS for mojang.com"
    - "Server startup completes without DNS resolution errors"
  artifacts:
    - path: "web-agent/src/handlers/runtime.rs"
      contains: "network_mode: Some(\"bridge\".to_string())"
  key_links: []
---

<objective>
Verify the DNS resolution fix for Minecraft server containers.

Purpose: The bug was fixed by adding `network_mode: Some("bridge")` to HostConfig. This plan verifies the fix works by deploying a test Minecraft server.

Output: Verified working container with network attached
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/debug/minecraft-dns-network-error.md

# Fix already applied
The code change is already in place at web-agent/src/handlers/runtime.rs:
- Line 142: network_mode: Some("bridge".to_string()) in handle_create
- Line 284: network_mode: Some("bridge".to_string()) in handle_start
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify DNS fix by testing Minecraft server creation</name>
  <files>web-agent/src/handlers/runtime.rs</files>
  <read_first>
- web-agent/src/handlers/runtime.rs (lines 135-160, 275-295 for the fix locations)
- .planning/debug/minecraft-dns-network-error.md (context about the bug)
  </read_first>
  <action>
Verify the bridge network fix is properly applied by:
1. Confirm code compiles: `cd web-agent && cargo check`
2. Use existing debugging/test infrastructure to create a test Minecraft server container
3. Check container network status via Docker API - verify it has networkMode="bridge"
4. Test DNS resolution inside the container (resolve mojang.com)
5. Verify server startup completes without DNS errors in logs
  </action>
  <verify>
<automated>cd web-agent && cargo check 2>&1 | grep -E "(error|warning)" | head -20</automated>
  </verify>
  <done>
- Code compiles without errors
- Test container created with bridge network
- DNS resolution works (container can reach mojang.com)
- Server starts without DNS errors
</done>
</task>

</tasks>

<verification>
1. Compile web-agent: `cargo check`
2. Create test Minecraft container via API
3. Verify container has network attached (check Docker inspect)
4. Test DNS inside container: `docker exec <container> nslookup launchermeta.mojang.com`
5. Check server logs for successful startup
</verification>

<success_criteria>
- web-agent compiles successfully
- Container created with bridge network mode
- DNS resolution succeeds for mojang.com
- Minecraft server starts without DNS errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-fix-minecraft-server-startup-network-error-dns-resolution-fa/13-01-SUMMARY.md`
</output>