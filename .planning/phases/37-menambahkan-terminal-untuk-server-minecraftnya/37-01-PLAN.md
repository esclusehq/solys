---
phase: 37
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/Terminal.jsx
  - app/src/pages/ServerDetailPage.jsx
autonomous: true
requirements: []

must_haves:
  truths:
    - User can see live console output from game server in browser
    - User can type and send commands to game server via RCON
    - Command history persists and can be recalled with arrow keys
    - Terminal uses dark theme matching app design
    - WebSocket reconnects automatically on disconnect
  artifacts:
    - path: "app/src/components/Terminal.jsx"
      provides: "xterm.js terminal emulator component"
      min_lines: 100
    - path: "app/src/hooks/useTerminal.js"
      provides: "WebSocket connection and RCON command hook"
      min_lines: 50
    - path: "api/src/presentation/handlers/terminal_ws_handler.rs"
      provides: "WebSocket handler for terminal streaming"
      exports: ["websocket", "terminal"]
  key_links:
    - from: "app/src/components/Terminal.jsx"
      to: "app/src/hooks/useTerminal.js"
      via: "useTerminal hook"
    - from: "app/src/hooks/useTerminal.js"
      to: "api endpoint"
      via: "WebSocket connection"
    - from: "ServerDetailPage.jsx"
      to: "Terminal.jsx"
      via: "Terminal tab panel"
---

<objective>
Create in-browser terminal emulator for game servers using xterm.js with WebSocket live output and RCON command input.

Purpose: Allow users to view real-time console output and send commands to their game servers directly from the browser, similar to an in-game console but accessible via web.

Output: Terminal component, WebSocket hook, server detail page integration
</objective>

<context>
@.planning/phases/37-menambahkan-terminal-untuk-server-minecraftnya/37-CONTEXT.md
@.planning/STATE.md
@app/src/components/PluginManager.jsx
@api/src/presentation/handlers/rcon_handlers.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Terminal component with xterm.js</name>
  <files>app/src/components/Terminal.jsx</files>
  <action>
    Create Terminal component using xterm.js:
    - Import xterm.js and xterm-addon-fit for terminal emulation
    - Use dark theme matching app design (match .glass-panel colors)
    - Implement terminal output rendering with proper ANSI support
    - Add input field for commands below terminal area
    - Include command history navigation (arrow up/down)
    - Add command autocomplete for common Minecraft commands (list, help, tp, give, kill, etc.)
    - Implement clear terminal command
    - Set proper height (400px min) and responsive width

    Use @monaco-editor/react pattern from codebase for integrating xterm.js.
    Follow component structure from PluginManager.jsx for consistency.
  </action>
  <verify>
    <automated>ls -la app/src/components/Terminal.jsx && grep -l "xterm" app/src/components/Terminal.jsx</automated>
  </verify>
  <done>Terminal component renders xterm.js in browser with dark theme, handles user input, displays output</done>
</task>

<task type="auto">
  <name>Task 2: Create useTerminal hook for WebSocket/RCON</name>
  <files>app/src/hooks/useTerminal.js</files>
  <action>
    Create useTerminal hook for managing terminal connection:
    - Connect to WebSocket endpoint at /ws/terminal/{server_id}
    - Handle incoming stream data (stdout/stderr from game server)
    - Send commands via WebSocket message (to be forwarded via RCON)
    - Implement auto-reconnect with exponential backoff (start: 1s, max: 30s)
    - Store command history in localStorage (key: terminal_history_{server_id})
    - Limit history to 50 commands max
    - Add disconnect/reconnect state management

    Follow pattern from useWebSocket.js in codebase if it exists.
  </action>
  <verify>
  <automated>ls -la app/src/hooks/useTerminal.js && grep -l "WebSocket\|reconnect" app/src/hooks/useTerminal.js</automated>
  </verify>
  <done>Hook provides connect, disconnect, sendCommand, and exposes output stream and connection status</done>
</task>

<task type="auto">
  <name>Task 3: Add Terminal tab to Server Detail Page</name>
  <files>app/src/pages/ServerDetailPage.jsx</files>
  <action>
    Add Terminal tab to server detail page:
    - Add "Terminal" tab alongside existing tabs (Overview, Settings, Files, Console, Backups)
    - Import and render Terminal component in the tab panel
    - Pass server_id to Terminal component
    - Handle tab state (show loading state while connecting)
    - Add "Connected"/"Disconnected" status indicator in tab

    Reference existing tab structure in ServerDetailPage.jsx.
  </action>
  <verify>
  <automated>grep -n "Terminal" app/src/pages/ServerDetailPage.jsx | head -10</automated>
  </verify>
  <done>Server detail page shows Terminal tab, clicking it renders Terminal component with live connection</done>
</task>

</tasks>

<verification>
1. Verify Terminal.jsx exists and imports xterm.js correctly
2. Verify useTerminal.js exports connect/disconnect/sendCommand functions
3. Verify ServerDetailPage.jsx has Terminal tab in tab navigation
4. Run build to ensure no import errors
</verification>

<success_criteria>
- Terminal component renders in browser with xterm.js
- User can see live output from game server via WebSocket
- User can type commands and send them to server via RCON
- Command history persists across page refreshes
- Terminal tab appears on server detail page
</success_criteria>

<output>
After completion, create `.planning/phases/37-menambahkan-terminal-untuk-server-minecraftnya/37-01-SUMMARY.md`
</output>