---
phase: 37
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - api/src/presentation/handlers/terminal_ws_handler.rs
  - api/src/presentation/routes.rs
  - web-agent/src/main.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - Backend WebSocket server accepts terminal connections from frontend
    - Terminal output from game server streams to connected WebSocket clients
    - Commands sent via WebSocket are forwarded to RCON and executed
    - Agent relays terminal stream between game server and backend WebSocket
  artifacts:
    - path: "api/src/presentation/handlers/terminal_ws_handler.rs"
      provides: "WebSocket handler for terminal connections"
      min_lines: 80
    - path: "api/src/presentation/routes.rs"
      provides: "WebSocket route registration for /ws/terminal/{server_id}"
      exports: ["websocket"]
  key_links:
    - from: "terminal_ws_handler.rs"
      to: "web-agent"
      via: "WebSocket connection"
    - from: "web-agent"
      to: "rcon_handlers.rs"
      via: "RCON client"
    - from: "routes.rs"
      to: "terminal_ws_handler.rs"
      via: "route registration"
---

<objective>
Implement backend WebSocket handler for terminal streaming and RCON command relay.

Purpose: Backend WebSocket endpoint receives terminal connections from frontend, streams game server output, and forwards commands to RCON for execution.

Output: WebSocket handler, route registration, agent terminal task
</objective>

<context>
@.planning/phases/37-menambahkan-terminal-untuk-server-minecraftnya/37-CONTEXT.md
@api/src/presentation/handlers/rcon_handlers.rs
@api/src/presentation/handlers/ws_handler.rs (if exists)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create terminal WebSocket handler</name>
  <files>api/src/presentation/handlers/terminal_ws_handler.rs</files>
  <action>
    Create WebSocket handler for terminal connections:
    - Define WebSocket upgrade endpoint at /ws/terminal/{server_id}
    - Validate server ownership (user must own the server)
    - On connect: establish connection to web-agent via internal channel
    - On message (command): forward to web-agent for RCON execution
    - On disconnect: clean up connection state
    - Handle ping/pong for keepalive
    - Implement reconnection coordination with frontend

    Use existing WebSocket patterns from ws_handler.rs if it exists.
    Follow tokio-tungstenite patterns from codebase.
  </action>
  <verify>
  <automated>ls -la api/src/presentation/handlers/terminal_ws_handler.rs && grep -l "WebSocket\| tungstenite" api/src/presentation/handlers/terminal_ws_handler.rs</automated>
  </verify>
  <done>WebSocket handler accepts connections, validates server access, forwards commands to agent</done>
</task>

<task type="auto">
  <name>Task 2: Register terminal WebSocket route</name>
  <files>api/src/presentation/routes.rs</files>
  <action>
    Register terminal WebSocket route:
    - Add route for /ws/terminal/:server_id to router
    - Mount terminal_ws_handler in the router
    - Ensure proper CORS if needed for WebSocket

    Find existing WebSocket route registration pattern in routes.rs.
  </action>
  <verify>
  <automated>grep -n "terminal\|ws" api/src/presentation/routes.rs | head -10</automated>
  </verify>
  <done>WebSocket endpoint accessible at /ws/terminal/{server_id}</done>
</task>

<task type="auto">
  <name>Task 3: Add terminal task handler to web-agent</name>
  <files>web-agent/src/main.rs</files>
  <action>
    Add terminal streaming task to web-agent:
    - Create task handler for receiving RCON output stream
    - Forward stdout/stderr from game server to backend WebSocket
    - Receive commands from backend WebSocket and execute via RCON
    - Handle connection state (connected/disconnected/reconnecting)

    Reference agent-task patterns from agent-core crates.
  </action>
  <verify>
  <automated>grep -n "terminal\|stream" web-agent/src/main.rs | head -10</automated>
  </verify>
  <done>Web-agent forwards terminal output to backend and executes RCON commands from frontend</done>
</task>

</tasks>

<verification>
1. Verify terminal_ws_handler.rs exists with WebSocket upgrade logic
2. Verify routes.rs registers /ws/terminal/{server_id} route
3. Verify web-agent has terminal task handler
4. Test WebSocket connection manually or via curl
</verification>

<success_criteria>
- WebSocket endpoint accepts terminal connections
- Terminal output from game server streams to frontend
- Commands from frontend execute via RCON on game server
- Connection handles reconnection gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/37-menambahkan-terminal-untuk-server-minecraftnya/37-02-SUMMARY.md`
</output>