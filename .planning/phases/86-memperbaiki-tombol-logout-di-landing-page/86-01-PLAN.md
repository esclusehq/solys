---
phase: 86
plan: 01
name: Fix landing page logout button
wave: 1
depends_on: []
files_modified:
  - landing-page-escluse/src/App.tsx
autonomous: true
---

## Fix Landing Page Logout Button

**Objective:** Replace the Navbar's inline `handleLogout` (which bypasses the zustand store) with a call to the existing `useAuthStore.logout()` method, then navigate to `/signin`.

## Tasks

### Task 1: Replace inline handleLogout with store.logout()

<read_first>
- landing-page-escluse/src/App.tsx — current Navbar component with buggy inline handleLogout
</read_first>

<action>
In `landing-page-escluse/src/App.tsx`, replace the Navbar's inline `handleLogout` function (approximately lines 103-124) with:

```typescript
const handleLogout = async () => {
    await logout();
    navigate('/signin');
};
```

Where `logout` is already destructured from `useAuthStore()` on line 82 (unchanged), and `navigate` is already available from `useNavigate()`.

Specifically:
1. Remove the entire existing `handleLogout` block (try/catch with fetch, supabase signOut, localStorage.clear(), 800ms delay, window.location.href).
2. Add the two-line replacement above in its place.
3. Verify that `logout` is still destructured from `useAuthStore()` (line 82: `const { user, isAuthenticated, isLoading, logout, checkAuth } = useAuthStore();`).
4. Verify `navigate` is from `useNavigate()` hook.
5. Remove the `supabase` import if it's no longer used elsewhere in the file (check if `supabase` is used for avatar fetch or other purposes — the research notes it may still be used on line ~94).
</action>

<acceptance_criteria>
1. File `landing-page-escluse/src/App.tsx` no longer contains `localStorage.clear()` in the logout handler.
2. File contains `await logout();` followed by `navigate('/signin');` as the logout handler.
3. `logout` is destructured from `useAuthStore()` call.
4. The `supabase` import is removed if no longer used; otherwise preserved.
5. Running `npm run build` in `landing-page-escluse/` exits 0 with no errors.
</acceptance_criteria>

### Task 2: Verify the fix

<read_first>
- landing-page-escluse/src/App.tsx — after Task 1 changes
</read_first>

<action>
1. Run `npm run build` in `landing-page-escluse/` to verify no TypeScript or build errors.
2. Confirm the following code pattern exists in the logout handler:
   - Call to `logout()` (from zustand store)
   - No `localStorage.clear()` call
   - No `window.location.href` assignment
   - No 800ms `setTimeout` delay
3. Verify the `supabase` import is only present if actually referenced elsewhere in the file.
</action>

<acceptance_criteria>
1. `npm run build` exits 0.
2. `grep -n "localStorage.clear()" landing-page-escluse/src/App.tsx` returns nothing.
3. `grep -n "window.location.href" landing-page-escluse/src/App.tsx` returns nothing (or only non-logout-related occurrences).
4. `grep -n "handleLogout" landing-page-escluse/src/App.tsx` shows the new implementation with `logout()` and `navigate()`.
</acceptance_criteria>
