---
phase: 46-multi-platform
plan: 04
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - release/package/install.ps1
autonomous: true
requirements: []

must_haves:
  truths:
    - "Windows installer can run non-interactively"
    - "Installer creates config in APPDATA"
  artifacts:
    - path: "release/package/install.ps1"
      provides: "PowerShell-based Windows installer"
      contains: "Install-Solys"
---

<objective>
Create PowerShell installer for Windows with non-interactive mode support.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-multi-platform/46-CONTEXT.md
@release/package/install.sh (reference)
</context>

<tasks>

<task>
<read_first>
- release/package/install.sh
</read_first>
<action>
Create release/package/install.ps1 with:
- Download binary from get.esluce.com
- Create C:\Program Files\solys directory
- Create config in APPDATA/solys/config.toml
- Optional BACKEND_URL and API_KEY env vars
- NSSM service installation (optional, handled in 46-03)
- Uninstall function
</action>
<acceptance_criteria>
- [ ] install.ps1 created
- [ ] Supports -BackendUrl and -ApiKey parameters for non-interactive mode
- [ ] Creates config.toml in APPDATA
- [ ] Contains Uninstall function
</acceptance_criteria>
</task>

</tasks>