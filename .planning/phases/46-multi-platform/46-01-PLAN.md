---
phase: 46-multi-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web-agent/Cargo.toml
  - .cargo/config.toml
autonomous: true
requirements: []

must_haves:
  truths:
    - "Windows binary can be built with x86_64-pc-windows-msvc target"
    - "Cross-compilation works from Linux to Windows"
  artifacts:
    - path: "web-agent/Cargo.toml"
      provides: "Windows build target configured"
      contains: "x86_64-pc-windows-msvc"
---

<objective>
Add Windows build target to enable compiling solys for Windows systems.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-multi-platform/46-CONTEXT.md
@web-agent/Cargo.toml
</context>

<tasks>

<task>
<read_first>
- web-agent/Cargo.toml
</read_first>
<action>
Add Windows target to Cargo.toml build targets section:
```toml
[target.x86_64-pc-windows-msvc]
linker = "x86_64-w64-mingw32-gcc"
```
</action>
<acceptance_criteria>
- [ ] web-agent/Cargo.toml contains "x86_64-pc-windows-msvc"
- [ ] .cargo/config.toml created with cross-compile settings (if needed)
</acceptance_criteria>
</task>

<task>
<read_first>
- .cargo/config.toml (create if not exists)
</read_first>
<action>
Create .cargo/config.toml for cross-compilation:
```toml
[target.x86_64-pc-windows-msvc]
linker = "x86_64-w64-mingw32-gcc"
```
</action>
<acceptance_criteria>
- [ ] .cargo/config.toml exists with Windows target config
</acceptance_criteria>
</task>

</tasks>