---
phase: 46-multi-platform
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-core/crates/agent-config/src/loader.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Config loader detects Windows and uses APPDATA paths"
    - "Fallback to user home on Windows"
  artifacts:
    - path: "agent-core/crates/agent-config/src/loader.rs"
      provides: "Windows-aware config path resolution"
      contains: "APPDATA"
---

<objective>
Update config loader to use Windows-appropriate paths (APPDATA) instead of XDG on Windows.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-multi-platform/46-CONTEXT.md
@agent-core/crates/agent-config/src/loader.rs
</context>

<tasks>

<task>
<read_first>
- agent-core/crates/agent-config/src/loader.rs
</read_first>
<action>
Add Windows path detection in loader.rs - add Windows detection:
```rust
#[cfg(target_os = "windows")]
fn get_config_dir() -> PathBuf {
    std::env::var("APPDATA")
        .map(PathBuf::from)
        .unwrap_or_else(|_| {
            dirs::home_dir()
                .unwrap_or_else(|| PathBuf::from("."))
                .join("AppData/Roaming")
        })
        .join("solys")
}

#[cfg(not(target_os = "windows"))]
fn get_config_dir() -> PathBuf {
    // Existing XDG logic
    dirs::config_dir()
        .unwrap_or_else(|| PathBuf::from("."))
        .join("solys")
}
```
</action>
<acceptance_criteria>
- [ ] loader.rs contains #[cfg(target_os = "windows")]
- [ ] loader.rs contains APPDATA path logic
- [ ] loader.rs contains fallback to home directory
</acceptance_criteria>
</task>

</tasks>