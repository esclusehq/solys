---
phase: 66
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - landing-page-escluse/index.html
  - app/index.html
autonomous: true
requirements: []
must_haves:
  truths:
    - D-05: Track all subdomains — esluce.com, app.esluce.com, API, docs
    - Landing page visitors are tracked by Umami analytics
    - React app users are tracked by Umami analytics
    - Tracking script uses renamed path (analytics.js) to evade ad blockers
    - SPA navigation in React app is auto-tracked without manual umami.track() calls
    - Each frontend has its own data-website-id matching its subdomain
    - Tracking is deferred (defer attribute) to not block page rendering
  artifacts:
    - path: landing-page-escluse/index.html
      provides: Landing page with Umami tracking script
      contains: analytics.esluce.com
    - path: app/index.html
      provides: React app with Umami tracking script
      contains: analytics.esluce.com
  key_links:
    - from: landing-page-escluse/index.html
      to: https://analytics.esluce.com/analytics.js
      via: script src attribute
      pattern: src="https://analytics.esluce.com
    - from: app/index.html
      to: https://analytics.esluce.com/analytics.js
      via: script src attribute
      pattern: src="https://analytics.esluce.com
---

<objective>
Inject Umami tracking scripts into both frontend properties' index.html files to enable analytics tracking across all esluce.com subdomains.

**Purpose:** Implement D-05 (track all subdomains) by adding the Umami tracking script tag to landing-page-escluse/index.html (esluce.com) and app/index.html (app.esluce.com). Uses the renamed tracking script path (analytics.js via TRACKER_SCRIPT_NAME) to evade ad blockers. Uses placeholder data-website-id values that the deployer replaces with real IDs from the Umami dashboard after first login.

**Output:** Modified landing-page-escluse/index.html and app/index.html with tracking script tags
</objective>

<execution_context>
@/home/rhnbztnl/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rhnbztnl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-integrasikan-umami-analitycs-dashboard-dengan-rds-di-project/66-CONTEXT.md
@.planning/phases/66-integrasikan-umami-analitycs-dashboard-dengan-rds-di-project/66-RESEARCH.md
@landing-page-escluse/index.html
@app/index.html

**Key references:**
- landing-page-escluse/index.html is a Vite SPA with `<head>` containing meta, link, title — tracking script goes in `<head>` before closing tag
- app/index.html is a React SPA with `<head>` containing meta, link to Inter + Fira Code fonts, title — tracking script goes in `<head>` before closing tag
- SPA tracking is automatic via History API (Pitfall 4) — no manual umami.track() calls needed
- TRACKER_SCRIPT_NAME is set to analytics.js in the docker-compose.yml (from Plan 66-01)
- data-website-id values are placeholders because website ID is generated inside Umami Dashboard after first login (Pitfall 3)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Umami tracking script to landing-page-escluse/index.html</name>
  <files>landing-page-escluse/index.html</files>
  <read_first>
    - @landing-page-escluse/index.html — current state before modification
  </read_first>
  <action>
    Edit `landing-page-escluse/index.html` to add the Umami tracking script as a deferred `<script>` tag in the `<head>` section, before the closing `</head>` tag.

    Current state (lines 1-13):
    ```html
    <!doctype html>
    <html lang="en">
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="icon" type="image/svg+xml" href="/assets/logo.svg" />
        <title>Escluse - Distributed Infrastructure Platform</title>
      </head>
      <body>
        <div id="root"></div>
        <script type="module" src="/src/main.tsx"></script>
      </body>
    </html>
    ```

    Add after the `<title>` line (before `</head>`):
    ```html
        <script
            defer
            src="https://analytics.esluce.com/analytics.js"
            data-website-id="REPLACE_WITH_LANDING_PAGE_WEBSITE_ID"
            data-domains="esluce.com"
        ></script>
    ```

    The resulting `<head>` section should be:
    ```html
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="icon" type="image/svg+xml" href="/assets/logo.svg" />
        <title>Escluse - Distributed Infrastructure Platform</title>
        <script
            defer
            src="https://analytics.esluce.com/analytics.js"
            data-website-id="REPLACE_WITH_LANDING_PAGE_WEBSITE_ID"
            data-domains="esluce.com"
        ></script>
      </head>
    ```

    Key details:
    - `defer` attribute: script loads asynchronously and executes after HTML parsing — does NOT block page rendering
    - `src`: points to `analytics.js` (not the default script.js — matches TRACKER_SCRIPT_NAME from docker-compose.yml)
    - `data-website-id`: PLACEHOLDER value — the deployer replaces this with the real ID from Umami dashboard (from Settings -> Websites -> esluce.com entry)
    - `data-domains`: set to `esluce.com` (the landing page domain only)
    - No manual `umami.track()` calls needed — Umami auto-tracks SPA navigation via History API
  </action>
  <verify>
    <automated>
      grep -c 'analytics.esluce.com/analytics.js' landing-page-escluse/index.html
      grep -c 'data-website-id=' landing-page-escluse/index.html
      grep -c 'defer' landing-page-escluse/index.html
      grep -c 'data-domains="esluce.com"' landing-page-escluse/index.html
      grep -c 'REPLACE_WITH_LANDING_PAGE_WEBSITE_ID' landing-page-escluse/index.html
    </automated>
  </verify>
  <acceptance_criteria>
    1. landing-page-escluse/index.html contains `<script defer src="https://analytics.esluce.com/analytics.js"`
    2. Contains `data-website-id="REPLACE_WITH_LANDING_PAGE_WEBSITE_ID"` placeholder
    3. Contains `data-domains="esluce.com"`
    4. Script tag is inside `<head>` before `</head>` (not in `<body>`)
    5. Script has `defer` attribute (non-blocking)
    6. The existing `<meta>`, `<link>`, `<title>`, and `<body>` content is preserved
  </acceptance_criteria>
  <done>Landing page index.html updated with deferred Umami tracking script targeting esluce.com</done>
</task>

<task type="auto">
  <name>Task 2: Add Umami tracking script to app/index.html</name>
  <files>app/index.html</files>
  <read_first>
    - @app/index.html — current state before modification
  </read_first>
  <action>
    Edit `app/index.html` to add the Umami tracking script as a deferred `<script>` tag in the `<head>` section, before the closing `</head>` tag.

    Current state (lines 1-19):
    ```html
    <!DOCTYPE html>
    <html lang="en">

    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="icon" type="image/svg+xml" href="/logo.svg" />
        <title>Escluse — Server Control Platform</title>
        <link
            href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Fira+Code:wght@400;500&display=swap"
            rel="stylesheet">
    </head>

    <body>
        <div id="root"></div>
        <script type="module" src="/src/main.jsx"></script>
    </body>

    </html>
    ```

    Add after the Google Fonts `<link>` line (before `</head>`):
    ```html
        <script
            defer
            src="https://analytics.esluce.com/analytics.js"
            data-website-id="REPLACE_WITH_APP_WEBSITE_ID"
            data-domains="app.esluce.com"
        ></script>
    ```

    The resulting `<head>` section should be:
    ```html
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="icon" type="image/svg+xml" href="/logo.svg" />
        <title>Escluse — Server Control Platform</title>
        <link
            href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Fira+Code:wght@400;500&display=swap"
            rel="stylesheet">
        <script
            defer
            src="https://analytics.esluce.com/analytics.js"
            data-website-id="REPLACE_WITH_APP_WEBSITE_ID"
            data-domains="app.esluce.com"
        ></script>
    </head>
    ```

    Key details:
    - `defer` attribute: non-blocking script loading
    - `src`: `analytics.js` matching TRACKER_SCRIPT_NAME
    - `data-website-id`: PLACEHOLDER — deployer replaces with real ID from Umami Settings -> Websites -> app.esluce.com entry
    - `data-domains`: set to `app.esluce.com` (the dashboard subdomain only)
    - No manual umami.track() calls — SPA navigation auto-tracked via History API (Pitfall 4: prevent double pageviews)
  </action>
  <verify>
    <automated>
      grep -c 'analytics.esluce.com/analytics.js' app/index.html
      grep -c 'data-website-id=' app/index.html
      grep -c 'defer' app/index.html
      grep -c 'data-domains="app.esluce.com"' app/index.html
      grep -c 'REPLACE_WITH_APP_WEBSITE_ID' app/index.html
    </automated>
  </verify>
  <acceptance_criteria>
    1. app/index.html contains `<script defer src="https://analytics.esluce.com/analytics.js"`
    2. Contains `data-website-id="REPLACE_WITH_APP_WEBSITE_ID"` placeholder
    3. Contains `data-domains="app.esluce.com"`
    4. Script tag is inside `<head>` before `</head>`
    5. Script has `defer` attribute (non-blocking)
    6. The existing Google Fonts link, meta tags, and body content are preserved
  </acceptance_criteria>
  <done>React app index.html updated with deferred Umami tracking script targeting app.esluce.com</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser loads tracking script | analytics.esluce.com -> visitor's browser (third-party domain) |
| Tracking script sends data | Browser -> analytics.esluce.com/api/collect (renamed from /api/send) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-07 | Spoofing | Tracking script source | mitigate | Script loaded over HTTPS from analytics.esluce.com; Caddy enforces TLS (T-66-03) |
| T-66-08 | Denial of Service | Ad blocker blocks script | mitigate | TRACKER_SCRIPT_NAME set to analytics.js (not umami.js or script.js which are on filter lists); COLLECT_API_ENDPOINT renamed to /api/collect |
| T-66-09 | Information Disclosure | analytics.esluce.com exposure | accept | Umami is self-hosted analytics; the domain appears in HTML source but reveals only that analytics is in use — no PII or sensitive data disclosed |
</threat_model>

<verification>
1. Both index.html files contain the tracking script with correct src URL, data-website-id, and data-domains
2. Both scripts use `defer` attribute
3. Existing page structure and content is preserved in both files
4. Script URL uses analytics.js (matching TRACKER_SCRIPT_NAME from Plan 66-01)
5. Placeholder website IDs are distinctly named (REPLACE_WITH_LANDING_PAGE_WEBSITE_ID vs REPLACE_WITH_APP_WEBSITE_ID)
</verification>

<success_criteria>
1. landing-page-escluse/index.html has Umami tracking script with data-domains="esluce.com"
2. app/index.html has Umami tracking script with data-domains="app.esluce.com"
3. Both use analytics.js path (ad blocker evasion)
4. Both use defer for non-blocking load
5. Both have distinct placeholder website IDs for deployer to replace after Umami setup
6. D-05 (track all subdomains) implemented
</success_criteria>

<output>
After completion, create `.planning/phases/66-integrasikan-umami-analitycs-dashboard-dengan-rds-di-project/66-02-SUMMARY.md`
</output>
