---
phase: 01-foundation-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - .gitignore
  - bot/package.json
  - bot/tsconfig.json
  - bot/Dockerfile
  - bot/drizzle.config.ts
  - bot/scripts/deploy-commands.ts
  - bot/src/index.ts
  - bot/src/config.ts
  - bot/src/client.ts
  - bot/src/db/schema.ts
  - bot/src/handlers/command-handler.ts
  - bot/src/handlers/event-handler.ts
  - bot/src/handlers/component-handler.ts
  - bot/src/events/ready.ts
  - bot/src/events/interactionCreate.ts
  - bot/src/commands/info/ping.ts
  - bot/src/api/client.ts
  - api/Cargo.toml
  - api/Dockerfile
  - api/src/main.rs
  - api/src/config.rs
  - api/src/state.rs
  - api/src/error.rs
  - api/src/routes/mod.rs
  - api/src/routes/health.rs
  - api/src/routes/guilds.rs
  - api/src/handlers/guild_handlers.rs
  - api/src/services/guild_service.rs
  - api/src/repository/guild_repo.rs
  - api/src/models/guild.rs
  - api/migrations/001_create_guilds.sql
autonomous: true
requirements: [ONB-01, ONB-02, ONB-03, PERM-01, ADMN-03]
user_setup:
  - service: discord_developer_portal
    why: "Bot token, client ID, and privileged gateway intents required"
    env_vars:
      - name: DISCORD_TOKEN
        source: "Discord Developer Portal → Application → Bot → Token"
      - name: DISCORD_CLIENT_ID
        source: "Discord Developer Portal → Application → OAuth2 → Client ID"
      - name: DEV_GUILD_ID
        source: "Your dev Discord server ID (right-click server → Copy ID)"
    dashboard_config:
      - task: "Enable Privileged Gateway Intents"
        location: "Discord Developer Portal → Application → Bot → Privileged Gateway Intents"
        toggle: ["SERVER MEMBERS INTENT", "MESSAGE CONTENT INTENT"]

must_haves:
  truths:
    - "Developer can run `docker compose up` and all 4 services (postgres, redis, api, bot) start without errors"
    - "Bot appears online in Discord with status 'Watching /escluse help'"
    - "`/ping` slash command responds with 'Pong!' and latency in milliseconds"
    - "API health endpoint returns 200 with DB connection status confirmed"
    - "Guild settings can be written via API and read back (end-to-end CRUD)"
    - "Slash commands are registered (guild-scoped for dev) via deploy-commands script"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Service orchestration for all 4 containers"
      min_lines: 80
    - path: "bot/src/index.ts"
      provides: "Discord.js bot entry point"
      contains: "client.login"
    - path: "api/src/main.rs"
      provides: "Axum server entry point"
      contains: "axum::serve"
    - path: "bot/src/db/schema.ts"
      provides: "Drizzle ORM guild schema"
      contains: "pgTable"
    - path: "bot/scripts/deploy-commands.ts"
      provides: "Standalone slash command registration"
      contains: "REST"
    - path: "api/migrations/001_create_guilds.sql"
      provides: "Initial SQLx migration for guilds table"
      contains: "CREATE TABLE"
  key_links:
    - from: "bot/src/commands/info/ping.ts"
      to: "bot/src/handlers/command-handler.ts"
      via: "auto-load from commands/ directory"
      pattern: "loadCommands|readdirSync"
    - from: "bot/src/api/client.ts"
      to: "api/src/routes/health.rs"
      via: "HTTP fetch to API_URL/health"
      pattern: "fetch|health"
    - from: "docker-compose.yml"
      to: "bot/Dockerfile"
      via: "Docker Compose build context"
      pattern: "context: ./bot"
---

<objective>
**Plan 01: Walking Skeleton** — Scaffold the entire monorepo, Docker Compose infrastructure, Discord.js bot client with command infrastructure, and Axum API with database connectivity.

**Purpose:** Establish the foundational project structure, service topology, and end-to-end communication path (Discord → Bot → API → DB). This is the thinnest vertical slice of the entire system — a user can run `docker compose up`, see the bot come online, and get a response from `/ping`.

**Output:** A fully containerized monorepo with 4 Docker services (postgres, redis, bot, api), a working bot that responds to slash commands, and an API that reads/writes to the database.
</objective>

<execution_context>
@/home/rhnbztnl/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rhnbztnl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation-onboarding/01-CONTEXT.md
@.planning/phases/01-foundation-onboarding/01-SKELETON.md

The ARCHITECTURE.md file contains full code examples for every component being built here. Use them as templates — adapt, don't rewrite from scratch.

**Key decisions to honor per D-21 through D-24:**
- D-21: Workspace monorepo within `escluse-bot/` with `bot/` and `api/` subdirectories
- D-22: Turborepo — add `package.json` with turbo config at root  
- D-23: Each service manages its own migrations (bot: Drizzle, api: sqlx)
- D-24: Each service has its own `.env` file (bot/.env, api/.env)

**Multi-Guild Schema (Pitfall 5 prevention):** Every guild-scoped table uses `PRIMARY KEY (guild_id, key)` composite keys from day one.

**Token Security (Pitfall 4 prevention):** `.env` in `.gitignore` before any git operation.

**Command Deployment (Pitfall 1 prevention):** Standalone `deploy-commands.ts` script — NOT registered in the `ready` event.

**Gateway Intents (Pitfall 3 prevention):** `GatewayIntentBits.GuildMembers` and `GatewayIntentBits.MessageContent` enabled from day one.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold monorepo workspace, Docker Compose, and config files</name>
  <files>
    docker-compose.yml
    .env.example
    .gitignore
    bot/package.json
    bot/tsconfig.json
    bot/Dockerfile
    api/Cargo.toml
    api/Dockerfile
    package.json (root — Turborepo)
  </files>
  <read_first>
    .planning/research/STACK.md — dependency versions and Docker patterns
    .planning/research/ARCHITECTURE.md §8.1/8.2 — Docker Compose and Dockerfile patterns
    .planning/phases/01-foundation-onboarding/01-SKELETON.md — directory layout decisions
  </read_first>
  <action>
    Create the monorepo structure following D-21/D-22/D-24:

    1. **Root `package.json`** — Turborepo workspace config:
       - name: "escluse-bot" (private)
       - scripts: `dev`, `build`, `lint`
       - `workspaces: ["bot"]` (api/ is Rust, not npm)
       - devDependencies: `turbo@latest`

    2. **`.gitignore`** — Must include: `.env`, `node_modules/`, `target/`, `dist/`, `.turbo/`, `bot/drizzle/migrations/`, `*.log`

    3. **`.env.example`** — Document ALL env vars:
       - DISCORD_TOKEN, DISCORD_CLIENT_ID, DEV_GUILD_ID
       - DB_USER (default: bot_user), DB_PASSWORD, DB_NAME (default: escluse_bot)
       - DATABASE_URL (constructed from above)
       - REDIS_PASSWORD, REDIS_URL (constructed from above)
       - API_URL (default: http://api:8080)
       - RUST_LOG (default: info)

    4. **`bot/package.json`** — Bun project with:
       - dependencies: `discord.js@^14.26.4`, `drizzle-orm@^0.45`, `ioredis@^5.10.1`, `zod@^3`
       - devDependencies: `drizzle-kit@^0.30`, `@types/node`, `zlib-sync`, `bufferutil`
       - scripts: `dev` (bun --watch src/index.ts), `start` (bun src/index.ts), `deploy` (bun scripts/deploy-commands.ts), `db:generate` (drizzle-kit generate), `db:push` (drizzle-kit push), `db:studio` (drizzle-kit studio)

    5. **`bot/tsconfig.json`** — Strict mode, ES2022 target, moduleResolution: bundler, paths for @/* → ./src/*

    6. **`bot/Dockerfile`** — Multi-stage build per ARCHITECTURE.md §8.2:
       - Stage 1 (deps): `oven/bun:1.3-alpine`, copy package.json + bun.lock, `bun install --frozen-lockfile`
       - Stage 2 (runner): `oven/bun:1.3-alpine`, copy node_modules + src, `CMD ["bun", "src/index.ts"]`
       - First `bun install` creates `bun.lock`

    7. **`api/Cargo.toml`** — Per STACK.md dependencies:
       - Package name: `escluse-bot-api`
       - axum = "0.8", tokio = { version = "1", features = ["full"] }
       - tower-http = { version = "0.6", features = ["cors", "trace", "compression-gzip"] }
       - serde = { version = "1", features = ["derive"] }, serde_json = "1"
       - sqlx = { version = "0.8", features = ["runtime-tokio", "postgres", "migrate"] }
       - uuid = { version = "1", features = ["v4"] }, chrono = { version = "0.4", features = ["serde"] }
       - tracing = "0.1", tracing-subscriber = { version = "0.3", features = ["json", "env-filter"] }
       - thiserror = "2"

    8. **`api/Dockerfile`** — Multi-stage Rust build per ARCHITECTURE.md §8.2:
       - Stage 1 (builder): `rust:1.80-slim-bookworm`, cargo-chef for caching
       - Stage 2 (runtime): `debian:bookworm-slim` with ca-certificates
       - Copy binary + migrations/ directory

    9. **`docker-compose.yml`** — 4 services per ARCHITECTURE.md §8.1:
       - `postgres`: `postgres:16-alpine`, health check (pg_isready), port 5433, named volume
       - `redis`: `redis:7-alpine`, health check (redis-cli ping), port 6380, requirepass, named volume
       - `api`: build context api/, depends_on: postgres + redis (condition: service_healthy), port 8080
       - `bot`: build context bot/, depends_on: redis (condition: service_healthy), no ports exposed
       - All services on `bot-network` (bridge driver)
       - API_URL env var for bot: `http://api:8080`

    **Critical DO NOTs:**
    - Do NOT add `.env` files (they contain secrets) — only `.env.example` as template
    - Do NOT use `docker-compose` v1 (deprecated) — use `docker compose` v2
    - Do NOT embed secrets in docker-compose.yml — all via `${VAR}` references
  </action>
  <verify>
    <automated>ls docker-compose.yml .gitignore .env.example bot/package.json bot/tsconfig.json bot/Dockerfile api/Cargo.toml api/Dockerfile && grep -c "drizzle-orm" bot/package.json && grep -c "axum" api/Cargo.toml</automated>
  </verify>
  <done>
    - docker-compose.yml exists with 4 services and health checks
    - bot/package.json has all required dependencies
    - api/Cargo.toml has all required dependencies with correct versions
    - .gitignore excludes .env, node_modules, target
    - .env.example documents all required env vars
    - Docker Compose syntax validated (`docker compose config` would succeed)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Drizzle ORM schema + Axum API server with health/guild endpoints</name>
  <files>
    bot/src/db/schema.ts
    bot/drizzle.config.ts
    api/src/main.rs
    api/src/config.rs
    api/src/state.rs
    api/src/error.rs
    api/src/routes/mod.rs
    api/src/routes/health.rs
    api/src/routes/guilds.rs
    api/src/handlers/guild_handlers.rs
    api/src/services/guild_service.rs
    api/src/repository/guild_repo.rs
    api/src/models/guild.rs
    api/migrations/001_create_guilds.sql
  </files>
  <read_first>
    .planning/research/STACK.md — Drizzle ORM setup, sqlx patterns
    .planning/research/ARCHITECTURE.md §5 — Axum patterns (AppState, routes, handlers)
    .planning/research/ARCHITECTURE.md §2.2 — API directory structure
  </read_first>
  <action>
    Create the database schema and API layer following ARCHITECTURE.md patterns:

    **Part A: Drizzle ORM Schema (`bot/src/db/schema.ts` + config)**

    Create the `guilds` table per STACK.md schema patterns with these exact columns:
    ```typescript
    export const guilds = pgTable('guilds', {
      id: bigint('id', { mode: 'bigint' }).primaryKey(),    // Discord guild ID
      name: varchar('name', { length: 100 }).notNull(),
      settings: jsonb('settings').$type<{
        welcomeChannelId?: string;
        logChannelId?: string;
        defaultRoleId?: string;
        language?: 'id' | 'en';
      }>(),
      createdAt: timestamp('created_at').defaultNow().notNull(),
      updatedAt: timestamp('updated_at').defaultNow().notNull(),
    });
    ```
    Export `guilds` as named export. Also export a `type Guild = typeof guilds.$inferSelect`.

    `bot/drizzle.config.ts`:
    ```typescript
    import { defineConfig } from 'drizzle-kit';
    export default defineConfig({
      schema: './src/db/schema.ts',
      out: './drizzle',
      dialect: 'postgresql',
      dbCredentials: { url: process.env.DATABASE_URL! },
    });
    ```

    **Part B: SQLx Migration for API side (`api/migrations/001_create_guilds.sql`)**

    Since the API manages its own DB access via sqlx:
    ```sql
    CREATE TABLE IF NOT EXISTS guilds (
        id BIGINT PRIMARY KEY,
        name VARCHAR(100) NOT NULL,
        settings JSONB NOT NULL DEFAULT '{}',
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );

    -- Trigger to auto-update updated_at
    CREATE OR REPLACE FUNCTION update_updated_at_column()
    RETURNS TRIGGER AS $$
    BEGIN
        NEW.updated_at = NOW();
        RETURN NEW;
    END;
    $$ LANGUAGE plpgsql;

    CREATE TRIGGER guilds_updated_at
        BEFORE UPDATE ON guilds
        FOR EACH ROW
        EXECUTE FUNCTION update_updated_at_column();
    ```

    **Part C: Axum API Core**

    Following ARCHITECTURE.md §5 patterns exactly:

    `api/src/main.rs`:
    - Initialize tracing subscriber with env filter
    - Load config from env
    - Create PgPool with sqlx::PgPool::connect()
    - Run migrations with sqlx::migrate!()
    - Create AppState
    - Build Router with health and guild routes
    - Bind to 0.0.0.0:8080 and serve

    `api/src/config.rs`:
    - Config struct with: database_url, redis_url, discord_token, jwt_secret, rust_log
    - Load from env vars with dotenv or std::env
    - Implement Default with sensible fallbacks for dev

    `api/src/state.rs`:
    - AppState struct with: db (PgPool), config (Arc<Config>)
    - Derive Clone
    - Implement FromRef for PgPool

    `api/src/error.rs`:
    - AppError enum: NotFound, BadRequest, Internal, Database
    - Implement IntoResponse for AppError (returns appropriate HTTP status codes)
    - Implement From<sqlx::Error> for AppError

    `api/src/models/guild.rs`:
    - Guild struct with fields matching the DB schema
    - Derive: Serialize, Deserialize, sqlx::FromRow, Debug, Clone
    - GuildSettings struct for the JSONB field (Serialize, Deserialize, Default)

    `api/src/repository/guild_repo.rs`:
    - `fn upsert_guild(pool, id, name) -> Result<Guild>` — INSERT ON CONFLICT
    - `fn get_guild(pool, id) -> Result<Option<Guild>>` — SELECT by id
    - `fn update_settings(pool, id, settings) -> Result<Guild>` — UPDATE settings

    `api/src/services/guild_service.rs`:
    - `fn get_or_create_guild(pool, id, name) -> Result<Guild>`
    - `fn get_guild_settings(pool, id) -> Result<GuildSettings>`

    `api/src/handlers/guild_handlers.rs`:
    - `GET /api/guilds/:id` — get guild by ID
    - `PUT /api/guilds/:id/settings` — update guild settings
    - Thin handlers: extract params, call service, return JSON

    `api/src/routes/health.rs`:
    - `GET /health` — returns `{ "status": "ok", "database": "connected" }` after pinging DB
    - `GET /ready` — same as health but for k8s readiness probes

    `api/src/routes/guilds.rs`:
    - Returns a Router with guild routes nested under /api/guilds
    - `GET /:id` route
    - `PUT /:id/settings` route
    - Extract Path<u64> for guild ID

    `api/src/routes/mod.rs`:
    - Re-exports all route functions
    - `fn create_router(state: AppState) -> Router` — composes all routes

    **Do NOT**:
    - Do NOT use unwrap() in production paths — use proper error handling with ?
    - Do NOT hardcode DB connection strings
    - Do NOT use `prisma` or any ORM in API — sqlx only
    - Do NOT use actix-web or rocket — must be Axum
  </action>
  <verify>
    <automated>cd /home/rhnbztnl/Downloads/Berguna/Projects/escluse && ls api/src/main.rs api/src/config.rs api/src/state.rs api/src/error.rs api/src/routes/mod.rs api/src/routes/health.rs api/src/routes/guilds.rs api/src/handlers/guild_handlers.rs api/src/services/guild_service.rs api/src/repository/guild_repo.rs api/src/models/guild.rs api/migrations/001_create_guilds.sql bot/src/db/schema.ts bot/drizzle.config.ts</automated>
  </verify>
  <done>
    - Drizzle schema defines `guilds` table with guild_id PK, name, settings JSONB, timestamps
    - SQLx migration creates identical guilds table in api/migrations/
    - Axum server starts on port 8080 with /health, /ready, /api/guilds/:id, /api/guilds/:id/settings
    - Health endpoint pings DB and returns connected status
    - Guild CRUD works end-to-end through handler → service → repository pattern
    - Bot API client can reach the Axum server (connectivity established)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Discord.js bot client, handler infrastructure, /ping command, and deploy script</name>
  <files>
    bot/src/index.ts
    bot/src/config.ts
    bot/src/client.ts
    bot/src/handlers/command-handler.ts
    bot/src/handlers/event-handler.ts
    bot/src/handlers/component-handler.ts
    bot/src/events/ready.ts
    bot/src/events/interactionCreate.ts
    bot/src/commands/info/ping.ts
    bot/scripts/deploy-commands.ts
    bot/src/api/client.ts
  </files>
  <read_first>
    .planning/research/ARCHITECTURE.md §4 — All Discord.js patterns (command template, event handler, interaction router, component handler, deploy script)
    .planning/research/PITFALLS.md — Pitfall 1 (deploy), Pitfall 2 (deferReply), Pitfall 3 (intents)
  </read_first>
  <action>
    Create the Discord.js bot following ARCHITECTURE.md §4 patterns exactly:

    **`bot/src/config.ts`:**
    - Load all env vars at startup (DISCORD_TOKEN, DISCORD_CLIENT_ID, DEV_GUILD_ID, API_URL, REDIS_URL)
    - Validate required vars exist (throw descriptive error if missing)
    - Return frozen config object
    - Use static init pattern (not class) — call once at startup, immutable afterward

    **`bot/src/client.ts`:**
    - Create Discord.js Client with:
      - `intents: [GatewayIntentBits.Guilds, GatewayIntentBits.GuildMembers, GatewayIntentBits.GuildMessages, GatewayIntentBits.MessageContent]`
      - No partials initially (add when needed)
    - Attach config and logger to client via `client.config = config` and `client.logger = console`
    - Per D-21/D-22: Not a class, just export a `createClient(config)` function

    **`bot/src/handlers/command-handler.ts`:**
    - `loadCommands()` — recursively read `commands/` directory, import each module, validate it has `data` and `execute` properties, return Map<name, Command>
    - `registerCommands(commands, config)` — use REST API to PUT commands to Discord
      - If DEV_GUILD_ID is set → guild-scoped (instant)
      - Otherwise → global (takes up to 1 hour)
    - Per ARCHITECTURE.md §4.5: Do NOT register in ready event
    - `handleCommand(interaction)` — look up command by name from client.commands, call execute()
    - Export TypeScript type for Command interface: `{ data: SlashCommandBuilder, execute: fn, cooldown?: number }`

    **`bot/src/handlers/event-handler.ts`:**
    - `loadEvents(client)` — read `events/` directory, import each module, bind to client using `client.on()` or `client.once()`
    - Each event module exports `{ name: Events.X, once?: boolean, execute: fn }`

    **`bot/src/handlers/component-handler.ts`:**
    - Component registry Map<string, ComponentHandler> with registerComponent()
    - `handleComponent(interaction)` — match customId by exact match, then by prefix (e.g., "interest:pick" matches "interest:pick:123")
    - Per ARCHITECTURE.md §4.4 pattern

    **`bot/src/events/ready.ts`:**
    - Set bot presence: `Watching /escluse help`
    - Log: `Logged in as {tag} serving {count} guilds`
    - Run `once: true`

    **`bot/src/events/interactionCreate.ts`:**
    - Per ARCHITECTURE.md §4.3 pattern exactly:
      - ChatInputCommand → handleCommand()
      - MessageComponent → handleComponent()
      - ModalSubmit → handleComponent()
      - Autocomplete → find command's autocomplete handler
    - Wrap entire handler in try/catch
    - If interaction not replied/deferred → send ephemeral error message

    **`bot/src/commands/info/ping.ts`:**
    - SlashCommandBuilder: name = "ping", description = "Check bot latency"
    - Execute: deferReply (ephemeral), calculate client.ws.ping, editReply with "Pong! 🏓 {ping}ms"
    - Satisfies Command interface

    **`bot/scripts/deploy-commands.ts`:**
    - Standalone script per ARCHITECTURE.md §4.5:
      1. Load config (DISCORD_TOKEN, DISCORD_CLIENT_ID, DEV_GUILD_ID)
      2. Load commands via loadCommands()
      3. Create REST instance
      4. If DEV_GUILD_ID → Routes.applicationGuildCommands()
      5. Else → Routes.applicationCommands()
      6. PUT commandData
      7. Log success/failure

    **`bot/src/api/client.ts`:**
    - Simple fetch() wrapper:
      - Constructor takes baseUrl (from config)
      - `get<T>(path)` — GET request with JSON response
      - `put<T>(path, body)` — PUT request with JSON body and response
      - Error handling: non-2xx responses throw typed ApiError
      - Include Content-Type: application/json header
    - Export singleton `apiClient` instance

    **`bot/src/index.ts`:**
    - Entry point per ARCHITECTURE.md §4:
      1. Load config
      2. Create client
      3. Load commands → attach to client
      4. Load events
      5. Login via client.login(config.discordToken)
    - Log startup completion
    - Handle graceful shutdown (SIGTERM → client.destroy())

    **Critical DO NOTs:**
    - Do NOT register commands in the ready event (Pitfall 1) — use deploy-commands.ts standalone
    - Do NOT forget `deferReply()` before async operations (Pitfall 2)
    - Do NOT forget try/catch on interactionCreate (Pitfall 13)
    - Do NOT use `any` types — use proper TypeScript types
    - Do NOT make the event handler a single massive file — use auto-load pattern
    - Do NOT use CommonJS require() — use ESM imports throughout
  </action>
  <verify>
    <automated>cd /home/rhnbztnl/Downloads/Berguna/Projects/escluse && ls bot/src/index.ts bot/src/config.ts bot/src/client.ts bot/src/handlers/command-handler.ts bot/src/handlers/event-handler.ts bot/src/handlers/component-handler.ts bot/src/events/ready.ts bot/src/events/interactionCreate.ts bot/src/commands/info/ping.ts bot/scripts/deploy-commands.ts bot/src/api/client.ts && grep -c "deferReply\|Pong" bot/src/commands/info/ping.ts && grep -c "GatewayIntentBits" bot/src/client.ts