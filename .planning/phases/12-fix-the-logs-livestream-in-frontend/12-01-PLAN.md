---
phase: 12-fix-the-logs-livestream-in-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/features/logs/LogViewer.jsx
  - api/src/presentation/handlers/server_handlers.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "ANSI escape codes are stripped from log output before display"
    - "Logs show immediately on first load instead of 'Waiting for logs...' when API succeeds"
    - "Stop button works on servers without node_id via auto-node-assignment"
  artifacts:
    - path: "app/src/features/logs/LogViewer.jsx"
      contains:
        - "function stripAnsiCodes"
        - "setHasLogs(true)" after successful fetch
      min_lines: 180
    - path: "api/src/presentation/handlers/server_handlers.rs"
      contains:
        - "auto.*node" or "node.*assign" in stop_server
        - "UPDATE servers SET node_id"
      min_lines: 600
  key_links:
    - from: "LogViewer.jsx"
      to: "stripAnsiCodes function"
      via: "imports and uses in useEffect"
    - from: "server_handlers.rs"
      to: "stop_server handler"
      via: "auto-node-assignment block"
---

<objective>
Verify that all 3 log viewer issues from Phase 12 context are properly fixed in the codebase.
</objective>

<context>
@.planning/phases/12-fix-the-logs-livestream-in-frontend/12-CONTEXT.md
@.planning/STATE.md

Files to verify:
</context>

<tasks>

<task type="auto">
  <name>Verify ANSI stripping in LogViewer</name>
  <files>app/src/features/logs/LogViewer.jsx</files>
  <action>
Verify the stripAnsiCodes function exists and is properly used:

1. Check that stripAnsiCodes function is defined at top of file (lines 6-17)
2. Verify it removes patterns: \x1b[*, \x1b, and bracket patterns like [33m
3. Confirm it's called on log lines in:
   - Line 34: `stripAnsiCodes(payload.line)` in WebSocket handler
   - Line 65: `stripAnsiCodes(data.logs)` in initial fetch
   - Lines 75, 79: additional stripAnsiCodes calls for different data formats
</action>
  <verify>
<automated>
grep -c "stripAnsiCodes" app/src/features/logs/LogViewer.jsx | grep -q "5" && echo "PASS: 5 usages found"
</automated>
  </verify>
  <done>stripAnsiCodes function defined and applied to all log output paths</done>
</task>

<task type="auto">
  <name>Verify hasLogs fix for first-load</name>
  <files>app/src/features/logs/LogViewer.jsx</files>
  <action>
Verify the hasLogs state is set correctly on API success:

1. Check line 66: `fetchSucceeded = true` after getting data.logs
2. Check line 68: `setHasLogs(true)` called whenever data.logs exists (not just when content)
3. Check lines 76-77: setHasLogs(true) on message format
4. Check lines 80-81: setHasLogs(true) on string format  
5. Check lines 89-90: setHasLogs(true) on empty "No logs available" case

The key fix: setHasLogs(true) when API call succeeds, not just when logs.trim() has content.
</action>
  <verify>
<automated>
grep -c "setHasLogs(true)" app/src/features/logs/LogViewer.jsx | grep -q "5" && echo "PASS: 5 setHasLogs(true) calls found"
</automated>
  </verify>
  <done>hasLogs set to true whenever API returns (even empty), showing logs area instead of "Waiting..."</done>
</task>

<task type="auto">
  <name>Verify auto-node-assignment in stop_server</name>
  <files>api/src/presentation/handlers/server_handlers.rs</files>
  <action>
Verify the stop_server handler auto-assigns node when missing:

1. Check lines 594-603: Uses server.node_id if set and connected
2. Check lines 606-627: If not connected, finds first online node from node_repository
3. Check lines 630-635: Saves node_id to database with UPDATE query
4. Confirm similar pattern exists in restart_server handler

The fix: When server.node_id is null or node not connected, auto-find an online node and persist it.
</action>
  <verify>
<automated>
grep -c "UPDATE servers SET node_id" api/src/presentation/handlers/server_handlers.rs | grep -q "1" && echo "PASS: node_id update found"
</automated>
  </verify>
  <done>stop_server auto-assigns node and persists to database when missing</done>
</task>

</tasks>

<verification>
All 3 fixes verified via grep checks. The implementation matches the decisions in CONTEXT.md.
</verification>

<success_criteria>
- [x] ANSI stripping function present and used on all log output paths
- [x] hasLogs set true on any successful API response (not just when content exists)
- [x] stop_server handler auto-finds and persists node_id when missing
</success_criteria>

<output>
After completion, create `.planning/phases/12-fix-the-logs-livestream-in-frontend/12-01-SUMMARY.md`
</output>