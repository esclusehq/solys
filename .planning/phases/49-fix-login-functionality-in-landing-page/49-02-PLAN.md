---
phase: 49
name: Fix login functionality in landing page
goal: Configure Supabase OAuth and backend API authentication for the landing page login/signup flows
status: planned
wave: 2
requirements:
  - REQ-49-002: OAuth providers (Google, GitHub, Discord) must be configured in Supabase
  - REQ-49-004: OAuth redirect URLs must be configured in Supabase
depends_on: 49-01
---

## Plan 49-02: Configure OAuth providers in Supabase

**Wave:** 2  
**Depends on:** Plan 49-01  
**Files modified:** Supabase Dashboard (documentation needed)

**Tasks:**

1. **Document OAuth provider setup**
   - <read_first>Supabase Dashboard → Authentication → Providers</read_first>
   - <acceptance_criteria>
     - Google OAuth provider configured with valid credentials
     - GitHub OAuth provider configured with valid credentials
     - Discord OAuth provider configured with valid credentials
     - All providers have redirect URL set to `https://esluce.com/oauth/callback`
   </acceptance_criteria>
   - <action>Document the steps to configure OAuth providers in Supabase. Update AUTH_SETUP.md or create if not exists.</action>

2. **Configure redirect URLs**
   - <read_first>Supabase Dashboard → Authentication → URL Configuration</read_first>
   - <acceptance_criteria>
     - Site URL set to `https://esluce.com`
     - Redirect URLs include `https://esluce.com/oauth/callback`
     - Redirect URLs include `https://esluce.com/signin`
     - Redirect URLs include `https://esluce.com/signup`
   </acceptance_criteria>
   - <action>Update Supabase redirect URL configuration</action>