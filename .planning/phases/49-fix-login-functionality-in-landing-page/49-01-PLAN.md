---
phase: 49
name: Fix login functionality in landing page
goal: Configure Supabase OAuth and backend API authentication for the landing page login/signup flows
status: planned
wave: 1
requirements:
  - REQ-49-001: Supabase credentials must be configured in .env with real values
  - REQ-49-003: Backend API URL must point to production API endpoint
depends_on: Phase 48
---

## Plan 49-01: Configure Supabase credentials and API URL

**Wave:** 1  
**Files modified:** `landing-page-escluse/.env`

**Tasks:**

1. **Obtain real Supabase credentials**
   - <read_first>`.env` - Current placeholder values</read_first>
   - <acceptance_criteria>
     - `VITE_SUPABASE_URL` contains real Supabase project URL (format: `https://xxxxx.supabase.co`)
     - `VITE_SUPABASE_ANON_KEY` contains real anon key (starts with `eyJ...`)
   </acceptance_criteria>
   - <action>Update `.env` with real Supabase credentials from Supabase Dashboard → Project Settings → API</action>

2. **Configure production API URL**
   - <read_first>`.env` - Current localhost URL</read_first>
   - <acceptance_criteria>
     - `VITE_API_URL` points to production backend (e.g., `https://api.esluce.com`)
   </acceptance_criteria>
   - <action>Update `VITE_API_URL` to production API endpoint</action>

3. **Verify credentials are loaded**
   - <read_first>`src/lib/supabase.ts` - How supabase client is initialized</read_first>
   - <acceptance_criteria>
     - Supabase client initializes without console errors about missing credentials
     - Network requests to Supabase succeed (not 400/403 errors)
   </acceptance_criteria>
   - <action>Build and test that Supabase client initializes correctly</action>