---
phase: 49
name: Fix login functionality in landing page
goal: Configure Supabase OAuth and backend API authentication for the landing page login/signup flows
status: planned
wave: 3
requirements:
  - REQ-49-006: User must be redirected to esluce.com (landing page) after successful login
depends_on: 49-01, 49-02
---

## Plan 49-04: Redirect to landing page after login

**Wave:** 3  
**Depends on:** Plans 49-01 and 49-02  
**Files modified:** `src/lib/stores/authStore.ts`, `src/pages/oauth/OAuthCallback.tsx`

**Tasks:**

1. **Change login redirect destination**
   - <read_first>`src/lib/stores/authStore.ts` - Auth store login logic</read_first>
   - <acceptance_criteria>
     - After successful email/password login, user is redirected to `https://esluce.com` (or `/` for local development)
     - NOT redirected to `/onboarding`
   </acceptance_criteria>
   - <action>Update the redirect destination in authStore.ts from `/onboarding` to `/` (landing page)</action>

2. **Change OAuth callback redirect**
   - <read_first>`src/pages/oauth/OAuthCallback.tsx` - OAuth callback handler</read_first>
   - <acceptance_criteria>
     - After successful OAuth login, user is redirected to `https://esluce.com` (or `/` for local development)
     - NOT redirected to `/onboarding`
   </acceptance_criteria>
   - <action>Update the redirect destination in OAuthCallback.tsx from `/onboarding` to `/` (landing page)</action>

3. **Test redirect behavior**
   - <acceptance_criteria>
     - Email/password login redirects to landing page
     - OAuth login redirects to landing page
     - Original URL is preserved (if coming from a specific page)
   </acceptance_criteria>
   - <action>Test both login flows to verify correct redirect behavior</action>