---
phase: 49
name: Fix login functionality in landing page
goal: Configure Supabase OAuth and backend API authentication for the landing page login/signup flows
status: planned
wave: 3
requirements:
  - REQ-49-005: Login flow must work end-to-end (email/password and OAuth)
depends_on: 49-01, 49-02
---

## Plan 49-03: Test end-to-end authentication flow

**Wave:** 3  
**Depends on:** Plans 49-01 and 49-02  
**Files modified:** None (testing only)

**Tasks:**

1. **Test email/password login**
   - <read_first>`src/components/auth/SignInForm.tsx` - Login form component</read_first>
   - <read_first>`src/lib/stores/authStore.ts` - Auth store with login logic</read_first>
   - <acceptance_criteria>
     - Can login with email/password
     - Error messages display correctly for invalid credentials
     - Redirects to landing page on successful login
   </acceptance_criteria>
   - <action>Manual test: Register new user, logout, login with same credentials</action>

2. **Test OAuth login (Google)**
   - <read_first>`src/lib/oauth.ts` - OAuth sign-in logic</read_first>
   - <read_first>`src/pages/oauth/OAuthCallback.tsx` - OAuth callback handler</read_first>
   - <acceptance_criteria>
     - Clicking "Sign in with Google" opens Google OAuth flow
     - After authorization, redirected to `/oauth/callback`
     - User is authenticated and redirected to landing page
   </acceptance_criteria>
   - <action>Manual test: Click Google sign-in button, complete OAuth flow</action>

3. **Test session persistence**
   - <acceptance_criteria>
     - After login, refreshing page keeps user logged in
     - Session persists across browser sessions (if "Remember me" checked)
   </acceptance_criteria>
   - <action>Test: Login, close browser, reopen, verify still logged in</action>