---
phase: 63-create-architecture-md-technical-documentation-module-level
plan: 01
type: execute
wave: 1
depends_on: [62-01-PLAN.md]
files_modified:
  - ARCHITECTURE.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Developer can find ARCHITECTURE.md at repo root describing the system architecture"
    - "Developer can understand how API, Worker, Web Agent, Frontend, and Agent-Core interact"
    - "Developer can see Mermaid.js diagrams for all major data flows (server creation, agent registration, background jobs, WebSocket lifecycle)"
    - "Developer can find per-service module structure with key files and entry points"
    - "Developer can identify the meta-repo architecture and which repo to look at for each subsystem"
    - "Developer can find technology stack versions for all services"
    - "Developer knows where to go next (DEVELOPMENT.md for setup, CONTRIBUTING.md for contributing)"
  artifacts:
    - path: "ARCHITECTURE.md"
      provides: "Technical architecture reference at repo root"
      min_lines: 180
      contains: "Mermaid"
  key_links:
    - from: "ARCHITECTURE.md"
      to: "DEVELOPMENT.md"
      via: "relative link in Next Steps section"
      pattern: "DEVELOPMENT.md"
    - from: "ARCHITECTURE.md"
      to: "CONTRIBUTING.md"
      via: "relative link in Next Steps section or meta-repo context"
      pattern: "CONTRIBUTING.md"
---

<objective>
Create ARCHITECTURE.md at repo root as the canonical technical architecture reference for Esluce, documenting module-level architecture — service boundaries, data flows, component relationships, and key abstractions.

**Purpose:** Give developers and contributors a comprehensive understanding of how the Esluce platform is architected — how services connect, how data flows between them, the module-level structure of each service, and the key design patterns used throughout.

**Output:** Single `ARCHITECTURE.md` at repo root (180-250 lines) with Mermaid.js inline diagrams, following the same formatting conventions as DEVELOPMENT.md and CONTRIBUTING.md.

**Must NOT do:** Include local setup instructions, contributing workflows, or duplicate content from DEVELOPMENT.md or CONTRIBUTING.md. Those are separate docs linked from ARCHITECTURE.md.

**Must NOT do:** Include internal planning references, analysis dates, or .planning/ directory paths. This is a public-facing document.
</objective>

<execution_context>
@/home/rhnbztnl/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rhnbztnl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key interfaces and patterns extracted from the codebase that executors need -->

From DEVELOPMENT.md and CONTRIBUTING.md (the analog patterns for repo-root docs):
- ATX heading: `# Escluse — <Title>`
- Summary paragraph below heading
- Shields.io badge row (badges per doc purpose)
- Horizontal rule `---` after badges
- GFM pipe tables with alignment dashes
- Fenced code blocks with language tags (`rust`, `bash`, `text`, `mermaid`)
- `> **Note:**` and `> **Warning:**` callouts
- Bullet list doc index: `- **[link](path)** — description`
- ASCII tree with `├──` / `└──` branches (for directory layout)
- Section anchor links in TOC

Mermaid.js syntax notes for GitHub rendering:
- ` ```mermaid ` fenced code blocks — rendered natively by GitHub
- `graph TD` for block/flow diagrams (Directional Top-Down)
- `sequenceDiagram` for sequential flows (participants, ->>, -->>, Note over)
- `stateDiagram-v2` for stateful lifecycle diagrams
- `classDiagram` for interface/trait relationships
- Loops: `loop <label>` ... `end`
- Conditionals: `alt <condition>` ... `else` ... `end`
- Parallel: `par` ... `and` ... `end`
- Arrows: `->` (solid), `-->` (dashed), `->>` (solid arrow), `-->>` (dashed arrow)

Source material adapted from `.planning/codebase/ARCHITECTURE.md` (207 lines):
- Service layers (API Backend, Worker, Web Agent, Frontend, Agent-Core)
- Data flows (server creation, agent registration, background jobs)
- Key abstractions (Repository pattern, Executor Factory, Task Dispatch)
- Entry points per service
- Error handling strategy
- Cross-cutting concerns (logging, validation, auth, caching)

Verified codebase facts (from live codebase, 2026-05-31):
- Agent-core location: `agent/agent-core/crates/` (12 crates)
- API repository traits: 10+ in `api/src/domain/repositories/`
- API use cases: 18+ in `api/src/application/use_cases/`
- Worker source: `worker/src/` with queue, agent, webhook modules
- Web Agent source: `agent/solys/src/` with handlers (runtime, backup, rcon, metrics, ssh, sftp)
- Frontend: `app/src/` with pages, components, hooks, store (Zustand), lib
- Tech stack: Axum 0.7, React 19.2, sqlx 0.7, tokio 1, redis 0.25, Bollard 0.18
- `migrations/` is symlink: `migrations -> api/migrations`

Repo mapping (from CONTRIBUTING.md):
| Directory | GitHub Repo | Description |
|-----------|-------------|-------------|
| `api/` + `worker/` | `esclusehq/escluse-cloud` | Backend API + Worker |
| `app/` | `esclusehq/escluse-dashboard` | Frontend Dashboard |
| `agent/solys/` | `esclusehq/solys` | Web Agent |
| `agent/agent-core/` | `esclusehq/agent-core` | Shared Agent Crates |
| `docs/` | `esclusehq/escluse-docs` | Documentation Site |
| `gateway/` | `esclusehq/escluse-infra` | Infrastructure |
| `landing-page-escluse/` | `esclusehq/escluse-landing-page` | Marketing Site |
| `packages/` | `esclusehq/escluse-sdk` | SDK |
| `./` | `esclusehq/escluse` | Orchestration root |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ARCHITECTURE.md at repo root</name>
  <files>ARCHITECTURE.md</files>
  <read_first>
    - .planning/codebase/ARCHITECTURE.md (the 207-line internal source document — all architecture content, data flows, key abstractions, entry points)
    - .planning/codebase/STRUCTURE.md (directory layout per service, key files, where to add new code)
    - .planning/codebase/STACK.md (tech stack versions — Axum 0.7, React 19.2, sqlx 0.7, etc.)
    - .planning/codebase/CONVENTIONS.md (import patterns, module design descriptions — useful for Module Reference section)
    - DEVELOPMENT.md (formatting pattern: 103-line repo-root entry point)
    - CONTRIBUTING.md (formatting pattern: 155-line single-file with sections, tables, code blocks)
    - .planning/INTEGRATIONS.md if it exists (external integrations like PostgreSQL, Redis, Discord, Stripe)
    - No existing ARCHITECTURE.md at repo root — this is a greenfield creation (verify it doesn't exist)
  </read_first>
  <action>
    Create `ARCHITECTURE.md` at repo root (`/home/rhnbztnl/Downloads/Berguna/Projects/escluse/ARCHITECTURE.md`) following the CONTEXT.md decisions and formatting conventions from DEVELOPMENT.md/CONTRIBUTING.md.

    **Structure:**

    Line 1: `# Escluse — Architecture`

    Lines 3-6: Summary paragraph: "Esluce is a game server management platform built as a distributed microservices architecture with agent-based node management. This document describes the system architecture — service boundaries, data flows, component relationships, and key abstractions."

    Lines 7-8: Shields.io badge row:
    ```
    ![Rust](https://img.shields.io/badge/rust-1.70%2B-orange)
    ![React](https://img.shields.io/badge/react-19-blue)
    ![Axum](https://img.shields.io/badge/axum-0.7-green)
    ![PostgreSQL](https://img.shields.io/badge/postgresql-16-336791)
    ```

    Line 9: `---` (horizontal rule)

    Lines 11-15: `## Table of Contents` with anchor links to each major section.

    Line 17: `---`

    Lines 19-40: `## System Overview` — Mermaid `graph TD` block diagram showing:
    - Frontend → API (REST)
    - API → PostgreSQL, Redis
    - API → Web Agent (WebSocket)
    - Worker → Redis (jobs), PostgreSQL (data)
    - Web Agent → API (WebSocket), Container Runtime (Bollard)
    - API → External Services (Stripe, Modrinth, Discord)

    Use `subgraph` for grouping services. Include descriptive text below the diagram explaining the architecture pattern (distributed microservices, clean architecture in API, WebSocket for real-time agent communication).

    Lines 42-80: `## Service Architecture` — Per-service breakdown with sub-sections:

    Line 42: `### API Backend (api/)`
    - Mermaid `graph TD` showing layers: Presentation → Application → Domain → Infrastructure
    - Description: Clean Architecture with Axum v0.7, DI via AppContainer
    - Layers: Presentation (routes/handlers/middleware), Application (use cases/services/DTOs), Domain (entities/repository traits/billing/RBAC), Infrastructure (Postgres repos/executors/external services)
    - Entry point: `api/src/main.rs`
    - Port: 3000
    - Dependencies: PostgreSQL, Redis, external services

    Line 43: `### Worker Service (worker/)`
    - Description: Async job processor consuming Redis queues, emitting webhooks
    - Key files: `worker/src/main.rs`, `src/queue.rs`, `src/agent/`, `src/webhook/`
    - Depends on: Redis, API database

    Line 44: `### Web Agent (agent/solys/)`
    - Description: Node-resident agent for container operations
    - Key files: `src/main.rs`, `src/agent_connection.rs`, `src/handlers/` (runtime, backup, rcon, metrics, ssh, sftp)
    - Depends on: Backend WebSocket, Podman/Docker via Bollard
    - Reconnection logic, task dispatch, health monitoring

    Line 45: `### Frontend (app/)`
    - Description: React 19 SPA with Zustand state management
    - Key dirs: `pages/`, `components/`, `hooks/`, `store/`, `lib/`
    - Depends on: Backend REST API, Supabase for auth

    Line 46: `### Agent-Core (agent/agent-core/crates/)`
    - Description: Rust workspace of 12 shared crates
    - Table of crates with purpose:
      | Crate | Purpose |
      |-------|---------|
      | `agent-proto` | Task/result definitions, protocol messages |
      | `agent-config` | Configuration loading and validation |
      | `agent-runtime` | Docker/Podman runtime detection |
      | `agent-ssh` | SSH client and connection pooling |
      | `agent-rcon` | RCON client for game server management |
      | `agent-backup` | Backup compression utilities |
      | `agent-health` | Health monitoring, circuit breakers, retry |
      | `agent-metrics` | Metrics collection |
      | `agent-task` | Task queue and dispatcher |
      | `agent-security` | JWT, rate limiting, audit |
      | `agent-event` | Event handling |
      | `agent-capability` | Capability registry |

    Lines 82-180: `## Data Flows` — Mermaid sequence diagrams with descriptions:

    Line 82: `### Server Creation Flow` — sequenceDiagram:
    ```
    participant User
    participant Frontend
    participant API
    participant Database
    participant Redis
    participant Web Agent
    participant Container Runtime
    ```
    Flow: User → Frontend (POST /api/servers) → API handler → UseCase → Database (create entity) → Executor Factory (select executor) → Redis (enqueue job) → API → Frontend (202 Accepted, polling) ... Worker → Redis (dequeue) → Web Agent (WS send task) → Runtime (create container via Bollard) → Web Agent (TaskResult) → Worker → Database (update status) → Frontend (Server ready)

    Line 83: `### Agent Registration Flow` — sequenceDiagram:
    ```
    participant Web Agent
    participant API
    participant Database
    ```
    Flow: Web Agent → API (WS connect /api/ws/node) → API (accept) → Web Agent (send Register with capabilities) → API → Database (create Node record, assign UUID) → API → Web Agent (RegisterAck with node_id) → Web Agent (periodic heartbeat) → API (track health)

    Line 84: `### Background Job Flow` — sequenceDiagram:
    ```
    participant API
    participant Redis
    participant Worker
    participant Database
    participant External
    ```
    Flow: API → Redis (enqueue job: backup, alert) → Worker (poll Redis, dequeue) → Worker → Database (query/update) → Worker → External (webhook, email) → Worker → Database (mark complete)

    Line 85: `### WebSocket Lifecycle` — stateDiagram-v2 (or sequenceDiagram):
    States: Disconnected → Connecting → Connected (with Registering → Registered sub-states) → Reconnecting → Disconnected
    Include event triggers: connection drop, reconnection timer, heartbeat timeout

    Lines 182-200: `## Key Abstractions` — Mermaid classDiagram + code blocks:

    `### Repository Pattern`:
    ```mermaid
    classDiagram
      class ServerRepository {
        <<interface>>
        +create(Server) Server
        +find_by_id(Uuid) Option~Server~
        +update(Server) Server
        +delete(Uuid) void
      }
      class PostgresServerRepository {
        +create(Server) Server
        +find_by_id(Uuid) Option~Server~
        +update(Server) Server
        +delete(Uuid) void
      }
      ServerRepository <|.. PostgresServerRepository
    ```
    + Rust code snippet showing trait definition (from `api/src/domain/repositories/server_repository.rs`).

    `### Executor Factory`:
    classDiagram + code snippet showing ExecutorFactory trait and implementations (AgentServerExecutor, SSHServerExecutor, RCONServerExecutor, MockServerExecutor).

    `### Task Dispatch`:
    classDiagram + code snippet showing TaskDispatcher from agent-core.

    Lines 202-215: `## Technology Stack` — Table with service, language, framework, database, key dependencies reference.

    Lines 217-235: `## Module Reference` — Per-service module structure with key files:

    API Backend (`api/src/`):
    - `domain/` — Business entities, repository traits, services (auth, billing, RBAC)
    - `application/use_cases/` — 18+ use cases (create_server, start_server, etc.)
    - `application/services/` — Background services (monitoring, webhook, backup)
    - `infrastructure/repositories/` — Postgres implementations
    - `infrastructure/executors/` — Agent, SSH, RCON, mock executors
    - `presentation/routes/` — HTTP routes
    - `presentation/handlers/` — Request handlers
    - `presentation/middleware/` — Auth, RBAC, rate limiting, CORS
    - `shared/` — Constants, utilities, error types
    - `bootstrap/` — App initialization, DI container

    Web Agent (`agent/solys/src/`):
    - `agent_connection.rs` — WebSocket client, message handling
    - `handlers/` — Task handlers (runtime, backup, rcon, metrics, ssh, sftp)
    - `task_state.rs` — Task tracking
    - `api/` — Internal HTTP server (health checks)

    Agent-Core (`agent/agent-core/crates/`):
    - 12 crates as listed above

    Frontend (`app/src/`):
    - `pages/` — Full page components (auth, dashboard, servers, billing, settings)
    - `components/` — Reusable UI (Sidebar, IDE, FileManager, Toast)
    - `hooks/` — Custom hooks (useServers, useNodes, useWebSocket)
    - `store/` — Zustand stores (authStore, serverStore, uiStore)
    - `lib/` — API client, Supabase client
    - `api/` — API client utilities

    Lines 237-242: `## Error Handling` — Strategy: centralized error types with application-specific codes. Retry with exponential backoff + circuit breaker (agent-health crate).

    Line 244: `## Cross-Cutting Concerns` — Logging (tracing), Validation (agent-config, request handlers), Authentication (JWT + RBAC), Caching (Redis).

    Lines 246-250: `## Next Steps` — Bullet list:
    - **[DEVELOPMENT.md](DEVELOPMENT.md)** — Local development setup guide
    - **[CONTRIBUTING.md](CONTRIBUTING.md)** — How to contribute

    **Verifiable values:**
    - File exists at repo root: `ARCHITECTURE.md`
    - Heading: `# Escluse — Architecture`
    - Contains at least one Mermaid diagram ( ```mermaid code block)
    - Contains all 5 services: API Backend, Worker, Web Agent, Frontend, Agent-Core
    - Contains meta-repo reference (at least one repo mapping mention for `esclusehq/`)
    - Contains links to DEVELOPMENT.md and CONTRIBUTING.md
    - Contains server creation flow description
    - Contains agent registration flow description
    - Contains background job flow description
    - Contains at least one Rust code block with trait/struct definition
    - Contains Agent-Core 12-crate table
    - Minimum 180 lines
    - Does NOT contain `.planning/` internal paths
    - Does NOT contain internal analysis dates like "2026-04-08"
  </action>
  <verify>
    <automated>
      # Verify ARCHITECTURE.md exists
      test -f ARCHITECTURE.md || { echo "MISSING: ARCHITECTURE.md"; exit 1; }
      # Verify minimum line count
      lines=$(wc -l < ARCHITECTURE.md)
      [ "$lines" -ge 180 ] || { echo "TOO_SHORT: $lines lines (minimum 180)"; exit 1; }
      # Verify heading
      grep -q '^# Escluse — Architecture' ARCHITECTURE.md || { echo "MISSING: heading"; exit 1; }
      # Verify Mermaid diagram(s)
      grep -q '```mermaid' ARCHITECTURE.md || { echo "MISSING: Mermaid diagrams"; exit 1; }
      # Verify all 5 services covered
      grep -qi 'API Backend' ARCHITECTURE.md || { echo "MISSING: API Backend section"; exit 1; }
      grep -qi 'Worker' ARCHITECTURE.md || { echo "MISSING: Worker section"; exit 1; }
      grep -qi 'Web Agent' ARCHITECTURE.md || { echo "MISSING: Web Agent section"; exit 1; }
      grep -qi 'Frontend' ARCHITECTURE.md || { echo "MISSING: Frontend section"; exit 1; }
      grep -qi 'Agent-Core' ARCHITECTURE.md || { echo "MISSING: Agent-Core section"; exit 1; }
      # Verify repo mapping reference (at least one esclusehq/ mention)
      grep -q 'esclusehq/' ARCHITECTURE.md || { echo "MISSING: esclusehq/ meta-repo reference"; exit 1; }
      # Verify links to other docs
      grep -q 'DEVELOPMENT.md' ARCHITECTURE.md || { echo "MISSING: DEVELOPMENT.md link"; exit 1; }
      grep -q 'CONTRIBUTING.md' ARCHITECTURE.md || { echo "MISSING: CONTRIBUTING.md link"; exit 1; }
      # Verify data flows
      grep -qi 'server creation' ARCHITECTURE.md || { echo "MISSING: server creation flow"; exit 1; }
      grep -qi 'agent registration' ARCHITECTURE.md || { echo "MISSING: agent registration flow"; exit 1; }
      grep -qi 'background job' ARCHITECTURE.md || { echo "MISSING: background job flow"; exit 1; }
      # Verify code example
      grep -q '```rust' ARCHITECTURE.md || { echo "MISSING: Rust code block"; exit 1; }
      # Verify Agent-Core crate table
      grep -q 'agent-proto' ARCHITECTURE.md || { echo "MISSING: agent-proto crate reference"; exit 1; }
      grep -q 'agent-task' ARCHITECTURE.md || { echo "MISSING: agent-task crate reference"; exit 1; }
      # Verify no internal planning references
      grep -q '\.planning/' ARCHITECTURE.md && { echo "SHOULD_NOT: .planning/ path in public doc"; exit 1; }
      grep -q '2026-04-08' ARCHITECTURE.md && { echo "SHOULD_NOT: internal analysis date in public doc"; exit 1; }
      echo "ALL CHECKS PASSED"
    </automated>
  </verify>
  <acceptance_criteria>
    - "ARCHITECTURE.md exists at repo root with 180+ lines"
    - "Heading is '# Escluse — Architecture' with Shields.io badge row"
    - "Contains at least one Mermaid.js diagram rendered via ```mermaid code block"
    - "Covers all 5 services: API Backend, Worker, Web Agent, Frontend, Agent-Core"
    - "Contains meta-repo architecture reference (esclusehq/ repo mapping)"
    - "Links to DEVELOPMENT.md and CONTRIBUTING.md"
    - "Documents all data flows: server creation, agent registration, background jobs, WebSocket lifecycle"
    - "Contains Rust code snippets for key abstractions (Repository, ExecutorFactory, TaskDispatcher)"
    - "Contains Agent-Core 12-crate table with purposes"
    - "Contains per-service module reference with key files"
    - "No .planning/ internal paths or analysis dates leaked"
    - "Formatting matches DEVELOPMENT.md/CONTRIBUTING.md conventions (ATX headings, GFM tables, admonitions, code blocks)"
  </acceptance_criteria>
  <done>ARCHITECTURE.md exists at repo root, passes all verify checks, with Mermaid.js diagrams for all major data flows, per-service module-level documentation, key abstractions with Rust code snippets, and cross-references to DEVELOPMENT.md and CONTRIBUTING.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure documentation phase. ARCHITECTURE.md is a static markdown file with Mermaid.js diagrams rendered natively by GitHub. No runtime code, network requests, authentication, or data handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | T (Tampering) | ARCHITECTURE.md | accept | Documentation-only file. Misleading architecture docs could confuse developers. Mitigated by git history and PR review. |
| T-63-02 | I (Info Disclosure) | ARCHITECTURE.md | accept | Public-facing document describing system architecture — no sensitive information (API keys, credentials, internal URLs) included. Architecture patterns are already public in source code. |
</threat_model>

<verification>
- ARCHITECTURE.md created at repo root
- Passes all verify automated checks
- Mermaid diagrams render correctly in GitHub preview
- Cross-references to DEVELOPMENT.md and CONTRIBUTING.md present
- No .planning/ internal paths or analysis dates leaked
- ROADMAP.md updated to reflect completed plan
</verification>

<success_criteria>
- ARCHITECTURE.md at repo root: >=180 lines, correct heading, Mermaid diagrams, all 5 services documented, data flows (server creation, agent registration, background jobs, WebSocket lifecycle), key abstractions with Rust code, Agent-Core 12-crate table, links to DEVELOPMENT.md and CONTRIBUTING.md
- No internal planning artifacts (no .planning/ paths, no analysis dates)
- Formatting conventions consistent with DEVELOPMENT.md and CONTRIBUTING.md
</success_criteria>

<output>
After completion, create `.planning/phases/63-create-architecture-md-technical-documentation-module-level/63-01-SUMMARY.md`
</output>
