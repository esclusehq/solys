---
phase: 36-menambahkan-fungsi-untuk-server-untuk-bedrock-pocket
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/domain/server/template/model.rs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Bedrock game type appears in create server dropdown"
    - "Bedrock servers deploy with correct docker image (itzg/minecraft-bedrock-server)"
    - "Bedrock servers use correct default port (19132)"
    - "Backend compiles without errors"
  artifacts:
    - path: "api/src/domain/server/template/model.rs"
      provides: "Bedrock template with default port 19132, docker image itzg/minecraft-bedrock-server"
  key_links:
    - from: "app/src/features/server/CreateServerModal.jsx"
      to: "/api/v1/templates"
      via: "fetch templates on mount"
      pattern: "templates\\.filter.*game_type"
---

<objective>
Add Bedrock/Pocket Edition as a new game type option in the server deployment system.

Purpose: Enable users to deploy Minecraft Bedrock Edition servers alongside Java Edition
Output: Bedrock template in fallback templates, appears in game type dropdown
</objective>

<context>
@.planning/phases/32-server-templates-for-hobby-and-pro-plans/32-01-SUMMARY.md
@api/src/domain/server/template/model.rs
@app/src/features/server/CreateServerModal.jsx

# Pattern from Phase 32:
# - Templates stored in database with fallback to hardcoded defaults
# - Frontend reads templates from GET /api/v1/templates
# - Game type dropdown dynamically populated from template.game_type values
# - When user selects game type, variants shown from filtered templates
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Bedrock template to fallback templates</name>
  <files>api/src/domain/server/template/model.rs</files>
  <action>
    Add a new Bedrock template to the `Template::fallback()` method in model.rs.
    
    Template details per D-01, D-02, D-03:
    - game_type: "bedrock"
    - variant: "default"
    - display_name: "Minecraft Bedrock"
    - description: "Minecraft Bedrock Edition dedicated server"
    - docker_image: "itzg/minecraft-bedrock-server"
    - default_port: 19132 (per D-02 - different from Java 25565)
    - default_env: Bedrock-specific env vars (GAMEMODE, DIFFICULTY, etc.)
    - default_startup_command: null (uses container default)
    - is_active: true
    
    Add after Valheim template entry (line ~174 in fallback()).
  </action>
  <verify>
    <automated>cargo check --package api 2>&1 | head -30</automated>
  </verify>
  <done>
    Backend compiles, Bedrock template exists in fallback list
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Bedrock appears in frontend dropdown</name>
  <files>api/src/domain/server/template/model.rs</files>
  <action>
    No code changes needed - verify that the templates API returns the Bedrock template.
    The frontend CreateServerModal.jsx already dynamically reads from /api/v1/templates
    and populates the game type dropdown using: [...new Set(templates.map(t => t.game_type))]
    
    Confirm by checking that calling GET /api/v1/templates includes "bedrock" in the game_type list.
  </action>
  <verify>
    <automated>cargo build --package api 2>&1 | tail -5</automated>
  </verify>
  <done>
    Backend builds successfully, Bedrock template available via API
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Database | Template data flows from DB (or fallback) to API response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Information Disclosure | templates API | accept | Non-sensitive game type data, no auth risk |
| T-36-02 | Injection | template fallback | mitigate | Hardcoded template values, no user input |
</threat_model>

<verification>
- Backend compiles: `cd api && cargo check`
- Template model has bedrock entry in fallback list
- No database migration needed (uses fallback pattern)
</verification>

<success_criteria>
1. Bedrock template exists in fallback templates
2. GET /api/v1/templates returns bedrock in game_type list
3. Frontend create server dropdown shows "Bedrock" as option
4. Server created with game_type=bedrock uses docker_image=itzg/minecraft-bedrock-server
5. Server created with game_type=bedrock uses default_port=19132
</success_criteria>

<output>
After completion, create `.planning/phases/36-menambahkan-fungsi-untuk-server-untuk-bedrock-pocket/36-01-SUMMARY.md`
</output>