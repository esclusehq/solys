---
phase: 20-streamline-agent-installation-di-vps
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/pages/Nodes.jsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "User can get installation token with one click"
    - "User can copy token to clipboard with one click"
    - "User can see download link for agent"
    - "User can see installation guide"
  artifacts:
    - path: app/src/pages/Nodes.jsx
      provides: NodeDetails component with installation modal
      contains: GetInstallationToken button, InstallationModal
  key_links:
    - from: NodeDetails
      to: generatedToken
      via: state or prop
      pattern: generatedToken|onGenerateToken
---

<objective>
Add streamlined agent installation UI to NodeDetails page - enabling users to get installation token, copy to clipboard, see download link, and view installation guide all in one modal.

Purpose: Simplify node agent installation on VPS by providing clear instructions and one-click copy functionality
Output: Updated Nodes.jsx with InstallationModal component
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-streamline-agent-installation-di-vps/20-CONTEXT.md
@.planning/STATE.md
@.planning/ROADMAP.md

# Read existing NodeDetails component to understand current structure
@app/src/pages/Nodes.jsx
</context>

<read_first>
app/src/pages/Nodes.jsx - Understand current NodeDetails component, token modal, and copy functionality
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Add GetInstallationToken button to NodeDetails</name>
  <files>app/src/pages/Nodes.jsx</files>
  <action>
Add a "Get Installation Token" button in the NodeDetails component near the existing token generation controls. The button should open the installation modal. Use a distinct style (e.g., cyan accent) to differentiate from API key controls.

Example placement: After the "Generate Token" button in the token section of NodeDetails.

Per D-01 (UI Improvements):
- Button text: "Get Installation Token"
- Style: Uses .glow-cyan class for accent
- OnClick: Opens installation modal
  </action>
  <verify>
<automated>grep -n "Get Installation Token" app/src/pages/Nodes.jsx</automated>
  </verify>
  <done>Button visible in NodeDetails near token controls</done>
</task>

<task type="auto">
  <name>Task 2: Create InstallationModal component with copy functionality</name>
  <files>app/src/pages/Nodes.jsx</files>
  <action>
Create and render the InstallationModal when the user clicks "Get Installation Token". The modal displays:

1. **Registration Token** - Shows the generated token (from generatedToken or onGenerateToken)
2. **Copy Button** - One-click copy using navigator.clipboard.writeText(token)
   - Button text: "Copy" → "Copied!" on success
   - Uses clipboard API
3. **Download Link** - Link to agent download
   - For binary: https://github.com/escluse/agent/releases/latest/download/agent-${os}-${arch}
   - For Docker: docker-compose.yml link or inline command
4. **Installation Guide** - Simple 3-step guide:
   ```
   1. Copy registration token above
   2. Download agent: [Download Link]
   3. Run: curl -sL <agent-url> | bash -s --token <token>
   ```

Modal structure:
- Header: "Install Agent on VPS"
- Body: Token display + copy button, download link, install commands
- Footer: Close button

Use existing Modal component pattern from the codebase.
  </action>
  <verify>
<automated>grep -n "InstallationModal\|Install Agent" app/src/pages/Nodes.jsx</automated>
  </verify>
  <done>Modal displays token, copy button, download link, and 3-step installation guide</done>
</task>

<task type="auto">
  <name>Task 3: Add copy to clipboard functionality</name>
  <files>app/src/pages/Nodes.jsx</files>
  <action>
Implement copy button functionality in the InstallationModal:

```javascript
const [copied, setCopied] = useState(false);

const handleCopy = async () => {
  try {
    await navigator.clipboard.writeText(token);
    setCopied(true);
    setTimeout(() => setCopied(false), 2000);
  } catch (err) {
    console.error('Failed to copy:', err);
  }
};
```

The button shows "Copy" initially, changes to "Copied!" for 2 seconds after successful copy.
  </action>
  <verify>
<automated>grep -n "navigator.clipboard.writeText\|setCopied" app/src/pages/Nodes.jsx</automated>
  </verify>
  <done>Copy button works - changes to "Copied!" for 2 seconds after click</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Token passes to frontend via API - no sensitive data exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | Installation token displayed in UI | accept | Tokens are short-lived (24h expiry by default), user must explicitly view |
</threat_model>

<verification>
Overall phase checks:
- [ ] Get Installation Token button visible in NodeDetails
- [ ] InstallationModal renders with token, copy button, download link
- [ ] Copy button copies token to clipboard
- [ ] Installation guide displays 3 steps
</verification>

<success_criteria>
Measurable completion:
- User clicks "Get Installation Token" → modal opens
- User sees registration token in modal
- User clicks "Copy" → token copied, button shows "Copied!"
- User sees download link for agent
- User sees installation commands
</success_criteria>

<output>
After completion, create `.planning/phases/20-streamline-agent-installation-di-vps/20-01-SUMMARY.md`
</output>