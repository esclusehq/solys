---
phase: 10-monitoring-integrations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "User can configure Discord webhook URL per server"
    - "Notification sent when server starts"
    - "Notification sent when server stops"
    - "Notification sent when server crashes"
    - "Notifications use rich embed format"
  artifacts:
    - path: "api/src/infrastructure/external_services/discord_client.rs"
      provides: "Server event notification method"
    - path: "app/src/features/settings/ServerSettingsPage.jsx"
      provides: "Webhook URL configuration UI"
    - path: "api/src/application/services/server_event_notifier.rs"
      provides: "Event emission on lifecycle actions"
  key_links:
    - from: "api/src/application/services/server_event_notifier.rs"
      to: "api/src/infrastructure/external_services/discord_client.rs"
      via: "send_event_notification"
      pattern: "discord.*send"
---

<objective>
Add per-server Discord webhook notifications for server lifecycle events.

Purpose: Users receive Discord notifications when their server starts, stops, or crashes.
Output: Server settings UI for webhook configuration, event emission on lifecycle changes.
</objective>

<context>
@api/src/infrastructure/external_services/discord_client.rs
@api/src/domain/entities/server.rs
@app/src/features/settings/ServerSettingsPage.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add server event notification methods to DiscordClient</name>
  <files>api/src/infrastructure/external_services/discord_client.rs</files>
  <action>
Extend DiscordClient with server event methods:
1. add send_server_event method with parameters:
   - webhook_url, server_name, event_type, message
2. event_type mapping:
   - "server.started" → 🟢 "Server Started" embed (green)
   - "server.stopped" → 🔴 "Server Stopped" embed (red)
   - "server.crashed" → 💥 "Server Crashed" embed (red, urgent)
   - "backup.complete" → 💾 "Backup Complete" embed (blue)
   - "restore.complete" → ♻️ "Restore Complete" embed (green)
3. Each message includes server name, timestamp, relevant details
  </action>
  <verify>
<automated>grep -q "send_server_event" api/src/infrastructure/external_services/discord_client.rs</automated>
  </verify>
  <done>DiscordClient has server event notification methods</done>
</task>

<task type="auto">
  <name>Task 2: Create ServerEventNotifier service</name>
  <files>api/src/application/services/server_event_notifier.rs</files>
  <action>
Create new ServerEventNotifier service:
1. Takes server_id and DiscordClient
2. fetch server details to get discord_webhook_url
3. If webhook_url is configured, call DiscordClient.send_server_event
4. Handle errors gracefully (log but don't fail)
5. Export notify_server_started, notify_server_stopped, notify_server_crashed, notify_backup_complete, notify_restore_complete functions
  </action>
  <verify>
<automated>ls api/src/application/services/server_event_notifier.rs</automated>
  </verify>
  <done>ServerEventNotifier service exists with event methods</done>
</task>

<task type="auto">
  <name>Task 3: Wire event notifications to lifecycle handlers</name>
  <files>api/src/presentation/handlers/server_handlers.rs</files>
  <action>
In server_handlers.rs, add event notifications:
1. Import ServerEventNotifier
2. In start handler: call notify_server_started on success
3. In stop handler: call notify_server_stopped on success
4. In restart handler: call notify_server_started after restart
5. In crash detection (from Phase 7 metrics): call notify_server_crashed
6. Ensure events fire after successful operation, not on failure
  </action>
  <verify>
<automated>grep -q "notify_server" api/src/presentation/handlers/server_handlers.rs</automated>
  </verify>
  <done>Server lifecycle events trigger Discord notifications</done>
</task>

<task type="auto">
  <name>Task 4: Add webhook URL field to server settings UI</name>
  <files>app/src/features/settings/ServerSettingsPage.jsx</files>
  <action>
1. Read existing ServerSettingsPage or create if not exists
2. Add Discord Webhook section:
   - Label: "Discord Webhook URL"
   - Input field for webhook URL (text input)
   - Help text: "Optional - Get from Discord server settings > Integrations"
   - Save button to update server config
3. On save: call PATCH /api/servers/:id with discord_webhook_url
4. Show success/error toast after save
  </action>
  <verify>
<automated>grep -q "discord_webhook" app/src/features/settings/ServerSettingsPage.jsx</automated>
  </verify>
  <done>Server settings UI has Discord webhook configuration</done>
</task>

</tasks>

<verification>
- [ ] Discord webhook URL can be saved per server
- [ ] Starting server sends Discord notification
- [ ] Stopping server sends Discord notification
- [ ] Crashed server sends Discord notification
- [ ] Notifications use rich embed format with appropriate colors
</verification>

<success_criteria>
Discord notifications sent for server start, stop, crash events when webhook URL is configured.
</success_criteria>

<output>
After completion, create `.planning/phases/10-monitoring-integrations/10-02-SUMMARY.md`
</output>
