---
phase: 10-monitoring-integrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "User can view historical CPU usage graph (24h)"
    - "User can view historical RAM usage graph (24h)"
    - "User can view historical disk usage graph (24h)"
    - "Graphs are interactive (hover shows values)"
  artifacts:
    - path: "api/src/presentation/handlers/metrics_handlers.rs"
      provides: "Historical metrics API endpoints"
      exports: ["GET /metrics/:server_id/history"]
    - path: "app/src/features/monitoring/ResourceGraph.jsx"
      provides: "Recharts line chart component"
    - path: "app/src/pages/servers/ServerDetailsPage.jsx"
      provides: "Server details with historical graphs"
  key_links:
    - from: "app/src/features/monitoring/ResourceGraph.jsx"
      to: "api/src/presentation/handlers/metrics_handlers.rs"
      via: "fetch /metrics/:server_id/history"
      pattern: "fetch.*metrics.*history"
---

<objective>
Add historical resource graphs to server details page using Recharts.

Purpose: Users can visualize CPU, RAM, and disk usage trends over the past 24 hours.
Output: Interactive line charts on server details page showing historical resource data.
</objective>

<context>
@api/src/domain/entities/server_metrics.rs
@api/src/infrastructure/repositories/postgres_metrics_repository.rs
@app/src/pages/servers/ServerDetailsPage.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts and create ResourceGraph component</name>
  <files>app/package.json, app/src/features/monitoring/ResourceGraph.jsx</files>
  <action>
1. Install recharts: npm install recharts
2. Create app/src/features/monitoring/ResourceGraph.jsx with:
   - Recharts LineChart component for historical data
   - Props: data (array), dataKey (string), color, title, unit
   - XAxis showing time, YAxis with unit
   - Tooltip and Legend components
   - ResponsiveContainer wrapper
3. Export ResourceGraph as default
  </action>
  <verify>
    <automated>grep -q "recharts" app/package.json && ls app/src/features/monitoring/ResourceGraph.jsx</automated>
  </verify>
  <done>Recharts installed, ResourceGraph component exists</done>
</task>

<task type="auto">
  <name>Task 2: Add historical metrics API endpoint</name>
  <files>api/src/presentation/handlers/metrics_handlers.rs, api/src/presentation/routes.rs</files>
  <action>
1. In metrics_handlers.rs, add get_metrics_history function:
   - Accept server_id, optional hours parameter (default 24)
   - Query postgres_metrics_repository for metrics in time range
   - Return Vec<ServerMetrics> ordered by created_at
2. Add route: GET /metrics/:server_id/history in routes.rs
   - Mount at /api prefix
  </action>
  <verify>
<automated>grep -q "history" api/src/presentation/handlers/metrics_handlers.rs</automated>
  </verify>
  <done>Historical metrics endpoint exists and returns data</done>
</task>

<task type="auto">
  <name>Task 3: Integrate historical graphs into ServerDetailsPage</name>
  <files>app/src/pages/servers/ServerDetailsPage.jsx</files>
  <action>
1. Add state for historical metrics: const [metricsHistory, setMetricsHistory] = useState([])
2. Add useEffect to fetch historical data on mount:
   - fetch /api/metrics/${id}/history
   - Transform data for each metric type
3. Add ResourceGraph components after metrics display:
   - CPU Usage graph (cpu_usage %)
   - Memory Usage graph (memory_usage_mb)
   - Disk Usage graph (disk_usage_mb)
4. Use different colors: cyan for CPU, purple for RAM, yellow for disk
  </action>
  <verify>
<automated>grep -q "ResourceGraph" app/src/pages/servers/ServerDetailsPage.jsx</automated>
  </verify>
  <done>Server details page shows historical resource graphs</done>
</task>

</tasks>

<verification>
- [ ] Historical data fetches from API successfully
- [ ] Three graphs display (CPU, RAM, Disk) with 24h data
- [ ] Graphs are interactive with hover tooltips
- [ ] Page renders without errors
</verification>

<success_criteria>
Historical resource graphs visible on server details page showing 24h of data with interactive tooltips.
</success_criteria>

<output>
After completion, create `.planning/phases/10-monitoring-integrations/10-01-SUMMARY.md`
</output>
