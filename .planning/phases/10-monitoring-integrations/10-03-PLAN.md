---
phase: 10-monitoring-integrations
plan: 03
type: execute
wave: 2
depends_on: [10-02]
files_modified: []
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "User can schedule automated tasks via UI time picker"
    - "Scheduled tasks execute at configured times"
    - "Task types: backup, restart, stop, custom command"
    - "User can view/edit/delete scheduled tasks"
  artifacts:
    - path: "api/migrations/XXX_add_cron_tasks_table.sql"
      provides: "Cron task storage schema"
    - path: "api/src/domain/entities/cron_task.rs"
      provides: "CronTask entity model"
    - path: "api/src/application/services/scheduler_service.rs"
      provides: "Cron task scheduler execution"
    - path: "app/src/features/scheduling/ScheduledTasksPage.jsx"
      provides: "UI for task scheduling"
  key_links:
    - from: "api/src/application/services/scheduler_service.rs"
      to: "api/src/infrastructure/repositories"
      via: "cron_schedule crate"
      pattern: "cron.*schedule"
---

<objective>
Add UI-based cron task scheduling for server automation.

Purpose: Users can schedule automated tasks (backups, restarts, commands) using a time picker UI.
Output: Scheduled tasks page, scheduler service executing tasks at configured times.
</objective>

<context>
@api/src/domain/entities/server.rs
@app/src/features/settings/ServerSettingsPage.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cron_tasks table migration</name>
  <files>api/migrations/XXX_add_cron_tasks_table.sql</files>
  <action>
Create migration for cron_tasks table:
1. Columns: id (UUID), server_id (UUID FK), user_id (UUID FK), task_type (TEXT), schedule_cron (TEXT), command (TEXT nullable), enabled (BOOL), last_run (TIMESTAMP), next_run (TIMESTAMP), created_at, updated_at
2. task_type enum: backup, restart, stop, command
3. schedule_cron: cron expression or simplified time format
4. Add index on server_id and next_run for efficient querying
  </action>
  <verify>
<automated>grep -q "cron_tasks" api/migrations/*.sql</automated>
  </verify>
  <done>Cron tasks table exists in database schema</done>
</task>

<task type="auto">
  <name>Task 2: Create CronTask entity and repository</name>
  <files>api/src/domain/entities/cron_task.rs, api/src/infrastructure/repositories/postgres_cron_task_repository.rs</files>
<action>
1. Create CronTask entity in domain/entities/cron_task.rs:
   - id, server_id, user_id, task_type, schedule_cron, command, enabled, last_run, next_run, created_at, updated_at
2. Create PostgresCronTaskRepository in infrastructure/repositories:
   - find_by_server_id, create, update, delete, find_due_tasks methods
   - Update next_run after each execution
  </action>
  <verify>
<automated>ls api/src/domain/entities/cron_task.rs && ls api/src/infrastructure/repositories/postgres_cron_task_repository.rs</automated>
  </done>
  <done>CronTask entity and repository exist</done>
</task>

<task type="auto">
  <name>Task 3: Create SchedulerService for task execution</name>
  <files>api/src/application/services/scheduler_service.rs</files>
<action>
Create SchedulerService:
1. Uses cron crate for parsing schedules
2. find_due_tasks queries tasks where next_run <= now
3. For each task:
   - backup: trigger backup via existing backup service
   - restart: trigger server restart
   - stop: trigger server stop
   - command: send via RCON if available
4. Update last_run and calculate next_run after execution
5. Handle errors gracefully (log, mark task as failed)
6. Run as background task in API service
  </action>
  <verify>
<automated>grep -q "SchedulerService" api/src/application/services/scheduler_service.rs</automated>
  </verify>
  <done>SchedulerService executes scheduled tasks</done>
</task>

<task type="auto">
<parameter name="Task 4: Add scheduled tasks CRUD API endpoints</name>
  <files>api/src/presentation/handlers/cron_task_handlers.rs, api/src/presentation/routes.rs</files>
  <action>
1. Create cron_task_handlers.rs with:
   - GET /servers/:server_id/tasks - list all tasks for server
   - POST /servers/:server_id/tasks - create new scheduled task
   - PATCH /tasks/:task_id - update task
   - DELETE /tasks/:task_id - delete task
2. Request body: { task_type, schedule_cron, command?, enabled }
3. Validate schedule_cron format
4. Return created task with next_run calculated
  </action>
  <verify>
<automated>grep -q "tasks" api/src/presentation/handlers/cron_task_handlers.rs</automated>
  </verify>
  <done>API endpoints for scheduled task CRUD exist</done>
</task>

<task type="auto">
  <name>Task 5: Create ScheduledTasksPage UI</name>
  <files>app/src/features/scheduling/ScheduledTasksPage.jsx, app/src/App.jsx</files>
  <action>
1. Create app/src/features/scheduling/ScheduledTasksPage.jsx:
   - Fetch tasks from GET /api/servers/:server_id/tasks
   - Display tasks in list with: type, schedule, enabled toggle, actions
   - Add new task form: task type dropdown, time picker/cron input, command field (if type=command)
   - Delete confirmation modal
2. Add route in App.jsx: /servers/:id/scheduled-tasks
   - Link from ServerDetailsPage sidebar
3. Use consistent styling with existing pages
  </action>
  <verify>
<automated>grep -q "ScheduledTasksPage" app/src/App.jsx</automated>
  </verify>
  <done>UI exists for managing scheduled tasks</done>
</task>

</tasks>

<verification>
- [ ] User can create scheduled tasks with time picker
- [ ] Tasks execute at configured times
- [ ] Task list shows all scheduled tasks per server
- [ ] User can enable/disable and delete tasks
- [ ] UI integrates with server details page
</verification>

<success_criteria>
Users can schedule automated tasks via UI, tasks execute at configured times.
</success_criteria>

<output>
After completion, create `.planning/phases/10-monitoring-integrations/10-03-SUMMARY.md`
</output>
