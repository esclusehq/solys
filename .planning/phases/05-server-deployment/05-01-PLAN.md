---
phase: 05-server-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/migrations/20260409000001_create_game_types_table.sql
  - api/src/domain/server/entities/game_type.rs
  - api/src/application/use_cases/game_type_use_cases.rs
autonomous: true
requirements:
  - DEPLOY-01

must_haves:
  truths:
    - "User can select game type (Minecraft, Palworld, etc.) when deploying"
    - "Game type configuration is database-driven with code fallback"
  artifacts:
    - path: "api/migrations/20260409000001_create_game_types_table.sql"
      provides: "game_types table with docker_image, default_ports, default_env, startup_command"
    - path: "api/src/domain/server/entities/game_type.rs"
      provides: "GameType entity with all configuration fields"
    - path: "api/src/application/use_cases/game_type_use_cases.rs"
      provides: "list_game_types, get_game_type by identifier"
  key_links:
    - from: "create_server_use_case.rs"
      to: "game_type_use_cases.rs"
      via: "get_game_type(game_type).ok_or_else(|| fallback)"
      pattern: "game_type.*map.*docker_image"
---

<objective>
Implement database-driven game types with code fallback (D-16).

Purpose: Allow adding new game types without code deployment. Store docker image, default ports, environment variables, startup command in database. Provide code-based fallback for schema validation and default templates.

Output: game_types table, GameType entity, use cases for listing/retrieving game types.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@api/src/domain/server/model.rs (game_type field exists)
@api/src/application/dto/server_dtos.rs (game_type in DTOs)
@api/src/infrastructure/executors/agent_server_executor.rs (hardcoded docker image)
@api/migrations/20260324000004_create_plans_table.sql (existing plans table pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create game_types database table</name>
  <files>api/migrations/20260409000001_create_game_types_table.sql</files>
  <action>
Create migration with:
- id (UUID, PK)
- identifier (VARCHAR, unique) - e.g., "minecraft", "palworld", "valheim"
- display_name (VARCHAR) - e.g., "Minecraft", "Palworld"
- description (TEXT)
- docker_image (VARCHAR) - e.g., "itzg/minecraft-server"
- default_ports (JSONB) - {"game": 25565, "rcon": 25575}
- default_env (JSONB) - {"EULA": "TRUE", "MODE": "survival"}
- startup_command (TEXT) - optional override
- capabilities (JSONB) - {"rcon": true, "mods": true, "backup": true}
- is_active (BOOLEAN, default true)
- sort_order (INTEGER)
- created_at, updated_at

Insert default game types: minecraft, palworld, valheim, fabric, forge
  </action>
  <verify>
Migration runs successfully against PostgreSQL
  </verify>
  <done>
game_types table exists with 5 default game types
</done>
</task>

<task type="auto">
  <name>Task 2: Create GameType entity</name>
  <files>api/src/domain/server/entities/game_type.rs</files>
  <action>
Create GameType struct with:
- id: Uuid
- identifier: String
- display_name: String
- description: Option<String>
- docker_image: String
- default_ports: serde_json::Value (JSON object)
- default_env: serde_json::Value (JSON object)
- startup_command: Option<String>
- capabilities: serde_json::Value (JSON object)
- is_active: bool
- sort_order: i32
- created_at, updated_at

Implement FromRow for sqlx
  </action>
  <verify>
File compiles with cargo check
  </verify>
  <done>
GameType entity defined and usable in queries
</done>
</task>

<task type="auto">
  <name>Task 3: Create game type use cases</name>
  <files>api/src/application/use_cases/game_type_use_cases.rs</files>
  <action>
Create:
- list_game_types() -> Vec<GameType> - returns active game types sorted by sort_order
- get_game_type(identifier: &str) -> Option<GameType> - lookup by identifier
- get_fallback_game_type(identifier: &str) -> GameType - returns hardcoded fallback for unknown types

Fallback for "minecraft": docker_image="itzg/minecraft-server", ports={"game":25565,"rcon":25575}
Fallback for unknown: return error with suggestion to use known types
  </action>
  <verify>
cargo check passes
  </verify>
  <done>
Use cases enable game type lookup with database + fallback pattern
</done>
</task>

</tasks>

<verification>
Game types queryable via use case, fallback works for unknown types
</verification>

<success_criteria>
User can select game type when deploying - game type config comes from database, falls back to code defaults
</success_criteria>

<output>
After completion, create `.planning/phases/05-server-deployment/05-01-SUMMARY.md`
</output>