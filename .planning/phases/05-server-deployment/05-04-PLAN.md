---
phase: 05-server-deployment
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - api/migrations/20260409000004_create_deployment_configs_table.sql
  - api/src/domain/server/entities/deployment_config.rs
  - api/src/application/use_cases/deployment_config_use_cases.rs
  - api/migrations/20260409000005_add_deployment_snapshot_to_servers.sql
autonomous: true
requirements:
  - DEPLOY-01

must_haves:
  truths:
    - "Deployment configs stored in separate table (source of truth)"
    - "Server stores immutable snapshot at deployment time"
  artifacts:
    - path: "api/migrations/20260409000004_create_deployment_configs_table.sql"
      provides: "deployment_configs table for templates"
    - path: "api/src/domain/server/entities/deployment_config.rs"
      provides: "DeploymentConfig entity"
    - path: "api/src/application/use_cases/deployment_config_use_cases.rs"
      provides: "create_deployment_config(), get_config(), create_snapshot()"
    - path: "api/migrations/20260409000005_add_deployment_snapshot_to_servers.sql"
      provides: "servers.deployment_snapshot column for immutable runtime config"
  key_links:
    - from: "create_server_use_case.rs"
      to: "deployment_config_use_cases.rs"
      via: "create_snapshot(server, config) -> JSON snapshot"
      pattern: "snapshot.*deployment_config"
---

<objective>
Implement deployment config storage with hybrid approach (D-19).

Purpose: deployment_configs table for source of truth (templates that can be updated). servers.deployment_snapshot for immutable runtime config created at deployment time. Ensures consistency - config changes don't affect running servers.

Output: deployment_configs table, DeploymentConfig entity, snapshot logic, servers column
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@api/src/domain/server/model.rs (Server has config JSONB field)
@api/src/infrastructure/executors/agent_server_executor.rs (build_deploy_config uses server fields)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deployment_configs table</name>
  <files>api/migrations/20260409000004_create_deployment_configs_table.sql</files>
  <action>
Create migration with:
- id (UUID, PK)
- name (VARCHAR) - config template name
- game_type_id (UUID, FK to game_types) - which game type
- docker_image (VARCHAR) - can override game type default
- ports (JSONB) - {"game": 25565, "rcon": 25575}
- env_vars (JSONB) - environment variables
- startup_command (TEXT) - override default startup
- resources (JSONB) - default resource limits
- volume_path (VARCHAR) - data mount path
- network_name (VARCHAR) - Docker network
- is_default (BOOLEAN) - whether this is default for game type
- created_at, updated_at

Insert defaults for minecraft: "minecraft-default" with standard port/env config
  </action>
  <verify>
Migration runs successfully
  </verify>
  <done>
deployment_configs table exists with default configs
</done>
</task>

<task type="auto">
  <name>Task 2: Create DeploymentConfig entity</name>
<files>api/src/domain/server/entities/deployment_config.rs</files>
<action>
Create DeploymentConfig struct:
- id: Uuid
- name: String
- game_type_id: Option<Uuid>
- docker_image: String
- ports: serde_json::Value (JSON object)
- env_vars: serde_json::Value (JSON object)
- startup_command: Option<String>
- resources: serde_json::Value (JSON object)
- volume_path: String
- network_name: String
- is_default: bool
- created_at, updated_at
  </action>
  <verify>
cargo check passes
  </verify>
  <done>
DeploymentConfig entity defined
</done>
</task>

<task type="auto">
  <name>Task 3: Add deployment_snapshot column to servers</name>
<files>api/migrations/20260409000005_add_deployment_snapshot_to_servers.sql</files>
  <action>
Add column:
ALTER TABLE servers ADD COLUMN deployment_snapshot JSONB NOT NULL DEFAULT '{}';

This stores the immutable config at deployment time.
  </action>
  <verify>
Migration runs successfully
  </verify>
  <done>
servers table has deployment_snapshot column
</done>
</task>

<task type="auto">
  <name>Task 4: Create deployment config use cases</name>
<files>api/src/application/use_cases/deployment_config_use_cases.rs</files>
<action>
Create:
- list_deployment_configs(game_type_id: Option<Uuid>) -> Vec<DeploymentConfig>
- get_deployment_config(id: Uuid) -> Option<DeploymentConfig>
- get_default_config(game_type_id: Uuid) -> Option<DeploymentConfig>
- create_snapshot(server: &Server, config: &DeploymentConfig) -> serde_json::Value

create_snapshot merges: server-specific values + config values + game_type defaults
Snapshot includes: image, ports, env_vars, resources, volume_path, network_name, timestamp
  </action>
  <verify>
cargo check passes
  </verify>
  <done>
Deployment config use cases with snapshot creation implemented
</done>
</task>

</tasks>

<verification>
Deployment config template retrievable, snapshot created at server creation
</verification>
<success_criteria>
Running servers unaffected by config template changes - snapshot preserves runtime config
</success_criteria>

<output>
After completion, create `.planning/phases/05-server-deployment/05-04-SUMMARY.md`
</output>