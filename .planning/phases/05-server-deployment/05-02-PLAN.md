---
phase: 05-server-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/migrations/20260409000002_create_port_pools_table.sql
  - api/src/domain/server/entities/port_pool.rs
  - api/src/application/use_cases/port_allocation_use_case.rs
autonomous: true
requirements:
  - DEPLOY-01

must_haves:
  truths:
    - "Ports allocated dynamically from pool"
    - "Database tracks allocated ports to prevent conflicts"
  artifacts:
    - path: "api/migrations/20260409000002_create_port_pools_table.sql"
      provides: "port_pools table tracking allocations"
    - path: "api/src/domain/server/entities/port_pool.rs"
      provides: "PortPool entity"
    - path: "api/src/application/use_cases/port_allocation_use_case.rs"
      provides: "allocate_port(), release_port(), get_available_port()"
  key_links:
    - from: "create_server_use_case.rs"
      to: "port_allocation_use_case.rs"
      via: "allocate_port(node_id) -> port"
      pattern: "port.*allocate_port"
---

<objective>
Implement dynamic port pool allocation (D-17).

Purpose: System allocates ports from pool, database tracks allocated ports to prevent conflicts. Each node has its own port range to avoid cross-node conflicts.

Output: port_pools table, PortPool entity, allocation use case
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@api/src/domain/server/model.rs (port field exists on Server)
@api/src/infrastructure/executors/agent_server_executor.rs (uses port for game_port, rcon_port = port+10)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create port_pools table</name>
  <files>api/migrations/20260409000002_create_port_pools_table.sql</files>
  <action>
Create migration with:
- id (UUID, PK)
- node_id (UUID, FK to nodes) - nullable for global pool
- port_range_start (INTEGER) - start of allowed range
- port_range_end (INTEGER) - end of allowed range
- current_port (INTEGER) - next port to try (cursor)
- allocated_ports (JSONB) - array of currently allocated ports
- protocol (VARCHAR) - "tcp" or "udp" or "both"
- is_active (BOOLEAN)
- created_at, updated_at

Insert default pool: node_id=NULL (global), range 25565-25665, protocol="tcp"
  </action>
  <verify>
Migration runs successfully
  </verify>
  <done>
port_pools table exists with default global pool
</done>
</task>

<task type="auto">
  <name>Task 2: Create PortPool entity</name>
<files>api/src/domain/server/entities/port_pool.rs</files>
  <action>
Create PortPool struct:
- id: Uuid
- node_id: Option<Uuid>
- port_range_start: i32
- port_range_end: i32
- current_port: i32
- allocated_ports: Vec<i32>
- protocol: String
- is_active: bool
- created_at, updated_at
  </action>
  <verify>
cargo check passes
  </verify>
  <done>
PortPool entity defined
</done>
</task>

<task type="auto">
  <name>Task 3: Create port allocation use case</name>
<files>api/src/application/use_cases/port_allocation_use_case.rs</files>
  <action>
Create:
- allocate_port(node_id: Option<Uuid>) -> Result<i32> - finds available port, marks as allocated
- release_port(port: i32, node_id: Option<Uuid>) -> Result<()> - frees port back to pool
- get_next_available_port(pool: &PortPool) -> Option<i32> - finds unallocated port in range

Algorithm: Start from current_port, scan forward checking allocated_ports array, wrap around at range_end
  </action>
  <verify>
cargo check passes
  </verify>
  <done>
Port allocation with conflict prevention implemented
</done>
</task>

</tasks>

<verification>
Port allocation works - allocate returns unique port, release makes it available again
</verification>

<success_criteria>
Ports allocated correctly - no conflicts, ports freed on server deletion
</success_criteria>

<output>
After completion, create `.planning/phases/05-server-deployment/05-02-SUMMARY.md`
</output>