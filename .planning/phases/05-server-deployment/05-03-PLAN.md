---
phase: 05-server-deployment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - api/migrations/20260409000003_create_resource_plans_table.sql
  - api/src/domain/server/entities/resource_plan.rs
  - api/src/application/use_cases/resource_plan_use_cases.rs
autonomous: true
requirements:
  - DEPLOY-01

must_haves:
  truths:
    - "User selects from predefined resource plans (2GB, 4GB, 8GB, 16GB)"
    - "Each plan has fixed CPU ratio based on RAM"
  artifacts:
    - path: "api/migrations/20260409000003_create_resource_plans_table.sql"
      provides: "resource_plans table with 2GB, 4GB, 8GB, 16GB options"
    - path: "api/src/domain/server/entities/resource_plan.rs"
      provides: "ResourcePlan entity"
    - path: "api/src/application/use_cases/resource_plan_use_cases.rs"
      provides: "list_resource_plans(), get_resource_plan(ram_mb)"
  key_links:
    - from: "create_server_use_case.rs"
      to: "resource_plan_use_cases.rs"
      via: "get_resource_plan(ram_mb) -> (cpu_cores, ram_mb)"
      pattern: "ram_mb.*resource_plan"
---

<objective>
Implement plan-based resource limits (D-18).

Purpose: User picks from predefined plans: 2GB, 4GB, 8GB, 16GB. Each has fixed CPU ratio (2GB=2 cores, 4GB=3 cores, 8GB=4 cores, 16GB=6 cores). Simplifies UX, prevents over-provisioning.

Output: resource_plans table, ResourcePlan entity, use case for lookup
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@api/src/domain/server/model.rs (resources field has ram, cpu, disk)
@api/src/application/dto/server_dtos.rs (ram_mb field in CreateServerRequest)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create resource_plans table</name>
  <files>api/migrations/20260409000003_create_resource_plans_table.sql</files>
  <action>
Create migration with:
- id (UUID, PK)
- name (VARCHAR) - e.g., "2gb", "4gb", "8gb", "16gb"
- display_name (VARCHAR) - e.g., "2 GB", "4 GB"
- ram_mb (INTEGER) - 2048, 4096, 8192, 16384
- cpu_cores (INTEGER) - 2, 3, 4, 6 (fixed ratios)
- disk_gb (INTEGER) - 10, 20, 50, 100
- price_monthly (DECIMAL) - optional cost tracking
- is_active (BOOLEAN)
- sort_order (INTEGER)
- created_at, updated_at

Insert default plans with CPU ratios: 2GB→2 cores, 4GB→3 cores, 8GB→4 cores, 16GB→6 cores
  </action>
  <verify>
Migration runs successfully
  </verify>
  <done>
resource_plans table exists with 4 predefined plans
</done>
</task>

<task type="auto">
  <name>Task 2: Create ResourcePlan entity</name>
<files>api/src/domain/server/entities/resource_plan.rs</files>
  <action>
Create ResourcePlan struct:
- id: Uuid
- name: String
- display_name: String
- ram_mb: i32
- cpu_cores: i32
- disk_gb: i32
- price_monthly: Option<Decimal>
- is_active: bool
- sort_order: i32
- created_at, updated_at
  </action>
  <verify>
cargo check passes
  </verify>
  <done>
ResourcePlan entity defined
</done>
</task>

<task type="auto">
  <name>Task 3: Create resource plan use cases</name>
<files>api/src/application/use_cases/resource_plan_use_cases.rs</files>
  <action>
Create:
- list_resource_plans() -> Vec<ResourcePlan> - returns active plans sorted
- get_resource_plan(ram_mb: i32) -> Option<ResourcePlan> - finds plan closest to requested RAM
- get_resource_plan_by_name(name: &str) -> Option<ResourcePlan> - lookup by name like "4gb"

Enforce: User can only pick from predefined plans - round up to nearest plan
  </action>
  <verify>
cargo check passes
  </verify>
  <done>
Resource plan lookup with plan enforcement implemented
</done>
</task>

</tasks>

<verification>
Resource plans queryable - UI can display 4 options, backend enforces plan selection
</verification>

<success_criteria>
Resource limits applied - server created with correct RAM and CPU from selected plan
</success_criteria>

<output>
After completion, create `.planning/phases/05-server-deployment/05-03-SUMMARY.md`
</output>