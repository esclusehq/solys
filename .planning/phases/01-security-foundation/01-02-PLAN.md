---
phase: 01-security-foundation
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - api/src/presentation/handlers/ws_handler.rs
  - api/src/presentation/handlers/terminal_handlers.rs
  - api/src/presentation/handlers/profiling_handlers.rs
  - api/src/presentation/handlers/docker_log_handler.rs
  - api/src/presentation/handlers/build_handlers.rs
  - api/src/infrastructure/executors/podman_server_executor.rs
  - api/src/domain/rbac/middleware.rs
  - api/src/domain/auth/middleware.rs
  - api/src/bootstrap/container.rs
  - api/src/infrastructure/executors/ssh_server_executor.rs
  - api/src/domain/usage/service.rs
  - api/src/shared/utils/mod.rs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "All presentation handlers use proper error types instead of .unwrap()"
    - "Infrastructure executors handle errors gracefully"
    - "No panic possible from unwrap() in production code paths"
  artifacts:
    - path: "api/src/presentation/handlers/ws_handler.rs"
      provides: "WebSocket handler with proper error handling"
    - path: "api/src/presentation/handlers/terminal_handlers.rs"
      provides: "Terminal handler with proper error handling"
    - path: "api/src/domain/usage/service.rs"
      provides: "Usage service with proper error handling"
  key_links:
    - from: "handlers"
      to: "AppError"
      via: "?".map_err() patterns
      pattern: "map_err.*AppError"
---

<objective>
Replace .unwrap() calls with proper error handling per D-02.

Purpose: Eliminate panic possible in production from unwrap() calls
Output: All handlers use Result types and error propagation
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@api/src/shared/errors/app_error.rs

# Files with unwrap() that need fixing (from grep):
@api/src/presentation/handlers/ws_handler.rs
@api/src/presentation/handlers/terminal_handlers.rs
@api/src/presentation/handlers/profiling_handlers.rs
@api/src/presentation/handlers/docker_log_handler.rs
@api/src/presentation/handlers/build_handlers.rs
@api/src/infrastructure/executors/podman_server_executor.rs
@api/src/domain/rbac/middleware.rs
@api/src/domain/auth/middleware.rs
@api/src/bootstrap/container.rs
@api/src/infrastructure/executors/ssh_server_executor.rs
@api/src/domain/usage/service.rs
@api/src/shared/utils/mod.rs
</context>

<interfaces>
<!-- Key error types to use -->

From api/src/shared/errors/app_error.rs:
```rust
pub enum AppError {
    InternalError(#[from] anyhow::Error),
    Domain(String),
    Infrastructure(String),
    NotFound,
    BadRequest(String),
    Forbidden,
}

impl IntoResponse for AppError { ... }
```

Pattern to replace:
```rust
// BAD - can panic
let value = something.unwrap();

// GOOD - proper error handling
let value = something.map_err(|e| AppError::BadRequest(e.to_string()))?;
```

Note: Some unwrap() in utils/mod.rs (regex compilation) are safe at startup - mark those as acceptable with comment explaining why.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix WebSocket and Terminal handlers</name>
  <files>api/src/presentation/handlers/ws_handler.rs, api/src/presentation/handlers/terminal_handlers.rs</files>
  <action>
ws_handler.rs line 28:
- Change: `cookie.strip_prefix("access_token=").unwrap().to_string()`
- To: `cookie.strip_prefix("access_token=").ok_or_else(|| AppError::BadRequest("Missing access token".to_string()))?.to_string()`

terminal_handlers.rs lines 34, 93, 108, 131:
- Convert json parsing with .unwrap() to .map_err() with AppError::BadRequest
- Use ? operator with proper error propagation
  </action>
  <verify>
    <automated>cargo check --package api 2>&1 | grep -E "error\[" | head -10</automated>
  </verify>
  <done>WebSocket and terminal handlers use proper error types</done>
</task>

<task type="auto">
<name>Task 2: Fix Profiling, Docker Log, and Build handlers</name>
<files>api/src/presentation/handlers/profiling_handlers.rs, api/src/presentation/handlers/docker_log_handler.rs, api/src/presentation/handlers/build_handlers.rs</files>
<action>
profiling_handlers.rs lines 437, 441:
- Fix content-type parsing with proper error handling

docker_log_handler.rs line 48:
- Fix container log retrieval with proper error handling

build_handlers.rs line 251:
- Fix build operation with proper error handling
  </action>
<verify>
    <automated>cargo check --package api 2>&1 | grep -E "error\[" | head -10</automated>
  </verify>
  <done>Profiling, docker log, and build handlers use proper error types</done>
</task>

<task type="auto">
<name>Task 3: Fix Infrastructure executors and domain middleware</name>
<files>api/src/infrastructure/executors/podman_server_executor.rs, api/src/infrastructure/executors/ssh_server_executor.rs, api/src/domain/rbac/middleware.rs, api/src/domain/auth/middleware.rs</files>
<action>
podman_server_executor.rs line 58:
- Handle container_host Option properly instead of unwrap

ssh_server_executor.rs line 27:
- Handle Session::new() error properly instead of unwrap

rbac/middleware.rs line 48:
- Handle permission check errors properly

auth/middleware.rs line 101:
- Handle extension extraction properly
  </action>
<verify>
    <automated>cargo check --package api 2>&1 | grep -E "error\[" | head -10</automated>
  </verify>
  <done>Executors and middleware use proper error types</done>
</task>

<task type="auto">
<name>Task 4: Fix remaining files</name>
<files>api/src/bootstrap/container.rs, api/src/domain/usage/service.rs, api/src/shared/utils/mod.rs</files>
<action>
container.rs line 183:
- Handle executor factory errors properly

usage/service.rs lines 231, 232, 350, 353:
- Handle chrono parsing with proper error handling (and_hms_opt returns Option)

utils/mod.rs lines 63, 74, 87:
- These regex compilations are safe at application startup (not runtime data)
- Add comments explaining they are startup-time only and won't panic from user input
- This is ACCEPTABLE - do NOT change these
  </action>
  <verify>
    <automated>cargo check --package api 2>&1 | grep -E "error\[" | head -10</automated>
  </verify>
  <done>Container, usage service fixed; utils marked with safe-startup comments</done>
</task>

</tasks>

<verification>
- [ ] cargo check passes with no errors
- [ ] No .unwrap() in handlers (except explicitly marked startup-safe)
- [ ] All error paths use AppError types
</verification>

<success_criteria>
- All .unwrap() calls replaced with proper error handling
- No panic possible from unwrap() in production code paths
- Error messages are user-friendly
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-foundation/01-02-SUMMARY.md`
</output>
