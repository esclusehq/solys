---
phase: 08-operations-integration
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/presentation/handlers/terminal_handlers.rs
  - api/src/presentation/handlers/file_handlers.rs
  - api/src/presentation/routes/server_routes.rs
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "RCON commands work for agent-based servers (no remote_id)"
    - "File list works for agent-based servers"
  artifacts:
    - path: "api/src/presentation/handlers/file_handlers.rs"
      provides: "Direct filesystem access for file operations"
      interface: "list_files, read_file, write_file"
    - path: "api/src/presentation/handlers/terminal_handlers.rs"
      provides: "Terminal command execution"
      interface: "exec_terminal, ws_terminal"
  key_links:
    - from: "POST /servers/:id/command"
      to: "terminal_handlers.rs"
      via: "route"
      pattern: "terminal_handlers::exec_terminal"
    - from: "GET /servers/:id/files"
      to: "file_handlers.rs"
      via: "route"
      pattern: "file_handlers::list_files"
---

<objective>
Fix gap: Operations (command, files, upload) fail for agent-based servers due to remote_id requirement.

Gap Analysis:
- POST /servers/:id/command → uses solys_client which requires remote_id
- POST /servers/:id/files/list → uses solys_client which requires remote_id
- Agent-based servers use "agent" executor_type, no remote_id set

Solution:
- Terminal commands should use terminal_handlers.rs (podman exec) - already works
- File operations should use file_handlers.rs (direct filesystem) - already works
- Fix: Ensure routes are properly connected to working handlers
</objective>

<context>
@.planning/phases/08-operations-integration/08-UAT.md
@.planning/phases/08-operations-integration/08-01-SUMMARY.md
@.planning/phases/08-operations-integration/08-02-SUMMARY.md
@.planning/phases/08-operations-integration/08-03-SUMMARY.md
@.planning/phases/08-operations-integration/08-04-SUMMARY.md
@.planning/phases/08-operations-integration/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify current route configuration and identify exact gaps</name>
  <files>api/src/presentation/routes/server_routes.rs</files>
  <action>
    1. Review server_routes.rs to see exact route configuration for:
       - /:id/command (line 109 in server_handlers shows ".route(\"/:id/command\", post(send_command))")
       - /:id/files - see which handler serves this
    2. Review server_handlers.rs router configuration
    3. List all handlers that require remote_id
    4. Confirm which routes use working handlers vs broken handlers
  </action>
  <verify>
    <automated>grep -n "remote_id" api/src/presentation/handlers/server_handlers.rs | wc -l</automated>
  </verify>
  <done>Clear document of which endpoints are broken and why</done>
</task>

<task type="auto">
  <name>Task 2: Fix POST /servers/:id/command to use terminal_handlers</name>
  <files>api/src/presentation/routes/server_routes.rs</files>
  <action>
    The POST /servers/:id/command route in server_handlers.rs uses solys_client.send_command which requires remote_id.
    
    FIX: Add route in server_routes.rs that points to terminal_handlers::exec_terminal instead.
    - terminal_handlers::exec_terminal uses podman exec (works without remote_id)
    - Verify terminal_handlers::exec_terminal exists and works (it does - uses workspace_root)
    
    After adding the route, test that it works for agent-based servers.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/servers/{test-server-id}/terminal/exec -H "Content-Type: application/json" -d '{"command":"echo test"}'</automated>
  </verify>
  <done>POST /servers/:id/command works via terminal handler for agent-based servers</done>
</task>

<task type="auto">
  <name>Task 3: Fix file operations to use file_handlers instead of server_handlers</name>
  <files>api/src/presentation/routes/server_routes.rs</files>
  <action>
    For file operations, check if there are duplicate routes:
    - server_handlers.rs has list_files requiring remote_id (lines 1233-1256)
    - file_handlers.rs has list_files using direct filesystem (lines 234-286)
    
    FIX: Ensure all file routes in server_routes.rs use file_handlers, not server_handlers.
    The routes in server_routes.rs already use file_handlers (verified in grep earlier).
    But test UAT reports issues - investigate why POST /files/list specifically fails.
    
    If there's a POST /files/list in server_handlers, it should be removed or fixed to delegate to file_handlers.
  </action>
  <verify>
    <automated>curl http://localhost:3000/api/servers/{test-server-id}/files</automated>
  </verify>
  <done>File list endpoint works for agent-based servers</done>
</task>

<task type="auto">
  <name>Task 4: Verify chunked upload works</name>
  <files>api/src/presentation/routes/server_routes.rs</files>
  <action>
    Verify chunked upload endpoint works:
    - PUT /servers/:id/files/upload/chunked → file_handlers::upload_chunk
    - file_handlers::upload_chunk uses direct filesystem (already verified)
    
    FIX: Ensure route is properly connected.
  </action>
  <verify>
    <automated>curl -X PUT http://localhost:3000/api/servers/{test-server-id}/files/upload/chunked -H "Content-Type: application/json" -d '{"filename":"test.txt","chunk_index":0,"total_chunks":1,"data":"dGVzdA=="}'</automated>
  </verify>
  <done>Chunked upload works for agent-based servers</done>
</task>

</tasks>

<verification>
## Gap Closure Verification

After fixes, run UAT tests again:
- Test 1: POST /servers/:id/command → should work (not "Server has no remote_id")
- Test 2: POST /files/list → should return file tree (not "Server has no remote_id")
- Test 3: Chunked upload → should work
</verification>

<success_criteria>
All UAT tests pass:
- [ ] POST /servers/:id/command works (returns command output, not error)
- [ ] GET /servers/:id/files returns file list (not empty [] or error)
- [ ] Chunked upload completes without remote_id error

Gap closure complete when agent-based servers can use all operations.
</success_criteria>

<output>
After completion, create `.planning/phases/08-operations-integration/08-06-SUMMARY.md`
</output>