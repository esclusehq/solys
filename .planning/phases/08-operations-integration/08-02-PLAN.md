---
phase: 08-operations-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/presentation/handlers/file_handlers.rs
  - app/src/components/FileManager.jsx
autonomous: true
requirements:
  - FILE-01
  - FILE-02
  - FILE-03

must_haves:
  truths:
    - "User can browse server files via file browser UI"
    - "User can upload files to server"
    - "User can download files from server"
  artifacts:
    - path: "api/src/presentation/handlers/file_handlers.rs"
      provides: "File CRUD operations"
      exports: ["list_files", "upload_file", "download_file", "read_file", "write_file"]
    - path: "app/src/components/FileManager.jsx"
      provides: "Web-based file browser UI"
      contains: "tree view"
  key_links:
    - from: "FileManager.jsx"
      to: "/api/servers/:id/files"
      via: "fetchApi"
      pattern: "fetchApi.*files"
    - from: "file_handlers.rs"
      to: "server filesystem"
      via: "get_secure_path"
      pattern: "get_secure_path"
---

<objective>
Verify and enhance file browser capability.

**Purpose:** Users need to browse, upload, and download server files via web interface. This plan verifies existing file handlers and web UI integration.

**Output:** Verified file browser with tree view, upload/download support.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@api/src/presentation/handlers/file_handlers.rs
@app/src/components/FileManager.jsx

<interfaces>
<!-- Key interfaces for file operations -->

From api/src/presentation/handlers/file_handlers.rs:
```rust
pub async fn list_files(
    Path(id): Path<Uuid>,
    Query(query): Query<FileQuery>,
) -> Result<impl IntoResponse, AppError>

pub async fn upload_file(
    Path(id): Path<Uuid>,
    Query(query): Query<FileQuery>,
    mut multipart: Multipart,
) -> Result<impl IntoResponse, AppError>

pub async fn download_file(
    Path(id): Path<Uuid>,
    Query(query): Query<FileQuery>,
) -> Result<impl IntoResponse, AppError>
```

Security function (file_handlers.rs):
```rust
async fn get_secure_path(
    container: &ApiState,
    server_id: Uuid,
    requested_path: &str,
) -> Result<(PathBuf, PathBuf), AppError> {
    // Blocks path traversal (..)
    // Validates user owns server
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify file handlers exist and work</name>
  <files>api/src/presentation/handlers/file_handlers.rs</files>
  <action>
    1. Verify list_files, upload_file, download_file handlers exist
    2. Verify get_secure_path validates path traversal (D-31)
    3. Verify server ownership check exists in handlers
    4. Document any gaps found
  </action>
  <verify>
    <automated>grep -n "^pub async fn" api/src/presentation/handlers/file_handlers.rs</automated>
  </verify>
  <done>All file handlers implemented with security checks</done>
</task>

<task type="auto">
<name>Task 2: Verify FileManager UI integration</name>
<files>app/src/components/FileManager.jsx</files>
<action>
  1. Verify FileManager calls /servers/:id/files endpoint
  2. Verify upload uses /servers/:id/files/upload
  3. Verify download uses /servers/:id/files/download
  4. Check if tree view exists or needs enhancement
</action>
<verify>
  <automated>grep -n "fetchApi\|/files" app/src/components/FileManager.jsx | head -20</automated>
</verify>
<done>FileManager UI integrates with backend endpoints</done>
</task>

<task type="auto">
  <name>Task 3: Add tree view support to file browser (D-29)</name>
  <files>app/src/components/FileManager.jsx</files>
<action>
  1. Add tree expand/collapse state for directories
  2. Show folder icon with expand arrow
  3. Lazy load children on expand (API call to list subdirectory)
  4. Show breadcrumb navigation
</action>
<verify>
  <automated>grep -n "tree\|expand\|collapse" app/src/components/FileManager.jsx</automated>
</verify>
<done>FileManager displays tree view with expand/collapse</done>
</task>

</tasks>

<verification>
- [ ] File list endpoint returns directory structure
- [ ] Upload endpoint accepts multipart files
- [ ] Download endpoint streams file to client
- [ ] Path traversal blocked
- [ ] Tree view UI working
</verification>

<success_criteria>
User can browse files in tree view, upload files with progress, and download files. Path security enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/08-operations-integration/08-02-SUMMARY.md`
</output>
