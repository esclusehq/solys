---
phase: 08-operations-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/presentation/handlers/terminal_handlers.rs
  - api/src/application/use_cases/send_command_use_case.rs
  - api/src/infrastructure/executors/rcon_server_executor.rs
autonomous: true
requirements:
  - RCON-01
  - RCON-02

must_haves:
  truths:
    - "User can execute RCON commands via REST API"
    - "Commands routed to correct executor (RconServerExecutor)"
    - "Command history stored in Redis for session"
  artifacts:
    - path: "api/src/application/use_cases/send_command_use_case.rs"
      provides: "RCON command execution via executor"
      contains: "executor.send_command"
    - path: "api/src/presentation/handlers/terminal_handlers.rs"
      provides: "WebSocket terminal for interactive commands"
      exports: ["ws_terminal", "exec_terminal"]
    - path: "api/src/infrastructure/executors/rcon_server_executor.rs"
      provides: "RCON protocol implementation"
      contains: "send_command"
  key_links:
    - from: "api/src/presentation/handlers/server_handlers.rs"
      to: "send_command_use_case"
      via: "send_command_use_case.execute"
      pattern: "send_command_use_case"
    - from: "ws_terminal"
      to: "RconServerExecutor"
      via: "podman exec rcon-cli"
      pattern: "rcon-cli"
---

<objective>
Verify and enhance RCON command execution capability.

**Purpose:** Users need to execute game server console commands via RCON protocol (Minecraft, etc.). This plan verifies existing RCON executor and REST endpoint integration.

**Output:** Verified RCON command execution with WebSocket terminal support.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@api/src/infrastructure/executors/rcon_server_executor.rs
@api/src/application/use_cases/send_command_use_case.rs
@api/src/presentation/handlers/terminal_handlers.rs
@api/src/presentation/routes/server_routes.rs

<interfaces>
<!-- Key interfaces from codebase -->

From api/src/domain/server_executor.rs:
```rust
#[async_trait]
pub trait ServerExecutor: Send + Sync {
    async fn send_command(&self, server: &Server, command: &str) -> Result<String>;
}
```

From api/src/infrastructure/executors/rcon_server_executor.rs:
```rust
impl RconServerExecutor {
    pub async fn send_command(&self, server: &Server, command: &str) -> Result<String> {
        // Uses rcon crate to connect to server.host:rcon_port
        // Sends command via RCON protocol
    }
}
```

From api/src/application/use_cases/send_command_use_case.rs:
```rust
pub async fn execute(&self, server_id: Uuid, command: String) -> Result<String> {
    // Gets executor from factory, calls executor.send_command()
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify RCON executor is wired to use cases</name>
  <files>api/src/application/use_cases/send_command_use_case.rs, api/src/bootstrap/container.rs</files>
  <action>
    1. Read send_command_use_case.rs to confirm it uses executor.send_command()
    2. Verify container.rs wires SendCommandUseCase with executor factory
    3. If not wired, update to route RCON commands through RconServerExecutor
  </action>
  <verify>
    <automated>grep -n "executor.send_command" api/src/application/use_cases/send_command_use_case.rs</automated>
  </verify>
  <done>SendCommandUseCase calls executor.send_command()</done>
</task>

<task type="auto">
  <name>Task 2: Ensure RCON command endpoint exists</name>
  <files>api/src/presentation/handlers/server_handlers.rs, api/src/presentation/routes/server_routes.rs</files>
  <action>
    1. Check if /:id/command endpoint exists in server_routes.rs (line 99 shows it does)
    2. Verify the handler (send_command at line 892) calls send_command_use_case
    3. If using Solys client directly, update to use send_command_use_case instead
  </action>
  <verify>
    <automated>grep -n "send_command_use_case" api/src/presentation/handlers/server_handlers.rs</automated>
  </verify>
  <done>REST endpoint routes through SendCommandUseCase</done>
</task>

<task type="auto">
  <name>Task 3: Add command history to Redis (D-28)</name>
  <files>api/src/infrastructure/redis.rs, api/src/presentation/handlers/terminal_handlers.rs</files>
  <action>
    1. Store executed commands in Redis with key pattern: "terminal:history:{server_id}"
    2. Limit history to last 50 commands per server
    3. Return history on WebSocket connection
  </action>
  <verify>
    <automated>grep -n "terminal.*history" api/src/ -r</automated>
  </verify>
  <done>Command history stored in Redis, retrievable</done>
</task>

</tasks>

<verification>
- [ ] RCON commands execute via REST API
- [ ] WebSocket terminal can send commands
- [ ] Command history persists in Redis
</verification>

<success_criteria>
User can execute RCON commands via REST API and see response. Command history available for session continuity.
</success_criteria>

<output>
After completion, create `.planning/phases/08-operations-integration/08-01-SUMMARY.md`
</output>
