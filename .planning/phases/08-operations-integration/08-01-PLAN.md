---
phase: 08-operations-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/app/App.jsx
  - app/src/pages/Console.jsx
autonomous: true
requirements:
  - RCON-01  # User can connect to server via RCON protocol
  - RCON-02  # User can execute console commands via RCON

must_haves:
  truths:
    - "D-28: WebSocket terminal — Console.jsx connects to /ws/terminal/:id via Terminal.jsx (xterm.js) for real-time interaction"
    - "User can navigate to /console route in the app"
    - "User can select a server from dropdown to open its console"
    - "User can see xterm.js terminal with cosmic dark theme after selecting a server"
    - "User can type and send commands via the terminal input bar"
    - "User sees connection status indicator (connected/disconnected)"
    - "User sees empty state prompt when no server is selected"
    - "Command history is navigable with arrow keys, autocomplete with Tab"
  artifacts:
    - path: "app/src/app/App.jsx"
      provides: "/console route registered"
      min_lines: 140
      contains: "import ConsolePage"
    - path: "app/src/pages/Console.jsx"
      provides: "Console page with Terminal.jsx integration"
      min_lines: 120
      contains: "import Terminal from"
  key_links:
    - from: "App.jsx"
      to: "Console.jsx"
      via: "Route path='/console'"
      pattern: "Route.*console.*ConsolePage"
    - from: "Console.jsx"
      to: "Terminal.jsx"
      via: "import + render with serverId prop"
      pattern: "Terminal serverId="
    - from: "Terminal.jsx"
      to: "/ws/terminal/:server_id"
      via: "WebSocket connection"
      pattern: "ws/terminal/"
---

<objective>
Add the missing `/console` route to the frontend app and rewrite Console.jsx to integrate the existing Terminal.jsx (xterm.js) component with server selector dropdown, matching the UI-SPEC design contract for the RCON Console page.

**Purpose:** The UI-SPEC requires a Console page at `/console` with xterm.js terminal, server selector, and connection status indicator. The sidebar already links to `/console` but the route doesn't exist in App.jsx. The existing Console.jsx uses REST sendCommand + Docker log streaming instead of the xterm.js WebSocket terminal.

**Output:**
- `/console` route registered in App.jsx
- Console.jsx rewritten to use Terminal.jsx (xterm.js) with server dropdown selector
- UI-SPEC contract fulfilled (copywriting, layout, interaction patterns)
</objective>

<execution_context>
@/home/rhnbztnl/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rhnbztnl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-operations-integration/08-CONTEXT.md
@.planning/phases/08-operations-integration/08-UI-SPEC.md

<interfaces>
<!-- Terminal.jsx component interface -->
From app/src/components/Terminal.jsx:
- Props: { serverId: string, connected?: boolean }
- Renders xterm.js terminal emulator with theme matching cosmic design
- Connects to /ws/terminal/:serverId via WebSocket
- Has command history (ArrowUp/Down), Tab autocomplete for Minecraft commands
- Ctrl+C to cancel, "clear" to clear terminal
- Internal connected state + external connected prop
- Min height: 400px

From app/src/hooks/useServers.js:
- useServers() returns { servers, loading, error, refetch }
- servers is an array of { id, name, host, port, status, ... }

From app/src/hooks/useTerminal.js:
- useTerminal(serverId) returns { connected, connecting, output, error, sendCommand, ... }
- Connects to /ws/terminal/:serverId

From app/src/api/client.js:
- fetchApi(endpoint, options) — wraps fetch with API_BASE = '/api/v1'
- Automatically adds Authorization header from authStore

UI-SPEC Copywriting contract:
- Page title: "Console"
- Empty state: "Select a server to open its console"
- Disconnected: "Disconnected. Reconnecting..."
- Command prompt: "$"

UI-SPEC Design tokens:
- Background: #080b15 (deep-space)
- Accent: #0ddff2 (cosmic-cyan) for active tab, primary button, connection indicator
- Success: #10b981 for connected status
- Font: JetBrains Mono for terminal
- Terminal font size: 13px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /console route to App.jsx</name>
  <files>app/src/app/App.jsx</files>

  <read_first>
    app/src/app/App.jsx (current routes structure)
    app/src/pages/Console.jsx (the component to route)
    app/src/components/Sidebar.jsx (verifies /console is in sidebar nav)
  </read_first>

  <action>
    1. Read app/src/app/App.jsx — it currently has 12 protected routes inside the inner `<Routes>` block (lines 112-125). Note the existing import pattern at lines 1-30.
    
    2. Add import for Console page. The existing pattern is:
       `import ConsolePage from '../pages/Console'`
       
       BUT the existing page is named `Console.jsx` (not ConsolePage). The current import would be:
       `import ConsolePage from '../pages/Console'`
       
       Actually wait — check the actual export. The file exports `export default function Console()`.
       The existing import in App.jsx for similar pages uses the PascalCase component name as the local name (e.g., `import ServerManagerPage from '../pages/servers/ServerManagerPage'`).
       
       Add the import with the rest of the page imports (after line 26):
       `import ConsolePage from '../pages/Console'`

    3. Add the route inside the inner `<Routes>` block (after line 124, before closing `</Routes>`):
       `<Route path="/console" element={<ConsolePage />} />`

    4. Order it alphabetically with the other routes — insert between line 121 (`/billing`) and line 122 (`/settings`):
       ```
       <Route path="/console" element={<ConsolePage />} />
       ```

    The file must still compile (no undefined components, valid JSX, valid route structure).
  </action>

  <verify>
    <automated>grep -n "import ConsolePage" app/src/app/App.jsx && grep -n "path=\"/console\"" app/src/app/App.jsx</automated>
  </verify>

  <acceptance_criteria>
    - app/src/app/App.jsx contains `import ConsolePage from '../pages/Console'`
    - app/src/app/App.jsx contains `<Route path="/console" element={<ConsolePage />} />`
    - The route is inside the inner `<Routes>` block (the protected routes)
    - No duplicate route definitions
  </acceptance_criteria>

  <done>App.jsx has Console import and /console route registered</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Console.jsx to use Terminal.jsx (xterm.js) with server selector</name>
  <files>app/src/pages/Console.jsx</files>

  <read_first>
    app/src/pages/Console.jsx (current implementation — 175 lines, needs complete rewrite)
    app/src/components/Terminal.jsx (the xterm.js component to integrate — 401 lines)
    app/src/hooks/useServers.js (useServers hook for server list)
    .planning/phases/08-operations-integration/08-UI-SPEC.md (UI design contract)
  </read_first>

  <action>
    Rewrite the entire Console.jsx file. The new implementation MUST:

    1. **Imports**: Remove `useServers` import and keep `useState` replace the hook with:
       ```jsx
       import { useState, useEffect, useRef } from 'react';
       import { useServers } from '../hooks/useServers';
       import Terminal from '../components/Terminal';
       ```

    2. **Component state**: Single piece of state — `const [selectedId, setSelectedId] = useState('');`

    3. **Server list**: Use `const { servers } = useServers();` to get list of servers.

    4. **Layout structure** (per UI-SPEC, exact copywriting required):
       ```
       ┌─ FULL HEIGHT FLEX COLUMN ───────────────────────┐
       │ Header: "Console" text + server selector         │
       │         dropdown + connection status dot          │
       ├──────────────────────────────────────────────────┤
       │ Terminal component (flex-1, min-h-[400px])       │
       │   - Terminal.jsx renders xterm.js here            │
       │   - Receives selectedId as serverId prop          │
       └──────────────────────────────────────────────────┘
       ```

    5. **Header section** — Render at top, shrink-0:
       - `<h1 className="text-2xl font-bold">Console</h1>` (per UI-SPEC copy)
       - Server selector `<select>` dropdown with option "-- Select Server --" (value="")
       - Must use existing cosmic theme CSS variables: `var(--color-cosmic-border)`, `var(--color-cosmic-cyan)`, `var(--color-text-main)`, `var(--color-text-muted)`, `var(--color-cosmic-green)`, `var(--color-cosmic-red)`
       - Connection status indicator: A `<span>` dot (w-2, h-2, rounded-full) that is green (`bg-[var(--color-cosmic-green)] animate-pulse-glow`) when `selectedId` is truthy, red (`bg-[var(--color-cosmic-red)]`) when falsy
       - Text label: "Connected" (when server selected) or "Disconnected" (when no server selected)

    6. **Terminal area** — flex-1, with conditional rendering:
       - If `!selectedId`: Show empty state `<div>` centered with text: `"Select a server to open its console"` (per UI-SPEC copywriting contract) in `text-[var(--color-text-muted)]`
       - If `selectedId`: Render `<Terminal serverId={selectedId} />` inside a container with `className="h-full min-h-[400px]"`

    7. **Server selector** `onChange` handler:
       - `(e) => setSelectedId(e.target.value)`

    8. **CSS/Styling**: Must match cosmic theme patterns already used in the codebase:
       - Page background: Inherits from parent (deep-space)
       - Glass panels: `bg-[rgba(255,255,255,0.03)]`, `border border-[var(--color-cosmic-border)]`
       - Dropdown: styled per existing pattern in Console.jsx (cosmic theme)
       - Header border: `border-b border-[var(--color-cosmic-border)]`
       - Padding: `px-8 py-6` for header, `px-8` for content area

    9. **Copywriting contract** (exact strings from UI-SPEC):
       - Page title: `"Console"`
       - Empty state: `"Select a server to open its console"`
       - Dropdown placeholder: `"— Select Server —"`
       - Dropdown options: `{s.name} ({s.host}:{s.port})`

    10. **Removed from old implementation** (do NOT include):
        - Docker log stream WebSocket (`dockerWsRef`, `/ws/docker-logs`)
        - Event bus WebSocket (`useWebSocket` with snapshot/event handling)
        - Log line rendering (no more `logs.map()`)
        - `sendCommand` REST call (terminal commands go through Terminal.jsx's WS)
        - `handleSend`, `handleServerChange`, `colorMap`, `logsEndRef`
        - The old terminal-like interface with log lines and color-coded messages

    The new file should be approximately 90-120 lines (much simpler than the 175-line original).
    Write the complete file using the Write tool.
  </action>

  <verify>
    <automated>grep -c "import Terminal" app/src/pages/Console.jsx && grep -c "Select a server" app/src/pages/Console.jsx && grep -c "Terminal serverId=" app/src/pages/Console.jsx</automated>
  </verify>

  <acceptance_criteria>
    - Console.jsx contains `import Terminal from '../components/Terminal'`
    - Console.jsx contains the copy string "Select a server to open its console"
    - Console.jsx renders `<Terminal serverId={selectedId} />` when server is selected
    - Console.jsx does NOT contain `sendCommand`, `dockerWsRef`, or `useWebSocket`
    - Console.jsx has a `<select>` element for server selection
    - Console.jsx has a connection status dot (green/red indicator)
    - Console.jsx has the title text "Console"
  </acceptance_criteria>

  <done>Console page uses xterm.js via Terminal.jsx with server dropdown, matching UI-SPEC design contract</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | User-submitted commands cross from untrusted client to trusted API |
| WS Terminal → Server | Terminal WebSocket carries command strings to container exec |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-001 | Tampering | Console.jsx command input | mitigate | Terminal.jsx encodes commands as JSON before WS send; WS handler validates via TerminalExecRequest deserialization |
| T-08-002 | Info Disclosure | WS terminal output | mitigate | Only authenticated users (JWT cookie) can open WS; server_id ownership verified in ws_terminal handler |
| T-08-003 | Spoofing | Server selector dropdown | mitigate | Server list fetched from authenticated API; only user-owned servers returned |
</threat_model>

<verification>
1. App.jsx compiles without errors (check with ESLint/bundler)
2. Console.jsx renders correctly with/without server selected
3. Terminal.jsx WebSocket connects to /ws/terminal/:serverId when server selected
4. UI-SPEC copywriting strings present in Console.jsx
5. Connection status indicator visible and changes based on selection
</verification>

<success_criteria>
- [ ] /console route registered and accessible
- [ ] Console page renders xterm.js terminal when server selected
- [ ] Server dropdown populated from useServers
- [ ] Empty state shown when no server selected
- [ ] Connection status indicator present
- [ ] UI-SPEC copywriting contract fulfilled
- [ ] No REST sendCommand or Docker log WS in Console.jsx
</success_criteria>

<output>
After completion, create .planning/phases/08-operations-integration/08-01-SUMMARY.md
</output>
