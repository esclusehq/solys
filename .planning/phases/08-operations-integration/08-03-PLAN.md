---
phase: 08-operations-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/presentation/handlers/file_handlers.rs
  - app/src/components/FileManager.jsx
autonomous: true
requirements:
  - FILE-02

must_haves:
  truths:
    - "Large files upload in chunks"
    - "Uploads can resume after failure"
    - "Progress bar shows upload progress"
  artifacts:
    - path: "api/src/presentation/handlers/file_handlers.rs"
      provides: "Chunked upload with resume support"
      contains: "upload.*chunk"
    - path: "app/src/components/FileManager.jsx"
      provides: "Chunked upload UI with progress"
      contains: "progress"
  key_links:
    - from: "FileManager.jsx upload"
      to: "upload_file handler"
      via: "multipart chunks"
      pattern: "chunk.*upload"
---

<objective>
Implement chunked upload with resume support.

**Purpose:** Large files (world saves, mods) need chunked upload with resume capability for reliability. This plan adds chunked upload support per D-30.

**Output:** Chunked upload with resume, progress bar in UI.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@api/src/presentation/handlers/file_handlers.rs
@app/src/components/FileManager.jsx

<!-- Current upload_file implementation reads entire file at once -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add chunked upload endpoint</name>
  <files>api/src/presentation/handlers/file_handlers.rs</files>
  <action>
    1. Create new endpoint: POST /:id/files/upload/chunked
    2. Accept payload: { filename, chunkIndex, totalChunks, data (base64 or raw bytes) }
    3. Store chunk in temp location: /tmp/uploads/{session_id}/{filename}.part{n}
    4. When chunkIndex == totalChunks-1, assemble file and move to target
    5. Return session_id for resume capability
  </action>
  <verify>
    <automated>grep -n "chunk\|upload.*chunk" api/src/presentation/handlers/file_handlers.rs</automated>
  </verify>
  <done>Chunked upload endpoint exists</done>
</task>

<task type="auto">
  <name>Task 2: Add resume endpoint</name>
  <files>api/src/presentation/handlers/file_handlers.rs</files>
  <action>
    1. Create endpoint: GET /:id/files/upload/status?session_id=xxx
    2. Return uploaded chunks list and total expected
    3. Client can request next chunk based on missing chunks
    4. Allow cleanup of incomplete uploads after 24h
  </action>
  <verify>
    <automated>grep -n "resume\|session_id" api/src/presentation/handlers/file_handlers.rs</automated>
  </verify>
  <done>Resume endpoint returns upload status</done>
</task>

<task type="auto">
  <name>Task 3: Update FileManager to use chunked upload</name>
  <files>app/src/components/FileManager.jsx</files>
  <action>
    1. Split files >5MB into 1MB chunks
    2. Show progress bar with percentage
    3. On failure, call resume endpoint to check which chunks uploaded
    4. Resume from next missing chunk
  </action>
  <verify>
  <automated>grep -n "chunk\|progress" app/src/components/FileManager.jsx | head -15</automated>
  </verify>
  <done>FileManager uploads with chunked progress</done>
</task>

</tasks>

<verification>
- [ ] Chunked upload endpoint accepts chunks
- [ ] Resume endpoint returns upload status
- [ ] UI shows progress bar
- [ ] Upload resumes after failure
</verification>

<success_criteria>
Large files upload with progress bar. On network failure, upload resumes from last successful chunk.
</success_criteria>

<output>
After completion, create `.planning/phases/08-operations-integration/08-03-SUMMARY.md`
</output>
