---
phase: 25
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - app/src/pages/servers/ServerManagerPage.jsx
  - app/src/store/serverStore.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can see server table with columns: Name, Status, Game Type, IP, Port, Domain, Actions"
    - "User can search servers by name inline"
    - "User can paginate through servers list"
    - "User can access actions via 3-dot menu"
  artifacts:
    - path: "app/src/pages/servers/ServerManagerPage.jsx"
      provides: "Enhanced server table with all required columns, search, pagination, and 3-dot menu"
      min_lines: 150
  key_links:
    - from: "ServerManagerPage.jsx"
      to: "useServerStore"
      via: "serverStore.servers"
      pattern: "useServerStore|servers"
---

<objective>
Implement enhanced server table on Server Manager page with additional columns, search, pagination, and 3-dot actions menu.

Purpose: Per D-09 to D-11: Add columns (Game type, IP, Port, Domain) and 3-dot menu. Per D-12 to D-14: Search and pagination.
Output: Updated ServerManagerPage.jsx with enhanced table layout.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-update-ui-ux-dashboard-table-agent-nama-status-uuid-created-/25-CONTEXT.md
@app/src/pages/servers/ServerManagerPage.jsx (current implementation)

## Implementation Notes

### Server Table - Keep existing columns (D-10):
- Name, Status, Actions

### New columns to add (D-09):
- **Game Type**: server.config?.game_type or server.image-derived name
- **IP Address**: server.ip_address or derived from node
- **Port**: server.port or allocated port
- **Domain**: server.domain or subdomain (if configured)

### Search (D-12)
- Inline search input in list header
- Filter servers by name (existing: matchSearch)

### Pagination (D-13, D-14)
- Use existing pagination pattern
- "Previous/Next" or page numbers

### 3-Dot Menu Actions (D-11)
- **View** - navigate to server detail page
- **Start/Stop** - toggle server state (existing logic)
- **Delete** - confirm and delete server

Note: Current implementation uses card grid. Convert to table for consistency with Nodes page.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert server cards to table layout with all columns</name>
  <files>app/src/pages/servers/ServerManagerPage.jsx</files>
  <action>
Convert current card grid to table layout:

1. Replace grid of cards with HTML table:
   - <table className="w-full">
   - <thead> with columns: Name, Status, Game Type, IP, Port, Domain, Actions
   - <tbody> with row per server

2. Implement column rendering:
   - **Name**: server.name (clickable, navigates to detail)
   - **Status**: colored dot + status text (reuse getStatusColor)
   - **Game Type**: server.config?.game_type || server.image?.split('/')[1]?.split(':')[0] || 'Unknown'
   - **IP Address**: server.ip_address || 'Auto-assigned' (from node data if available)
   - **Port**: server.port || server.config?.ports?.[0] || 'Auto'
   - **Domain**: server.domain || '-'
   - **Actions**: 3-dot menu

3. Keep existing search/filter functionality (search state, filter state)
4. Apply consistent table styling with dark theme
  </action>
  <verify>
<automated>grep -n "table\|thead\|tbody\|Game Type\|IP\|Port\|Domain" app/src/pages/servers/ServerManagerPage.jsx | head -20</automated>
  </verify>
  <done>Table displays with all 7 columns</done>
</task>

<task type="auto">
  <name>Task 2: Ensure inline search works with new table</name>
  <files>app/src/pages/servers/ServerManagerPage.jsx</files>
  <action>
Verify and enhance inline search (D-12):

1. Current implementation already has search input in flex gap-4 section
2. Ensure search input is inline (not full-width):
   - Style: max-w-xs or ~200px width
   - Position: next to page title in header area

3. Verify filter applies to table rows:
   - const filtered = servers.filter(s => matchSearch && matchFilter)
   - Apply to table body, not cards
  </action>
  <verify>
<automated>grep -n "search\|setSearch" app/src/pages/servers/ServerManagerPage.jsx</automated>
  </verify>
  <done>Search filters table rows in real-time</done>
</task>

<task type="auto">
  <name>Task 3: Add pagination to server table</name>
  <files>app/src/pages/servers/ServerManagerPage.jsx</files>
  <action>
Implement pagination for server table (D-13, D-14):

1. Add pagination state:
   - const [currentPage, setCurrentPage] = useState(1)
   - const ITEMS_PER_PAGE = 10

2. Calculate pagination:
   - const totalPages = Math.ceil(filtered.length / ITEMS_PER_PAGE)
   - const startIndex = (currentPage - 1) * ITEMS_PER_PAGE
   - const paginatedServers = filtered.slice(startIndex, startIndex + ITEMS_PER_PAGE)

3. Add pagination UI at bottom of table:
   - "Previous" button (disabled on page 1)
   - "Page X of Y" text
   - "Next" button (disabled on last page)

4. Reset to page 1 when search or filter changes

5. Show "Showing X-Y of Z servers" text above pagination
  </action>
  <verify>
<automated>grep -n "currentPage\|totalPages\|ITEMS_PER_PAGE" app/src/pages/servers/ServerManagerPage.jsx</automated>
  </verify>
  <done>Pagination controls navigate through servers</done>
</task>

<task type="auto">
  <name>Task 4: Implement 3-dot menu for server actions</name>
  <files>app/src/pages/servers/ServerManagerPage.jsx</files>
  <action>
Add 3-dot menu dropdown for server row actions (D-11):

1. In Actions column, add button with 3 dots icon:
   - <button className="p-1 hover:bg-gray-700 rounded">
   - SVG icon: vertical 3 dots

2. Dropdown menu options:
   - **View** - Link to /servers/${server.id} (existing View button behavior)
   - **Start** / **Stop** - Toggle server (existing handleStartStop)
   - **Delete** - Confirm dialog, then call serversApi.delete(server.id)

3. Dropdown styling:
   - Position absolute, z-50
   - Background: gray-800 with border
   - Text: white, hover: gray-700

4. Close dropdown on outside click

Note: Reuse existing handleStartStop and add handleDelete similar to Nodes page.
  </action>
  <verify>
<automated>grep -n "Actions\|dropdown\|menu\|handleDelete" app/src/pages/servers/ServerManagerPage.jsx | head -20</automated>
  </verify>
  <done>3-dot menu shows View, Start/Stop, Delete options</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Server data from API, start/stop/delete actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Tampering | Server table | accept | User can only manage own servers, delete requires confirmation |
</threat_model>

<verification>
[ ] Table shows columns: Name, Status, Game Type, IP, Port, Domain, Actions
[ ] Search filters servers by name
[ ] Pagination controls navigate through list
[ ] 3-dot menu shows: View, Start/Stop, Delete
[ ] Responsive: horizontal scroll on mobile if needed
</verification>

<success_criteria>
Server Manager page displays full-featured table with enhanced columns, search, pagination, and 3-dot actions menu.
</success_criteria>

<output>
After completion, create `.planning/phases/25-update-ui-ux-dashboard-table-agent-nama-status-uuid-created-/25-03-SUMMARY.md`
</output>