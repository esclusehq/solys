---
phase: 25
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/pages/dashboard/DashboardPage.jsx
  - app/src/store/authStore.js
  - app/src/api/auth.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "User sees personalized greeting based on account age"
    - "User sees exactly 3 dashboard cards: Servers, Billing, Agents"
    - "Server card shows total servers and running servers count"
    - "Billing card shows subscription status, days remaining, renewal date"
    - "Agent card shows total agents and online agents count"
  artifacts:
    - path: "app/src/pages/dashboard/DashboardPage.jsx"
      provides: "Redesigned dashboard with personalized greeting and 3 metric cards"
      min_lines: 80
    - path: "app/src/store/authStore.js"
      provides: "User state with created_at for account age calculation"
    - path: "app/src/api/auth.js"
      provides: "getMe endpoint returning user.created_at"
  key_links:
    - from: "DashboardPage.jsx"
      to: "authStore.user.created_at"
      via: "useAuthStore hook"
      pattern: "user\\?.created_at"
    - from: "DashboardPage.jsx"
      to: "billingApi.getCurrentSubscription()"
      via: "useEffect on mount"
      pattern: "billingApi\\.getCurrentSubscription"
    - from: "DashboardPage.jsx"
      to: "useNodes()"
      via: "useNodes hook"
      pattern: "useNodes\\(\\)"
---

<objective>
Implement personalized welcome message and 3 dashboard metric cards (Servers, Billing, Agents).

Purpose: Per D-01, D-02: personalized greeting based on account age. Per D-03 to D-06: exactly 3 cards with specific metrics.
Output: Updated DashboardPage.jsx with greeting personalization and 3 metric cards.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-update-ui-ux-dashboard-table-agent-nama-status-uuid-created-/25-CONTEXT.md
@app/src/pages/dashboard/DashboardPage.jsx (current implementation)
@app/src/store/authStore.js (user state)
@app/src/hooks/useNodes.js (node data fetching)

## Implementation Notes

### Welcome Message Logic (D-01, D-02)
- Account created <= 2 days ago: "Welcome, {user}!"
- Account created > 2 days ago: "Welcome back, {user}!"
- Calculate: `(now - user.created_at) <= 2 days`

### Dashboard Cards (D-03 to D-06) - Exactly 3 cards
1. **Servers Card**: Total servers count + Running servers count
2. **Billing Card**: Subscription status + Days remaining + Renewal date
3. **Agents Card**: Total agents count + Online agents count

### Auth Store Integration
The auth store currently stores user but may not have created_at. Need to verify getMe API returns created_at.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify user.created_at available from API</name>
  <files>app/src/api/auth.js</files>
  <action>
Check if getMe or auth/me endpoint returns user.created_at. If not, document what fields are available and create a task to add it to backend if needed.
  </action>
  <verify>
    <automated>grep -n "created_at\|createdAt" app/src/api/auth.js</automated>
  </verify>
  <done>API returns user.created_at field or workaround identified</done>
</task>

<task type="auto">
  <name>Task 2: Implement personalized welcome message</name>
  <files>app/src/pages/dashboard/DashboardPage.jsx</files>
  <action>
Add account age calculation logic to DashboardPage.jsx:
- Import useAuthStore to get user
- Calculate account age: const daysSinceCreation = (Date.now() - new Date(user.created_at).getTime()) / (1000 * 60 * 60 * 24)
- Conditionally render: daysSinceCreation <= 2 ? "Welcome, {user}!" : "Welcome back, {user}!"
- Use existing h1 text-2xl font-bold text-white structure
  </action>
  <verify>
    <automated>grep -n "Welcome.*user\|daysSinceCreation" app/src/pages/dashboard/DashboardPage.jsx</automated>
  </verify>
  <done>Personalized greeting displays based on account age</done>
</task>

<task type="auto">
  <name>Task 3: Implement 3 dashboard metric cards</name>
  <files>app/src/pages/dashboard/DashboardPage.jsx</files>
  <action>
Replace existing 3-card layout (Total Servers, Active, Stopped) with exactly 3 cards per D-03:

1. **Servers Card** (blue theme):
   - Label: "Servers"
   - Show: totalServers (from servers.length), runningServers (filter status === 'running')

2. **Billing Card** (purple theme):
   - Label: "Billing"
   - Fetch subscription data via billingApi.getCurrentSubscription()
   - Show: subscription status (active/inactive), days remaining, renewal date
   - If no subscription: show "Free Plan" or "No subscription"

3. **Agents Card** (green theme):
   - Label: "Agents"
   - Use useNodes() hook to get nodes data
   - Show: totalNodes (nodes.length), onlineNodes (filter status === 'online')

Card design: Use glass-panel or existing card styling with icon, label, and value. Responsive: grid-cols-1 md:grid-cols-3.
  </action>
  <verify>
    <automated>grep -n "Servers\|Billing\|Agents" app/src/pages/dashboard/DashboardPage.jsx | head -20</automated>
  </verify>
  <done>3 cards display with correct metrics: Servers (total/running), Billing (status/days/renewal), Agents (total/online)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted user data from auth store, billing API call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | DashboardPage | accept | Billing data already requires auth, no sensitive data exposed |
</threat_model>

<verification>
[ ] Personalized greeting shows "Welcome, {user}!" for accounts <= 2 days old
[ ] Personalized greeting shows "Welcome back, {user}!" for accounts > 2 days old
[ ] Servers card shows total and running count
[ ] Billing card shows subscription status, days remaining, renewal date
[ ] Agents card shows total and online count
[ ] Cards are responsive (1 col mobile, 3 col desktop)
</verification>

<success_criteria>
Dashboard displays personalized welcome message and exactly 3 metric cards with correct data.
</success_criteria>

<output>
After completion, create `.planning/phases/25-update-ui-ux-dashboard-table-agent-nama-status-uuid-created-/25-01-SUMMARY.md`
</output>