---
phase: 25
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - app/src/pages/Nodes.jsx
  - app/src/hooks/useNodes.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can see agent table with columns: Name, Status (colored dots), UUID, Created, Uptime, Connected Servers, Actions"
    - "User can view node details via existing NodeDetails pattern"
    - "User can search nodes by name inline"
    - "User can paginate through nodes list"
    - "User can access actions via 3-dot menu"
  artifacts:
    - path: "app/src/pages/Nodes.jsx"
      provides: "Agent table with all required columns, search, pagination, and 3-dot menu"
      min_lines: 150
    - path: "app/src/hooks/useNodes.js"
      provides: "Pagination support for nodes list"
  key_links:
    - from: "Nodes.jsx"
      to: "useNodes()"
      via: "useNodes hook with pagination params"
      pattern: "useNodes\\(|page.*limit"
---

<objective>
Implement enhanced agent table on Nodes page with all required columns, search, pagination, and 3-dot actions menu.

Purpose: Per D-07: Agent table columns (Name, Status, UUID, Created, Uptime, Connected Servers, Actions). Per D-12 to D-14: Search and pagination.
Output: Updated Nodes.jsx with enhanced table layout.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-update-ui-ux-dashboard-table-agent-nama-status-uuid-created-/25-CONTEXT.md
@app/src/pages/Nodes.jsx (current implementation)
@app/src/hooks/useNodes.js (node data fetching)

## Implementation Notes

### Agent Table Columns (D-07)
- **Name**: node.name (string)
- **Status**: node.status with colored dots (online=green, offline=gray/red)
- **UUID**: node.id (full UUID, truncate display with tooltip for full)
- **Created**: node.created_at formatted as date (e.g., "Jan 15, 2026")
- **Uptime**: Calculate based on node first_seen or created_at - "2 hours", "3 days", "2 weeks" format
- **Connected Servers**: Count of servers on this node (need to fetch or have in node data)
- **Actions**: 3-dot menu with: View Details, Generate Key, Delete (per D-08 reuse NodeDetails)

### Search (D-12)
- Inline search input in list header (not full-width top bar)
- Filter nodes by name as user types

### Pagination (D-13, D-14)
- Use existing pagination pattern from other pages
- Page numbers or "Previous/Next" buttons
- Show current page / total pages

### 3-Dot Menu Actions
- Reuse existing patterns: View Details (opens NodeDetails panel), Generate Key (existing modal), Delete (existing confirm)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert node list to table layout with columns</name>
  <files>app/src/pages/Nodes.jsx</files>
  <action>
Convert the current card-based node list to a table layout:

1. Replace card div list with HTML table structure:
   - <table className="w-full">
   - <thead> with columns: Name, Status, UUID, Created, Uptime, Servers, Actions
   - <tbody> with row per node

2. Implement column rendering:
   - **Name**: node.name
   - **Status**: colored dot (green for online, gray for offline) + status text
   - **UUID**: node.id (show first 8 chars + "...", full in title/tooltip)
   - **Created**: new Date(node.created_at).toLocaleDateString()
   - **Uptime**: Calculate from node.created_at or node.first_seen (if exists). Format as "2 hours", "3 days", "2 weeks"
   - **Servers**: Show server count (node.server_count or calculate from node.servers array)
   - **Actions**: 3-dot menu button

3. Keep existing selection state (selectedNode) for detail panel
4. Apply consistent table styling with glass-panel aesthetics
  </action>
  <verify>
    <automated>grep -n "table\|thead\|tbody\|Name\|Status\|UUID\|Created\|Uptime" app/src/pages/Nodes.jsx | head -30</automated>
  </verify>
  <done>Table displays with all 7 columns</done>
</task>

<task type="auto">
  <name>Task 2: Add inline search input</name>
  <files>app/src/pages/Nodes.jsx</files>
  <action>
Add inline search input in the node list header (per D-12):

1. Find the header section with "Nodes" title
2. Add search input next to the title (inline, not full-width):
   - Placeholder: "Search nodes..."
   - Width: max-w-xs or ~200px
   - Style: consistent with app's dark theme (bg-[rgba(0,0,0,0.3)], border, text-white)

3. Implement filter logic:
   - const filteredNodes = nodes.filter(node => node.name.toLowerCase().includes(search.toLowerCase()))
   - Apply filter to table rows rendering
  </action>
  <verify>
<automated>grep -n "search\|Search\|filter" app/src/pages/Nodes.jsx</automated>
  </verify>
  <done>Search input filters nodes by name in real-time</done>
</task>

<task type="auto">
  <name>Task 3: Add pagination to nodes table</name>
  <files>app/src/pages/Nodes.jsx</files>
  <action>
Implement pagination controls for the nodes table (per D-13, D-14):

1. Add pagination state:
   - const [currentPage, setCurrentPage] = useState(1)
   - const ITEMS_PER_PAGE = 10 (or similar)

2. Calculate pagination:
   - const totalPages = Math.ceil(filteredNodes.length / ITEMS_PER_PAGE)
   - const startIndex = (currentPage - 1) * ITEMS_PER_PAGE
   - const paginatedNodes = filteredNodes.slice(startIndex, startIndex + ITEMS_PER_PAGE)

3. Add pagination UI at bottom of table:
   - "Previous" button (disabled on page 1)
   - "Page X of Y" text
   - "Next" button (disabled on last page)

4. Reset to page 1 when search filter changes

Note: If API supports server-side pagination (page/limit params), prefer that over client-side.
  </action>
  <verify>
<automated>grep -n "currentPage\|totalPages\|ITEMS_PER_PAGE" app/src/pages/Nodes.jsx</automated>
  </verify>
  <done>Pagination controls appear and navigate through nodes</done>
</task>

<task type="auto">
  <name>Task 4: Implement 3-dot actions menu</name>
  <files>app/src/pages/Nodes.jsx</files>
  <action>
Add 3-dot menu dropdown for row actions:

1. In the Actions column, add a button with 3 dots icon (⋮ or similar):
   - <button className="p-1 hover:bg-[rgba(255,255,255,0.1)] rounded">
   - SVG icon: 3 vertical dots

2. On click, show dropdown menu with options:
   - **View Details** - sets selectedNode, triggers detail panel
   - **Generate Key** - opens existing key modal (reuse handleGenerateKey)
   - **Delete** - shows confirm dialog, then calls handleDelete

3. Dropdown styling:
   - Position absolute, z-50
   - Background: dark glass-panel style
   - Options: View, Generate Key, Delete with icons

4. Close dropdown when clicking outside
  </action>
  <verify>
<automated>grep -n "Actions\|dropdown\|menu" app/src/pages/Nodes.jsx | head -20</automated>
  </verify>
  <done>3-dot menu opens with View, Generate Key, Delete options</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Node data from API, delete action requires confirmation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02 | Tampering | Nodes table | accept | User can only affect own nodes, no cross-tenant risk |
</threat_model>

<verification>
[ ] Table shows columns: Name, Status, UUID, Created, Uptime, Servers, Actions
[ ] Status shows colored dots (green=online, gray=offline)
[ ] UUID truncated with tooltip for full value
[ ] Created shows formatted date
[ ] Uptime shows human-readable duration
[ ] Search filters nodes by name
[ ] Pagination controls navigate through list
[ ] 3-dot menu shows actions: View Details, Generate Key, Delete
</verification>

<success_criteria>
Nodes page displays full-featured agent table with search, pagination, and 3-dot actions menu.
</success_criteria>

<output>
After completion, create `.planning/phases/25-update-ui-ux-dashboard-table-agent-nama-status-uuid-created-/25-02-SUMMARY.md`
</output>