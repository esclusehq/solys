---
phase: 07-server-status-metrics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/domain/entities/server_metrics.rs
  - api/src/infrastructure/executors/podman_server_executor.rs
  - api/src/application/services/monitoring_service.rs
  - api/src/presentation/routes/server_routes.rs
  - api/src/presentation/handlers/server_handlers.rs
autonomous: true
requirements:
  - STATUS-01
  - STATUS-02

must_haves:
  truths:
    - "User can view current server status (online/offline/starting/stopping)"
    - "User can view CPU usage percentage"
    - "User can view RAM usage (used/allocated)"
    - "User can view disk usage"
  artifacts:
    - path: "api/src/domain/entities/server_metrics.rs"
      provides: "ServerMetrics entity with all resource fields"
      contains: "disk_usage_mb"
    - path: "api/src/application/services/monitoring_service.rs"
      provides: "Background metrics collection at 30s interval"
      contains: "Duration::from_secs(30)"
    - path: "api/src/infrastructure/executors/podman_server_executor.rs"
      provides: "collect_metrics includes disk usage"
      contains: "disk"
  key_links:
    - from: "podman_server_executor.rs"
      to: "server_metrics.rs"
      via: "ServerMetrics struct"
      pattern: "collect_metrics.*ServerMetrics"
---

<objective>
Backend infrastructure for server status polling and metrics collection.

Purpose: Implement D-24 (Redis cache), D-25 (30s interval), and add disk usage to metrics.
Output: Backend API returns server status and complete metrics (CPU, RAM, disk).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-server-status-metrics/07-CONTEXT.md
@api/src/domain/entities/server_metrics.rs
@api/src/infrastructure/executors/podman_server_executor.rs
@api/src/application/services/monitoring_service.rs

# Key interfaces from codebase

From api/src/domain/entities/server_metrics.rs:
```rust
pub struct ServerMetrics {
    pub id: Uuid,
    pub server_id: Uuid,
    pub cpu_usage: f32,
    pub memory_usage_mb: i64,
    pub tps: Option<f32>,
    pub players: i32,
    pub created_at: DateTime<Utc>,
}
```

From api/src/infrastructure/executors/podman_server_executor.rs:
```rust
async fn collect_metrics(&self, server: &Server) -> Result<ServerMetrics> {
    // Uses: podman stats --no-stream --format "{{.CPUPerc}}|{{.MemUsage}}"
    // Also queries RCON for players and TPS
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add disk_usage to ServerMetrics entity</name>
  <files>api/src/domain/entities/server_metrics.rs</files>
  <action>
Add `disk_usage_mb: i64` field to ServerMetrics struct. This tracks disk space used by the server's data directory.

Reference: disk_usage_percent exists in NodeMetrics (api/src/domain/entities/node_metrics.rs line 52).
  </action>
  <verify>
<automated>grep -n "disk_usage_mb" api/src/domain/entities/server_metrics.rs</automated>
  </verify>
  <done>ServerMetrics entity includes disk_usage_mb field</done>
</task>

<task type="auto">
  <name>Task 2: Update collect_metrics to include disk usage</name>
  <files>api/src/infrastructure/executors/podman_server_executor.rs</files>
  <action>
Update collect_metrics() to:
1. Query disk usage using `podman inspect --format '{{.Size}}'` or calculate from mounted volume
2. Use server data directory path (from get_data_path()) to get disk usage
3. Return disk_usage_mb in ServerMetrics

Example approach: `podman exec <container> du -sb /data 2>/dev/null` or parse from `podman inspect`
  </action>
  <verify>
<automated>grep -n "disk_usage_mb" api/src/infrastructure/executors/podman_server_executor.rs</automated>
  </verify>
  <done>collect_metrics returns disk_usage_mb from podman</done>
</task>

<task type="auto">
  <name>Task 3: Update MonitoringService to 30s interval (D-25)</name>
  <files>api/src/application/services/monitoring_service.rs</files>
  <action>
In the start() method, change the interval from 15 seconds to 30 seconds:
- Line 50: `tokio::time::interval(Duration::from_secs(15))` → `Duration::from_secs(30)`
- This implements D-25: 30s collection interval

Reference: CONTEXT.md D-25 specifies 30s interval.
  </action>
  <verify>
<automated>grep -n "from_secs(30)" api/src/application/services/monitoring_service.rs</automated>
  </verify>
  <done>MonitoringService collects metrics every 30 seconds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent→API | Untrusted metrics data from agent, must be validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | collect_metrics | mitigate | Validate disk/CPU values are within reasonable bounds (0-100% for CPU, >=0 for disk) |
| T-07-02 | Denial of Service | MonitoringService | accept | 30s interval is reasonable, no mitigation needed |
</threat_model>

<verification>
- ServerMetrics entity has disk_usage_mb field
- collect_metrics includes disk usage query
- MonitoringService runs at 30s interval
</verification>

<success_criteria>
Backend returns server status and metrics (CPU, RAM, disk) via API.
</success_criteria>

<output>
After completion, create `.planning/phases/07-server-status-metrics/07-01-SUMMARY.md`
</output>
