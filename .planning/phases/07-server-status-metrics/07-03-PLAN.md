---
phase: 07-server-status-metrics
plan: "03"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - api/src/presentation/routes/server_routes.rs
  - api/src/presentation/handlers/server_handlers.rs
  - api/src/infrastructure/repositories/postgres_metrics_repository.rs
autonomous: true
requirements:
  - STATUS-01
  - STATUS-02

must_haves:
  truths:
    - "User can view current server status (online/offline/starting/stopping)"
    - "User can view CPU usage percentage"
    - "User can view RAM usage (used/allocated)"
    - "User can view disk usage"
  artifacts:
    - path: "api/src/presentation/routes/server_routes.rs"
      provides: "GET /api/servers/:id/metrics and /api/servers/:id/metrics/history/:limit"
      contains: "metrics"
    - path: "api/src/infrastructure/repositories/postgres_metrics_repository.rs"
      provides: "Query for metrics history with disk_usage_mb"
      contains: "disk_usage_mb"
  key_links:
    - from: "server_routes.rs"
      to: "server_handlers.rs"
      via: "handler function"
      pattern: "get_server_metrics"
---

<objective>
Backend API endpoints for server metrics and history.

Purpose: Provide API endpoints for current metrics and 24h history (required by Plan 02 sparklines).
Output: GET /api/servers/:id/metrics returns latest, GET /api/servers/:id/metrics/history/:limit returns history.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-server-status-metrics/07-CONTEXT.md
@api/src/infrastructure/repositories/postgres_metrics_repository.rs
@api/src/presentation/handlers/server_handlers.rs

# Existing pattern from node_routes.rs (lines 35-39):
```rust
.route(
    "/api/nodes/:id/metrics",
    get(node_handlers::get_node_metrics),
)
.route(
    "/api/nodes/:id/metrics/history/:limit",
    get(node_handlers::get_node_metrics_history),
)
```

# Existing pattern from postgres_node_metrics_repository.rs for reference on history query.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add metrics history query to PostgresMetricsRepository</name>
  <files>api/src/infrastructure/repositories/postgres_metrics_repository.rs</files>
  <action>
Add get_history method to MetricsRepository trait and implementation:
1. Query server_metrics table for last N records (limit parameter)
2. Include disk_usage_mb in SELECT and mapping
3. Order by created_at DESC for most recent first
4. Return Vec<ServerMetrics>

Reference: postgres_node_metrics_repository.rs has similar history query (line 102).
  </action>
  <verify>
<automated>grep -n "get_history" api/src/infrastructure/repositories/postgres_metrics_repository.rs && grep -n "disk_usage_mb" api/src/infrastructure/repositories/postgres_metrics_repository.rs</automated>
  </verify>
  <done>MetricsRepository.get_history returns metrics with disk_usage_mb</done>
</task>

<task type="auto">
  <name>Task 2: Add get_server_metrics handler</name>
  <files>api/src/presentation/handlers/server_handlers.rs</files>
  <action>
Add handler functions:
1. get_server_metrics - GET /api/servers/:id/metrics - returns latest metrics for server
2. get_server_metrics_history - GET /api/servers/:id/metrics/history/:limit - returns history

Use existing pattern from node_handlers.rs (lines 165-189) as reference.
Return ApiResponse with ServerMetrics or Vec<ServerMetrics>.
  </action>
  <verify>
<automated>grep -n "get_server_metrics\|get_server_metrics_history" api/src/presentation/handlers/server_handlers.rs</automated>
  </verify>
  <done>Server metrics handlers exist and return metrics data</done>
</task>

<task type="auto">
<name>Task 3: Add server metrics routes</name>
  <files>api/src/presentation/routes/server_routes.rs</files>
  <action>
Add routes to server_routes.rs:
1. GET /api/servers/:id/metrics - calls get_server_metrics handler
2. GET /api/servers/:id/metrics/history/:limit - calls get_server_metrics_history handler

Add after line 111 (existing .route("/metrics", get(metrics))):
```rust
.route("/api/servers/:id/metrics", get(server_handlers::get_server_metrics))
.route("/api/servers/:id/metrics/history/:limit", get(server_handlers::get_server_metrics_history))
```
  </action>
  <verify>
<automated>grep -n "servers/:id/metrics" api/src/presentation/routes/server_routes.rs</automated>
  </verify>
  <done>Server metrics API routes registered</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→Database | SQL query for metrics history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Accept Reason |
|-----------|----------|-----------|-------------|---------------|
| T-07-04 | Denial of Service | metrics/history | accept | Limit parameter prevents unbounded queries, default to reasonable limit |
</threat_model>

<verification>
- GET /api/servers/:id/metrics returns latest metrics with disk_usage_mb
- GET /api/servers/:id/metrics/history/2880 returns ~24h of data (30s interval = 2880 points)
</verification>

<success_criteria>
Frontend can fetch metrics history for sparkline visualization.
</success_criteria>

<output>
After completion, create `.planning/phases/07-server-status-metrics/07-03-SUMMARY.md`
</output>
