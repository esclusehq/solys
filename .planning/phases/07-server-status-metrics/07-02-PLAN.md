---
phase: 07-server-status-metrics
plan: "02"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - app/src/pages/servers/ServerDetailsPage.jsx
  - app/src/features/servers/api/serversApi.js
  - app/src/features/metrics/hooks/useServerMetrics.js
  - app/src/components/MetricsCard.jsx
autonomous: true
requirements:
  - STATUS-01
  - STATUS-02

must_haves:
  truths:
    - "User can view current server status (online/offline/starting/stopping)"
    - "User can view CPU usage percentage"
    - "User can view RAM usage (used/allocated)"
    - "User can view disk usage"
  artifacts:
    - path: "app/src/pages/servers/ServerDetailsPage.jsx"
      provides: "Metrics display cards with current values"
      contains: "cpu_usage"
    - path: "app/src/features/metrics/hooks/useServerMetrics.js"
      provides: "Sparkline data fetching and caching"
      contains: "last 24h"
  key_links:
    - from: "ServerDetailsPage.jsx"
      to: "/api/servers/:id/metrics"
      via: "useServerMetrics hook"
      pattern: "api.get.*metrics"
---

<objective>
Frontend display for server metrics with current values and sparklines.

Purpose: Implement D-26 (current values + sparkline display).
Output: ServerDetailsPage shows CPU, RAM, disk metrics with 24h sparklines.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-server-status-metrics/07-CONTEXT.md
@app/src/pages/servers/ServerDetailsPage.jsx
@app/src/features/servers/api/serversApi.js
@api/src/domain/entities/server_metrics.rs

# ServerDetailsPage currently has metrics display at lines 153-172
# It shows cpu_usage, memory_usage_mb, tps, players - but NOT disk_usage

# Existing pattern for metrics display
```javascript
{server.metrics && (
  <div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6">
    <div className="bg-gray-800 border border-gray-700 rounded-lg p-4">
      <h3 className="text-gray-400 text-sm">CPU</h3>
      <p className="text-2xl text-white">{server.metrics.cpu_usage?.toFixed(1)}%</p>
    </div>
    ...
  </div>
)}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add disk usage to ServerDetailsPage metrics display</name>
  <files>app/src/pages/servers/ServerDetailsPage.jsx</files>
  <action>
In the existing metrics display grid (lines 153-172), add a fourth card for disk usage:
1. Add disk_usage_mb display (convert to GB if >1024MB)
2. Use consistent styling with existing metrics cards
3. Add to the grid - change from grid-cols-4 to grid-cols-5 OR add a new row

The ServerMetrics now includes disk_usage_mb from backend (Plan 01).
  </action>
  <verify>
<automated>grep -n "disk_usage" app/src/pages/servers/ServerDetailsPage.jsx</automated>
  </verify>
  <done>Disk usage displayed in ServerDetailsPage metrics section</done>
</task>

<task type="auto">
  <name>Task 2: Create useServerMetrics hook for sparkline data</name>
  <files>app/src/features/metrics/hooks/useServerMetrics.js</files>
  <action>
Create a new hook that fetches metrics history:
1. Create directory: app/src/features/metrics/hooks/
2. Create useServerMetrics.js with:
   - fetchMetricsHistory(serverId, hours=24) - GET /api/servers/:id/metrics/history
   - Returns array of {timestamp, cpu, memory, disk}
   - Use React Query or simple fetch with caching
   - Auto-refresh every 30 seconds

This supports D-26: "Last 24h sparkline in UI"
  </action>
  <verify>
<automated>ls app/src/features/metrics/hooks/useServerMetrics.js 2>/dev/null && grep -n "fetchMetricsHistory" app/src/features/metrics/hooks/useServerMetrics.js</automated>
  </verify>
  <done>useServerMetrics hook exists and fetches 24h history</done>
</task>

<task type="auto">
<name>Task 3: Add sparkline visualization to MetricsCard</name>
<files>app/src/components/MetricsCard.jsx</files>
<action>
Create MetricsCard component with sparkline support:
1. Create app/src/components/MetricsCard.jsx
2. Props: label, value, unit, sparklineData[], threshold
3. Simple SVG sparkline (last 24 points)
4. Color coding: green (< threshold), yellow (near), red (exceeded)

Use existing .glow-cyan or similar classes from app/index.css.
  </action>
  <verify>
<automated>grep -n "sparkline" app/src/components/MetricsCard.jsx</automated>
  </verify>
  <done>MetricsCard component renders sparkline SVG</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→Frontend | API returns sanitized metrics data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Accept Reason |
|-----------|----------|-----------|-------------|---------------|
| T-07-03 | Information Disclosure | Metrics display | accept | Metrics are not sensitive, no PII |
</threat_model>

<verification>
- Disk usage visible in ServerDetailsPage
- useServerMetrics hook fetches history data
- Sparklines render in MetricsCard
</verification>

<success_criteria>
User sees current metrics and 24h sparklines for CPU, RAM, disk.
</success_criteria>

<output>
After completion, create `.planning/phases/07-server-status-metrics/07-02-SUMMARY.md`
</output>
