---
phase: 34-modpacks-templates-for-hobby-and-pro-plans
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/domain/server/mod.rs
  - api/src/domain/server/modpack_template/mod.rs
  - api/src/domain/server/modpack_template/model.rs
  - api/src/domain/server/modpack_template/repository.rs
  - api/src/presentation/routes/mod.rs
  - api/src/presentation/handlers/mod.rs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Modpack templates stored in database with game type support"
    - "API provides endpoint to fetch available modpack templates"
    - "Modpack templates include CurseForge/Modrinth project IDs and versions"
    - "Template access check respects user plan tier (Hobby/Pro only)"
  artifacts:
    - path: "api/src/domain/server/modpack_template/model.rs"
      provides: "ModpackTemplate entity with game_type, name, mods list"
      min_lines: 60
    - path: "api/src/domain/server/modpack_template/repository.rs"
      provides: "ModpackTemplate CRUD operations"
      exports: ["list_modpack_templates", "get_modpack_templates_by_game"]
    - path: "api/src/presentation/routes/mod.rs"
      provides: "Modpack template API route registration"
---

<objective>
Create backend modpack template system to store pre-configured modpack lists per game type.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@api/src/domain/server/template/model.rs
@api/src/domain/server/plugin_template/model.rs
@api/src/application/dto/plugin_dtos.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create modpack_template domain module</name>
  <files>api/src/domain/server/modpack_template/mod.rs</files>
  <action>
Create new module at api/src/domain/server/modpack_template/mod.rs with exports for model and repository.

```rust
pub mod model;
pub mod repository;

pub use model::*;
pub use repository::*;
```
  </action>
  <verify>File exists with module exports</verify>
  <done>ModpackTemplate module created and exports available</done>
</task>

<task type="auto">
  <name>Task 2: Create modpack_template model/entity</name>
  <files>api/src/domain/server/modpack_template/model.rs</files>
  <action>
Create modpack template model with fields:
- id: Uuid (primary key)
- game_type: String (minecraft only for now)
- display_name: String (e.g., "Direwolf 20", "Enigmatica 2")
- description: Option<String>
- source: String (curseforge or modrinth)
- project_id: String (CurseForge/Modrinth project ID)
- version_id: String (specific version ID)
- version_name: String (e.g., "1.12.2")
- mod_count: i32 (number of mods in pack)
- image_url: Option<String>
- min_plan: String (hobby, pro, enterprise - who can use this template)
- is_active: bool
- created_at, updated_at: NaiveDateTime

Include FromRow derive for sqlx, Serialize/Deserialize for JSON.

Implement fallback method that returns hardcoded modpack templates:
- Minecraft: Direwolf 20, Enigmatica 2, All the Mods, StoneBlock, etc.
- Each with correct CurseForge/Modrinth project IDs
  </action>
  <verify>File compiles, model has all required fields</verify>
  <done>ModpackTemplate model with database fields and fallback method</done>
</task>

<task type="auto">
  <name>Task 3: Create modpack_template repository</name>
  <files>api/src/domain/server/modpack_template/repository.rs</files>
  <action>
Create modpack template repository trait and implementation:
- list_modpack_templates() -> Vec<ModpackTemplate>
- list_modpack_templates_by_game(game_type: &str) -> Vec<ModpackTemplate>
- get_modpack_template(id: &Uuid) -> Option<ModpackTemplate>
- list_by_plan(plan: &str) -> Vec<ModpackTemplate> // for Hobby/Pro filtering

Use sqlx::query_as for async DB queries. Include fallback to hardcoded templates if DB query returns empty.
  </action>
  <verify>File compiles, repository has required methods</verify>
  <done>ModpackTemplate repository with async DB operations and fallback</done>
</task>

<task type="auto">
  <name>Task 4: Register modpack_template module in server domain</name>
  <files>api/src/domain/server/mod.rs</files>
  <action>
Add "pub mod modpack_template;" to api/src/domain/server/mod.rs to expose the modpack template module.
Also add re-exports: pub use modpack_template::{ModpackTemplate, ModpackTemplateRepository};
  </action>
  <verify>Module compiles in server domain</verify>
  <done>ModpackTemplate module accessible from server domain</done>
</task>

<task type="auto">
  <name>Task 5: Add modpack template API routes</name>
  <files>api/src/presentation/routes/mod.rs</files>
  <action>
Add modpack template routes to the API:
- GET /api/v1/modpack-templates - list all modpack templates
- GET /api/v1/modpack-templates?game_type={type} - filter by game type

Create handler that checks user plan tier before returning templates (Hobby/Pro only).
Include user plan check in handler - only Hobby/Pro/Enterprise can access modpack templates.
  </action>
  <verify>Routes compile, endpoint responds to GET /api/v1/modpack-templates</verify>
  <done>Modpack template API endpoint available</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Database | Untrusted input (query params) filtered in handler |
| API → Plan Check | Verify user plan before returning template data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Injection | modpack_template query | mitigate | Use parameterized queries via sqlx, no raw SQL |
| T-34-02 | Authorization | template endpoint | mitigate | Check user plan tier before returning templates |
| T-34-03 | Information Disclosure | template endpoint | accept | Non-sensitive data, but limited to allowed plans |
</threat_model>

<verification>
[ ] Backend compiles: `cd api && cargo check`
[ ] ModpackTemplate module accessible: `use crate::domain::server::modpack_template::ModpackTemplate`
[ ] Routes registered: check router includes modpack-templates routes
[ ] Plan check implemented: Hobby/Pro only returns templates
</verification>

<success_criteria>
Modpack templates can be fetched via API endpoint, filtered by user plan tier, database model supports game type, fallback templates available when DB is empty
</success_criteria>

<output>
After completion, create `.planning/phases/34-modpacks-templates-for-hobby-and-pro-plans/34-01-SUMMARY.md`
</output>