---
phase: 33-plugins-templates-for-hobby-and-pro-plans
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/domain/server/mod.rs
  - api/src/domain/server/plugin_template/mod.rs
  - api/src/domain/server/plugin_template/model.rs
  - api/src/domain/server/plugin_template/repository.rs
  - api/src/presentation/routes/mod.rs
  - api/src/presentation/handlers/mod.rs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Plugin templates stored in database with game type + variant support"
    - "API provides endpoint to fetch available plugin templates"
    - "Plugin templates include Modrinth project IDs and versions"
    - "Template access check respects user plan tier (Hobby/Pro only)"
  artifacts:
    - path: "api/src/domain/server/plugin_template/model.rs"
      provides: "PluginTemplate entity with game_type, variant, plugins list"
      min_lines: 60
    - path: "api/src/domain/server/plugin_template/repository.rs"
      provides: "PluginTemplate CRUD operations"
      exports: ["list_plugin_templates", "get_plugin_templates_by_game"]
    - path: "api/src/presentation/routes/mod.rs"
      provides: "Plugin template API route registration"
---

<objective>
Create backend plugin template system to store pre-configured plugin lists per game type and variant.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@api/src/domain/server/template/model.rs
@api/src/domain/server/template/repository.rs
@api/src/application/dto/plugin_dtos.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin_template domain module</name>
  <files>api/src/domain/server/plugin_template/mod.rs</files>
  <action>
Create new module at api/src/domain/server/plugin_template/mod.rs with exports for model and repository.

```rust
pub mod model;
pub mod repository;

pub use model::*;
pub use repository::*;
```
  </action>
  <verify>File exists with module exports</verify>
  <done>PluginTemplate module created and exports available</done>
</task>

<task type="auto">
  <name>Task 2: Create plugin_template model/entity</name>
  <files>api/src/domain/server/plugin_template/model.rs</files>
  <action>
Create plugin template model with fields:
- id: Uuid (primary key)
- game_type: String (minecraft, palworld, etc.)
- variant: String (vanilla, forge, fabric, paper, spigot)
- display_name: String (e.g., "Survival Pack", "Creative Pack")
- description: Option<String>
- plugins: JSONB (array of plugin configs: {project_id, version_id, name})
- min_plan: String (hobby, pro, enterprise - who can use this template)
- is_active: bool
- created_at, updated_at: NaiveDateTime

Include FromRow derive for sqlx, Serialize/Deserialize for JSON.

Implement fallback method that returns hardcoded plugin templates:
- Minecraft Paper: EssentialsX, Vault, LuckPerms, CoreProtect
- Minecraft Forge: ForgeEss, JEI, Just Enough Items  
- Minecraft Fabric: FabricAPI, Lithium, Phosphor
- Palworld: (game-specific plugins)
- Rust: (game-specific plugins)
- Valheim: (game-specific plugins)
  </action>
  <verify>File compiles, model has all required fields</verify>
  <done>PluginTemplate model with database fields and fallback method</done>
</task>

<task type="auto">
  <name>Task 3: Create plugin_template repository</name>
  <files>api/src/domain/server/plugin_template/repository.rs</files>
  <action>
Create plugin template repository trait and implementation:
- list_plugin_templates() -> Vec<PluginTemplate>
- list_plugin_templates_by_game(game_type: &str) -> Vec<PluginTemplate>
- get_plugin_templates(game_type: &str, variant: &str) -> Option<PluginTemplate>
- list_by_plan(plan: &str) -> Vec<PluginTemplate> // for Hobby/Pro filtering

Use sqlx::query_as for async DB queries. Include fallback to hardcoded templates if DB query returns empty.
  </action>
  <verify>File compiles, repository has required methods</verify>
  <done>PluginTemplate repository with async DB operations and fallback</done>
</task>

<task type="auto">
  <name>Task 4: Register plugin_template module in server domain</name>
  <files>api/src/domain/server/mod.rs</files>
  <action>
Add "pub mod plugin_template;" to api/src/domain/server/mod.rs to expose the plugin template module.
Also add re-exports: pub use plugin_template::{PluginTemplate, PluginTemplateRepository};
  </action>
  <verify>Module compiles in server domain</verify>
  <done>PluginTemplate module accessible from server domain</done>
</task>

<task type="auto">
  <name>Task 5: Add plugin template API routes</name>
  <files>api/src/presentation/routes/mod.rs</files>
  <action>
Add plugin template routes to the API:
- GET /api/v1/plugin-templates - list all plugin templates
- GET /api/v1/plugin-templates?game_type={type} - filter by game type

Create handler that checks user plan tier before returning templates (Hobby/Pro only).
  </action>
  <verify>Routes compile, endpoint responds to GET /api/v1/plugin-templates</verify>
  <done>Plugin template API endpoint available</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Database | Untrusted input (query params) filtered in handler |
| API → Plan Check | Verify user plan before returning template data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Injection | plugin_template query | mitigate | Use parameterized queries via sqlx, no raw SQL |
| T-33-02 | Authorization | template endpoint | mitigate | Check user plan tier before returning templates |
| T-33-03 | Information Disclosure | template endpoint | accept | Non-sensitive data, but limited to allowed plans |
</threat_model>

<verification>
[ ] Backend compiles: `cd api && cargo check`
[ ] PluginTemplate module accessible: `use crate::domain::server::plugin_template::PluginTemplate`
[ ] Routes registered: check router includes plugin-template routes
[ ] Plan check implemented: Hobby/Pro only returns templates
</verification>

<success_criteria>
Plugin templates can be fetched via API endpoint, filtered by user plan tier, database model supports game type + variant, fallback templates available when DB is empty
</success_criteria>

<output>
After completion, create `.planning/phases/33-plugins-templates-for-hobby-and-pro-plans/33-01-SUMMARY.md`
</output>