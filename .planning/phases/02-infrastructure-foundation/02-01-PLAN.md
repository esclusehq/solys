---
phase: 02-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "PostgreSQL schema accessible via sqlx with compile-time checking"
    - "Redis pool configured and used for rate limiting"
    - "WebSocket endpoint exists for node agent communication"
  artifacts:
    - path: "api/migrations/*.sql"
      provides: "Database schema"
    - path: "api/src/infrastructure/cache/redis.rs"
      provides: "Redis connection pooling"
    - path: "api/src/presentation/handlers/node_ws_handler.rs"
      provides: "Node WebSocket handler"
    - path: "api/src/bootstrap/container.rs"
      provides: "Dependency injection container"
  key_links: []
---

<objective>
Verify PostgreSQL schema and Redis caching infrastructure are properly implemented and accessible.
</objective>

<context>
@.planning/phases/02-infrastructure-foundation/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify PostgreSQL migrations and schema</name>
  <files>api/migrations/, api/src/bootstrap/mod.rs</files>
  <action>
    1. Check that migrations can be compiled by running `cargo build -p backend` in api/
    2. Verify PgPool is created successfully in bootstrap/mod.rs
    3. Confirm sqlx query macros would compile (no missing tables/columns)
    4. Check database URL is properly loaded from environment
  </action>
  <verify>
    <automated>cd /home/rhnbztnl/Downloads/Projects/escluse-deploy/api && cargo check 2>&1 | head -50</automated>
  </verify>
  <done>PostgreSQL infrastructure verified - migrations accessible, schema compiles</done>
</task>

<task type="auto">
  <name>Task 2: Verify Redis pool and rate limiting</name>
  <files>api/src/infrastructure/cache/redis.rs, api/src/presentation/middleware/rate_limit.rs</files>
  <action>
    1. Verify RedisPool struct exists with MultiplexedConnection
    2. Verify RateLimitMiddleware uses RedisPool for rate limiting
    3. Check REDIS_URL is loaded from environment config
    4. Verify rate limiting keys follow pattern: rate_limit:user:*, rate_limit:global
  </action>
  <verify>
    <automated>grep -n "redis" /home/rhnbztnl/Downloads/Projects/escluse-deploy/api/src/config/app_config.rs</automated>
  </verify>
  <done>Redis infrastructure verified - pool available, rate limiting functional</done>
</task>

<task type="auto">
  <name>Task 3: Verify WebSocket infrastructure for nodes</name>
  <files>api/src/presentation/routes/node_routes.rs, api/src/presentation/handlers/node_ws_handler.rs</files>
  <action>
    1. Verify /api/ws/node route exists in node_routes.rs
    2. Verify WebSocket upgrade handler processes NodeMessage types
    3. Check NodeConnectionManager stores WebSocket senders
    4. Confirm registration, heartbeat, and command handling implemented
  </action>
  <verify>
    <automated>grep -n "ws_node_handler" /home/rhnbztnl/Downloads/Projects/escluse-deploy/api/src/presentation/routes/node_routes.rs</automated>
  </verify>
  <done>WebSocket infrastructure verified - node agent communication supported</done>
</task>

</tasks>

<verification>
- All infrastructure components compile successfully
- PostgreSQL, Redis, and WebSocket modules are imported and accessible
- Rate limiting middleware exists (even if not wired to router)
</verification>

<success_criteria>
PostgreSQL schema accessible, Redis pool configured, WebSocket handler exists
</success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure-foundation/02-01-SUMMARY.md`
</output>