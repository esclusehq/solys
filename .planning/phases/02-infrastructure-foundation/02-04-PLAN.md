---
phase: 02-infrastructure-foundation
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/presentation/handlers/server_handlers.rs
  - api/src/presentation/handlers/node_handlers.rs
  - api/src/presentation/handlers/billing_handlers.rs
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "Code compiles without errors (cargo check passes)"
  artifacts:
    - path: "api/src/presentation/handlers/server_handlers.rs"
      provides: "Fix import and method call"
    - path: "api/src/presentation/handlers/node_handlers.rs"
      provides: "Fix type argument and method call"
    - path: "api/src/presentation/handlers/billing_handlers.rs"
      provides: "Fix type argument mismatches"
  key_links:
    - from: "server_handlers.rs:262"
      to: "PostgresNodeRepository::find_by_user_id"
      via: "correct import path"
    - from: "node_handlers.rs:401"
      to: "SqlxSubscriptionRepository::find_by_user_id"
      via: "correct Uuid type"
    - from: "node_handlers.rs:403"
      to: "SqlxPlanRepository::find_by_id"
      via: "correct Uuid type"
---

<objective>
Fix compilation errors preventing `cargo check` from passing. The gaps are type mismatches and wrong import paths in handler files.
</objective>

<context>
@.planning/phases/02-infrastructure-foundation/02-UAT.md

# Gap Analysis from UAT.md

The following errors prevent cargo check from passing:
1. server_handlers.rs:262 - find_by_user_id called but type mismatch on &Uuid
2. node_handlers.rs:401 - find_by_user_id called with wrong argument type  
3. node_handlers.rs:403 - find_by_id called with wrong argument type
4. billing_handlers.rs:408, 421, 452 - type mismatches between &Uuid and Uuid

# Repository Methods Available

From domain/repositories/node_repository.rs (trait):
- find_by_user_id(&self, user_id: &Uuid) - takes reference

From domain/subscription/sqlx_repository.rs:
- find_by_user_id(&self, user_id: Uuid) - takes value (not reference)

From domain/plan/sqlx_repository.rs:
- find_by_id(&self, id: Uuid) - takes value (not reference)

# Fix Reference

- Traits take &Uuid references: NodeRepository trait
- Sqlx implementations take Uuid values: SubscriptionRepository, PlanRepository
- The code passes &tenant_id but trait expects Uuid (value) for subscription/plan repos
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix server_handlers.rs import and type</name>
  <files>api/src/presentation/handlers/server_handlers.rs</files>
  <action>
    The import at line 15 is correct (uses infrastructure PostgresNodeRepository).
    The issue is at line 262: node_repo.find_by_user_id(&auth_user.tenant_id) - this correctly passes &Uuid and trait expects &Uuid.
    BUT the error says method not found. This suggests the trait or implementation isn't in scope properly.
    
    Check if the trait NodeRepository is properly imported. The trait is defined in:
    - api/src/domain/repositories/node_repository.rs (trait)
    - But the handler imports implementation from infrastructure layer!
    
    FIX: Add proper trait import at top of server_handlers.rs:
    ```
    use crate::domain::repositories::node_repository::NodeRepository;
    ```
    Then the find_by_user_id call at line 262 should work.
  </action>
  <verify>
    <automated>cd /home/rhnbztnl/Downloads/Projects/escluse-deploy/api && cargo check 2>&1 | grep -c "^error"</automated>
  </verify>
  <done>server_handlers.rs errors resolved</done>
</task>

<task type="auto">
  <name>Task 2: Fix node_handlers.rs type arguments</name>
  <files>api/src/presentation/handlers/node_handlers.rs</files>
  <action>
    Fix two type mismatches:

    1. Line 401: `sub_repo.find_by_user_id(tenant_id)` 
       - Currently passes &Uuid but SubscriptionRepository trait expects Uuid (value)
       - FIX: Change to `sub_repo.find_by_user_id(*tenant_id).await` or clone
       
    2. Line 403: `plan_repo.find_by_id(&subscription.plan_id)`
       - Currently passes &Uuid but PlanRepository trait expects Uuid (value)  
       - FIX: Change to `plan_repo.find_by_id(subscription.plan_id).await`

    Add proper trait imports at top of node_handlers.rs:
    ```
    use crate::domain::subscription::repository::SubscriptionRepository;
    use crate::domain::plan::repository::PlanRepository;
    ```
  </action>
  <verify>
    <automated>cd /home/rhnbztnl/Downloads/Projects/escluse-deploy/api && cargo check 2>&1 | grep -c "^error"</automated>
  </verify>
  <done>node_handlers.rs errors resolved</done>
</task>

<task type="auto">
  <name>Task 3: Fix billing_handlers.rs type arguments</name>
  <files>api/src/presentation/handlers/billing_handlers.rs</files>
  <action>
    Fix three type mismatches:
    
    1. Line 408: `sub_repo.find_by_user_id(&auth_user.user_id)` 
       - Passes &Uuid but expects Uuid
       - FIX: Change to `sub_repo.find_by_user_id(auth_user.user_id).await` (use value not reference)
       
    2. Line 421: `plan_repo.find_by_id(&subscription.plan_id)` 
       - Passes &Uuid but expects Uuid  
       - FIX: Change to `plan_repo.find_by_id(subscription.plan_id).await`
       
    3. Line 452: Same issue as 408
       - FIX: Change to `sub_repo.find_by_user_id(auth_user.user_id).await`

    Add proper trait imports if not already present:
    ```
    use crate::domain::subscription::repository::SubscriptionRepository;
    use crate::domain::plan::repository::PlanRepository;
    ```
  </action>
  <verify>
    <automated>cd /home/rhnbztnl/Downloads/Projects/escluse-deploy/api && cargo check 2>&1 | grep -c "^error"</automated>
  </verify>
  <done>billing_handlers.rs errors resolved</done>
</task>

<task type="auto">
  <name>Task 4: Verify compilation</name>
  <files>api/</files>
  <action>
    Run final verification that cargo check passes with no errors:
    ```
    cd api && cargo check
    ```
    Expected: warnings only, no errors.
  </action>
  <verify>
    <automated>cd /home/rhnbztnl/Downloads/Projects/escluse-deploy/api && cargo check 2>&1 | grep -E "^error" | wc -l</automated>
  </verify>
  <done>All compilation errors fixed, cargo check passes</done>
</task>

</tasks>

<verification>
After all tasks complete:
- cargo check runs with warnings only
- No "error[E0599]: no method named find_by_user_id"
- No "error[E0282]: type annotations needed"  
- No "error[E0308]: mismatched types"
</verification>

<success_criteria>
Code compiles without errors. Task 1-3 each fix specific handler file, Task 4 confirms overall compilation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure-foundation/02-04-SUMMARY.md`
</output>