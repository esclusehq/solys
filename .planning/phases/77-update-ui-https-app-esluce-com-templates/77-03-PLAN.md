---
phase: 77-update-ui-https-app-esluce-com-templates
plan: 03
type: execute
wave: 2
depends_on:
  - 77-02
files_modified:
  - app/src/pages/templates/TemplateCreatePage.jsx
autonomous: true
requirements: [D-05, D-06, D-07]
user_setup: []

must_haves:
  truths:
    - "All form inputs, selects, and textarea have cosmic-cyan focus rings"
    - "Checkbox Active toggle uses accent-[var(--color-cosmic-cyan)] instead of accent-cyan-500"
    - "Back link uses cosmic CSS variables (text-muted / text-main)"
    - "Page title uses text-[var(--color-text-main)] instead of text-white"
    - "Save button has hover:brightness-110 for consistency with Phases 75/76"
    - "No new form sections or fields added (D-07)"
    - "Form retains existing 2-section layout: Basic Info + Configuration (D-05)"
  artifacts:
    - path: "app/src/pages/templates/TemplateCreatePage.jsx"
      provides: "Cosmic theme consistency pass on create/edit form"
      min_lines: 230
      contains: "accent-\\[var\\(--color-cosmic-cyan\\)\\]|focus:ring-\\[var\\(--color-cosmic-cyan\\)\\]|brightness-110"
  key_links:
    - from: "Checkbox Active toggle"
      to: "cosmic-cyan accent color"
      via: "accent-[var(--color-cosmic-cyan)]"
      pattern: "accent-\\[var\\(--color-cosmic-cyan\\)\\]"
    - from: "Form inputs/selects/textarea"
      to: "cosmic focus rings"
      via: "focus:outline-none focus:ring-2 focus:ring-[var(--color-cosmic-cyan)]/50 focus:border-[var(--color-cosmic-cyan)]/50"
      pattern: "focus:ring-\\[var\\(--color-cosmic-cyan\\)\\]"
    - from: "Page title"
      to: "cosmic text color"
      via: "text-[var(--color-text-main)]"
      pattern: "text-\\[var\\(--color-text-main\\)\\].*Create Template|Edit Template"

---

<objective>
Apply a cosmic theme consistency pass on the Template Create/Edit form at `/templates/create` and `/templates/:id/edit`. Add focus rings to all 8 inputs/selects/textarea, update checkbox accent color from `accent-cyan-500` to `accent-[var(--color-cosmic-cyan)]`, update back link and page title to use CSS variables, and add hover brightness effect to the Save button. No structural changes — only styling updates (D-05: keep 2-section layout, D-07: no new fields).

Purpose: Ensure the template form matches the cosmic theme consistency of the rest of the dashboard (Phases 75/76 pattern), providing uniform visual experience.

Output: Modified `app/src/pages/templates/TemplateCreatePage.jsx` with cosmic theme refinements.
</objective>

<execution_context>
@/home/rhnbztnl/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rhnbztnl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-update-ui-https-app-esluce-com-templates/77-CONTEXT.md
@.planning/phases/77-update-ui-https-app-esluce-com-templates/77-UI-SPEC.md
@.planning/phases/77-update-ui-https-app-esluce-com-templates/77-02-SUMMARY.md

<interfaces>
<!-- Key contracts from existing code. Executor should NOT need to explore codebase. -->

**Existing TemplateCreatePage.jsx current state (236 lines):**
- Uses cosmic CSS variables for most inputs, selects, textarea (bg, border, text colors)
- Checkbox Active toggle uses `accent-cyan-500` (line 174) — needs `accent-[var(--color-cosmic-cyan)]`
- Back link uses `text-gray-400 hover:text-white` (line 99) — needs cosmic variables
- Page title uses `text-white` (line 100) — needs `text-[var(--color-text-main)]`
- Form inputs/selects/textarea missing focus rings — need `focus:outline-none focus:ring-2 focus:ring-[var(--color-cosmic-cyan)]/50 focus:border-[var(--color-cosmic-cyan)]/50`
- Save button already uses cosmic bg/text — needs `hover:brightness-110` for consistency

**8 form elements that need focus rings:**
1. Display Name input (line 116-117): `text-white"`
2. Game Type select (line 123-124): `text-white"`
3. Category select (line 136-137): `text-white"`
4. Visibility select (line 153-154): `text-white"`
5. Description textarea (line 165-166): `text-white"`
6. Docker Image input (line 199-200): `text-white"`
7. Default Port input (line 207-208): `text-white"`
8. Environment Variables textarea (line 220-221): `text-white font-mono"`

**Existing imports (lines 1-5):**
```javascript
import { useState, useEffect } from 'react'
import { useNavigate, useParams } from 'react-router-dom'
import { templatesApi } from '../../api/templatesApi'
import { useUIStore } from '../../store/uiStore'
import { EscluseSpinner } from '../../components/SkeletonLoader'
```
Keep all existing imports unchanged.

**Elements verified as already correct (no changes needed):**
- Input/select/textarea backgrounds: `bg-[var(--color-cosmic-card)]/60` ✓
- Input/select/textarea borders: `border border-[var(--color-cosmic-border)]` ✓
- Labels: `text-xs font-bold text-[var(--color-text-muted)] mb-2` ✓
- Section panels: `glass-panel p-6` ✓
- Section headings: `text-lg font-bold mb-1` ✓
- Section descriptions: `text-xs text-[var(--color-text-muted)] mb-5` ✓
- Save button bg: `bg-[var(--color-cosmic-cyan)]/10 text-[var(--color-cosmic-cyan)]` ✓
- Env JSON textarea: `font-mono` ✓
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focus rings, update checkbox accent, back link, page title, and save button to cosmic theme</name>
  <files>app/src/pages/templates/TemplateCreatePage.jsx</files>
  <action>
    Apply cosmic theme consistency refinements to TemplateCreatePage.jsx. Make 6 targeted edits — no structural changes, only styling updates.

    **Edit 1 — Back link (current line 99).** Replace the text-gray-400/hover:text-white with cosmic variables:
    
    Find:
    ```
    className="text-gray-400 hover:text-white text-sm">&larr; Back</button>
    ```
    Replace with:
    ```
    className="text-[var(--color-text-muted)] hover:text-[var(--color-text-main)] text-sm">&larr; Back</button>
    ```

    **Edit 2 — Page title (current line 100).** Replace text-white with text-[var(--color-text-main)]:
    
    Find:
    ```
    <h2 className="text-2xl font-bold text-white">{isEdit ? 'Edit Template' : 'Create Template'}</h2>
    ```
    Replace with:
    ```
    <h2 className="text-2xl font-bold text-[var(--color-text-main)]">{isEdit ? 'Edit Template' : 'Create Template'}</h2>
    ```

    **Edit 3 — Add focus rings to all 8 inputs/selects/textarea.**
    
    Each element's className currently ends with `text-white"` (or `text-white font-mono"` for the env textarea). Append focus ring classes before the closing double-quote in each className string:
    
    The focus ring pattern to append:
    ```
     focus:outline-none focus:ring-2 focus:ring-[var(--color-cosmic-cyan)]/50 focus:border-[var(--color-cosmic-cyan)]/50
    ```
    (note the leading space before `focus:outline-none`)

    **8 elements to update (each has `text-white"` at the end of className):**
    
    1. **Display Name** (line ~117): `text-white"` → `text-white focus:outline-none focus:ring-2 focus:ring-[var(--color-cosmic-cyan)]/50 focus:border-[var(--color-cosmic-cyan)]/50"`
    
    2. **Game Type** (line ~124): `text-white"` → same append
    
    3. **Category** (line ~137): `text-white"` → same append
    
    4. **Visibility** (line ~154): `text-white"` → same append
    
    5. **Description** textarea (line ~166): `text-white"` → same append
    
    6. **Docker Image** (line ~200): `text-white"` → same append
    
    7. **Default Port** (line ~208): `text-white"` → same append
    
    8. **Environment Variables** textarea (line ~221): `text-white font-mono"` → `text-white font-mono focus:outline-none focus:ring-2 focus:ring-[var(--color-cosmic-cyan)]/50 focus:border-[var(--color-cosmic-cyan)]/50"`

    **⚠️ Warning:** The env textarea has `font-mono"` not `text-white"` as the class end. Make sure to find `text-white font-mono"` specifically for this one.

    **Edit 4 — Checkbox accent (current line 174).** Replace generic cyan-500 with cosmic variable:
    
    Find:
    ```
    className="w-4 h-4 rounded accent-cyan-500"
    ```
    Replace with:
    ```
    className="w-4 h-4 rounded accent-[var(--color-cosmic-cyan)]"
    ```

    **Edit 5 — Save button hover brightness (current line ~230).** Add hover:brightness-110:
    
    Find:
    ```
    disabled:opacity-50 transition-all">
    ```
    Replace with:
    ```
    disabled:opacity-50 hover:brightness-110 transition-all">
    ```

    **After all edits, verify:**
    ```bash
    grep -c "text-gray-400" app/src/pages/templates/TemplateCreatePage.jsx  # Must be 0
    grep -c "accent-cyan-500" app/src/pages/templates/TemplateCreatePage.jsx  # Must be 0
    grep -c 'text-white"' app/src/pages/templates/TemplateCreatePage.jsx  # Must be 0 (title and back link migrated)
    grep -c 'text-white font-mono"' app/src/pages/templates/TemplateCreatePage.jsx  # Must be 0
    ```
    Note: `text-white` may appear inside JSX text content (e.g., in body text), not just className attributes — that's fine. Only className occurrences matter.
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const content = fs.readFileSync('app/src/pages/templates/TemplateCreatePage.jsx', 'utf-8');
const checks = [];

// 1. Check focus rings on all 8 elements
const focusRingCount = (content.match(/focus:ring-\[var\(--color-cosmic-cyan\)\]/g) || []).length;
checks.push(focusRingCount >= 8);
console.log('Focus ring occurrences:', focusRingCount);

// 2. Checkbox accent
checks.push(content.includes('accent-[var(--color-cosmic-cyan)]'));
console.log('Has cosmic checkbox:', content.includes('accent-[var(--color-cosmic-cyan)]'));

// 3. Back link uses cosmic variables
checks.push(content.includes('text-[var(--color-text-muted)]') && content.includes('hover:text-[var(--color-text-main)]'));
console.log('Back link cosmic:', content.includes('text-[var(--color-text-muted)]') && content.includes('hover:text-[var(--color-text-main)]'));

// 4. Page title uses text-main
const titleMatch = content.match(/text-2xl font-bold text-\[\w+/);
console.log('Title class:', titleMatch ? titleMatch[0] : 'NOT FOUND');

// 5. Save button brightness
checks.push(content.includes('brightness-110'));
console.log('Has brightness:', content.includes('brightness-110'));

// 6. No accent-cyan-500
checks.push(!content.includes('accent-cyan-500'));
console.log('No accent-cyan-500:', !content.includes('accent-cyan-500'));

// 7. No text-gray-400 on back link
// (It may appear elsewhere; check that it's not the back link pattern)
console.log('text-gray-400 occurrences:', (content.match(/text-gray-400/g) || []).length);

console.log('ALL CHECKS PASS:', checks.every(Boolean));
checks.forEach((c, i) => console.log('check', i, c));
"
npm run build --prefix app 2>&1 | tail -5</automated>
  </verify>
  <done>Back link uses cosmic text-muted/main colors. Page title uses text-[var(--color-text-main)]. All 8 inputs/selects/textarea have focus rings with cosmic-cyan. Checkbox uses cosmic-cyan accent. Save button has hover:brightness-110. No structural changes. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure styling changes — no logic, no data flow, no API changes, no new auth/authorization concerns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-13 | N/A | Styling changes only | accept | No new attack surface introduced. Focus rings are visual only (CSS pseudo-class). Accent color change is cosmetic. No data handling changes. No API parameter changes. All existing form validation (required attributes, try/catch in handleSave) remains unchanged. |
</threat_model>

<verification>
## Overall Phase Verification

1. **Build passes:** `npm run build` from `app/` directory — must exit with code 0
2. **Focus rings:** `grep -c "focus:ring-\[var(--color-cosmic-cyan)\]" app/src/pages/templates/TemplateCreatePage.jsx` returns 8+
3. **Checkbox accent:** `grep -n "accent-\[var(--color-cosmic-cyan)\]" app/src/pages/templates/TemplateCreatePage.jsx` returns 1 match
4. **Page title color:** `grep -c "text-\[var(--color-text-main)\].*Template" app/src/pages/templates/TemplateCreatePage.jsx` returns 1 match
5. **Back link color:** `grep -c "text-\[var(--color-text-muted)\].*Back" app/src/pages/templates/TemplateCreatePage.jsx` returns 1 match (or equivalent pattern)
6. **No accent-cyan-500:** `grep -c "accent-cyan-500" app/src/pages/templates/TemplateCreatePage.jsx` returns 0
7. **No text-gray-400 on back link:** `grep -c "text-gray-400" app/src/pages/templates/TemplateCreatePage.jsx` returns 0 (if other text-gray-400 exists, verify it's not the back link)
8. **Save button hover:** `grep -c "brightness-110" app/src/pages/templates/TemplateCreatePage.jsx` returns 1
</verification>

<success_criteria>
1. **Build passes:** `npm run build` in `app/` exits with code 0
2. **Focus rings:** All 8 form elements have cosmic-cyan focus rings
3. **Checkbox accent:** Changed from `accent-cyan-500` to `accent-[var(--color-cosmic-cyan)]`
4. **Back link:** Uses `text-[var(--color-text-muted)]` / `hover:text-[var(--color-text-main)]`
5. **Page title:** Uses `text-[var(--color-text-main)]` instead of `text-white`
6. **Save button:** Has `hover:brightness-110` for consistency with Phases 75/76
7. **No regression:** All existing form fields, sections, labels, and validation unchanged
8. **Form functionality:** Create and edit modes work identically — same API calls, toast messages, navigation
</success_criteria>

<output>
After completion, create `.planning/phases/77-update-ui-https-app-esluce-com-templates/77-03-SUMMARY.md`
</output>