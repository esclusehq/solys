---
phase: 84-perbaiki-layout-yang-janggal-ataupun-tidak-bagus-di-app-eslu
plan: 02
type: execute
wave: 2
depends_on:
  - 84-01-PLAN.md
files_modified:
  - app/src/pages/Nodes.jsx
  - app/src/pages/settings/SettingsPage.jsx
  - app/src/pages/Alerts.jsx
  - app/src/pages/Console.jsx
  - app/src/pages/auth/LoginPage.jsx
  - app/src/pages/auth/RegisterPage.jsx
  - app/src/pages/templates/TemplateLibraryPage.jsx
  - app/src/pages/templates/TemplateCreatePage.jsx
  - app/src/pages/templates/ModBrowserPage.jsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "All pages use p-8 as the standard content padding"
    - "All h1 headings use text-3xl font-semibold"
    - "All h2 headings use text-2xl font-semibold"
    - "Data-heavy pages (Dashboard, Servers, Nodes, Billing) keep full-width layout"
    - "Other pages (Settings, Alerts, Console, Templates) use max-w-6xl centered container"
    - "Auth pages (Login, Register) keep centered layout but use consistent p-8 padding"
    - "Build compiles without errors"
  artifacts:
    - path: "All modified page files"
      provides: "Consistent padding, heading hierarchy, and page width strategy"
      contains: "p-8, text-3xl font-semibold, max-w-6xl"
  key_links:
    - from: "All page components"
      to: "UI-SPEC.md padding/heading contracts"
      via: "p-8 standard, h1=text-3xl, h2=text-2xl"
      pattern: "text-3xl font-semibold"
---

<objective>
Standardize layout consistency across all app pages: unify page padding to `p-8`, enforce heading hierarchy (h1 = `text-3xl font-semibold`, h2 = `text-2xl font-semibold`), apply max-width container (`max-w-6xl`) to non-data pages, and keep full-width layout for data-heavy pages.

Purpose: Every page should feel like part of the same app — consistent spacing and heading hierarchy eliminates the visual disjointedness between pages.
Output: 9 page files with standardized padding and heading patterns.
</objective>

<execution_context>
@/home/rhnbztnl/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rhnbztnl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-perbaiki-layout-yang-janggal-ataupun-tidak-bagus-di-app-eslu/84-CONTEXT.md
@.planning/phases/84-perbaiki-layout-yang-janggal-ataupun-tidak-bagus-di-app-eslu/84-RESEARCH.md
@.planning/phases/84-perbaiki-layout-yang-janggal-ataupun-tidak-bagus-di-app-eslu/84-UI-SPEC.md

<interfaces>
Padding standard (D-04):
| Page | Current | Target |
|------|---------|--------|
| DashboardPage | `p-8` | `p-8` ✓ (keep) |
| ServerManagerPage | `p-8` | `p-8` ✓ (keep) |
| Nodes | `px-8 py-6` (header) + `p-8` (body) | `p-8` (unify) |
| BillingPage | `p-8` | `p-8` ✓ (keep) |
| SettingsPage | `p-6` | `p-8` |
| Alerts | `px-8 py-6` (header) + body area | `p-8` |
| Console | flex-1 layout | `p-8` content area |
| LoginPage | varies | `p-8` centered |
| RegisterPage | varies | `p-8` centered |
| TemplateLibraryPage | varies | `p-8` |
| TemplateCreatePage | varies | `p-8` |
| ModBrowserPage | varies | `p-8` |

Heading hierarchy (D-06):
| Role | Current (varies) | Target |
|------|-----------------|--------|
| h1 | `text-3xl font-semibold` (Dashboard) / `text-2xl font-bold` (Nodes, Alerts) / `text-2xl font-semibold` (Settings) | `text-3xl font-semibold text-[var(--color-text-main)]` |
| h2 | `text-2xl font-semibold` (Dashboard) / varies | `text-2xl font-semibold text-[var(--color-text-main)]` |

Page width strategy (D-07):
- Full-width (`w-full` with `p-8`): Dashboard, ServerManager, Nodes, Billing
- Max-width container (`max-w-6xl mx-auto` within `p-8`): Settings, Alerts, Console, Templates, Auth pages
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize Nodes.jsx and Alerts.jsx — padding and heading</name>
  <files>app/src/pages/Nodes.jsx, app/src/pages/Alerts.jsx</files>

  <read_first>app/src/pages/Nodes.jsx (first 80 lines — header area), app/src/pages/Alerts.jsx (first 50 lines — header area)</read_first>

  <action>
    Edit both files to standardize padding and heading.

    **A. `app/src/pages/Nodes.jsx`:**
    Current header structure:
    ```jsx
    <header className="px-8 py-6 border-b border-[var(--color-cosmic-border)] flex items-center justify-between">
        <h1 className="text-2xl font-bold">Node Management</h1>
        ...
    </header>
    ```

    Change to:
    ```jsx
    <header className="p-8 border-b border-[var(--color-cosmic-border)] flex items-center justify-between">
        <h1 className="text-3xl font-semibold text-[var(--color-text-main)]">Node Management</h1>
        ...
    </header>
    ```

    Also ensure the content area below the header uses consistent padding. Currently it likely uses `p-8` — verify and keep.

    **B. `app/src/pages/Alerts.jsx`:**
    Current:
    ```jsx
    <header className="px-8 py-6 border-b border-[var(--color-cosmic-border)]">
        <h1 className="text-2xl font-bold">Alerts & Notifications</h1>
    </header>
    ```

    Change to:
    ```jsx
    <header className="p-8 border-b border-[var(--color-cosmic-border)]">
        <h1 className="text-3xl font-semibold text-[var(--color-text-main)]">Alerts & Notifications</h1>
    </header>
    ```

    Both pages are data-heavy (tables, node cards) — keep full-width (no max-width container).
  </action>

  <verify>
    <automated>
      cd /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app && \
      npm run build 2>&1 && \
      grep -q 'text-3xl font-semibold' app/src/pages/Nodes.jsx && \
      grep -q 'p-8' app/src/pages/Nodes.jsx && \
      grep -q 'text-3xl font-semibold' app/src/pages/Alerts.jsx && \
      grep -q 'p-8' app/src/pages/Alerts.jsx && \
      ! grep -q 'text-2xl font-bold' app/src/pages/Nodes.jsx && \
      ! grep -q 'px-8 py-6' app/src/pages/Nodes.jsx
    </automated>
  </verify>

  <acceptance_criteria>
    - Nodes.jsx: h1 uses `text-3xl font-semibold` not `text-2xl font-bold`
    - Nodes.jsx: header uses `p-8` not `px-8 py-6`
    - Alerts.jsx: h1 uses `text-3xl font-semibold` not `text-2xl font-bold`
    - Alerts.jsx: header uses `p-8` not `px-8 py-6`
    - Both pages keep full-width layout (no max-w-6xl)
    - npm run build exits with code 0
  </acceptance_criteria>

  <done>
    Nodes.jsx and Alerts.jsx: heading upgraded to text-3xl font-semibold, header padding standardized to p-8.
  </done>
</task>

<task type="auto">
  <name>Task 2: Standardize SettingsPage.jsx, Console.jsx — padding, heading, and max-width container</name>
  <files>app/src/pages/settings/SettingsPage.jsx, app/src/pages/Console.jsx</files>

  <read_first>app/src/pages/settings/SettingsPage.jsx (full file — 63 lines), app/src/pages/Console.jsx (full file — 78 lines)</read_first>

  <action>
    Edit both files. These pages should use a max-width container (`max-w-6xl mx-auto`) within the `p-8` area.

    **A. `app/src/pages/settings/SettingsPage.jsx`:**
    Current:
    ```jsx
    <div className="p-6">
        <h1 className="text-2xl font-semibold text-[var(--color-text-primary)] mb-6">Settings</h1>
        ...
    </div>
    ```

    Change to:
    ```jsx
    <div className="p-8">
        <div className="max-w-6xl mx-auto">
            <h1 className="text-3xl font-semibold text-[var(--color-text-primary)] mb-6">Settings</h1>
            ...
        </div>
    </div>
    ```

    Keep `mb-6` on the h1 for spacing below the heading.

    **B. `app/src/pages/Console.jsx`:**
    Read the full file first. Look for the outer container and heading. Apply the same pattern:
    - Outer container: `p-8`
    - Content wrapper: `max-w-6xl mx-auto`
    - h1: `text-3xl font-semibold text-[var(--color-text-main)]`
  </action>

  <verify>
    <automated>
      cd /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app && \
      npm run build 2>&1 && \
      grep -q 'p-8' app/src/pages/settings/SettingsPage.jsx && \
      grep -q 'text-3xl font-semibold' app/src/pages/settings/SettingsPage.jsx && \
      grep -q 'max-w-6xl' app/src/pages/settings/SettingsPage.jsx && \
      ! grep -q 'p-6' app/src/pages/settings/SettingsPage.jsx
    </automated>
  </verify>

  <acceptance_criteria>
    - SettingsPage.jsx: padding `p-8` not `p-6`
    - SettingsPage.jsx: h1 `text-3xl font-semibold` not `text-2xl font-semibold`
    - SettingsPage.jsx: content wrapped in `max-w-6xl mx-auto`
    - Console.jsx: padding standardized to `p-8`
    - Console.jsx: heading uses `text-3xl font-semibold`
    - Console.jsx: content uses `max-w-6xl mx-auto`
    - npm run build exits with code 0
  </acceptance_criteria>

  <done>
    SettingsPage.jsx and Console.jsx: padding standardized to p-8, heading to text-3xl font-semibold, content constrained to max-w-6xl.
  </done>
</task>

<task type="auto">
  <name>Task 3: Standardize template and auth pages — padding, heading, and max-width container</name>
  <files>app/src/pages/templates/TemplateLibraryPage.jsx, app/src/pages/templates/TemplateCreatePage.jsx, app/src/pages/templates/ModBrowserPage.jsx, app/src/pages/auth/LoginPage.jsx, app/src/pages/auth/RegisterPage.jsx</files>

  <read_first>app/src/pages/templates/TemplateLibraryPage.jsx (first 40 lines), app/src/pages/auth/LoginPage.jsx (first 60 lines for container structure)</read_first>

  <action>
    Read each file to identify the current outer container and heading pattern. Apply the standard pattern:

    **Template pages (TemplateLibraryPage, TemplateCreatePage, ModBrowserPage):**
    1. Outer div padding: `p-8`
    2. Content wrapper: `max-w-6xl mx-auto` (add if not present)
    3. h1: `text-3xl font-semibold text-[var(--color-text-main)]`
    4. h2 (if any): `text-2xl font-semibold text-[var(--color-text-main)]`
    5. Do NOT restructure the page layout — only change padding values, heading classes, and add the max-width container wrapper.

    **Auth pages (LoginPage, RegisterPage):**
    1. Outer container: keep centered/flex layout but ensure inner content area uses `p-8` padding
    2. Keep the card centered pattern — do NOT add max-w-6xl (auth pages have their own constrained layout)
    3. h1 heading inside the card: `text-3xl font-semibold text-[var(--color-text-main)]`
    4. Auth pages may already have a consistent pattern from Phase 82. Only adjust padding and heading classes.
  </action>

  <verify>
    <automated>
      cd /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app && \
      npm run build 2>&1 && \
      for f in app/src/pages/templates/TemplateLibraryPage.jsx app/src/pages/templates/TemplateCreatePage.jsx app/src/pages/templates/ModBrowserPage.jsx; do \
        if [ -f "$f" ]; then \
          grep -q 'p-8' "$f" || { echo "$f: p-8 ✗"; exit 1; }; \
          grep -q 'text-3xl font-semibold' "$f" || { echo "$f: h1 ✗"; exit 1; }; \
        fi; \
      done && \
      grep -q 'text-3xl font-semibold' app/src/pages/auth/LoginPage.jsx || { echo "LoginPage: h1 ✗"; exit 1; } && \
      grep -q 'text-3xl font-semibold' app/src/pages/auth/RegisterPage.jsx || { echo "RegisterPage: h1 ✗"; exit 1; } && \
      echo "All template and auth pages verified ✓"
    </automated>
  </verify>

  <acceptance_criteria>
    - TemplateLibraryPage: padding p-8, h1 text-3xl font-semibold, max-w-6xl wrapper
    - TemplateCreatePage: padding p-8, h1 text-3xl font-semibold, max-w-6xl wrapper
    - ModBrowserPage: padding p-8, h1 text-3xl font-semibold, max-w-6xl wrapper
    - LoginPage: h1 uses text-3xl font-semibold, keeps centered layout
    - RegisterPage: h1 uses text-3xl font-semibold, keeps centered layout
    - npm run build exits with code 0
  </acceptance_criteria>

  <done>
    All template and auth pages: padding standardized to p-8, heading to text-3xl font-semibold, template pages wrapped in max-w-6xl, auth pages keep centered layout.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. All changes are Tailwind class string replacements in JSX — no logic changes, no new endpoints, no user input handling changed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-84-02 | S (Spoofing) | All modified pages | accept | CSS class string replacements only — no functionality changes. |
</threat_model>

<verification>
### Plan-Level Verification

1. `npm run build` passes with exit code 0
2. All 12 modified pages verified:
   - Each page's primary heading h1 uses `text-3xl font-semibold`
   - Pages with `p-6` padding now use `p-8`
   - Pages using `px-8 py-6` now use `p-8`
   - Non-data pages wrapped in `max-w-6xl mx-auto`
3. Manual: navigate through all pages, verify padding and heading consistency
</verification>

<success_criteria>
- [ ] Build passes (`npm run build`)
- [ ] Nodes.jsx: h1 `text-3xl font-semibold`, header `p-8`
- [ ] Alerts.jsx: h1 `text-3xl font-semibold`, header `p-8`
- [ ] SettingsPage.jsx: `p-8` not `p-6`, h1 `text-3xl font-semibold`, `max-w-6xl` wrapper
- [ ] Console.jsx: `p-8`, h1 `text-3xl font-semibold`, `max-w-6xl` wrapper
- [ ] TemplateLibraryPage: `p-8`, h1 `text-3xl font-semibold`
- [ ] TemplateCreatePage: `p-8`, h1 `text-3xl font-semibold`
- [ ] ModBrowserPage: `p-8`, h1 `text-3xl font-semibold`
- [ ] LoginPage: h1 `text-3xl font-semibold`
- [ ] RegisterPage: h1 `text-3xl font-semibold`
- [ ] DashboardPage, ServerManagerPage, BillingPage: already correct, no changes needed
- [ ] No page still uses `text-2xl font-bold` for h1
- [ ] No page still uses `p-6` or `px-8 py-6` for outer padding
</success_criteria>

<output>
After completion, create `.planning/phases/84-perbaiki-layout-yang-janggal-ataupun-tidak-bagus-di-app-eslu/84-02-SUMMARY.md`
</output>
