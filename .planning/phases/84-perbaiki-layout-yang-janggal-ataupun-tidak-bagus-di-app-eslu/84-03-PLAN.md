---
phase: 84-perbaiki-layout-yang-janggal-ataupun-tidak-bagus-di-app-eslu
plan: 03
type: execute
wave: 3
depends_on:
  - 84-02-PLAN.md
files_modified:
  - app/src/pages/dashboard/DashboardPage.jsx
  - app/src/pages/Nodes.jsx
  - app/src/pages/billing/BillingPage.jsx
  - app/src/pages/Alerts.jsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "All tables across DashboardPage, Nodes, BillingPage, and Alerts use consistent thead style"
    - "All tables use consistent row height (px-4 py-3), border pattern, and hover state"
    - "All empty states use consistent messaging pattern and styling"
    - "Table headers use text-xs font-medium uppercase tracking-wider with muted text color"
    - "Table rows use hover:bg-[rgba(255,255,255,0.02)] transition-colors"
    - "Build compiles without errors"
  artifacts:
    - path: "4 page files"
      provides: "Unified table patterns across all pages"
      contains: "consistent thead, tbody, row, empty state patterns"
  key_links:
    - from: "All table-containing pages"
      to: "UI-SPEC.md table pattern contract"
      via: "standard table structure"
      pattern: "text-xs font-medium uppercase tracking-wider"
---

<objective>
Unify table patterns across all pages: apply consistent thead style (`text-xs font-medium uppercase tracking-wider text-[var(--color-text-muted)] bg-[rgba(255,255,255,0.02)]`), row height (`px-4 py-3`), border pattern (`divide-y divide-[var(--color-cosmic-border)]`), hover state (`hover:bg-[rgba(255,255,255,0.02)] transition-colors`), and empty state messaging.

Purpose: Tables are the most common data-display pattern in the app. Inconsistent styling across pages creates a disjointed feel. This plan makes every table look like it belongs to the same system.
Output: 4 page files with unified table styling.
</objective>

<execution_context>
@/home/rhnbztnl/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rhnbztnl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-perbaiki-layout-yang-janggal-ataupun-tidak-bagus-di-app-eslu/84-CONTEXT.md
@.planning/phases/84-perbaiki-layout-yang-janggal-ataupun-tidak-bagus-di-app-eslu/84-RESEARCH.md
@.planning/phases/84-perbaiki-layout-yang-janggal-ataupun-tidak-bagus-di-app-eslu/84-UI-SPEC.md

<interfaces>
Standard table structure from UI-SPEC.md:

```html
<div class="glass-panel overflow-hidden border border-[var(--color-cosmic-border)]">
  <table class="w-full">
    <thead>
      <tr>
        <th class="px-4 py-3 text-left text-xs font-medium text-[var(--color-text-muted)] uppercase tracking-wider bg-[rgba(255,255,255,0.02)]">Column</th>
      </tr>
    </thead>
    <tbody class="divide-y divide-[var(--color-cosmic-border)]">
      <tr class="hover:bg-[rgba(255,255,255,0.02)] transition-colors">
        <td class="px-4 py-3 text-sm text-[var(--color-text-main)]">Value</td>
      </tr>
    </tbody>
  </table>
</div>
```

Empty state:
```html
<div class="glass-panel p-12 text-center border border-[var(--color-cosmic-border)]">
  <p class="text-[var(--color-text-muted)] text-lg mb-4">No {items} match your filters</p>
  <button class="px-6 py-3 bg-[var(--color-cosmic-cyan)] text-[var(--color-deep-space)] rounded-lg font-semibold hover:brightness-110 transition-all">Create your first {item}</button>
</div>
```

Key patterns to enforce:
- thead: `bg-[rgba(255,255,255,0.02)] text-xs font-medium uppercase tracking-wider text-[var(--color-text-muted)] px-4 py-3 text-left`
- td: `px-4 py-3 text-sm text-[var(--color-text-main)]`
- tbody: `divide-y divide-[var(--color-cosmic-border)]`
- row hover: `hover:bg-[rgba(255,255,255,0.02)] transition-colors`
- empty state: `glass-panel p-12 text-center` with consistent messaging
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and unify DashboardPage tables (server overview table, recent activity)</name>
  <files>app/src/pages/dashboard/DashboardPage.jsx</files>

  <read_first>app/src/pages/dashboard/DashboardPage.jsx (full file — 488 lines, focus on table sections)</read_first>

  <action>
    Read the full DashboardPage.jsx. Identify all `<table>` elements and their surrounding structure. Apply the standard table pattern from `<interfaces>` to each table:

    1. **Table wrapper:** Ensure every table is wrapped in `glass-panel overflow-hidden border border-[var(--color-cosmic-border)]`
    2. **Table element:** `w-full`
    3. **thead:** `bg-[rgba(255,255,255,0.02)]` on the `<thead>` element, not on individual `<th>` if already consistent
    4. **th:** `px-4 py-3 text-left text-xs font-medium text-[var(--color-text-muted)] uppercase tracking-wider`
    5. **tbody:** `divide-y divide-[var(--color-cosmic-border)]`
    6. **tr:** `hover:bg-[rgba(255,255,255,0.02)] transition-colors`
    7. **td:** `px-4 py-3 text-sm text-[var(--color-text-main)]`

    Also find any empty state sections and ensure they follow the standard empty state pattern.

    DashboardPage already follows the cosmic theme in many areas. Only adjust table-specific classes that deviate from the standard pattern.
  </action>

  <verify>
    <automated>
      cd /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app && \
      npm run build 2>&1 && \
      echo "=== Table pattern audit ===" && \
      grep -c 'text-xs font-medium uppercase tracking-wider' app/src/pages/dashboard/DashboardPage.jsx
    </automated>
  </verify>

  <acceptance_criteria>
    - DashboardPage tables use `text-xs font-medium uppercase tracking-wider` on th elements
    - Table rows use `hover:bg-[rgba(255,255,255,0.02)] transition-colors`
    - tbody uses `divide-y divide-[var(--color-cosmic-border)]`
    - Empty states follow standard pattern if present
    - npm run build exits with code 0
  </acceptance_criteria>

  <done>
    DashboardPage tables unified to standard pattern: consistent thead, row height, hover state, and empty state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Unify Nodes.jsx table and Alerts.jsx table</name>
  <files>app/src/pages/Nodes.jsx, app/src/pages/Alerts.jsx</files>

  <read_first>app/src/pages/Nodes.jsx (full file — 900 lines, search for <table> sections), app/src/pages/Alerts.jsx (full file — 153 lines)</read_first>

  <action>
    Read both files. Identify all `<table>` elements and apply the standard table pattern.

    **A. `app/src/pages/Nodes.jsx` (900 lines):**
    - This is a large page. Search for `<table>` elements and their surrounding structure.
    - The node table may have custom styling from the original implementation.
    - Apply the standard pattern from `<interfaces>`:
      - Wrap in `glass-panel overflow-hidden border border-[var(--color-cosmic-border)]`
      - thead: `bg-[rgba(255,255,255,0.02)]`
      - th: `px-4 py-3 text-left text-xs font-medium text-[var(--color-text-muted)] uppercase tracking-wider`
      - tbody: `divide-y divide-[var(--color-cosmic-border)]`
      - tr: `hover:bg-[rgba(255,255,255,0.02)] transition-colors`
      - td: `px-4 py-3 text-sm text-[var(--color-text-main)]`
    - Also check if Nodes.jsx has empty state sections — apply standard empty state pattern.
    - IMPORTANT: Do NOT restructure the component logic, only update CSS classes.

    **B. `app/src/pages/Alerts.jsx` (153 lines):**
    - Smaller file, likely has an alert rules table and maybe an alert history table.
    - Apply the same standard table pattern.
    - Apply standard empty state pattern for any "no alerts" state.
  </action>

  <verify>
    <automated>
      cd /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app && \
      npm run build 2>&1 && \
      echo "=== Nodes table audit ===" && \
      grep -c 'text-xs font-medium uppercase tracking-wider' app/src/pages/Nodes.jsx && \
      echo "=== Alerts table audit ===" && \
      grep -c 'text-xs font-medium uppercase tracking-wider' app/src/pages/Alerts.jsx
    </automated>
  </verify>

  <acceptance_criteria>
    - Nodes.jsx tables use standard thead pattern
    - Alerts.jsx tables use standard thead pattern
    - Both pages use consistent row styling (hover, padding, text size)
    - Both pages use standard empty state pattern if applicable
    - npm run build exits with code 0
  </acceptance_criteria>

  <done>
    Nodes.jsx and Alerts.jsx tables unified to standard pattern.
  </done>
</task>

<task type="auto">
  <name>Task 3: Unify BillingPage.jsx payment history table</name>
  <files>app/src/pages/billing/BillingPage.jsx</files>

  <read_first>app/src/pages/billing/BillingPage.jsx (full file — 532 lines, focus on table sections and empty states)</read_first>

  <action>
    Read the full BillingPage.jsx. Identify the payment history table (and any other `<table>` elements). Apply the standard table pattern:

    1. **Table wrapper:** `glass-panel overflow-hidden border border-[var(--color-cosmic-border)]`
    2. **table:** `w-full`
    3. **thead:** `bg-[rgba(255,255,255,0.02)]`
    4. **th:** `px-4 py-3 text-left text-xs font-medium text-[var(--color-text-muted)] uppercase tracking-wider`
    5. **tbody:** `divide-y divide-[var(--color-cosmic-border)]`
    6. **tr:** `hover:bg-[rgba(255,255,255,0.02)] transition-colors`
    7. **td:** `px-4 py-3 text-sm text-[var(--color-text-main)]`
    8. **Empty state:** Standard pattern if applicable

    BillingPage was restyled in Phase 79 and likely already uses cosmic patterns. Only adjust table-specific classes.
  </action>

  <verify>
    <automated>
      cd /home/rhnbztnl/Downloads/Berguna/Projects/escluse/app && \
      npm run build 2>&1 && \
      grep -c 'text-xs font-medium uppercase tracking-wider' app/src/pages/billing/BillingPage.jsx
    </automated>
  </verify>

  <acceptance_criteria>
    - BillingPage payment history table uses standard thead pattern
    - Table rows use standard hover and padding
    - Empty state follows standard pattern if applicable
    - npm run build exits with code 0
  </acceptance_criteria>

  <done>
    BillingPage payment history table unified to standard pattern.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. All changes are Tailwind class string replacements in JSX — no logic changes, no new endpoints, no user input handling changed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-84-03 | S (Spoofing) | All table components | accept | CSS class string replacements only — no functionality changes. |
</threat_model>

<verification>
### Plan-Level Verification

1. `npm run build` passes with exit code 0
2. Full-app table pattern audit:
   ```bash
   grep -rn 'text-xs font-medium uppercase tracking-wider' app/src/pages/ --include='*.jsx' | wc -l
   ```
   Expected: at least 4 matches (one per page with tables)
3. Audit for remaining non-standard table patterns:
   ```bash
   grep -rn 'thead\|<table' app/src/pages/ --include='*.jsx' | grep -v 'node_modules'
   ```
4. Manual: navigate to Dashboard, Nodes, Billing, Alerts — verify tables look consistent
</verification>

<success_criteria>
- [ ] Build passes (`npm run build`)
- [ ] DashboardPage: tables use standard thead (`text-xs font-medium uppercase tracking-wider`), row hover, divide-y
- [ ] Nodes.jsx: tables use standard thead, row hover, divide-y
- [ ] BillingPage.jsx: tables use standard thead, row hover, divide-y
- [ ] Alerts.jsx: tables use standard thead, row hover, divide-y
- [ ] Empty states use `glass-panel p-12 text-center` with consistent messaging
- [ ] All tables wrapped in `glass-panel overflow-hidden border border-[var(--color-cosmic-border)]`
</success_criteria>

<output>
After completion, create `.planning/phases/84-perbaiki-layout-yang-janggal-ataupun-tidak-bagus-di-app-eslu/84-03-SUMMARY.md`
</output>
