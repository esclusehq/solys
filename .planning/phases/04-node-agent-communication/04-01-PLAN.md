---
phase: 04-node-agent-communication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Node agents can connect to API via WebSocket using JSON protocol"
    - "API key authentication validates nodes at connection time"
    - "Task distribution uses Redis queue for async processing"
    - "Reconnection handling has proper state management"
  artifacts:
    - path: "api/src/presentation/ws/node_protocol.rs"
      provides: "JSON message protocol with type tag"
    - path: "api/src/presentation/handlers/node_ws_handler.rs"
      provides: "WebSocket handler with API key validation"
    - path: "api/src/infrastructure/cache/queue.rs"
      provides: "Redis job queue implementation"
    - path: "web-agent/src/agent_connection.rs"
      provides: "Agent WebSocket client with reconnection"
  key_links:
    - from: "node_ws_handler.rs"
      to: "node_protocol.rs"
      via: "serde deserialization"
    - from: "node_connection_manager.rs"
      to: "node_ws_handler.rs"
      via: "mpsc channel for responses"
    - from: "agent_connection.rs"
      to: "node_ws_handler.rs"
      via: "WebSocket connection"
---

<objective>
Verify existing Node Agent Communication infrastructure

Purpose: Confirm all 4 key decisions (D-12 to D-15) are properly implemented in the codebase.

Output: Verification report confirming implementation matches decisions
</objective>

<context>
@api/src/presentation/ws/node_protocol.rs
@api/src/presentation/handlers/node_ws_handler.rs
@api/src/presentation/ws/node_connection_manager.rs
@api/src/infrastructure/cache/queue.rs
@web-agent/src/agent_connection.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify JSON Protocol (D-12)</name>
  <files></files>
  <action>
Confirm node_protocol.rs uses JSON + ping/pong:

1. Verify `#[serde(tag = "type")]` enables JSON message routing
2. Verify NodeMessage enum has all required message types
3. Confirm both API and agent handle ping/pong for keepalive
4. Check agent_connection.rs handles Message::Ping (line 312-313)
</action>
  <verify>
<automated>grep -n "serde(tag" api/src/presentation/ws/node_protocol.rs && grep -n "Message::Ping" web-agent/src/agent_connection.rs</automated>
  </verify>
  <done>JSON protocol with serde tag confirmed, ping/pong handling confirmed</done>
</task>

<task type="auto">
  <name>Task 2: Verify API Key Authentication (D-13)</name>
  <files></files>
  <action>
Confirm node_ws_handler.rs implements API key authentication:

1. Verify WsQueryParams extracts api_key from query params
2. Verify hash_api_key uses SHA256
3. Verify node_api_key_repository validates key
4. Confirm authenticated_node_id passed to handler
</action>
  <verify>
<automated>grep -n "api_key\|hash_api_key\|find_by_hash" api/src/presentation/handlers/node_ws_handler.rs</automated>
  </verify>
  <done>API key validation with SHA256 hashing confirmed</done>
</task>

<task type="auto">
  <name>Task 3: Verify Redis Queue (D-14)</name>
  <files></files>
  <action>
Confirm queue.rs implements Redis-based task distribution:

1. Verify JobQueue struct with enqueue/dequeue methods
2. Verify priority-based job ordering (High/Normal/Low)
3. Confirm job locking mechanism for workers
4. Verify job expiration (86400s)
</action>
  <verify>
<automated>grep -n "impl JobQueue\|async fn enqueue\|async fn dequeue" api/src/infrastructure/cache/queue.rs</automated>
  </verify>
  <done>Redis queue with priority and locking confirmed</done>
</task>

<task type="auto">
  <name>Task 4: Verify Reconnection Handling (D-15)</name>
  <files></files>
  <action>
Confirm agent_connection.rs has proper reconnection handling:

1. Verify reconnect_initial_secs and reconnect_max_secs config
2. Verify exponential backoff with multiplier
3. Confirm shutdown signal checked before and after sleep
4. Check for potential race conditions in connection state
</action>
  <verify>
<automated>grep -n "reconnect\|initial_delay\|shutdown" web-agent/src/agent_connection.rs | head -20</automated>
  </verify>
  <done>Reconnection with exponential backoff and shutdown handling confirmed</done>
</task>

</tasks>

<verification>
All 4 decision areas verified:
- D-12: JSON protocol with serde tag + ping/pong ✓
- D-13: API key auth with SHA256 hashing ✓
- D-14: Redis queue with priority levels ✓
- D-15: Reconnection with exponential backoff ✓
</verification>

<success_criteria>
1. JSON + ping/pong protocol confirmed (D-12)
2. API key authentication confirmed (D-13)
3. Redis task queue confirmed (D-14)
4. Reconnection handling confirmed (D-15)
</success_criteria>

<output>
After completion, create `.planning/phases/04-node-agent-communication/04-01-SUMMARY.md`
</output>