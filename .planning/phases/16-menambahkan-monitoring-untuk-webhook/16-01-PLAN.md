---
phase: 16-menambahkan-monitoring-untuk-webhook
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/domain/webhook/service.rs
  - api/src/domain/webhook/repository.rs
  - api/src/presentation/handlers/webhook_handlers.rs
  - app/src/pages/SettingsPage.tsx
  - app/src/api/webhooksApi.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - User can view list of webhooks with delivery status (failure_count, last_delivery_at, last_failure_at)
    - User can manually trigger test webhook
    - User can retry failed webhook delivery
    - Webhook data displayed in frontend Settings page
  artifacts:
    - path: api/src/domain/webhook/service.rs
      provides: retry_webhook method
    - path: api/src/presentation/handlers/webhook_handlers.rs
      provides: POST /webhooks/{id}/retry endpoint
    - path: app/src/api/webhooksApi.ts
      provides: retryWebhook, testWebhook API methods  
    - path: app/src/pages/SettingsPage.tsx
      provides: Webhook management UI with status display
  key_links:
    - from: app/src/api/webhooksApi.ts
      to: api/src/presentation/handlers/webhook_handlers.rs
      via: HTTP POST /api/v1/webhooks/{id}/retry
---

<objective>
Add webhook monitoring capabilities - display delivery status in frontend, enable manual retry and test functionality.

Purpose: Users can see webhook health status and manually retry failed deliveries.
Output: Frontend webhook management in Settings page with status display, test, and retry buttons.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@api/src/domain/webhook/model.rs
@api/src/domain/webhook/service.rs
@api/src/domain/webhook/sqlx_repository.rs
@api/src/presentation/handlers/webhook_handlers.rs

# Webhook model has these monitoring fields:
# - failure_count: i32
# - last_delivery_at: Option<DateTime<Utc>>
# - last_failure_at: Option<DateTime<Utc>>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add retry endpoint to backend</name>
  <files>api/src/domain/webhook/service.rs, api/src/presentation/handlers/webhook_handlers.rs</files>
  <action>
Add `retry_webhook` method to WebhookService that re-delivers the last event. Add POST /webhooks/{id}/retry handler in webhook_handlers.rs. The handler should call WebhookService::retry_webhook which calls deliver() method again. After successful retry, reset failure_count to 0 and update last_delivery_at.
  </action>
  <verify>
<automATED>curl -X POST /api/v1/webhooks/{id}/retry -H "Authorization: Bearer $TOKEN" returns 200 with success status</automATED>
  </verify>
  <done>POST /api/v1/webhooks/{id}/retry returns 200, failure_count reset to 0</done>
</task>

<task type="auto">
  <name>Task 2: Add webhooksApi client methods</name>
  <files>app/src/api/webhooksApi.ts</files>
  <action>
Create app/src/api/webhooksApi.ts with methods: listWebhooks(), getWebhook(id), createWebhook(data), updateWebhook(id, data), deleteWebhook(id), testWebhook(id), retryWebhook(id). Use existing api.js pattern with fetch.
  </action>
  <verify>
<automATED>File exists with all 7 export methods</automATED>
  </verify>
  <done>webhooksApi.ts created with all required methods</done>
</task>

<task type="auto">
  <name>Task 3: Add webhook management UI to SettingsPage</name>
  <files>app/src/pages/SettingsPage.tsx</files>
  <action>
Add WebhookManagement component to SettingsPage.tsx. Display list of user's webhooks with: name, url (truncated), failure_count badge (red if >0), last_delivery_at, last_failure_at. Add "Test" button that calls testWebhook(id). Add "Retry" button that calls retryWebhook(id) - show only if failure_count > 0. Use existing card styles from SettingsPage.

Key UI elements:
- Card panel for each webhook
- Status badge: green checkmark if failure_count=0, red X with count if failure_count>0  
- Test button (always visible)
- Retry button (visible when failure_count > 0)
- Timestamps formatted as relative time (e.g., "2 hours ago")
  </action>
  <verify>
<automATED>npm run build passes without errors</automATED>
  </verify>
  <done>SettingsPage shows webhook list with status, test, and retry buttons</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify webhook monitoring UI</name>
  <files>app/src/pages/SettingsPage.tsx</files>
  <action>Verify webhook monitoring UI in Settings page</action>
  <verify>
    <automATED>Manual verification required: navigate to /settings</automATED>
  </verify>
  <done>Webhook monitoring UI displays correctly with status badges, test and retry buttons functional</done>
  <how-to-verify>
    1. Navigate to /settings in browser
    2. Create a webhook via API or check existing webhooks
    3. Verify webhook list displays with status badges
    4. Click "Test" button and verify success toast
    5. If any webhook has failures, click "Retry" button
    6. Verify retry succeeds and failure_count resets
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | User webhooks accept HTTP requests from trusted users only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | R | webhook_handlers.rs | accept | Retry endpoint only operates on user's own webhooks (checked via user_id) |
| T-16-02 | D | webhooksApi.ts | mitigate | Validate webhook URL before display (prevent XSS in truncated URL) |
</threat_model>

<verification>
- Backend: POST /api/v1/webhooks/{id}/retry returns 200
- Backend: failure_count resets to 0 after successful retry
- Frontend: Settings page renders webhook list without errors
- Frontend: Test button triggers API call successfully
</verification>

<success_criteria>
User can:
- See list of webhooks with failure_count and timestamps in Settings page
- Click Test to verify webhook delivery works
- Click Retry to re-deliver failed webhook
- See failure_count reset to 0 after successful retry
</success_criteria>

<output>
After completion, create `.planning/phases/16-menambahkan-monitoring-untuk-webhook/16-01-SUMMARY.md`
</output>