---
phase: 44-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-core/crates/agent-config/src/loader.rs
  - agent-core/crates/agent-config/src/schema.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "api_key can be loaded from config.toml file"
    - "Env vars override TOML config values"
    - "Registration works with api_key from config.toml"
  artifacts:
    - path: "agent-core/crates/agent-config/src/loader.rs"
      provides: "Config loading from TOML and env vars"
      contains: "api_key.*SecretString"
    - path: "agent-core/crates/agent-config/src/schema.rs"
      provides: "Config schema with api_key field"
      contains: "pub api_key: SecretString"
  key_links:
    - from: "loader.rs - load_toml_config"
      to: "schema.rs - SecretString"
      via: "serde deserialization"
      pattern: "api_key.*SecretString"
---

<objective>
Add config.toml support for api_key, enabling agents to use file-based authentication instead of only .env files.

Purpose: Complete the config.toml integration started in Phase 39 by adding api_key loading from TOML files.
Output: Working config.toml with api_key field; agents can authenticate using file-based credentials.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-authentication/44-CONTEXT.md
@agent-core/crates/agent-config/src/loader.rs
@agent-core/crates/agent-config/src/schema.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add api_key to TOML config loader</name>
  <files>agent-core/crates/agent-config/src/loader.rs</files>
  <read_first>agent-core/crates/agent-config/src/loader.rs</read_first>
  <behavior>
    - Test 1: config.toml with api_key="test-key" loads SecretString("test-key")
    - Test 2: ESCLUSE_AGENT_API_KEY env var overrides api_key in TOML
    - Test 3: Empty api_key in TOML works (default empty SecretString)
  </behavior>
  <action>
Add api_key loading to load_toml_config function in loader.rs. Add this code after the [server] section parsing:

```rust
// [server] - api_key (D-01: primary source from config.toml)
if let Some(v) = toml_map.get("server").and_then(|t| t.get("api_key")) {
    if let Some(s) = v.as_str() {
        if !s.is_empty() {
            config.api_key = SecretString::new(s.to_string());
        }
    }
}
```

The api_key should be placed in the [server] section per D-01, following the same pattern as backend_url.
  </action>
  <verify>
    <automated>cargo test --package agent-config -- --test-threads=1 test_load_from_env 2>&1 | head -20</automated>
  </verify>
  <done>api_key loads from TOML file, ESCLUSE_AGENT_API_KEY overrides TOML value</done>
</task>

<task type="auto">
  <name>Task 2: Verify config loading order is correct</name>
  <files>agent-core/crates/agent-config/src/loader.rs</files>
  <read_first>agent-core/crates/agent-config/src/loader.rs</read_first>
  <action>
Verify the config loading order in load() function matches the decisions:

1. TOML file loads first (D-01: config.toml primary)
2. Env vars with ESCLUSE_AGENT_* prefix override TOML (D-02: env override)

The current order in loader.rs is:
1. load_toml_config - loads TOML
2. Old-style AGENT_* env vars 
3. Legacy .env file
4. ESCLUSE_AGENT_* env overrides (D-02)

This is correct. Add a comment documenting this order for maintainability.
  </action>
  <verify>
    <automated>grep -n "ESCLUSE_AGENT_" agent-core/crates/agent-config/src/loader.rs | head -5</automated>
  </verify>
  <done>Config loading order documented and correct</done>
</task>

<task type="auto">
  <name>Task 3: Verify integration with agent_connection</name>
  <files>web-agent/src/agent_connection.rs</files>
  <read_first>web-agent/src/agent_connection.rs</read_first>
  <action>
Check that agent_connection.rs uses config.api_key when connecting. The WebSocket connection in agent_connection.rs sends a Register message but currently doesn't include api_key in the handshake.

Verify that the API expects api_key validation at connection time or trusts the WebSocket connection. Per D-04: No additional api_key validation during handshake (trust connection).

This means the backend validates the agent at WebSocket level via the connection itself, not via explicit api_key in the Register message.
  </action>
  <verify>
    <automated>grep -n "api_key\|config\." web-agent/src/agent_connection.rs | head -10</automated>
  </verify>
  <done>Auth flow verified - register handshake trusts WebSocket connection</done>
</task>

</tasks>

<verification>
Build and test the configuration changes:

1. cargo build --package agent-config
2. Verify TOML parsing includes api_key field
3. Verify env override precedence
</verification>

<success_criteria>
- [ ] api_key loads from config.toml file
- [ ] ESCLUSE_AGENT_API_KEY env var overrides TOML value  
- [ ] Config loading order is correct (TOML → env override)
- [ ] Register handshake works without additional api_key validation
</success_criteria>

<output>
After completion, create `.planning/phases/44-authentication/44-01-SUMMARY.md`
</output>