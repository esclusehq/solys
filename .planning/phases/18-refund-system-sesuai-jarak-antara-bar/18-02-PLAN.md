---
phase: 18-refund-system-sesuai-jarak-antara-bar
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/pages/Billing.jsx
  - app/src/api/client.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can see refund eligibility on billing page"
    - "User can request refund with one click"
    - "User can see refund history"
  artifacts:
    - path: app/src/pages/Billing.jsx
      provides: Refund button and eligibility display
      contains: refund

---

<objective>
Add frontend refund UI: eligibility display, refund request button, and refund history on billing page. Backend (18-01) already provides the API endpoints.
</objective>

<context>
@.planning/phases/18-refund-system-sesuai-jarak-antara-bar/18-RESEARCH.md
@app/src/pages/Billing.jsx
@app/src/api/client.js

Backend already implemented:
- POST /api/v1/billing/refund - request refund
- GET /api/v1/billing/refunds - list user's refunds
- GET /api/v1/billing/refund/eligibility - check eligibility
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add refund API methods</name>
  <files>app/src/api/client.js</files>
  <action>
Add to billingApi:
1. getRefundEligibility() - GET /api/v1/billing/refund/eligibility
2. requestRefund(subscriptionId, reason) - POST /api/v1/billing/refund
3. getRefunds() - GET /api/v1/billing/refunds

Example:
```javascript
async getRefundEligibility(subscriptionId) {
  const { data } = await axios.get(`/billing/refund/eligibility?subscription_id=${subscriptionId}`);
  return data;
}
```
  </action>
  <verify>
grep -n "getRefundEligibility\|requestRefund\|getRefunds" app/src/api/client.js
  </verify>
  <done>Refund API methods added to client.js</done>
</task>

<task type="auto">
  <name>Task 2: Add refund eligibility display to Billing page</name>
  <files>app/src/pages/Billing.jsx</files>
  <action>
In Billing.jsx:
1. Import billingApi.getRefundEligibility
2. Add state for eligibility data
3. Add eligibility display section showing:
   - Full refund (within 7 days) 🟢
   - Prorated (7-30 days) 🟡
   - No refund (after 30 days) 🔴
4. Show "Request Refund" button when eligible
  </action>
  <verify>
grep -n "refundEligibility\|Request Refund" app/src/pages/Billing.jsx
  </verify>
  <done>Eligibility displayed on billing page</done>
</task>

<task type="auto">
  <name>Task 3: Add refund history section</name>
  <files>app/src/pages/Billing.jsx</files>
  <action>
Add refund history section to Billing page:
1. Import billingApi.getRefunds
2. Fetch and display list of past refunds
3. Show status: pending, processed, rejected
4. Show amount and date for each
  </action>
  <verify>
grep -n "refundHistory\|Refund History" app/src/pages/Billing.jsx
  </verify>
  <done>Refund history shown on billing page</done>
</task>

</tasks>

<verification>
Automated checks:
- [ ] client.js has getRefundEligibility, requestRefund, getRefunds
- [ ] Billing.jsx shows eligibility with color coding
- [ ] Billing.jsx has refund history section
Manual: Visit billing page, verify refund UI
</verification>

<success_criteria>
[ ]Eligibility shown with color (🟢🟡🔴)
[ ]Request Refund button appears when eligible
[ ]Refund history displays past requests
</success_criteria>

<output>
After completion, update 18-02-SUMMARY.md