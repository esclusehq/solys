---
phase: 23-menambahkan-tombol-toggle-theme-light-dan-dark
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified:
  - app/src/index.css
  - app/src/app/App.jsx
autonomous: true
requirements: []
must_haves:
  truths:
    - 'User can toggle between light and dark theme'
    - 'Theme preference persists across page refreshes'
    - 'Button icon changes based on current theme'
  artifacts:
    - path: app/src/index.css
      provides: Light theme CSS variables
      min_lines: 20
    - path: app/src/app/App.jsx
      provides: Theme toggle button in header
      contains: setTheme
  key_links:
    - from: app/src/app/App.jsx
      to: app/src/store/uiStore.js
      via: useUIStore hook
      pattern: useUIStore.*setTheme
---

<objective>
Add theme toggle button (light/dark) to switch between themes. Currently the theme is hardcoded to 'dark' in uiStore but no toggle mechanism exists.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-menambahkan-tombol-toggle-theme-light-dan-dark/23-CONTEXT.md
@app/src/store/uiStore.js
@app/src/index.css
@app/src/app/App.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add light theme CSS variables</name>
  <files>app/src/index.css</files>
  <action>
Add light theme CSS variables using Tailwind v4 @theme syntax. Add after the existing dark theme variables (around line 19). Use inverted colors - light background (#ffffff), dark text (#1a1a1a), etc.
  </action>
  <verify>
File contains [data-theme="light"] block with --color-deep-space: #f8fafc (slate-50), --color-nebula: #ffffff, --color-cosmic-card: rgba(0,0,0,0.03), --color-cosmic-border: rgba(0,0,0,0.08), --color-text-main: #1e293b (slate-800), --color-text-muted: #64748b (unchanged)
  </verify>
  <done>Light theme CSS variables added to index.css</done>
</task>

<task type="auto">
  <name>Task 2: Add theme toggle button to header</name>
  <files>app/src/app/App.jsx</files>
  <action>
1. Import theme and setTheme from useUIStore (line 3 already has import, add theme, setTheme to destructured imports)
2. Add useEffect to apply theme to document root (after imports, before return):
   useEffect(() => { document.documentElement.setAttribute('data-theme', theme) }, [theme])
3. Add toggle button in header (line 75-83, after NotificationCenter):
   <button onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')} className="text-gray-400 hover:text-white text-xl">
     {theme === 'dark' ? '☀️' : '🌙'}
   </button>
  </action>
  <verify>
grep -n "setTheme\|data-theme" app/src/app/App.jsx
  </verify>
  <done>Theme toggle button appears in header, clicking toggles theme and icon updates</done>
</task>

</tasks>

<verification>
Test by running app, observe toggle button after login, click to toggle, verify colors change. Theme persists on page refresh.
</verification>

<success_criteria>
1. Toggle button visible in header top-right
2. Click toggles between ☀️ and 🌙 icons
3. Light theme colors apply when toggled
4. Theme persists via zustand (check uiStore.js - uses persist middleware)
</success_criteria>

<output>
After completion, create `.planning/phases/23-menambahkan-tombol-toggle-theme-light-dan-dark/23-01-SUMMARY.md`
</output>