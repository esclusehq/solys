---
phase: 30-pakai-agent-executor-untuk-mengambil-metrics-dengan-benar
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/infrastructure/executors/agent_server_executor.rs
  - api/src/application/services/monitoring_service.rs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Server metrics (CPU, memory, disk) are collected via AgentServerExecutor from WebSocket heartbeat cache"
    - "When node is disconnected, frontend shows 'not connected to agent' instead of zeros"
    - "Servers with executor_type='agent' use AgentServerExecutor.collect_metrics()"
  artifacts:
    - path: "api/src/infrastructure/executors/agent_server_executor.rs"
      provides: "AgentServerExecutor with collect_metrics() using heartbeat cache"
      exports: ["collect_metrics", "check_status"]
    - path: "api/src/application/services/monitoring_service.rs"
      provides: "Monitoring service that uses executor for metrics"
      contains: "collect_metrics"
  key_links:
    - from: "MonitoringService"
      to: "AgentServerExecutor.collect_metrics"
      via: "factory.get_executor(server).collect_metrics(server)"
      pattern: "executor.*collect_metrics"
---

<objective>
Enable AgentServerExecutor to collect container metrics (CPU, memory, disk) via WebSocket heartbeat from agent, replacing any existing SSH/RCON-based metrics collection. Show "not connected to agent" when node is disconnected.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-pakai-agent-executor-untuk-mengambil-metrics-dengan-benar/30-CONTEXT.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From api/src/infrastructure/node_client/mod.rs:
```rust
pub struct ContainerInfo {
    pub id: String,
    pub name: String,
    pub image: String,
    pub status: String,
    pub cpu_usage: Option<f32>,
    pub memory_usage_mb: Option<i64>,
    pub memory_limit_mb: Option<i64>,
    pub disk_usage_mb: Option<i64>,
    pub players: Option<i32>,
    pub tps: Option<f32>,
}
```

From api/src/domain/entities/server_metrics.rs:
```rust
pub struct ServerMetrics {
    pub id: Uuid,
    pub server_id: Uuid,
    pub cpu_usage: f32,       // Percentage (0.0 - 100.0)
    pub memory_usage_mb: i64,  // In Megabytes
    pub disk_usage_mb: i64,    // Disk space used by server data directory (in Megabytes)
    pub tps: Option<f64>,
    pub players: Option<i32>,
    pub created_at: DateTime<Utc>,
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify AgentServerExecutor metrics collection uses heartbeat cache</name>
  <files>api/src/infrastructure/executors/agent_server_executor.rs</files>
  <read_first>
    - api/src/infrastructure/executors/agent_server_executor.rs (lines 244-281 for collect_metrics)
    - api/src/infrastructure/node_client/mod.rs (ContainerInfo struct)
    - api/src/presentation/ws/node_connection_manager.rs (heartbeat cache)
  </read_first>
  <action>
    Verify that AgentServerExecutor.collect_metrics():
    1. Calls node_client.get_containers(node_id) to get cached container data from heartbeat
    2. Finds container by name using pattern mc-{server_id} or server.container_name
    3. Extracts cpu_usage, memory_usage_mb, disk_usage_mb, tps, players from ContainerInfo
    4. Returns ServerMetrics with those values

    The current implementation at lines 244-281 already does this correctly. Verify it compiles and works.
  </action>
  <verify>
    <automated>cargo check --package api 2>&1 | head -30</automated>
  </verify>
  <done>AgentServerExecutor.collect_metrics() compiles and uses heartbeat cache correctly</done>
</task>

<task type="auto">
  <name>Task 2: Ensure disconnected node returns "not connected" message per D-03</name>
  <files>api/src/infrastructure/executors/agent_server_executor.rs</files>
  <read_first>
    - api/src/infrastructure/executors/agent_server_executor.rs (lines 25-35 validate_node_connection, lines 244-281 collect_metrics)
    - api/src/application/services/monitoring_service.rs
  </read_first>
  <action>
    Per D-03: "Jika agent/node disconnected, tampilkan 'not connected to agent' - bukan return zeros atau skip"

    Current behavior check:
    1. validate_node_connection() returns error "Node {} is not connected" if disconnected AND no cached containers
    2. collect_metrics() returns error if container not found
    3. check_status() returns "stopped" if not connected

    Ensure MonitoringService handles these errors and propagates "not connected to agent" message to frontend instead of returning zeros or fake data.
  </action>
  <verify>
    <automated>grep -n "not connected" api/src/infrastructure/executors/agent_server_executor.rs api/src/application/services/monitoring_service.rs 2>/dev/null || echo "Need to add error message"</automated>
  </verify>
  <done>Disconnected node returns explicit "not connected to agent" message, not zeros</done>
</task>

<task type="auto">
  <name>Task 3: Verify executor_type 'agent' routes to AgentServerExecutor in factory</name>
  <files>api/src/infrastructure/factories/simple_executor_factory.rs</files>
  <read_first>
    - api/src/infrastructure/factories/simple_executor_factory.rs (lines 53-69 get_executor)
  </read_first>
  <action>
    Verify factory mapping:
    - "agent" → AgentServerExecutor (lines 59-66)
    - Fallback: If no node_client configured, logs warning and falls back to SshServerExecutor

    This is already implemented. No changes needed unless testing shows otherwise.
  </action>
  <verify>
    <automated>grep -A5 '"agent"' api/src/infrastructure/factories/simple_executor_factory.rs</automated>
  </verify>
  <done>Factory correctly routes executor_type='agent' to AgentServerExecutor</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent→API (WebSocket) | Agent sends container metrics via heartbeat; API trusts cached data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Repudiation | Agent heartbeat | accept | Metrics are informational; no financial/security impact |
| T-30-02 | Information Disclosure | Cached container data | mitigate | No sensitive data in metrics; heartbeat is internal |
</threat_model>

<verification>
- [ ] AgentServerExecutor.collect_metrics() compiles without errors
- [ ] Factory correctly creates AgentServerExecutor when executor_type='agent'
- [ ] Disconnected node returns explicit error message, not zeros
</verification>

<success_criteria>
Server metrics are collected via AgentServerExecutor from WebSocket heartbeat cache. When node is disconnected, frontend shows "not connected to agent" message. The implementation uses existing code in agent_server_executor.rs.
</success_criteria>

<output>
After completion, create `.planning/phases/30-pakai-agent-executor-untuk-mengambil-metrics-dengan-benar/30-01-SUMMARY.md`
</output>
