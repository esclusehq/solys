---
status: complete
quick_id: 260613-5a9
description: Add PocketMine-MP and Nukkit as bedrock variants
date: 2026-06-12
commit: 9e7aff6 (parent) + f7051eb (api submodule)
---

## Summary

Added PocketMine-MP and Nukkit as selectable variants for Minecraft Bedrock game type.

### Changes

1. **SQL seed migration** (`api/migrations/20260531_create_templates_table.sql`):
   - Added `pocketmine` and `nukkit` template rows for `game_type='bedrock'`

2. **Rust fallback templates** (`api/src/domain/server/template/model.rs`):
   - Added PocketMine-MP and Nukkit to hardcoded fallback templates

3. **Data migration** (`api/migrations/20260613000003_add_bedrock_variants.sql`):
   - Created and applied to production DB (2 rows inserted)

4. **Backend deployment**:
   - Built and pushed to ECR (`escluse-backend:latest`)
   - Pulled and restarted on main EC2

### No frontend changes needed

The variant dropdown in `CreateServerModal.jsx` already dynamically renders variants from the templates API response - no code changes required.

### Verification

Production DB now has 3 bedrock templates:
- `default` → Minecraft Bedrock
- `pocketmine` → PocketMine-MP
- `nukkit` → Nukkit
