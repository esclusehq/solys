---
status: complete
---

# Quick Task 260611-5m0: Update pricing section on landing page

## Summary
Added scroll-triggered animations and button navigation to the pricing section on the landing page. Previously the pricing buttons had no onClick handlers and the section had no scroll animations unlike other sections.

## Changes Made
- **Pricing header** — wrapped in `motion.div` with `whileInView` scroll animation
- **Free plan card** — wrapped in `motion.div` with `whileInView`, added `onClick` → `/signin`
- **Hobby plan card** — wrapped in `motion.div` with `whileInView`, added `onClick` → `/signin`
- **Pro plan card** — wrapped in `motion.div` with `whileInView`, added `onClick` → `/signin`
- **BYO infra section** — wrapped in `motion.div` with `whileInView`
- Staggered delay animation (0.1s, 0.2s, 0.3s, 0.4s) for visual flow

## Commit
- `a0582ca` in `landing-page-escluse` repo
