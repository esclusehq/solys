# Escluse — Database Schema

Database schema reference for the Esluce platform. This document describes all PostgreSQL tables, columns, relationships, constraints, and indexes organized by business domain. Generated from live database introspection.

![PostgreSQL](https://img.shields.io/badge/postgresql-16-336791)
![Rust](https://img.shields.io/badge/generator-rust-orange)

---

## Table of Contents

- [Backups Tables](#backups-tables)
- [Billing/Subscriptions Tables](#billing-subscriptions-tables)
- [Events/Logs Tables](#events-logs-tables)
- [Games Tables](#games-tables)
- [Infrastructure Tables](#infrastructure-tables)
- [Jobs Tables](#jobs-tables)
- [Nodes Tables](#nodes-tables)
- [Servers Tables](#servers-tables)
- [Settings/Config Tables](#settings-config-tables)
- [Templates Tables](#templates-tables)
- [Usage Tables](#usage-tables)
- [Users/Auth Tables](#users-auth-tables)
- [Webhooks Tables](#webhooks-tables)
- [Migration History](#migration-history)

---

> **Note:** This document is auto-generated by `tools/db-schema-gen/`. Regenerate when the schema changes by running:
> ```bash
> cargo run --manifest-path tools/db-schema-gen/Cargo.toml -- --db-url "$DATABASE_URL"
> ```

## Backups Tables

Backup history records and S3-compatible storage profiles for server backup operations.

```mermaid
erDiagram
    backup_history {
        uuid id PK
        uuid server_id FK
        text file_name
        text provider
        text storage_path
        integer size_bytes
        text status
        text error_message
        timestamptz created_at
        timestamptz completed_at
    }
    servers || backup_history || : references
```

### `backup_history`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT uuid_generate_v4() |  |
| `server_id` | `uuid` | FK → servers.id, NOT NULL | References the server resource |
| `file_name` | `text` | NOT NULL |  |
| `provider` | `text` | NOT NULL, DEFAULT 'local'::text |  |
| `storage_path` | `text` | NULLABLE |  |
| `size_bytes` | `integer` | NOT NULL, DEFAULT 0 |  |
| `status` | `text` | NOT NULL, DEFAULT 'in_progress'::text |  |
| `error_message` | `text` | NULLABLE |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `completed_at` | `timestamptz` | NULLABLE |  |

## Billing/Subscriptions Tables

Billing plans, user subscriptions, payment processing, invoicing, and refund management. Integrates with Stripe/LemonSqueezy.

```mermaid
erDiagram
    plans {
        uuid id PK
        text name
        text display_name
        text description
        real price_monthly
        real price_yearly
        text stripe_price_id_monthly
        text stripe_price_id_yearly
        jsonb limits
        jsonb features
        bool is_active
        integer sort_order
        timestamptz created_at
        timestamptz updated_at
        text lemon_squeezy_variant_id_monthly
        text lemon_squeezy_variant_id_yearly
    }
    subscriptions {
        uuid id PK
        uuid user_id FK
        uuid plan_id FK
        text status
        text billing_provider
        text provider_subscription_id
        timestamptz current_period_start
        timestamptz current_period_end
        bool cancel_at_period_end
        timestamptz cancelled_at
        timestamptz trial_ends_at
        timestamptz created_at
        timestamptz updated_at
    }
    billing_customers {
        uuid id PK
        uuid user_id FK
        text provider
        text provider_customer_id
        timestamptz created_at
        timestamptz updated_at
    }
    payment_transactions {
        uuid id PK
        uuid user_id FK
        uuid subscription_id FK
        text provider
        text provider_transaction_id
        real amount
        text currency
        text status
        timestamptz created_at
        jsonb metadata
    }
    invoices {
        uuid id PK
        uuid user_id FK
        uuid subscription_id FK
        text invoice_number
        real amount
        text currency
        text status
        timestamptz period_start
        timestamptz period_end
        text pdf_url
        timestamptz created_at
    }
    refunds {
        uuid id PK
        uuid user_id FK
        uuid subscription_id FK
        integer amount_cents
        text refund_type
        text status
        text reason
        timestamptz processed_at
        timestamptz created_at
        timestamptz updated_at
    }
    users || subscriptions || : references
    plans || subscriptions || : references
    users || billing_customers || : references
    users || payment_transactions || : references
    subscriptions || payment_transactions o{ : references
    users || invoices || : references
    subscriptions || invoices o{ : references
    users || refunds || : references
    subscriptions || refunds || : references
```

### `billing_customers`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `user_id` | `uuid` | FK → users.id, NOT NULL | References the user resource |
| `provider` | `text` | NOT NULL |  |
| `provider_customer_id` | `text` | NOT NULL | References the provider_customer resource |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamptz` | NOT NULL, DEFAULT now() | Last modification timestamp |

### `invoices`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `user_id` | `uuid` | FK → users.id, NOT NULL | References the user resource |
| `subscription_id` | `uuid` | FK → subscriptions.id, NULLABLE | References the subscription resource |
| `invoice_number` | `text` | NOT NULL |  |
| `amount` | `real` | NOT NULL |  |
| `currency` | `text` | NOT NULL, DEFAULT 'USD'::character varying |  |
| `status` | `text` | NOT NULL |  |
| `period_start` | `timestamptz` | NOT NULL |  |
| `period_end` | `timestamptz` | NOT NULL |  |
| `pdf_url` | `text` | NULLABLE |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |

### `payment_transactions`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `user_id` | `uuid` | FK → users.id, NOT NULL | References the user resource |
| `subscription_id` | `uuid` | FK → subscriptions.id, NULLABLE | References the subscription resource |
| `provider` | `text` | NOT NULL |  |
| `provider_transaction_id` | `text` | NOT NULL | References the provider_transaction resource |
| `amount` | `real` | NOT NULL |  |
| `currency` | `text` | NOT NULL, DEFAULT 'USD'::character varying |  |
| `status` | `text` | NOT NULL |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `metadata` | `jsonb` | NULLABLE, DEFAULT '{}'::jsonb |  |

### `plans`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `name` | `text` | NOT NULL |  |
| `display_name` | `text` | NOT NULL |  |
| `description` | `text` | NULLABLE |  |
| `price_monthly` | `real` | NOT NULL |  |
| `price_yearly` | `real` | NULLABLE |  |
| `stripe_price_id_monthly` | `text` | NULLABLE |  |
| `stripe_price_id_yearly` | `text` | NULLABLE |  |
| `limits` | `jsonb` | NOT NULL, DEFAULT '{}'::jsonb |  |
| `features` | `jsonb` | NULLABLE, DEFAULT '[]'::jsonb |  |
| `is_active` | `bool` | NOT NULL, DEFAULT true |  |
| `sort_order` | `integer` | NOT NULL, DEFAULT 0 |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamptz` | NOT NULL, DEFAULT now() | Last modification timestamp |
| `lemon_squeezy_variant_id_monthly` | `text` | NULLABLE |  |
| `lemon_squeezy_variant_id_yearly` | `text` | NULLABLE |  |

### `refunds`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `user_id` | `uuid` | FK → users.id, NOT NULL | References the user resource |
| `subscription_id` | `uuid` | FK → subscriptions.id, NOT NULL | References the subscription resource |
| `amount_cents` | `integer` | NOT NULL |  |
| `refund_type` | `text` | NOT NULL |  |
| `status` | `text` | NOT NULL, DEFAULT 'pending'::character varying |  |
| `reason` | `text` | NULLABLE |  |
| `processed_at` | `timestamptz` | NULLABLE |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamptz` | NOT NULL, DEFAULT now() | Last modification timestamp |

### `subscriptions`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `user_id` | `uuid` | FK → users.id, NOT NULL | References the user resource |
| `plan_id` | `uuid` | FK → plans.id, NOT NULL | References the plan resource |
| `status` | `text` | NOT NULL, DEFAULT 'active'::character varying |  |
| `billing_provider` | `text` | NULLABLE |  |
| `provider_subscription_id` | `text` | NULLABLE | References the provider_subscription resource |
| `current_period_start` | `timestamptz` | NOT NULL |  |
| `current_period_end` | `timestamptz` | NOT NULL |  |
| `cancel_at_period_end` | `bool` | NOT NULL, DEFAULT false |  |
| `cancelled_at` | `timestamptz` | NULLABLE |  |
| `trial_ends_at` | `timestamptz` | NULLABLE |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamptz` | NOT NULL, DEFAULT now() | Last modification timestamp |

## Events/Logs Tables

Audit trail for security events and threshold-based alert rules for server and node monitoring.

```mermaid
erDiagram
    alert_rules {
        uuid id PK
        uuid server_id FK
        text metric_type
        text operator
        real threshold
        bool enabled
        timestamptz created_at
        integer duration_seconds
    }
    alert_states {
        uuid id PK
        uuid rule_id FK
        uuid server_id FK
        text status
        timestamptz last_triggered_at
        timestamptz last_recovered_at
        integer violation_count
        timestamptz updated_at
    }
    alert_history {
        uuid id PK
        uuid rule_id FK
        uuid server_id FK
        text event_type
        real metric_value
        timestamptz created_at
    }
    servers || alert_rules || : references
    alert_rules || alert_states || : references
    servers || alert_states || : references
    alert_rules || alert_history || : references
    servers || alert_history || : references
```

### `alert_history`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT uuid_generate_v4() |  |
| `rule_id` | `uuid` | FK → alert_rules.id, NOT NULL | References the rule resource |
| `server_id` | `uuid` | FK → servers.id, NOT NULL | References the server resource |
| `event_type` | `text` | NOT NULL |  |
| `metric_value` | `real` | NOT NULL |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |

### `alert_rules`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT uuid_generate_v4() |  |
| `server_id` | `uuid` | FK → servers.id, NOT NULL | References the server resource |
| `metric_type` | `text` | NOT NULL |  |
| `operator` | `text` | NOT NULL |  |
| `threshold` | `real` | NOT NULL |  |
| `enabled` | `bool` | NOT NULL, DEFAULT true |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `duration_seconds` | `integer` | NOT NULL, DEFAULT 0 |  |

### `alert_states`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT uuid_generate_v4() |  |
| `rule_id` | `uuid` | FK → alert_rules.id, NOT NULL | References the rule resource |
| `server_id` | `uuid` | FK → servers.id, NOT NULL | References the server resource |
| `status` | `text` | NOT NULL |  |
| `last_triggered_at` | `timestamptz` | NULLABLE |  |
| `last_recovered_at` | `timestamptz` | NULLABLE |  |
| `violation_count` | `integer` | NOT NULL, DEFAULT 0 |  |
| `updated_at` | `timestamptz` | NOT NULL, DEFAULT now() | Last modification timestamp |

### `audit_logs`

Immutable audit log table - INSERT only, no UPDATE or DELETE allowed

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `user_id` | `uuid` | FK → users.id, NULLABLE | References the user resource |
| `action` | `text` | NOT NULL |  |
| `resource_type` | `text` | NULLABLE |  |
| `resource_id` | `uuid` | NULLABLE | References the resource resource |
| `ip_address` | `inet` | NULLABLE |  |
| `user_agent` | `text` | NULLABLE |  |
| `metadata` | `jsonb` | NULLABLE, DEFAULT '{}'::jsonb |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `tenant_id` | `uuid` | FK → users.id, NULLABLE | References the tenant resource |
| `old_value` | `jsonb` | NULLABLE |  |
| `new_value` | `jsonb` | NULLABLE |  |
| `actor_id` | `uuid` | FK → users.id, NULLABLE | References the actor resource |
| `severity` | `text` | NULLABLE, DEFAULT 'info'::character varying |  |

## Games Tables

Game type definitions, port pools, resource allocation plans, and deployment configurations.

```mermaid
erDiagram
    game_types {
        uuid id PK
        text identifier
        text display_name
        text description
        text docker_image
        jsonb default_ports
        jsonb default_env
        text startup_command
        jsonb capabilities
        bool is_active
        integer sort_order
        timestamptz created_at
        timestamptz updated_at
    }
    deployment_configs {
        uuid id PK
        text name
        uuid game_type_id FK
        text docker_image
        jsonb ports
        jsonb env_vars
        text startup_command
        jsonb resources
        text volume_path
        text network_name
        bool is_default
        timestamptz created_at
        timestamptz updated_at
    }
    port_pools {
        uuid id PK
        uuid node_id FK
        integer port_range_start
        integer port_range_end
        integer current_port
        jsonb allocated_ports
        text protocol
        bool is_active
        timestamptz created_at
        timestamptz updated_at
    }
    resource_plans {
        uuid id PK
        text name
        text display_name
        integer ram_mb
        integer cpu_cores
        integer disk_gb
        real price_monthly
        bool is_active
        integer sort_order
        timestamptz created_at
        timestamptz updated_at
    }
    game_types || deployment_configs o{ : references
    nodes || port_pools o{ : references
```

### `deployment_configs`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `name` | `text` | NOT NULL |  |
| `game_type_id` | `uuid` | FK → game_types.id, NULLABLE | References the game_type resource |
| `docker_image` | `text` | NULLABLE |  |
| `ports` | `jsonb` | NULLABLE, DEFAULT '{"game": 25565, "rcon": 25575}'::jsonb |  |
| `env_vars` | `jsonb` | NULLABLE, DEFAULT '{}'::jsonb |  |
| `startup_command` | `text` | NULLABLE |  |
| `resources` | `jsonb` | NULLABLE, DEFAULT '{"cpu": 2, "ram": "2G", "disk": "10G"}'::jsonb |  |
| `volume_path` | `text` | NULLABLE, DEFAULT '/data'::character varying |  |
| `network_name` | `text` | NULLABLE, DEFAULT 'bridge'::character varying |  |
| `is_default` | `bool` | NULLABLE, DEFAULT false |  |
| `created_at` | `timestamptz` | NULLABLE, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamptz` | NULLABLE, DEFAULT now() | Last modification timestamp |

### `game_types`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `identifier` | `text` | NOT NULL |  |
| `display_name` | `text` | NOT NULL |  |
| `description` | `text` | NULLABLE |  |
| `docker_image` | `text` | NOT NULL |  |
| `default_ports` | `jsonb` | NULLABLE, DEFAULT '{"game": 25565, "rcon": 25575}'::jsonb |  |
| `default_env` | `jsonb` | NULLABLE, DEFAULT '{}'::jsonb |  |
| `startup_command` | `text` | NULLABLE |  |
| `capabilities` | `jsonb` | NULLABLE, DEFAULT '{"mods": true, "rcon": true, "backup": true}'::jsonb |  |
| `is_active` | `bool` | NULLABLE, DEFAULT true |  |
| `sort_order` | `integer` | NULLABLE, DEFAULT 0 |  |
| `created_at` | `timestamptz` | NULLABLE, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamptz` | NULLABLE, DEFAULT now() | Last modification timestamp |

### `port_pools`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `node_id` | `uuid` | FK → nodes.id, NULLABLE | References the node resource |
| `port_range_start` | `integer` | NOT NULL |  |
| `port_range_end` | `integer` | NOT NULL |  |
| `current_port` | `integer` | NOT NULL |  |
| `allocated_ports` | `jsonb` | NULLABLE, DEFAULT '[]'::jsonb |  |
| `protocol` | `text` | NULLABLE, DEFAULT 'tcp'::character varying |  |
| `is_active` | `bool` | NULLABLE, DEFAULT true |  |
| `created_at` | `timestamptz` | NULLABLE, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamptz` | NULLABLE, DEFAULT now() | Last modification timestamp |

### `resource_plans`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `name` | `text` | NOT NULL |  |
| `display_name` | `text` | NOT NULL |  |
| `ram_mb` | `integer` | NOT NULL |  |
| `cpu_cores` | `integer` | NOT NULL |  |
| `disk_gb` | `integer` | NOT NULL |  |
| `price_monthly` | `real` | NULLABLE |  |
| `is_active` | `bool` | NULLABLE, DEFAULT true |  |
| `sort_order` | `integer` | NULLABLE, DEFAULT 0 |  |
| `created_at` | `timestamptz` | NULLABLE, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamptz` | NULLABLE, DEFAULT now() | Last modification timestamp |

## Infrastructure Tables

Legacy agent records, node API key management, and one-time registration tokens for automated node enrollment.

```mermaid
erDiagram
    node_api_keys {
        uuid id PK
        uuid node_id FK
        text key_hash
        text name
        bool is_active
        timestamp created_at
        timestamp expires_at
    }
    node_registration_tokens {
        uuid id PK
        uuid node_id FK
        text token_hash
        text name
        bool is_active
        timestamp used_at
        timestamp expires_at
        timestamp created_at
    }
    nodes || node_api_keys || : references
    nodes || node_registration_tokens o{ : references
```

### `agents`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `name` | `text` | NOT NULL |  |
| `url` | `text` | NOT NULL |  |
| `api_key_hash` | `text` | NOT NULL |  |
| `api_key_prefix` | `text` | NOT NULL |  |
| `region` | `text` | NULLABLE |  |
| `location` | `text` | NULLABLE |  |
| `capacity` | `integer` | NOT NULL, DEFAULT 10 |  |
| `current_load` | `integer` | NOT NULL, DEFAULT 0 |  |
| `status` | `text` | NOT NULL, DEFAULT 'offline'::character varying |  |
| `version` | `text` | NULLABLE |  |
| `metadata` | `jsonb` | NULLABLE, DEFAULT '{}'::jsonb |  |
| `last_heartbeat_at` | `timestamptz` | NULLABLE |  |
| `created_by` | `uuid` | FK → users.id, NULLABLE |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamptz` | NOT NULL, DEFAULT now() | Last modification timestamp |

### `node_api_keys`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `node_id` | `uuid` | FK → nodes.id, NOT NULL | References the node resource |
| `key_hash` | `text` | NOT NULL |  |
| `name` | `text` | NULLABLE |  |
| `is_active` | `bool` | NULLABLE, DEFAULT true |  |
| `created_at` | `timestamp` | NULLABLE, DEFAULT now() | Record creation timestamp |
| `expires_at` | `timestamp` | NULLABLE |  |

### `node_registration_tokens`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `node_id` | `uuid` | FK → nodes.id, NULLABLE | References the node resource |
| `token_hash` | `text` | NOT NULL |  |
| `name` | `text` | NULLABLE |  |
| `is_active` | `bool` | NULLABLE, DEFAULT true |  |
| `used_at` | `timestamp` | NULLABLE |  |
| `expires_at` | `timestamp` | NOT NULL |  |
| `created_at` | `timestamp` | NULLABLE, DEFAULT now() | Record creation timestamp |

## Jobs Tables

Background asynchronous job queue with progress tracking, used for backups, plugin installation, and other async operations.

```mermaid
erDiagram
    jobs {
        uuid id PK
        uuid user_id FK
        uuid server_id FK
        uuid agent_id FK
        text job_type
        text status
        jsonb payload
        jsonb result
        text error
        integer progress
        integer priority
        timestamptz started_at
        timestamptz completed_at
        timestamptz created_at
        timestamptz updated_at
        text external_id
        text progress_message
    }
    users || jobs || : references
    servers || jobs o{ : references
    agents || jobs o{ : references
```

### `jobs`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `user_id` | `uuid` | FK → users.id, NOT NULL | References the user resource |
| `server_id` | `uuid` | FK → servers.id, NULLABLE | References the server resource |
| `agent_id` | `uuid` | FK → agents.id, NULLABLE | References the agent resource |
| `job_type` | `text` | NOT NULL |  |
| `status` | `text` | NOT NULL, DEFAULT 'pending'::character varying |  |
| `payload` | `jsonb` | NOT NULL, DEFAULT '{}'::jsonb |  |
| `result` | `jsonb` | NULLABLE |  |
| `error` | `text` | NULLABLE |  |
| `progress` | `integer` | NULLABLE, DEFAULT 0 |  |
| `priority` | `integer` | NULLABLE, DEFAULT 0 |  |
| `started_at` | `timestamptz` | NULLABLE |  |
| `completed_at` | `timestamptz` | NULLABLE |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamptz` | NOT NULL, DEFAULT now() | Last modification timestamp |
| `external_id` | `text` | NULLABLE | References the external resource |
| `progress_message` | `text` | NULLABLE |  |

## Nodes Tables

Compute node registration and monitoring. Each node runs the Solys Web Agent and can host multiple game server containers.

```mermaid
erDiagram
    nodes {
        uuid id PK
        text name
        text description
        text ip_address
        integer port
        text status
        timestamp last_seen
        timestamp first_seen
        text podman_version
        text os_info
        integer total_memory
        integer cpu_cores
        text agent_version
        jsonb agent_capabilities
        text api_key_hash
        jsonb metadata
        timestamp created_at
        timestamp updated_at
        jsonb current_metrics
        uuid user_id FK
    }
    node_metrics {
        uuid id PK
        uuid node_id FK
        real cpu_usage
        integer memory_used
        integer memory_total
        integer disk_used
        integer disk_total
        integer container_count
        timestamp created_at
    }
    node_events {
        uuid id PK
        uuid node_id FK
        text event_type
        text severity
        text message
        jsonb data
        timestamp created_at
    }
    server_nodes {
        uuid id PK
        uuid server_id FK
        uuid node_id FK
        bool is_primary
        timestamp created_at
        uuid user_id FK
    }
    users || nodes o{ : references
    nodes || node_metrics || : references
    nodes || node_events || : references
    servers || server_nodes || : references
    nodes || server_nodes || : references
    users || server_nodes o{ : references
```

### `node_events`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `node_id` | `uuid` | FK → nodes.id, NOT NULL | References the node resource |
| `event_type` | `text` | NOT NULL |  |
| `severity` | `text` | NULLABLE, DEFAULT 'info'::character varying |  |
| `message` | `text` | NULLABLE |  |
| `data` | `jsonb` | NULLABLE |  |
| `created_at` | `timestamp` | NULLABLE, DEFAULT now() | Record creation timestamp |

### `node_metrics`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `node_id` | `uuid` | FK → nodes.id, NOT NULL | References the node resource |
| `cpu_usage` | `real` | NULLABLE, DEFAULT 0 |  |
| `memory_used` | `integer` | NULLABLE, DEFAULT 0 |  |
| `memory_total` | `integer` | NULLABLE, DEFAULT 0 |  |
| `disk_used` | `integer` | NULLABLE, DEFAULT 0 |  |
| `disk_total` | `integer` | NULLABLE, DEFAULT 0 |  |
| `container_count` | `integer` | NULLABLE, DEFAULT 0 |  |
| `created_at` | `timestamp` | NULLABLE, DEFAULT now() | Record creation timestamp |

### `nodes`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `name` | `text` | NOT NULL |  |
| `description` | `text` | NULLABLE |  |
| `ip_address` | `text` | NOT NULL |  |
| `port` | `integer` | NOT NULL, DEFAULT 8080 |  |
| `status` | `text` | NULLABLE, DEFAULT 'offline'::character varying |  |
| `last_seen` | `timestamp` | NULLABLE |  |
| `first_seen` | `timestamp` | NULLABLE, DEFAULT now() |  |
| `podman_version` | `text` | NULLABLE |  |
| `os_info` | `text` | NULLABLE |  |
| `total_memory` | `integer` | NULLABLE |  |
| `cpu_cores` | `integer` | NULLABLE |  |
| `agent_version` | `text` | NULLABLE |  |
| `agent_capabilities` | `jsonb` | NULLABLE, DEFAULT '["podman", "metrics"]'::jsonb |  |
| `api_key_hash` | `text` | NULLABLE |  |
| `metadata` | `jsonb` | NULLABLE, DEFAULT '{}'::jsonb |  |
| `created_at` | `timestamp` | NULLABLE, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamp` | NULLABLE, DEFAULT now() | Last modification timestamp |
| `current_metrics` | `jsonb` | NULLABLE |  |
| `user_id` | `uuid` | FK → users.id, NULLABLE | References the user resource |

### `server_nodes`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `server_id` | `uuid` | FK → servers.id, NOT NULL | References the server resource |
| `node_id` | `uuid` | FK → nodes.id, NOT NULL | References the node resource |
| `is_primary` | `bool` | NULLABLE, DEFAULT true |  |
| `created_at` | `timestamp` | NULLABLE, DEFAULT now() | Record creation timestamp |
| `user_id` | `uuid` | FK → users.id, NULLABLE | References the user resource |

## Servers Tables

Game server records with connection details, runtime configuration, and deployment snapshots. Central entity for all server lifecycle management.

```mermaid
erDiagram
    servers {
        uuid id PK
        text name
        text host
        integer port
        text executor_type
        text status
        timestamp created_at
        timestamp updated_at
        text username
        text game
        text password_auth
        text server_path
        text start_command
        text stop_command
        text container_name
        text public_host
        text mc_loader
        bool auto_pause
        text mc_version
        text ram_allocation
        text discord_webhook_url
        bool auto_backup_enabled
        text backup_cron
        text backup_provider
        text backup_path
        integer max_retained_backups
        enum environment
        bool auto_restart
        integer restart_count
        bool enable_tailscale
        text tailscale_auth_key
        text custom_container_name
        text ip_binding
        text template
        text network_name
        text git_remote_url
        text git_remote_username
        text git_remote_token
        uuid node_id FK
        uuid user_id FK
        uuid agent_id FK
        uuid job_id FK
        text image
        text remote_id
        jsonb config
        jsonb resources
        jsonb endpoints
        timestamptz deleted_at
        jsonb deployment_snapshot
        bool auto_wake
        integer sleep_timeout_minutes
        timestamptz last_player_activity
        integer max_restart_attempts
        integer restart_cooldown_seconds
        timestamptz last_restart_at
        text last_restart_reason
        integer health_check_timeout_seconds
    }
    server_events {
        uuid id PK
        uuid server_id FK
        text event_type
        jsonb details
        timestamptz created_at
    }
    server_metrics {
        uuid id PK
        uuid server_id FK
        real cpu_usage
        integer memory_usage_mb
        integer disk_usage_mb
        real tps
        integer players
        timestamp created_at
    }
    server_crash_logs {
        uuid id PK
        uuid server_id FK
        timestamptz crashed_at
        integer exit_code
        text crash_type
        text log_excerpt
        text recovery_action
        timestamptz resolved_at
        timestamptz created_at
    }
    nodes || servers o{ : references
    users || servers o{ : references
    agents || servers o{ : references
    jobs || servers o{ : references
    servers || server_events || : references
    servers || server_metrics || : references
    servers || server_crash_logs || : references
```

### `server_crash_logs`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `server_id` | `uuid` | FK → servers.id, NOT NULL | References the server resource |
| `crashed_at` | `timestamptz` | NOT NULL, DEFAULT now() |  |
| `exit_code` | `integer` | NOT NULL |  |
| `crash_type` | `text` | NOT NULL |  |
| `log_excerpt` | `text` | NULLABLE |  |
| `recovery_action` | `text` | NOT NULL |  |
| `resolved_at` | `timestamptz` | NULLABLE |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |

### `server_events`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT uuid_generate_v4() |  |
| `server_id` | `uuid` | FK → servers.id, NOT NULL | References the server resource |
| `event_type` | `text` | NOT NULL |  |
| `details` | `jsonb` | NULLABLE |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |

### `server_metrics`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `server_id` | `uuid` | FK → servers.id, NOT NULL | References the server resource |
| `cpu_usage` | `real` | NOT NULL |  |
| `memory_usage_mb` | `integer` | NOT NULL |  |
| `disk_usage_mb` | `integer` | NOT NULL, DEFAULT 0 |  |
| `tps` | `real` | NULLABLE |  |
| `players` | `integer` | NOT NULL, DEFAULT 0 |  |
| `created_at` | `timestamp` | NOT NULL, DEFAULT now() | Record creation timestamp |

### `servers`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT uuid_generate_v4() |  |
| `name` | `text` | NOT NULL |  |
| `host` | `text` | NOT NULL, DEFAULT ''::text |  |
| `port` | `integer` | NOT NULL |  |
| `executor_type` | `text` | NOT NULL |  |
| `status` | `text` | NOT NULL, DEFAULT 'stopped'::text |  |
| `created_at` | `timestamp` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamp` | NOT NULL, DEFAULT now() | Last modification timestamp |
| `username` | `text` | NOT NULL, DEFAULT ''::text |  |
| `game` | `text` | NOT NULL, DEFAULT 'minecraft'::text |  |
| `password_auth` | `text` | NOT NULL, DEFAULT ''::text |  |
| `server_path` | `text` | NULLABLE, DEFAULT '~/server'::character varying |  |
| `start_command` | `text` | NULLABLE, DEFAULT './start.sh'::character varying |  |
| `stop_command` | `text` | NULLABLE, DEFAULT 'pkill -f server'::character varying |  |
| `container_name` | `text` | NULLABLE |  |
| `public_host` | `text` | NULLABLE |  |
| `mc_loader` | `text` | NULLABLE, DEFAULT 'PAPER'::character varying |  |
| `auto_pause` | `bool` | NULLABLE, DEFAULT false |  |
| `mc_version` | `text` | NULLABLE, DEFAULT 'LATEST'::character varying |  |
| `ram_allocation` | `text` | NULLABLE, DEFAULT '1G'::character varying |  |
| `discord_webhook_url` | `text` | NULLABLE |  |
| `auto_backup_enabled` | `bool` | NOT NULL, DEFAULT false |  |
| `backup_cron` | `text` | NULLABLE |  |
| `backup_provider` | `text` | NOT NULL, DEFAULT 'local'::text |  |
| `backup_path` | `text` | NULLABLE |  |
| `max_retained_backups` | `integer` | NOT NULL, DEFAULT 5 |  |
| `environment` | `enum` | NOT NULL, DEFAULT 'production'::server_environment |  |
| `auto_restart` | `bool` | NOT NULL, DEFAULT true |  |
| `restart_count` | `integer` | NOT NULL, DEFAULT 0 |  |
| `enable_tailscale` | `bool` | NOT NULL, DEFAULT false |  |
| `tailscale_auth_key` | `text` | NULLABLE |  |
| `custom_container_name` | `text` | NULLABLE |  |
| `ip_binding` | `text` | NULLABLE, DEFAULT '0.0.0.0'::text |  |
| `template` | `text` | NULLABLE, DEFAULT 'paper'::text |  |
| `network_name` | `text` | NULLABLE, DEFAULT 'devnode-minecraft'::text |  |
| `git_remote_url` | `text` | NULLABLE |  |
| `git_remote_username` | `text` | NULLABLE |  |
| `git_remote_token` | `text` | NULLABLE |  |
| `node_id` | `uuid` | FK → nodes.id, NULLABLE | References the node resource |
| `user_id` | `uuid` | FK → users.id, NULLABLE | References the user resource |
| `agent_id` | `uuid` | FK → agents.id, NULLABLE | References the agent resource |
| `job_id` | `uuid` | FK → jobs.id, NULLABLE | References the job resource |
| `image` | `text` | NOT NULL, DEFAULT 'nginx:latest'::text |  |
| `remote_id` | `text` | NULLABLE | References the remote resource |
| `config` | `jsonb` | NULLABLE, DEFAULT '{}'::jsonb |  |
| `resources` | `jsonb` | NULLABLE, DEFAULT '{"cpu": 1, "ram": "1G", "disk": "5G"}'::jsonb |  |
| `endpoints` | `jsonb` | NULLABLE, DEFAULT '[]'::jsonb |  |
| `deleted_at` | `timestamptz` | NULLABLE | Soft delete timestamp |
| `deployment_snapshot` | `jsonb` | NOT NULL, DEFAULT '{}'::jsonb |  |
| `auto_wake` | `bool` | NOT NULL, DEFAULT false |  |
| `sleep_timeout_minutes` | `integer` | NOT NULL, DEFAULT 30 |  |
| `last_player_activity` | `timestamptz` | NULLABLE |  |
| `max_restart_attempts` | `integer` | NOT NULL, DEFAULT 5 |  |
| `restart_cooldown_seconds` | `integer` | NOT NULL, DEFAULT 300 |  |
| `last_restart_at` | `timestamptz` | NULLABLE |  |
| `last_restart_reason` | `text` | NULLABLE |  |
| `health_check_timeout_seconds` | `integer` | NOT NULL, DEFAULT 5 |  |

## Settings/Config Tables

Platform-wide configuration stored in app_settings key-value table and scheduled cron task definitions.

```mermaid
erDiagram
    cron_tasks {
        uuid id PK
        uuid server_id FK
        uuid user_id FK
        text task_type
        text schedule_cron
        text command
        bool enabled
        timestamptz last_run
        timestamptz next_run
        timestamptz created_at
        timestamptz updated_at
        text timezone
        bool run_once
        text last_result
        text last_error
    }
    servers || cron_tasks || : references
    users || cron_tasks || : references
```

### `app_settings`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `key` | `text` | PK, NOT NULL |  |
| `value` | `jsonb` | NOT NULL, DEFAULT '{}'::jsonb |  |
| `updated_at` | `timestamptz` | NOT NULL, DEFAULT now() | Last modification timestamp |

### `cron_tasks`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `server_id` | `uuid` | FK → servers.id, NOT NULL | References the server resource |
| `user_id` | `uuid` | FK → users.id, NOT NULL | References the user resource |
| `task_type` | `text` | NOT NULL |  |
| `schedule_cron` | `text` | NOT NULL |  |
| `command` | `text` | NULLABLE |  |
| `enabled` | `bool` | NULLABLE, DEFAULT true |  |
| `last_run` | `timestamptz` | NULLABLE |  |
| `next_run` | `timestamptz` | NULLABLE |  |
| `created_at` | `timestamptz` | NULLABLE, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamptz` | NULLABLE, DEFAULT now() | Last modification timestamp |
| `timezone` | `text` | NOT NULL, DEFAULT 'UTC'::character varying |  |
| `run_once` | `bool` | NOT NULL, DEFAULT false |  |
| `last_result` | `text` | NULLABLE |  |
| `last_error` | `text` | NULLABLE |  |

## Templates Tables

Pre-configured server templates, modpack references, and plugin bundles for quick server deployment.

```mermaid
erDiagram
    templates {
        uuid id PK
        text game_type
        text category
        text display_name
        text description
        jsonb config
        text visibility
        uuid user_id FK
        bool is_builtin
        bool is_active
        timestamp created_at
        timestamp updated_at
    }
    modpack_templates {
        uuid id PK
        text game_type
        text display_name
        text description
        text source
        text project_id
        text version_id
        text version_name
        integer mod_count
        text image_url
        text min_plan
        bool is_active
        timestamp created_at
        timestamp updated_at
    }
    users || templates o{ : references
```

### `modpack_templates`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `game_type` | `text` | NOT NULL |  |
| `display_name` | `text` | NOT NULL |  |
| `description` | `text` | NULLABLE |  |
| `source` | `text` | NOT NULL |  |
| `project_id` | `text` | NOT NULL | References the project resource |
| `version_id` | `text` | NOT NULL | References the version resource |
| `version_name` | `text` | NOT NULL |  |
| `mod_count` | `integer` | NOT NULL, DEFAULT 0 |  |
| `image_url` | `text` | NULLABLE |  |
| `min_plan` | `text` | NOT NULL, DEFAULT 'hobby'::character varying |  |
| `is_active` | `bool` | NOT NULL, DEFAULT true |  |
| `created_at` | `timestamp` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamp` | NOT NULL, DEFAULT now() | Last modification timestamp |

### `templates`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `game_type` | `text` | NOT NULL |  |
| `category` | `text` | NOT NULL |  |
| `display_name` | `text` | NOT NULL |  |
| `description` | `text` | NULLABLE |  |
| `config` | `jsonb` | NOT NULL, DEFAULT '{}'::jsonb |  |
| `visibility` | `text` | NOT NULL, DEFAULT 'private'::character varying |  |
| `user_id` | `uuid` | FK → users.id, NULLABLE | References the user resource |
| `is_builtin` | `bool` | NOT NULL, DEFAULT false |  |
| `is_active` | `bool` | NOT NULL, DEFAULT true |  |
| `created_at` | `timestamp` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamp` | NOT NULL, DEFAULT now() | Last modification timestamp |

## Usage Tables

Metered usage tracking per billing period for resource consumption monitoring and quota enforcement.

```mermaid
erDiagram
    usage_tracking {
        uuid id PK
        uuid user_id FK
        text metric_type
        real metric_value
        timestamptz period_start
        timestamptz period_end
        timestamptz created_at
        timestamptz updated_at
    }
    users || usage_tracking || : references
```

### `usage_tracking`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `user_id` | `uuid` | FK → users.id, NOT NULL | References the user resource |
| `metric_type` | `text` | NOT NULL |  |
| `metric_value` | `real` | NOT NULL, DEFAULT 0 |  |
| `period_start` | `timestamptz` | NOT NULL |  |
| `period_end` | `timestamptz` | NOT NULL |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamptz` | NOT NULL, DEFAULT now() | Last modification timestamp |

## Users/Auth Tables

User identity, authentication, and role-based access control. Manages accounts, API keys, permissions, and authorization.

```mermaid
erDiagram
    users {
        uuid id PK
        text email
        text password_hash
        text role
        bool is_active
        timestamptz email_verified_at
        timestamptz last_login_at
        timestamptz created_at
        timestamptz updated_at
        timestamptz deleted_at
        text password_reset_token
        timestamptz password_reset_expires
        text verification_token
        timestamptz verification_expires
        text pending_email
    }
    api_keys {
        uuid id PK
        uuid user_id FK
        text name
        text key_hash
        text prefix
        jsonb permissions
        integer rate_limit
        timestamptz expires_at
        timestamptz last_used_at
        timestamptz created_at
        timestamptz revoked_at
    }
    roles {
        uuid id PK
        uuid user_id FK
        text name
        text description
        bool is_default
        timestamptz created_at
        timestamptz updated_at
    }
    permissions {
        uuid id PK
        uuid role_id FK
        text resource
        text action
        timestamptz created_at
    }
    user_roles {
        uuid user_id PK FK
        uuid role_id PK FK
        timestamptz assigned_at
        uuid assigned_by FK
    }
    users || api_keys || : references
    users || roles || : references
    roles || permissions || : references
    users || user_roles || : references
    roles || user_roles || : references
    users || user_roles o{ : references
```

### `api_keys`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `user_id` | `uuid` | FK → users.id, NOT NULL | References the user resource |
| `name` | `text` | NOT NULL |  |
| `key_hash` | `text` | NOT NULL |  |
| `prefix` | `text` | NOT NULL |  |
| `permissions` | `jsonb` | NOT NULL, DEFAULT '[]'::jsonb |  |
| `rate_limit` | `integer` | NULLABLE |  |
| `expires_at` | `timestamptz` | NULLABLE |  |
| `last_used_at` | `timestamptz` | NULLABLE |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `revoked_at` | `timestamptz` | NULLABLE |  |

### `permissions`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `role_id` | `uuid` | FK → roles.id, NOT NULL | References the role resource |
| `resource` | `text` | NOT NULL |  |
| `action` | `text` | NOT NULL |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |

### `roles`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `user_id` | `uuid` | FK → users.id, NOT NULL | References the user resource |
| `name` | `text` | NOT NULL |  |
| `description` | `text` | NULLABLE |  |
| `is_default` | `bool` | NOT NULL, DEFAULT false |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamptz` | NOT NULL, DEFAULT now() | Last modification timestamp |

### `user_roles`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `user_id` | `uuid` | PK, FK → users.id, NOT NULL | References the user resource |
| `role_id` | `uuid` | PK, FK → roles.id, NOT NULL | References the role resource |
| `assigned_at` | `timestamptz` | NOT NULL, DEFAULT now() |  |
| `assigned_by` | `uuid` | FK → users.id, NULLABLE |  |

### `users`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `email` | `text` | NOT NULL |  |
| `password_hash` | `text` | NOT NULL |  |
| `role` | `text` | NOT NULL, DEFAULT 'user'::character varying |  |
| `is_active` | `bool` | NOT NULL, DEFAULT true |  |
| `email_verified_at` | `timestamptz` | NULLABLE |  |
| `last_login_at` | `timestamptz` | NULLABLE |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamptz` | NOT NULL, DEFAULT now() | Last modification timestamp |
| `deleted_at` | `timestamptz` | NULLABLE | Soft delete timestamp |
| `password_reset_token` | `text` | NULLABLE |  |
| `password_reset_expires` | `timestamptz` | NULLABLE |  |
| `verification_token` | `text` | NULLABLE |  |
| `verification_expires` | `timestamptz` | NULLABLE |  |
| `pending_email` | `text` | NULLABLE |  |

## Webhooks Tables

Outgoing webhook configurations for event-driven integrations with external services.

```mermaid
erDiagram
    webhooks {
        uuid id PK
        uuid user_id FK
        text name
        text url
        text secret
        jsonb events
        bool is_active
        jsonb headers
        integer failure_count
        timestamptz last_delivery_at
        timestamptz last_failure_at
        timestamptz created_at
        timestamptz updated_at
    }
    users || webhooks || : references
```

### `webhooks`

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| `id` | `uuid` | PK, NOT NULL, DEFAULT gen_random_uuid() |  |
| `user_id` | `uuid` | FK → users.id, NOT NULL | References the user resource |
| `name` | `text` | NOT NULL |  |
| `url` | `text` | NOT NULL |  |
| `secret` | `text` | NULLABLE |  |
| `events` | `jsonb` | NOT NULL, DEFAULT '[]'::jsonb |  |
| `is_active` | `bool` | NOT NULL, DEFAULT true |  |
| `headers` | `jsonb` | NULLABLE, DEFAULT '{}'::jsonb |  |
| `failure_count` | `integer` | NOT NULL, DEFAULT 0 |  |
| `last_delivery_at` | `timestamptz` | NULLABLE |  |
| `last_failure_at` | `timestamptz` | NULLABLE |  |
| `created_at` | `timestamptz` | NOT NULL, DEFAULT now() | Record creation timestamp |
| `updated_at` | `timestamptz` | NOT NULL, DEFAULT now() | Last modification timestamp |

## Migration History

For the full schema evolution timeline showing each change by date, see `api/migrations/` in the repository. This file contains 69 timestamped SQL migration files.
