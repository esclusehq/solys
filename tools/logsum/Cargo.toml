[package]
name = "logsum"
version = "0.1.0"
edition = "2021"
description = "Escluse Agent log filter and summarizer"
license = "AGPL-3.0"

[dependencies]
clap = { version = "4", features = ["derive"] }
regex = "1"
chrono = "0.4"
